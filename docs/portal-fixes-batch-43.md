# Portal fixes — batch 43 (ACCUMULATING — opened Aug 30, 2026)

**Standing rules:** all prior (see batch 41/42 headers — incl. no must-happen work after the response on Vercel · normalize timestamp comparisons · scroll behavior verified in real Chrome · draft-review sends render the DRAFT body · composer-path · twins+seeds lockstep · billy@ only · review ≠ edit · UI labels not internal names · crashed gate ≠ green gate).

(CLOSED for batch 43 hand-off, Aug 30. Next PL: PL-418.)

**Batch closed Aug 31 — both items shipped.** Gate battery green: `tsc` · `next build` · `smoke:public` · `regress:links` · `regress:pronouns` · `regress:client-imports` · `regress:alert-pins`. No migrations (as expected — the flips are `email_templates.live` updates). One environment note: a fresh dev server served raw 404s for bad slugs until `.next` was cleared (the batch-22 wedge, inverse flavor) — prod was verified correct throughout; the smoke gate went green after the clear.

## ✅ PL-416 — ramp complete (Aug 31)
All six approved drafts flipped LIVE, bodies untouched (activation only — zero copy edits): **AL_CLOSE_MATCH v1 · BL_CONTINUE_OUTCOME v2 · T6_NOTES_WEEKLY v1 · IN_MIN_DECISION v1 · T2B_PAYMENT_REMINDER v2 · SR_PAYMENT_LINK v1** ("BL v2 autopay variants" = BL_CONTINUE_OUTCOME v2, per the batch-42 finding; BL_BLOCK_CONFIRM was already live). Per template, all verified: the composer path now serves the REGISTRY version (renderDbEmail non-null, sentinel twin never used, versionId = the active version) and the output matches the reviewed render **byte-for-byte** on subject + html (same fixtures as the batch-42 review sends); one spot send each → billy@ (`[RAMPED LIVE: {key} v{n}]`), all six `sent`. **The drafts-awaiting-ramp list is now EMPTY.** For completeness, three templates remain draft BY DESIGN and were never on any ramp list: `T3_RESCHEDULE_ACK` v1 (code twin sends identical copy — batch-26 note; flag it if Scarlett wants a review round), `SV_CLASS_SURVEY` ("draft as before" per batch 24, twin sends), `MK_OFFER` (campaign starter — campaigns pin a version at send; live is irrelevant to it).

## ✅ PL-417 — AL_CLOSE_MATCH pin + sweep (Aug 31)
The details block moved into a LEAF builder (`app/utils/close-match-copy.ts` — the autopay-nudge-copy/coverage-copy pattern); `close-match.ts` real sends compose through it, and the new `SAMPLE_EXTRA_BY_TEMPLATE.AL_CLOSE_MATCH` pin is **COMPUTED from that same builder** (PL-96/234 — never hand-typed) with near-duplicate fixture facts ("Ana Garcia" the lead vs "Ana García" the student — the alert's own story). `regress:alert-pins` green with the pin. **Sweep:** every registered template rendered through the editor's own preview path (renderVersion + SAMPLE_CONTEXT + sampleExtraFor) — **zero unresolved tokens anywhere; no additional gaps found** (the wrong-story class is covered by the alert-pins gate, also green).

## PL-416 — Ramp: activate ALL approved draft templates (Scarlett approved all 10 review renders, Aug 30)
Scarlett approved every `[DRAFT REVIEW]` render verbatim: activate the draft versions — AL_CLOSE_MATCH · BL_CONTINUE_OUTCOME v2 · T6_NOTES_WEEKLY · IN_MIN_DECISION · T2B_PAYMENT_REMINDER v2 · SR_PAYMENT_LINK v1 · BL v2 autopay variants — flipping each to LIVE so the registry body (not the code twin) sends. Bodies activate EXACTLY as reviewed (no edits in the same motion — an edit would need a new version + re-review). Post-flip verification per template: composer render matches the reviewed send byte-for-byte on the copy, live-gate now serves the registry version, one spot test-send → billy@ per template. Update the handoff's "drafts awaiting" list to empty in the ship note.

## PL-417 — AL_CLOSE_MATCH sample pin (batch-42 finding)
AL_CLOSE_MATCH is the lone alert template with no sample pin, so the in-app editor preview lies about it. Pin a composer-derived sample per the PL-96/234 rules (sample computed from the composer path, never hand-typed) so the editor preview is honest. Sweep: confirm every OTHER registered template has a live sample pin (report any additional gaps, don't just fix the one).
