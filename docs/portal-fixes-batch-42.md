# Portal fixes — batch 42 (ACCUMULATING — opened Aug 29, 2026)

**Standing rules:** all prior (see batch 41's header + additions: staff attributions via the one name resolver · staff displays never show IANA ids · gcal events carry hglSessionId, adopt-before-create · drift alerts ring once per signature · date formatters throw on non-ISO in dev · time-anchored one-shots need hour gates · plus the perennial set: composer-path, twins+seeds lockstep, billy@ only, inline confirms, real-renderer previews, review ≠ edit, UI labels not internal names, crashed gate ≠ green gate).

(CLOSED for batch 42 hand-off, Aug 29. Next PL: PL-416.)

**Batch closed Aug 29 — the TASK + all 10 items shipped** (per-item ✅ notes below). Gate battery green at close: `tsc` · `next build` · `smoke:public` · `regress:links` · `regress:pronouns` · `regress:client-imports`. Migrations `20260912000001` (schools.address) · `…02` (material anchors) · `…03` (prep time) · `…04` (schools.timezone default → Denver) · `…05` (gcal watch channels) — **all APPLIED via the Management API**.

## ✅ TASK — draft review test-sends (Scarlett's ramp)
**SENT Aug 29 → billy@**, all via the ACTUAL DRAFT VERSION bodies (renderVersion — the live gate would have silently swapped in the code twins) with contexts/extras mirrored line-for-line from each production composer (the PL-362 harness pattern; never editor samples), `[DRAFT REVIEW: {key} — variant]` subjects. **10 sends:**
1. `AL_CLOSE_MATCH` v1 (duplicate-person alert; details block per the real composer)
2. `BL_CONTINUE_OUTCOME` v2 — reserved · autopay OFF
3. `BL_CONTINUE_OUTCOME` v2 — reserved · autopay ON (nudge composes away)
4. `BL_CONTINUE_OUTCOME` v2 — staff-routed (no auto-reserve) · autopay OFF
5. `T6_NOTES_WEEKLY` v1 (3 missing-notes lines)
6. `IN_MIN_DECISION` v1 — run anyway (real ISD bundle counts)
7. `IN_MIN_DECISION` v1 — deadline extended (real ISD bundle)
8. `T2B_PAYMENT_REMINDER` v2 — autopay OFF
9. `T2B_PAYMENT_REMINDER` v2 — autopay ON (labeled review artifact — prod only reminds non-autopay families)
10. `SR_PAYMENT_LINK` v1 (REAL emailContext from the ISD bundle — signed resume-payment link resolves)

**Finding:** `BL_BLOCK_CONFIRM` is already **live** in the DB (v2) — not awaiting ramp, so no review send ("BL v2 autopay variants" = BL_CONTINUE_OUTCOME v2, which got three). **Loose end for a future batch:** `AL_CLOSE_MATCH` is the one `{alertDetailsBlock}` template with no PL-82 sample pin — the admin editor's preview shows the registration story for it (the PL-137 bug class); `regress:alert-pins` should be flagging it.

## PL-406 — `schools.address` + school maps links (Scarlett, Aug 29 — approving the PL-399 flag)
Add a street-address field to schools (edited in the Schools card editor, plain label "Street address — used for the map link"); the PL-399 maps resolver's school branch uses it (school class + address on record → maps link to the school; still honest absence when blank). Room-only school locations now get a useful pin. Verify: set ISD's address → its page/email maps links point there; blank school unchanged.

**✅ SHIPPED (Aug 29; migration 20260912000001 APPLIED).** Schools card editor gains "Street address — used for the map link" (+ honest-absence hint); saved via the same staff-RLS update. The class page's maps gate relaxes `!school` → school-with-address; JSON-LD prefers the school address; **emails**: `{classLocationLine}` appends `(map)` when a real address resolves (schoolAddress via new bundle→context plumbing, else the PL-399 at-HGL resolver) — room text never changes. VERIFIED live: ISD's address set through the editor UI; with a room-only location the page JSON-LD published `Place{name:"Room 204", address:"Niederrheinstraße 336…"}` and the email line rendered `in Room 204 (map→school pin)`; blank school unchanged; state restored. NOTE: the visible page "Open in Google Maps" section requires the course set to carry a `:location` block — only the at-HGL ACT set has one today, so school pages show no location section until one is written (unchanged from before; the plumbing is ready).

## PL-407 — Class timezone must never default to Mexico City (Scarlett, Aug 29)
`CLASS_TIMEZONE` defaults to America/Mexico_City (a relic of the first school). Ops is Salt Lake City: the default anywhere a timezone is assumed (roster-report clock, any class/report fallback) becomes **America/Denver**, labeled "Salt Lake City time" per PL-398. Audit every consumer of the env/default (the batch-24 note flagged the roster-report clock); real classes always carry their own timezone — this is only the fallback. Confirm the env var's production value and record it in ship notes; if the env is already Denver in prod, fix the CODE default to match so dev/prod can't diverge.

**✅ SHIPPED (Aug 29; migration 20260912000004 APPLIED).** Code default flipped to America/Denver AND the audit's real findings fixed: the `schools.timezone` COLUMN default was the deeper relic (new school rows silently landed on Mexico City, outranking the code fallback in every precedence chain — flipped; existing rows keep their real timezones); qbo-sync's class-receipt path now honors `classes.timezone` like every other chain (it alone skipped it); dead cron-route import removed. Consumers audited: roster-report Monday gate + liveness cutoff, QBO receipt txn dates + health-alert day key, the bundle tz fallback (fans out to every email clock/hour gate — only for classes with neither own tz nor school), register-close clock, /classes + /c states/labels. **Prod env value (recorded at close, from the deployed System health card):** `Default timezone (fallback only) — America/Denver`, **no `CLASS_TIMEZONE` env set in prod** — the old prod behavior was purely the code default (Mexico City), so the flip fully fixes prod with nothing to align in Vercel env. Dev has no env var either → identical.

## PL-408 — Ship the approved fonts: Source Serif 4 headings / Montserrat buttons (Scarlett approved, Aug 29)
PL-374 shipped body Pontano Sans and left headings/buttons awaiting sign-off — now approved. Apply Source Serif 4 (headings) + Montserrat (buttons/UI accents) on the public pages (/{code} views, /classes, /team, samples, capture pages) via next/font self-hosting like Pontano Sans; weights chosen to match the sqsp site's hierarchy (record which). Check the PL-374 wrap/overflow lessons at 375/768/1280 (a wider face exposed a bug last time). Admin unchanged.

**✅ SHIPPED (Aug 29).** Source Serif 4 (headings, **600/700**) + Montserrat (buttons, **600/700**) — the self-hostable stand-ins for the sqsp adonis-web/proxima-nova hierarchy, weights matching its semibold/bold usage — via next/font variables + ONE `publicSkin` class (`.public-skin` scope: h1–h3 serif; `.public-cta` buttons). Applied on /c + /{code}, /classes, /team, sample pages (same renderer), capture + honest-state cards (ClassStateCard/PublicNotice/EvergreenCapture now wear the skin THEMSELVES — they'd rendered outside the skinned roots) and the REAL register form (bare since PL-374). Admin unchanged. Wrap lessons checked in real Chrome + emulation: /isd, /classes, /team, /nido capture — **zero horizontal overflow at 375/768/1280**; computed families verified (headings "Source Serif 4", CTA "Montserrat", body Pontano).

## PL-409 — Payroll/timecard headers say "Salt Lake City time" (Scarlett, Aug 29 — the PL-398 inverse gap)
Timecard emails, the portal timecard panel, and admin payroll/timecards headers state pay-period dates/times with NO timezone. Payroll anchors America/Denver: label the period headers (and any specific times shown) "Salt Lake City time" via staffTimeCityLabel — labeling only, zero boundary-math changes (the Denver anchoring is correct and stays). Registry rule if T5 copy changes (new version, twin+seed).

**✅ SHIPPED (Aug 29).** — labeling shipped with the PL-398 sweep machinery: portal timecard panel + admin timecards/payroll headers + T5/timecard emails state "Salt Lake City time" via staffTimeCityLabel where a period or specific time renders; ZERO boundary-math changes (Denver anchoring untouched). T5's registry copy needed no version (the label rides composed variables, not template bodies). Also caught in passing: the Schools card subtitles still rendered raw IANA ids ("· Europe/Berlin") — now city time with the id in the tooltip.

## TASK (not a PL) — Test-send every draft template → billy@ for Scarlett's ramp review
Composer-path renders of ALL drafts awaiting ramp (AL_CLOSE_MATCH · BL_CONTINUE_OUTCOME · T6_NOTES_WEEKLY · IN_MIN_DECISION · T2B v2 · SR_PAYMENT_LINK · BL v2 autopay variants), realistic fixture contexts (both autopay cases where relevant), sent → billy@ with clear [DRAFT REVIEW: {key}] subjects tags. List what was sent in the batch doc so Scarlett can tick them off against her inbox.

## PL-410 — Google Calendar push notifications: near-instant drift detection (Scarlett, Aug 29 — un-deferred)
Replace polling-only drift detection with Google push: register a watch channel per connected tutor calendar (events.watch) pointing at a portal webhook (public HTTPS route; validate Google's channel token/id headers — treat unverifiable posts as noise, never as data). On a push, run the SAME audit for that calendar (the push is a doorbell, not a payload — Google sends no event details; re-fetch and diff exactly as the audit does today), so banners/Needs Attention/grouped alerts (PL-402 machinery unchanged) land within a minute of Billy's edit instead of up to an hour. **Channel lifecycle:** channels expire (~week max) — a renewal sweep on the existing hourly cron re-arms expiring channels and re-creates missing ones (state in a table: channel id, resource id, calendar, expiry); disconnecting a tutor's gcal stops its channel. **The hourly poll STAYS as backstop** (missed pushes, downtime, channels Google silently drops) — push is an accelerator, not a replacement; ship note states both paths run. Debounce: a burst of pushes for one calendar coalesces (a mass-delete like Billy's Aug-18 cleanup = one audit pass + ONE grouped email, not 13). Env/URL: the webhook rides the current vercel host and must survive the domain cutover (emailBaseUrl-style pinning; add a cutover-checklist line to re-verify channels after DNS). Verify E2E against real Google: move an event → banner + grouped email within ~1 min; expiry simulation → renewal re-arms; webhook rejects a forged post; poll still catches a synthetic missed push.

**✅ SHIPPED (Aug 29; migration 20260912000005 APPLIED).** `events.watch` channel per watched tutor calendar (active + tutoring_active; existing calendar.events scope — no re-consent) → `/api/webhooks/gcal`, pinned to PRODUCTION_ORIGIN. **Doorbell only:** the webhook validates `X-Goog-Channel-ID` + `X-Goog-Channel-Token` against our own row (we mint the high-entropy token; Google signs nothing) and 200-and-drops anything unverifiable — then re-runs the SAME per-tutor audit (PL-393/401/402 machinery byte-unchanged). **Debounce:** each push stamps `last_push_at`; a ~5s coalesce + epoch-compared ownership check means only the LAST push of a burst audits — a mass-delete = one audit + (PL-402 once-only) ONE grouped email. **Lifecycle:** hourly sweep re-arms channels expiring <24h, arms missing, stops orphans (org disconnect or tutor deactivation = stopped next pass; a non-Workspace calendar that can't be watched just stays on the poll). **BOTH PATHS RUN:** the hourly poll is unchanged as the backstop (missed pushes, downtime, silently-dropped channels). **Cutover:** channels store their webhook URL; a URL change makes the sweep re-register within the hour — checklist gained the post-DNS re-verification step. **E2E against real Google + the deployed webhook:** forged post → 200-and-drop, no audit ✓ · channel armed (expiry ~1 week out) ✓ · hand-moved the QA event → Google's push hit the deployed webhook **2–3 seconds** after every move ✓ · drift row + grouped alert within ~1 minute ✓ (after two REAL bugs the E2E caught: Vercel killing post-response work — the audit now runs inline before the 200 — and the ownership check comparing `toISOString()`'s 'Z' against Postgres's '+00:00', which made every audit supersede itself) · expiry simulation → sweep re-armed ✓ · restore → push cleared the banner ✓ · the poll path is the already-E2E'd PL-402 machinery, untouched.

## PL-411 — Family materials: fresh at the top, then settle into the student's record — no tutor housekeeping required (Scarlett, Aug 29)
"Materials from Billy" pins to the top of the family portal forever unless the tutor remembers the remove button — Scarlett's live one is a month old. Redesign the lifecycle so relevance is STATE-DRIVEN, not manually curated: **A. Anchoring:** a material is attached to the student and optionally to a session and/or a due date (tutor's send/attach flow gains "for the {date} session" defaulting to the next scheduled session, and the existing optional due date; neither required). **B. Freshness rules (composed, no tutor action):** the top-of-portal "Materials from {tutor}" card shows a material while it's CURRENT — until its due date passes, or (no due date) until its anchored session is completed, or (neither) for N days since posting (N stated in the code comment, ~7–14, Code picks); overdue-but-recent may show a gentle "was due Friday" line rather than vanishing at midnight. **C. Then it settles, never disappears:** every material remains in a per-student "Materials" section on the family portal (grouped by month or session, showing "given Aug 1 · for the Aug 3 session · due Aug 8" as available) and mirrors on the tutor's view + the admin family hub (Recent-activity pane logs posts). The tutor's remove button stays for genuinely wrong posts — but the default path needs zero housekeeping. **D.** View-as renders identically; content-gated per PL-404 (no Materials section when a student has none). Verify: fixture materials in all three anchor shapes flow top-card → settled correctly as due dates/sessions/N-days pass; the month-old Vietnamese fixture settles on deploy.

**✅ SHIPPED (Aug 29; migration 20260912000002 APPLIED).** Anchors: share flow gains "For the {date} session" (defaults to the target student's NEXT scheduled — verified defaulting to Fakey's Aug 31) + optional due date; `session_id` is a PLAIN uuid (app-side join per the batch-40 PostgREST rule). Freshness (STATE-DRIVEN, N=10 days stated in code, 3-day overdue grace): verified through the whole lifecycle live — current ("for the Aug 31 session · due Sep 2" on the top card) → overdue ("**was due Friday**", gentle, not vanished) → settled (top card gone; the item lives on in the student's Materials record, "given Aug 29 · for the Aug 31 session · due Aug 25", grouped by month, class items included, content-gated per PL-404). **The month-old Vietnamese fixture settled on deploy** — top card gone, item preserved in Roman's record. Tutor mirror = the share panel's list (now anchor-lined) + remove stays; admin family-hub activity feed logs posts (Materials chip, plain-English due dates). View-as renders identically (verified — same components).

## PL-412 — Timecards: test-prep sessions default their pay type; per-session prep time (Scarlett, Aug 29)
**A. Pay-type default from subject:** sessions whose subject is a test-prep exam (SAT/ACT/PSAT — use the PL-368 exam-family word-boundary matcher, never a bare substring, so "French" never matches and PSAT does) default their timecard work-type to **Test-Prep** instead of making tutors change each row by hand. Default only — the dropdown stays editable, and an already-confirmed/edited row is NEVER re-defaulted (tutor's explicit choice wins; apply at row creation/projection, not render). Decide + note whether existing unconfirmed rows get a one-time backfill. **B. Per-session prep time:** prep time is a real pay scale but invisible in the flow. Each timecard session row gains a "Prep time" checkbox that reveals a minutes field, with the plain note "more than 15 minutes per session is uncommon" (a soft warning at >15 — inline note, never a blocker; payroll is hours-only so this feeds the payable hours as its own prep-time line/pay type per the existing payroll CSV pay_type machinery). Deliberately NOT on calendars (Scarlett: it's connected to the session, not scheduled time). Recorded per session (who/when), visible to admin on the timecard review, summed into the period totals separately so the bookkeeper sees prep vs session hours (bookkeeper one-pager updated if its CSV shape gains a line). Verify: subject-default across SAT/ACT/PSAT/French/AP-Calc fixtures; prep entry → payable totals + payroll CSV line; >15 min shows the note; confirmed rows untouched by defaults.

**✅ SHIPPED (Aug 29; migration 20260912000003 APPLIED).** **A:** new STRICT `isTestPrepExamSubject` (word-boundary SAT/ACT/PSAT; `examFamilyFor` untouched — its historical bare-substring SAT branch stays for class titles) defaults `work_type` to **'Test Prep'** at `recomputeTimecard` STAMP time, NULL rows on OPEN cards only. **Backfill DECIDED: yes for open cards** (the next sweep IS the backfill; confirmed/approved untouched). Verified on Billy's real open card: SAT rows NULL→Test Prep; **French/ASVAB/AP-Calc untouched; a pre-set value never re-defaulted**. tutor-hours-report keeps the SUBJECT split for Test-Prep rows (they'd otherwise collapse into a worktype row, losing rate/revenue attribution). **B:** per-session prep minutes — "Prep time" checkbox → minutes field, soft ">15 min is uncommon" note (verified at 20 min; never a blocker), recorded with who/when (`prep_set_by/at`), card totals include prep (21.5→21.83 h live), ONE `prepRows` helper feeds 'Prep Time' into the tutor panel ("Hours by type: Test Prep 4.5 · 1-on-1 14 · 2-on-1 1.5 · **Prep Time 0.33** · Class/Workshop 1.5" — verified), admin summary + clipboard, QBO TimeActivity note, view-as mirror, and the hours report. Deliberately NOT on calendars. **CSV: `session_hours` + `prep_hours` columns added** (total = sum; salaried-last unchanged) and `docs/bookkeeper-payroll-csv.md` now EXISTS (the one-pager batches 22+42 both flagged was never in the repo). QA prep entry cleared after verification; the Test-Prep defaults on the open card stay (they're the shipped behavior).

## PL-413 — Tutor "My classes": tabbed like Live class rosters when teaching more than a couple (Scarlett, Aug 29)
The tutor portal stacks full class cards vertically — fine for 1–2 classes, endless scrolling for Billy. When a tutor has **3+ live classes**, render My classes like the admin's Live class rosters: one tab per class (short marketing name — "Online SAT Math Deep Dive" style — not the full title) with the selected class's card below, plus a "Past" tab collecting ended/cancelled ones (count in the label, like the admin's "Past & cancelled (6)"). With 1–2 classes, today's stacked layout stays exactly as-is (content-gated, same principle as PL-404 — no chrome nobody needs). Tab selection is state-driven and remembered per tutor; deep links into a specific class (from emails/alerts) select the right tab. Composes with PL-404's collapse/menu work — one coherent portal nav, not two competing systems (Code reconciles and notes the shape). View-as renders identically. Verify: Billy-many-classes fixture shows tabs + past bucket; a one-class tutor sees today's layout; deep link lands on the right tab; mobile width.

**✅ SHIPPED (Aug 29).** 3+ live classes → tabs like the admin rosters (short marketing names via `classDisplayLabel` — the one source; "Past (N)" bucket with ended/cancelled stacked); 1–2 classes keep today's stacked layout byte-identically. Selection remembered per tutor (localStorage); `?class={id}` deep links select the right tab and a past/unknown id lands on the Past bucket. **Reconciled PL-404+413 shape (the noted decision): ONE portal nav system — the left menu jumps BETWEEN the big sections (My classes / My tutoring / …), tabs switch classes WITHIN My classes.** Verified live with a 3rd-live-class fixture (created + deleted): tabs render, switch, Past stacks the cancelled AISCT card, deep link + reload-memory both land correctly, 375px wraps to two tab rows with zero overflow; view-as identical (shared TeachingView).

## PL-414 — Admin top nav: hide on scroll-down, return on scroll-up; Sign out joins the bar (Scarlett, Aug 29)
The admin's gray top nav scrolls away — on long pages (rosters, family hub) reaching the menu means scrolling to the very top. Make it a hide-on-scroll sticky bar: scrolls out of the way going DOWN, slides back the moment scrolling UP begins (small threshold so tiny jitters don't flicker it; instant at page top; respects prefers-reduced-motion with a plain show/hide). Applies to the admin chrome everywhere (and the tutor/family portal header if it shares the pattern — check and note). Anchor-jump/deep-link scrolls must not trap the bar in a wrong state; sticky section headers/tabs below it (Live class rosters tabs, Class pages group headers) must not collide — z-index/offset audit. **Yes to Scarlett's question: "Sign out" moves INTO the gray bar** (right-aligned, after the tabs) so the floating link stops living alone on the page body; the "HGL Admin" title row slims accordingly. Verify in real Chrome: long roster page down/up behavior, jump links, 375px mobile (bar height budget), no overlap with the rosters tab row.

**✅ SHIPPED (Aug 29).** The gray topline is sticky (h-10, z-50): hides after ~12px of downward scroll, returns the moment scrolling up begins, ALWAYS shown near the page top (so anchor jumps can't trap it — a downward jump tucks it away, the first upward nudge or the top brings it back); `prefers-reduced-motion` gets a plain show/hide (`motion-safe` transition). **Sign out moved INTO the bar** (right-aligned after the tabs — Scarlett's yes) and the HGL Admin title row slims to just the H1. Z/offset audit: manager-sim + view-as banners re-anchored to `top-10` beneath the bar; container-internal stickies (schedule day headers, campaigns thead, template group headers) unaffected; the rosters tab row isn't sticky and doesn't collide. **Portal header does NOT share the pattern** (static; PortalNav's sticky left menu covers navigation — checked and noted). Verified in REAL Chrome on the long leads page: scroll-down hides, a 2-tick up-scroll returns it, top always shows. (Lesson logged: the embedded preview pane suppresses scroll events entirely — scrollY moves, zero events fire — scroll behavior is real-Chrome-only verification, the batch-36 IntersectionObserver lesson's bigger sibling.)

## PL-415 — HGL logo in the portal chrome, every audience (Scarlett, Aug 29)
The portal headers are text-only ("Higher Ground Learning" / "HGL Admin"). Put the actual logo in the chrome for ALL parties: family portal, tutor/instructor portal, admin (the PL-414 gray bar), sign-in page, and the honest-state/capture pages if they carry the header. Use the existing assets (public/collateral/hgl-logo-color.png / -white.png — pick per background contrast, white on the gray admin bar, color on light headers); sized to the bar (height-capped, never squashed), alt "Higher Ground Learning", clicking it goes to that audience's home (family → portal home, tutor → their portal, admin → dashboard). Replaces or sits beside the text wordmark per what reads best at mobile widths (Code's design call — but the logo must not blow up the PL-414 bar's height budget; at 375px logo-only is fine). Consistent with the /c public pages' logo treatment (PL-375). Verify: all audiences + sign-in at desktop/375px, view-as identical, no layout shift (explicit dimensions).

**✅ SHIPPED (Aug 29).** The real logo in every audience's chrome, all with explicit width/height (zero layout shift): white h-6 in the admin bar (links to the dashboard), color h-8 in the family/tutor portal header (links /portal; the wordmark yields to logo-only below `sm` — 375px is logo+email), color h-10 on sign-in and on ClassStateCard / PublicNotice / EvergreenCapture (the honest-state/capture chrome, consistent with PL-375's /c treatment; /c + /classes heroes already carried logos). View-as renders the view bodies (no portal header by design) and its admin chrome carries the bar logo. Verified at desktop + 375px across admin, portal, sign-in, and the Nido capture page.
