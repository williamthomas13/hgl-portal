# Portal fixes — batch 42 (ACCUMULATING — opened Aug 29, 2026)

**Standing rules:** all prior (see batch 41's header + additions: staff attributions via the one name resolver · staff displays never show IANA ids · gcal events carry hglSessionId, adopt-before-create · drift alerts ring once per signature · date formatters throw on non-ISO in dev · time-anchored one-shots need hour gates · plus the perennial set: composer-path, twins+seeds lockstep, billy@ only, inline confirms, real-renderer previews, review ≠ edit, UI labels not internal names, crashed gate ≠ green gate).

(CLOSED for batch 42 hand-off, Aug 29. Next PL: PL-416.)

## PL-406 — `schools.address` + school maps links (Scarlett, Aug 29 — approving the PL-399 flag)
Add a street-address field to schools (edited in the Schools card editor, plain label "Street address — used for the map link"); the PL-399 maps resolver's school branch uses it (school class + address on record → maps link to the school; still honest absence when blank). Room-only school locations now get a useful pin. Verify: set ISD's address → its page/email maps links point there; blank school unchanged.

## PL-407 — Class timezone must never default to Mexico City (Scarlett, Aug 29)
`CLASS_TIMEZONE` defaults to America/Mexico_City (a relic of the first school). Ops is Salt Lake City: the default anywhere a timezone is assumed (roster-report clock, any class/report fallback) becomes **America/Denver**, labeled "Salt Lake City time" per PL-398. Audit every consumer of the env/default (the batch-24 note flagged the roster-report clock); real classes always carry their own timezone — this is only the fallback. Confirm the env var's production value and record it in ship notes; if the env is already Denver in prod, fix the CODE default to match so dev/prod can't diverge.

## PL-408 — Ship the approved fonts: Source Serif 4 headings / Montserrat buttons (Scarlett approved, Aug 29)
PL-374 shipped body Pontano Sans and left headings/buttons awaiting sign-off — now approved. Apply Source Serif 4 (headings) + Montserrat (buttons/UI accents) on the public pages (/{code} views, /classes, /team, samples, capture pages) via next/font self-hosting like Pontano Sans; weights chosen to match the sqsp site's hierarchy (record which). Check the PL-374 wrap/overflow lessons at 375/768/1280 (a wider face exposed a bug last time). Admin unchanged.

## PL-409 — Payroll/timecard headers say "Salt Lake City time" (Scarlett, Aug 29 — the PL-398 inverse gap)
Timecard emails, the portal timecard panel, and admin payroll/timecards headers state pay-period dates/times with NO timezone. Payroll anchors America/Denver: label the period headers (and any specific times shown) "Salt Lake City time" via staffTimeCityLabel — labeling only, zero boundary-math changes (the Denver anchoring is correct and stays). Registry rule if T5 copy changes (new version, twin+seed).

## TASK (not a PL) — Test-send every draft template → billy@ for Scarlett's ramp review
Composer-path renders of ALL drafts awaiting ramp (AL_CLOSE_MATCH · BL_CONTINUE_OUTCOME · T6_NOTES_WEEKLY · IN_MIN_DECISION · T2B v2 · SR_PAYMENT_LINK · BL v2 autopay variants), realistic fixture contexts (both autopay cases where relevant), sent → billy@ with clear [DRAFT REVIEW: {key}] subjects tags. List what was sent in the batch doc so Scarlett can tick them off against her inbox.

## PL-410 — Google Calendar push notifications: near-instant drift detection (Scarlett, Aug 29 — un-deferred)
Replace polling-only drift detection with Google push: register a watch channel per connected tutor calendar (events.watch) pointing at a portal webhook (public HTTPS route; validate Google's channel token/id headers — treat unverifiable posts as noise, never as data). On a push, run the SAME audit for that calendar (the push is a doorbell, not a payload — Google sends no event details; re-fetch and diff exactly as the audit does today), so banners/Needs Attention/grouped alerts (PL-402 machinery unchanged) land within a minute of Billy's edit instead of up to an hour. **Channel lifecycle:** channels expire (~week max) — a renewal sweep on the existing hourly cron re-arms expiring channels and re-creates missing ones (state in a table: channel id, resource id, calendar, expiry); disconnecting a tutor's gcal stops its channel. **The hourly poll STAYS as backstop** (missed pushes, downtime, channels Google silently drops) — push is an accelerator, not a replacement; ship note states both paths run. Debounce: a burst of pushes for one calendar coalesces (a mass-delete like Billy's Aug-18 cleanup = one audit pass + ONE grouped email, not 13). Env/URL: the webhook rides the current vercel host and must survive the domain cutover (emailBaseUrl-style pinning; add a cutover-checklist line to re-verify channels after DNS). Verify E2E against real Google: move an event → banner + grouped email within ~1 min; expiry simulation → renewal re-arms; webhook rejects a forged post; poll still catches a synthetic missed push.

## PL-411 — Family materials: fresh at the top, then settle into the student's record — no tutor housekeeping required (Scarlett, Aug 29)
"Materials from Billy" pins to the top of the family portal forever unless the tutor remembers the remove button — Scarlett's live one is a month old. Redesign the lifecycle so relevance is STATE-DRIVEN, not manually curated: **A. Anchoring:** a material is attached to the student and optionally to a session and/or a due date (tutor's send/attach flow gains "for the {date} session" defaulting to the next scheduled session, and the existing optional due date; neither required). **B. Freshness rules (composed, no tutor action):** the top-of-portal "Materials from {tutor}" card shows a material while it's CURRENT — until its due date passes, or (no due date) until its anchored session is completed, or (neither) for N days since posting (N stated in the code comment, ~7–14, Code picks); overdue-but-recent may show a gentle "was due Friday" line rather than vanishing at midnight. **C. Then it settles, never disappears:** every material remains in a per-student "Materials" section on the family portal (grouped by month or session, showing "given Aug 1 · for the Aug 3 session · due Aug 8" as available) and mirrors on the tutor's view + the admin family hub (Recent-activity pane logs posts). The tutor's remove button stays for genuinely wrong posts — but the default path needs zero housekeeping. **D.** View-as renders identically; content-gated per PL-404 (no Materials section when a student has none). Verify: fixture materials in all three anchor shapes flow top-card → settled correctly as due dates/sessions/N-days pass; the month-old Vietnamese fixture settles on deploy.

## PL-412 — Timecards: test-prep sessions default their pay type; per-session prep time (Scarlett, Aug 29)
**A. Pay-type default from subject:** sessions whose subject is a test-prep exam (SAT/ACT/PSAT — use the PL-368 exam-family word-boundary matcher, never a bare substring, so "French" never matches and PSAT does) default their timecard work-type to **Test-Prep** instead of making tutors change each row by hand. Default only — the dropdown stays editable, and an already-confirmed/edited row is NEVER re-defaulted (tutor's explicit choice wins; apply at row creation/projection, not render). Decide + note whether existing unconfirmed rows get a one-time backfill. **B. Per-session prep time:** prep time is a real pay scale but invisible in the flow. Each timecard session row gains a "Prep time" checkbox that reveals a minutes field, with the plain note "more than 15 minutes per session is uncommon" (a soft warning at >15 — inline note, never a blocker; payroll is hours-only so this feeds the payable hours as its own prep-time line/pay type per the existing payroll CSV pay_type machinery). Deliberately NOT on calendars (Scarlett: it's connected to the session, not scheduled time). Recorded per session (who/when), visible to admin on the timecard review, summed into the period totals separately so the bookkeeper sees prep vs session hours (bookkeeper one-pager updated if its CSV shape gains a line). Verify: subject-default across SAT/ACT/PSAT/French/AP-Calc fixtures; prep entry → payable totals + payroll CSV line; >15 min shows the note; confirmed rows untouched by defaults.

## PL-413 — Tutor "My classes": tabbed like Live class rosters when teaching more than a couple (Scarlett, Aug 29)
The tutor portal stacks full class cards vertically — fine for 1–2 classes, endless scrolling for Billy. When a tutor has **3+ live classes**, render My classes like the admin's Live class rosters: one tab per class (short marketing name — "Online SAT Math Deep Dive" style — not the full title) with the selected class's card below, plus a "Past" tab collecting ended/cancelled ones (count in the label, like the admin's "Past & cancelled (6)"). With 1–2 classes, today's stacked layout stays exactly as-is (content-gated, same principle as PL-404 — no chrome nobody needs). Tab selection is state-driven and remembered per tutor; deep links into a specific class (from emails/alerts) select the right tab. Composes with PL-404's collapse/menu work — one coherent portal nav, not two competing systems (Code reconciles and notes the shape). View-as renders identically. Verify: Billy-many-classes fixture shows tabs + past bucket; a one-class tutor sees today's layout; deep link lands on the right tab; mobile width.

## PL-414 — Admin top nav: hide on scroll-down, return on scroll-up; Sign out joins the bar (Scarlett, Aug 29)
The admin's gray top nav scrolls away — on long pages (rosters, family hub) reaching the menu means scrolling to the very top. Make it a hide-on-scroll sticky bar: scrolls out of the way going DOWN, slides back the moment scrolling UP begins (small threshold so tiny jitters don't flicker it; instant at page top; respects prefers-reduced-motion with a plain show/hide). Applies to the admin chrome everywhere (and the tutor/family portal header if it shares the pattern — check and note). Anchor-jump/deep-link scrolls must not trap the bar in a wrong state; sticky section headers/tabs below it (Live class rosters tabs, Class pages group headers) must not collide — z-index/offset audit. **Yes to Scarlett's question: "Sign out" moves INTO the gray bar** (right-aligned, after the tabs) so the floating link stops living alone on the page body; the "HGL Admin" title row slims accordingly. Verify in real Chrome: long roster page down/up behavior, jump links, 375px mobile (bar height budget), no overlap with the rosters tab row.

## PL-415 — HGL logo in the portal chrome, every audience (Scarlett, Aug 29)
The portal headers are text-only ("Higher Ground Learning" / "HGL Admin"). Put the actual logo in the chrome for ALL parties: family portal, tutor/instructor portal, admin (the PL-414 gray bar), sign-in page, and the honest-state/capture pages if they carry the header. Use the existing assets (public/collateral/hgl-logo-color.png / -white.png — pick per background contrast, white on the gray admin bar, color on light headers); sized to the bar (height-capped, never squashed), alt "Higher Ground Learning", clicking it goes to that audience's home (family → portal home, tutor → their portal, admin → dashboard). Replaces or sits beside the text wordmark per what reads best at mobile widths (Code's design call — but the logo must not blow up the PL-414 bar's height budget; at 375px logo-only is fine). Consistent with the /c public pages' logo treatment (PL-375). Verify: all audiences + sign-in at desktop/375px, view-as identical, no layout shift (explicit dimensions).
