# Portal fixes — batch 38 (ACCUMULATING — opened Aug 15, 2026)

**Standing rules:** plain-English statuses · no internal shorthand · every alert deep-links its record + click-through to a resolution surface · samples from composers · `git push` after committing · PL-x IDs in commits · check items off here when shipped · registry template edits ship as new versions via anchor guards, twins+seeds lockstep · verify composed blocks via the composer path · inline confirm banners only · NO native browser dialogs · schedule-change baseline = what the family SAW at registration · financial facts never render into marketing copy or structured markup (incl. /c pages) · two-phase column drops incl. post-drop grep · **test/QA email → billy@ / billy+*@ ONLY** · crashed gate ≠ green gate · public time labels = city names, never IANA zone cities.

(Empty — accumulating. Next PL: PL-360, after batch 37's PL-354–359 ship.)

## ✅ PL-360 — Location preposition: "at 380 W. Pierpont Ave", "in Room 204" (Scarlett, Aug 15, seen in a test email)
**Shipped:** `locationPreposition()` inside the ONE tail helper (`classLocationTailText`, comms-variables.ts): leading street number → "at"; room-ish prefixes (Room/Rm./Hall/Library/Lab/Auditorium/Gym(nasium)/Cafeteria/Classroom/Suite, with an optional leading "the") → "in"; anything else → "at" (named venues: "at the Community Center", "at Salt Lake City Public Library"). Online branch and the missing-location "in TBD" untouched. Every consumer inherits (E4/E5's {classLocationLine}, {classDetailsBlock}, entry hints via `classLocationSentence`) — editor samples resolve live through the composer, so no stored sample could drift. Verified via the composer path: rendered the two live templates carrying {classLocationLine} (E4_CLASS_DETAILS v4, E5_LOCATION v5) against a Room-204 class and a 380-W.-Pierpont-Ave class — 8/8 asserts (in/at correct, wrong preposition absent in both). Test-sent all four renders → billy@ (E4+E5 × room+street), all `sent`.
`classLocationTailText/Html` (comms-variables.ts ~363/1130 + the {classLocationLine} variable) hardcodes "in" for in-person locations — right for rooms ("in Room 204"), wrong for street addresses ("in 380 W. Pierpont Ave"). Add a preposition heuristic in the ONE tail helper: location starts with a street number (leading digits + space) → "at"; room-ish (Room/Hall/Library/Lab/Auditorium/Gym/Cafeteria prefixes) → "in"; otherwise "at" (safe default for named venues: "at the Community Center"). Online branch untouched. Since it's one helper, every consumer (E0/T_SCHEDULE_SET seeds, decision briefs, {classLocationLine}) inherits the fix — update the editor samples via the composer path and re-render the registered templates' samples so samples can't drift (PL-234 rule). Test-send → billy@ with both a room and a street-address class.

## ✅ PL-361 — Staff-assisted enrollment: add someone to a class from the admin, info + payment (Scarlett, Aug 15)
**Shipped** (migration `20260908000010`→`20260908000012` APPLIED): **A.** "Register a family" on every live class card opens a search-FIRST panel (parent/student name or email → pick the existing family/student, prefilled — names-are-doors, `upsertFamilyAndStudent` is still THE one path so no duplicate rows) or capture new (the registration form's fields incl. pronouns/grad year/accommodations/scores). Class state is the server's verdict: **cancelled → refused** (410, and the button doesn't render on cancelled cards) · **past deadline → 409** with an inline-confirmed "I know — register anyway" that records who/when (`deadline_override_by/at`) · **full → 409** offering "waitlist them instead" (creates a real Waitlisted row, W1 confirmation with position, same as the online join). One student per pass — register siblings by running it again (same family row attaches). **B.** Payment = two paths, never card-by-phone: **(1) Email the payment link** — new registered template `SR_PAYMENT_LINK` (seeded DRAFT v1 via scripts/seed-pl361-sr.mjs, code twin `staffPaymentLinkEmail` in email.ts sends the same copy until Scarlett flips it) carrying the SAME signed resume-checkout link machinery PR1-4 use → the family lands in the same Stripe checkout, and paying flips everything through the same webhook. **(2) Record offline payment** — check/bank (amount defaults to the record price, editable to what was actually received; note for check #) or comp ($0, reason REQUIRED); marks Paid by running `handleClassCheckoutCompleted` — the IDENTICAL webhook handler — via a synthetic offline session, so confirmations/lifecycle/QBO/milestones are structurally the same; recorded who/when/method/note on the enrollment; **QBO gets the sale with a deterministic `offline_…` reference and an honest "Offline payment recorded in the portal" private note (comps post nothing)**; roster shows "paid by check / comp ($0)" chips with the note+recorder in the tooltip. **C.** Downstream identical by construction: same Pending insert, same PL-314 baseline stamp (what the family was told at THIS registration), same comms pass + close-match scan behind the response, min-enrollment counts unchanged (Pending/Paid counting logic untouched). `source='staff'` + `source_recorded_by`; roster rows carry a "staff signup" chip and the term report's class rows show "N by staff" beside the enrolled count. **D.** A staff-created Pending older than 3 days → Needs Attention row ("Staff registration awaiting payment", deep-links the roster) and the roster row itself carries **resend payment link** + inline-confirmed **cancel** (→ Expired, the seat frees, no email). Verified: 22/22 E2E asserts on the prod build via the staff-cookie harness (create/source/baseline · SR send w/ by-hand badge · offline check $480 → Paid + confirmation + QBO row w/ offline ref · comp $0 → Paid, NO QBO row, re-record refused · full→waitlist→W1 pos 1 · deadline 409→override recorded · cancelled 410 · cancel→Expired), self-cleaned QA fixtures (billy+ only), tsc clean.
For phone signups and families who can't register online. A "Register a family" action on the class card / roster:
**A. Info.** Search existing families/students first (names-are-doors; no duplicate rows) or capture new (parent, student, emails, school/grade — the registration form's fields, staff-entered). Class pick respects real state: capacity → offer waitlist, past deadline → staff can override with an inline-confirmed "I know" (recorded who/when), cancelled → refuse. Price composes from the record (promo code applicable); provenance rules unchanged.
**B. Payment — two paths, NEVER card-by-phone into our UI (PCI + policy):**
1. **Send a payment link:** creates the enrollment Pending and emails the family the same Stripe checkout the online flow uses (registered template, draft+twin; reuses the existing checkout/webhook machinery — paid flips it Paid exactly like online registration, same emails, same QBO posting). Staff reads the link aloud or the family clicks from email.
2. **Record offline payment:** check/bank/comp — marks Paid with method + note + who recorded it (plain-English in the family's history; feeds QBO the same as other paid enrollments; comp = $0 with reason).
**C. Downstream identical to online registration:** confirmation + lifecycle emails fire the same (schedule-change baseline = what the family was told at THIS registration), min-enrollment counts include them, roster/attendance/waitlist all normal. The enrollment records source = 'staff' (+ who) so the report page can distinguish assisted signups.
**D. Abandoned pending:** staff-created Pending past N days without payment → Needs Attention row (deep-link, resend-link / cancel actions), not silent limbo.

## ✅ PL-362 — Autopay nudges: at signup and on EVERY invoice email to non-autopay families (Scarlett, Aug 15)
**Shipped.** **D — the inventory (every pre-existing autopay ask, as found):** (1) **T2_INVOICE** `{autopayBlock}` — composed inline in `t2Extras` (tutoring-stripe.ts), guarded only structurally (autopay families never receive hosted invoices; the block itself had no guard); (2) **T2's code twin** `t2InvoiceEmail` — the same copy hand-written a second time; (3) **T8_WELCOME_HANDOFF** — a HAND-WRITTEN sentence with `{autopayLink}` in the live v4 body **with NO autopay-family guard** (a returning family already on autopay was still pitched); (4) **T8's code twin** (intake-emails.ts) — same unguarded hand-written copy; (5) **the portal billing card** (portal/tutoring-section.tsx) — correctly guarded, shows "Autopay is on" when set — left as-is; (6) **AL_DUNNING_EXHAUSTED** — admin alert naming autopay (not a family ask — untouched); (7) **T4_PAYMENT_FAILED** — "update your saved payment method" (autopay MANAGEMENT for a failing card, not an ask — untouched). **A — ONE composed block:** new leaf `app/utils/autopay-nudge.ts` — `autopayNudgeHtml(family, tone)` returns '' for autopay families (never nag the converted), else one consistent pitch + the family's tokenized consent link (the existing /tutoring/autopay/{token} page — placement, not new consent machinery); `autopayNudgeCopyHtml` is the pure copy source the editor sample computes from (PL-96 drift guard). **B — templates that GAINED the block (new versions, twins+seeds lockstep, active bodies patched under exact-anchor guards — publish script refuses if Scarlett's edits moved the anchor):** T1_MONTHLY_PROPOSAL v3 (live) · T1B_PROPOSAL_NUDGE v2 (live) · T2B_PAYMENT_REMINDER v2 (draft — "never think about this again" on a reminder) · T8_WELCOME_HANDOFF v5 (live; the hand-written sentence → `{autopayBlock}`, now correctly empty for autopay families) · BL_CONTINUE_OUTCOME v2 (draft — the block-continue → monthly transition). T2_INVOICE needed NO new version (already bodies `{autopayBlock}`); its composer + twin now feed from the one source. **Deliberately skipped, with reasons:** payment **receipts** — the portal sends NO receipt email for paid tutoring invoices (Stripe's own hosted-invoice receipt isn't ours to template; doc premise correction — flagging for Scarlett in case she wants a portal receipt email someday) · BL_BLOCK_CONFIRM (it IS the continue ask — one ask per email) · T4_PAYMENT_FAILED + dunning/AL contexts (wrong tone: their saved method is failing) · refund/agreement emails (wrong tone per the doc) · class-side confirmations (not invoice emails; autopay is a tutoring concept). **C — signup placement:** the schedule-confirm page's just-said-yes state (both the fresh "Locked in — thank you!" and the already-confirmed state) now offers "Set up autopay →" via the family's tokenized link — never shown to families already on autopay; consent page unchanged. **Verified:** 22/22 composer-path asserts (each template's active body carries `{autopayBlock}` exactly once and zero hand-written asks; live templates rendered both cases — non-autopay = exactly ONE "Set up autopay", autopay family = ZERO; DB-wide sweep: no active body hand-writes the ask or still uses `{autopayLink}`); both-case test sends → billy@ (T2 with ask · T2 autopay-family without) both `sent`; regress:links 130 PASS / 0 FAIL; tsc clean.
More autopay = fewer chased payments. Partial machinery exists (T2 has {autopayBlock}, T8 carries {autopayLink}) — complete and make it systematic:
**A. ONE composed nudge block** (site-md'd, registered as a block variable, one source): short pitch + the family's tokenized autopay consent link (existing /tutoring/autopay/{token} page). Composes EMPTY for families already on autopay — never nag the converted — and empty where a nudge would be wrong (dunning-exhausted alerts, refund/agreement emails).
**B. Audit every invoice-family email** and include the block for non-autopay families: T1 proposal, T1B proposal nudge, T2 invoice, T2B payment reminders (a natural fit — "never think about this again"), payment receipts ("want this automatic next month?"), and the block-continue → monthly transition (BL_* where the family is choosing ongoing billing). List in the ship notes exactly which templates gained it and which were deliberately skipped, with reasons.
**C. Signup:** the tutoring signup/confirm flow (schedule confirm page + T8-family moment) offers autopay enrollment at the point of saying yes — the highest-intent moment. Existing consent page unchanged; this is placement, not new consent machinery.
**D. FIRST: inventory the existing autopay asks (Scarlett) — this consolidates, it doesn't pile on.** Before adding anything, grep templates/twins/seeds/portal pages for every place that already tells families to sign up for autopay (known: T2's {autopayBlock}, T8's {autopayLink}; there are likely more hand-written mentions in template copy and possibly the portal billing card / schedule-confirm page). List them all in the ship notes. Then REPLACE hand-written mentions with the ONE block so no email ever carries two autopay asks and no copy drifts — the outcome is exactly one ask per email, consistent wording, composed empty for autopay families everywhere (audit existing mentions for that guard too; a hand-written line wouldn't have it).
**E. Registry rule applies:** template copy changes ship as new versions, twins+seeds lockstep, samples re-rendered via composer path, test sends → billy@ (one autopay family = no block; one non-autopay = block present).

## ✅ PL-363 — Cutover: mid-flight class import + decommission checklist (Scarlett, Aug 15)
**Shipped.** **A — `scripts/import-class-registrations.mjs`:** imports a class's registrants from the Sheets/MailerLite CSV shape — column mapping interactive on a TTY (auto-guessed, saved to a reusable file) or via `--mapping file.json` (single full-name columns split automatically); families/students go through `upsertFamilyAndStudent` (THE one path — dedupes against QBO-imported and existing families by parent email, no duplicate rows); enrollments land Paid (CSV paid column or `--all-paid`; amount + `class_price_paid` from the CSV amount or the record; `enrolled_at`/`paid_at` from the CSV timestamp so history reads true) / Pending / **Waitlisted** (waitlist column); `source='import'` + who ran it. **The baseline is STAFF-SUPPLIED and required**: `--baseline file.json` = the schedule those families were shown at THEIR registration (location + sessions; verified importing a baseline that differs from the current schedule), or an explicit `--baseline-current`. **Imports fire NO emails** — nothing sends at import, and per Paid row the whole confirmation flow + every ALREADY-DUE sequence step is claimed via cancelled email_sends rows (the same fold mechanism as the LR welcome / PL-222), so lifecycle joins IN PROGRESS: future steps fire on their natural schedule, nothing retroactive. Imported Paid rows deliberately do NOT enqueue QBO (the old system already booked that revenue — importing would double-book). **Imported Pendings are exempt from the PR ladder AND the 168h expiry** (their original registration dates would have fired all four reminders and expired them on the first sweep — the reminders cron + comms projector both skip `source='import'`); instead they surface on **Needs Attention** ("Imported registration awaiting payment") and the roster row carries the PL-361 send-payment-link / cancel actions (extended to imports). Idempotent by student+class. **B — handoff:** the final sweep IS the same import re-run (verified: second run created 0, skipped 4); form/button repointing recorded in the checklist. **C — `docs/cutover-decommission-checklist.md`** records the full sequence per the phased-launch note (class imports = phase 1; QBO family import = phase 2; decommission = launch tail after 1–2 stable cycles): MailerLite export incl. **UNSUBSCRIBES → `scripts/import-mailerlite-suppressions.mjs`** (new; lands them in `marketing_suppressions`, the PL-201 choke-point gate, BEFORE any campaign sends — verified 2 added/idempotent re-run), sqsp **DOWNGRADE not cancel** (gated on the PL-364 sandbox round-trip), Zapier per plan. **Verified:** 12/12 asserts on a fixture class with a mid-flight schedule (mixed CSV: paid/pending/waitlist/duplicate/bad rows → 3 created with correct states + CSV-true timestamps + staff-supplied baseline; ZERO emails sent or scheduled; exactly the 11 due-step claims, future steps unclaimed; zero QBO rows; re-run fully idempotent), fixtures self-cleaned; tsc clean.
**A. Mid-flight class import.** The real classes arrive at different stages: two FULL starting next month, two TAKING REGISTRATIONS (MailerLite forms + Google Sheets accruing), one OPEN with no signups. Extend the cutover tooling: import registrants for an existing class from a CSV (the Sheets/MailerLite export shape — map columns interactively or via a small mapping file), creating families/students (dedupe against QBO-imported families by email — no duplicate rows) and enrollments in the right state (Paid where they've paid via sqsp; Pending otherwise). **CRITICAL: imports fire NO emails** — no welcome sequences, no confirmations; these families already registered and were told everything by the old system. Imported enrollments record source='import' + the schedule they were shown AT THEIR ORIGINAL REGISTRATION as the schedule-change baseline (staff supplies it per class — the standing baseline rule depends on this). Lifecycle emails join IN PROGRESS (e.g. T-minus reminders fire from their natural next point, nothing retroactive — same fold rule as PL-222/T8). Waitlist state importable for the full classes if any exists.
**B. Registration handoff for the two accruing classes:** at cutover their MailerLite forms/sqsp buttons repoint to the portal registration; final CSV import sweeps anyone who registered in the gap; dedupe makes a double-import harmless (idempotent by email+class).
**C. Decommission checklist (launch-tail addition, AFTER 1–2 stable cycles, not at cutover):** MailerLite — export all lists + consent history + UNSUBSCRIBES (suppression list must land in portal unsubscribe handling before any campaign sends), replace sqsp signup forms w/ portal-leads capture, then close. Squarespace — DOWNGRADE from Commerce to a site-only plan (the brand site stays); export order history first; **Printful notebooks: DECIDED (Scarlett, Aug 15) — option 1, portal add-on w/ Printful API. See PL-364.** Zapier per existing plan.

## PL-364 — Notebook add-ons in the registration flow, fulfilled via Printful (Scarlett, Aug 15 — the PL-363 decision)
**A. Physical add-ons.** The registration flow's second page (where 1-on-1 hours live) gains physical add-on products: the two notebooks (WTF Spiral, No Cramming Spiral — names/prices from a small products table, price-list-adjacent admin editing; sale pricing supported: "$35.00, regularly $48.00" composed, never hand-typed). Requires a shipping address at checkout when a physical add-on is in the order (families abroad: check Printful shipping coverage per country and say so honestly rather than failing at fulfillment).
**B. Printful fulfillment.** On payment confirmed (existing webhook), push the order to Printful's API (variant mapping stored per product; idempotent by enrollment+product so webhook retries can't double-order). Store the Printful order id + status; surface fulfillment state plainly on the enrollment/family record ("Notebook: shipped Aug 20, tracking …") and to the family in the receipt/confirmation (tracking when Printful provides it — their webhook or polling, whichever is simpler).
**C. Failure honesty.** Printful API failure ≠ lost order: enrollment still completes, a Needs Attention row surfaces the unfulfilled add-on (retry action; deep-link), never silent. Refund of an add-on (staff-marked) cancels the Printful order if not yet shipped.
**D. Scope fence.** Class-registration add-ons ONLY (that's where they sold anyway) — no standalone merch store, no cart. Squarespace Commerce can downgrade on the PL-363 schedule once this ships and one real order round-trips in Printful's sandbox/test mode. API key via env (launch-tail secret alongside the others), never in the repo.

## ✅ PL-365 — Team-page verdicts (Scarlett, Aug 15)
From `docs/pl358-flagged-team-entries.json`: CREATE Kelsie Rank and Linden Hughes (fill their profiles from the preserved data — headshots, credentials, bios, order; this also finally seeds the PL-283 pair). SKIP Trevor Smith, Kevin Fober, Kelsey Lee (leave the JSON as the record). Public name = **William Thomas** — the /team page and class-page cards display "William Thomas" while the portal row stays Billy internally (display-name field or equivalent; don't rename the row — timecards/QBO matching depend on it). R/W Deep Dive "Rome" label: fine as-is, class isn't real.
**Shipped:** (1) CREATED Kelsie Rank (team_order 3, "Operations Director", 320/512w webp headshot) and Linden Hughes (team_order 11, no credential per the record, 320/640/1200w webp headshot) — full bios from the JSON, `active=true, tutoring_active=false` (the non-teaching-staff pattern), `show_on_team=true`, `featured_on_classes=false`, and their PL-283 calendar colors finally landed (Kelsie Basil #0B8043, Linden Blueberry #3F51B5). Headshots rode the PL-351 pipeline into the class-pages bucket under team/{id}. **Emails follow the org convention (kelsie@ / linden@highergroundlearning.com — every existing row is firstname@; Scarlett: correct in the profile editor if either differs.)** Trevor/Kevin F./Kelsey skipped; JSON stays as the record. (2) Migration `20260908000010` (APPLIED): `instructors.public_name` — PUBLIC surfaces (/team incl. its Person JSON-LD, /c featured cards) render `public_name ?? name`; Billy's row set to "William Thomas" while `name` stays Billy for timecards/QBO. Editable in the profile editor's Public profile fieldset ("Public display name", blank = internal name). Verified live: /team = 16 cards in order, "William Thomas" present / "Billy Thomas" absent (page + JSON-LD), both new profiles rendering with headshots; ISD class page still renders exactly Eric/Jason/Kevin/Alexa. R/W Deep Dive "Rome": left as-is per verdict.

## LAUNCH-PLAN NOTE (Scarlett, Aug 15 — supersedes "Aug 20 first real generation" as a hard date)
Phased launch: **classes go live first** (class pages, registration, shortlinks, class comms); **1-on-1 tutoring cuts over separately, later** (monthly generation, autopay, tutoring imports). Aug 20 is no longer the forcing date. Resend paid remains blocking for WHICHEVER phase sends real email first (classes). Sequence the PL-363 import/decommission work accordingly: class-side imports (mid-flight classes) come with phase 1; QBO family import for tutoring can ride phase 2.

## ✅ PL-366 — Seed the Deep Dive course block set (gap found by Scarlett, Aug 15)
**Shipped:** the sqsp source turned out to be a HIDDEN Squarespace product page — not in the sitemap, site search, store catalog, or Google; found via the Squarespace admin (product "March SAT Deep Dive: Mastering Advanced Math Concepts", page `/classes/p/advanced-sat-math`). Migration `20260908000011` (APPLIED) mints the `sat-deep-dive-mastering-advanced-math-concepts` course set with the page's copy VERBATIM: hero-blurb (the course pitch paragraph), "Stamp Your Passport to Advanced Math" (both paragraphs), "Sample of Topics Covered" (all 17 topics), "Advanced Problem-Solving Techniques" (both paragraphs) — all `updated_by NULL` so the admin flags them unreviewed. Both section images ported per the PL-351 pipeline (480/960/full webp in class-pages bucket; passport→layout right, topics→layout left, descriptive alts). **FLAG for Scarlett:** the sqsp page's passport-section photo is the SAME stock image PL-354 attached to the shared "included-tutoring" card, so the Deep Dive page now shows it twice (once in What's included, once beside Stamp Your Passport) — ported honestly; swap either image in the admin if the repeat reads badly. The section-banner image under the sqsp "Master the Most Advanced SAT Math Concepts" H2 was NOT ported (our /c pages have their own hero); the March-2026 calendar image and the instructor headshots were skipped (dated / already profile-driven). Verified live on the real winter Deep Dive page: all four blocks + both images render (10/10 probes). **Sweep — course keys with NO course set (report, not invented):** `sat-deep-dive-mastering-the-reading-and-writing-sections` (the R/W Deep Dive — its sqsp sibling page, if one exists, was not found among the hidden products; only the Math page exists) and `psat-prep` (the fall PSAT class — no sqsp source page found; needs Scarlett's copy or a mint-from-skeleton in the admin). Both render shared blocks only until their sets exist.
The PL-355 machinery exists but the SAT Math Deep Dive course set was never created — the Squarespace Deep Dive page's course content was never ported (batch 36 ported shared evergreen; batch 37 seeded only the ACT set). Mint the course set for the Deep Dive course_key and port from the live sqsp page (https://highergroundlearning.com — the SAT Deep Dive: Mastering Advanced Math Concepts page): "Stamp Your Passport to Advanced Math" blurb, "Sample of Topics Covered" list, "Advanced Problem-Solving Techniques" section, + their images per the PL-351 pipeline. Seeded unreviewed (updated_by NULL) for Scarlett's walkthrough. Sweep: list every OTHER course key in the DB with no course set so nothing else is silently sectionless (report, don't invent content).

## ✅ PL-367 — Class-pages admin: organization + live preview (Scarlett, Aug 15)
**Shipped:** **A.** Settings → Class pages is now collapsible groups in page-render order: **Shared — every class page** (its sections as sub-headings, render order) → one group per **course set** labeled with the course's real display name (from the newest class of that key; prettified key when none) → "Mint a new course block set" directly beside/after the course groups → per-class blocks → **Registration flow only** last (the 1-on-1 pitch — no longer mixed into the page sections). Every group header carries a "N not yet reviewed" amber badge (updated_by NULL count) and groups with unreviewed seeds start OPEN so the badges point at visible work; open/closed is state-driven (typing in a card can't snap groups shut). **B.** Each group header has "Preview a page using these blocks →" (new tab) AND "preview here" — an inline iframe of THE SAME URL with a refresh button; every successful block save (and image change) remounts any open iframe, so edit → save → see is one surface. Preview targets resolve server-side in the GET: newest open class of the course key (Deep Dive → the real winter page), newest open school class for Shared (→ ISD), the newest open class's /register page for flow-only (labeled "appears on its second step"). **A set with NO class previews `/c/sample--{course-key}` — the REAL page component over a clearly-bannered synthetic class** (amber "SAMPLE PAGE — made-up class facts" banner, registration disabled, noindex, no JSON-LD, no analytics beacons; course blocks inherit by key exactly as on a real class; never a DB row). Verified on the prod build with a real staff session: group collapse/expand, badges (shared 16 · act-prep 5 "previews a labeled sample class" · deep dive 4 · flow 1), inline iframe rendered the real winter Deep Dive (passport/topics probes inside the frame), refresh remounts the frame; sample pages verified by probe (banner, noindex, no JSON-LD, `href="#"` register, act-prep/psat-prep render shared-only, Deep Dive sample renders its course set); API payload verified 10/10 via the staff-cookie harness.
**A. Organization.** Settings → Class pages currently reads as one long list. Structure it: collapsible groups — **Shared (every page)** · one group per **course set** (labeled with the course's display name, "Mint a new course block set" beside them) · flow-only blocks last — each group header carrying an unreviewed-count badge ("3 not yet reviewed") so Scarlett can see at a glance where her attention is needed. Within groups, blocks in render order.
**B. Live preview.** She shouldn't have to envision the layout: (1) each group header gets **"Preview a page using these blocks →"** — opens the real rendered /c page of a live class using that set (dark pages are directly viewable by URL; pick any class with the course key, or the newest live school class for Shared), in a new tab; (2) an inline **preview pane** in the admin (iframe of that same page, refresh button + auto-refresh on save) so edit → save → see is one surface. The preview IS the real renderer — never a parallel approximation (editor-samples-vs-composer rule, page edition). If no class exists for a set, render the preview against a clearly-labeled sample class (synthetic props, never a DB row).
