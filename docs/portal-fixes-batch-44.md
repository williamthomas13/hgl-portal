# Portal fixes — batch 44 (ACCUMULATING — opened Aug 30, 2026)

**Standing rules:** all prior (see batch 41/42/43 headers).

(CLOSED for batch 44 hand-off, Aug 31. Next PL: PL-431. Batch 43 [PL-416–417] shipped separately.)

## ✅ PL-418 — SHIPPED (Aug 31)
SessionCalendar's internal fallback now IS the one resolver (`publicTimeCityLabel({location, timezone})` — `friendlyZoneCity` retired from the component; identical output by construction for label-less callers), and every portal call site passes the class's FULL facts: parent-view, instructor-view (tutor), and CounselorClassCard (both its callers — the signed-in counselor view AND the tokenized /class-roster page — queries widened with `schools.city` + `display_cities` + timezones). A "Room 204" HGL in-person class now says "Salt Lake City", never the zone city "Denver" (`hglInPerson` fact wired). Tutor + counselor calendars previously rendered NO zone line at all (no timezone passed) — they now carry the honest resolver label (verified live: "(times shown in Cape Town and Düsseldorf time)" on the PSAT open class in the tutor view). Admin SessionCalendar call sites (admin page, wizard preview) unchanged — they pass no timezone and render no line, as before. Coordinated with PL-419: the resolver label is the family view's secondary/fallback exactly as specified.

## PL-418 (original) — SessionCalendar joins the one time-label resolver (Scarlett, Aug 30 — the batch-39 residual, promoted)
The portal SessionCalendar (family + tutor views) still labels times via the old location-heuristic fallback — a "Room 204" HGL class can read "Denver time" on the page families visit most, while every email and public page says "Salt Lake City time". Point it at `publicTimeCityLabel` with the class's full facts (school city, display_cities, location, hglInPerson), like the six call sites PL-382 converged. NOTE: PL-419 may supersede the family-facing label with device-local times — coordinate: the resolver label remains the fallback/secondary either way, so do this convergence regardless.

## ✅ PL-419 — SHIPPED (Aug 31) — **PREMISE CORRECTION: `families.timezone` has existed since phase 7a (20260713000001) — NO migration needed.** The column was read by the whole tutoring email path but NEVER WRITTEN; the real work was the write paths + class-side consumption.
**A. Portal:** SessionCalendar is now a client component with a `localTimes` mode (family view only) — post-mount, when the device zone differs from the class's, each row converts date chip + weekday + times to the BROWSER's zone with the class-city time labeled alongside ("1:00–3:00 AM your time · 10:00 AM–12:00 PM in Düsseldorf", footer "(times in your local time — the class itself runs on Düsseldorf time)"); SSR and the hydration paint keep the class-zone render exactly, so nothing flashes wrong and matching zones change nothing (verified live: Berlin ISD class on a Vancouver-zone browser). Tutoring card: new `local-time.tsx` client leaf (`LocalDay`/`LocalTimeRange`/`LocalZoneNote` + `useDeviceTimezone`) converts the Next-session line, the Upcoming list, and the reschedule drawer to the device zone; weekly-pattern chips keep the tutor's wall clock and the header note says so honestly ("Session times are in your local time. Weekly patterns are listed in X time.") — DESIGN NOTE: weekly wall-clock patterns don't convert across zones under DST, so they stay labeled instead (also retired the raw `tz.split('/')` label hack, PL-398 rule). Tutor/admin/counselor surfaces: UNCHANGED (verified — tutor view keeps wall-clock class times). View-as banner now says "times render in YOUR device's timezone, not the family's" for parent previews.
**B. Emails:** write paths added — registration derives silently (school class → school's timezone; open class → the registering device's validated IANA zone via a new `deviceTimezone` field; fill-only-when-null, an existing/staff-set zone always stands), intake derives from the device's validated zone (the Denver availability-math fallback is NEVER stored — that would be a guess), the PL-363 import script maps the school's timezone, and the admin family hub subheader got an inline `TimezoneEdit` (searchable TimezoneSelect) through the PL-386 route (`timezone` joined PARENT_FIELDS with IANA validation; "timezone not known" shown honestly when null). Rendering: `familyTimezone` rides the class bundle → `EnrollmentEmailContext`; `sessionScheduleBlock` converts each session (dates too) to the family's zone when known-and-different with a labeled note; NEW `contextZonedDeadline` replaces all four `zonedDeadline` composer call sites (expiryDate variable + twin, W2 claim deadline, waitlist offer) — family zone when known, today's labeled class-zone render byte-for-byte otherwise (composed-var change, live templates need no new version; twins share the same consts by construction); T3 reschedule-ack + schedule-change quote via NEW `familyWhenPhrase` — family clock leads with the tutor-city time secondary ("Wed, Sep 2, 8:00–9:30 PM in Rome (12:00–1:30 PM in Salt Lake City)", secondary carries its own date when it differs), unknown/same zone = today's output exactly. 14/14 compile-and-call assertions green (unknown-zone byte-identity proven for schedule block + deadlines).
**C. Fences:** admin/tutor unchanged; /c feeder-city groups untouched; ICS verified zone-correct and NOT touched (`DTSTART;TZID=Europe/Berlin:…T100000` matches the ISD wall clock; tutoring feed is UTC-stamped).

## PL-419 (original) — Families see THEIR OWN local time (Scarlett, Aug 30)
A Rome family tutoring with an SLC tutor, or a Munich family in an online class, currently sees the CLASS/tutor time with an honest city label — and does the math themselves. Goal: their own local time. **A. Portal (the device knows):** family-portal time renders (SessionCalendar, schedule cards, session lists) convert to the BROWSER's timezone and say so plainly ("times in your local time"); when the device zone differs from the class's zone, optionally show the class-city time secondary ("7:00 PM your time · 11:00 AM in Salt Lake City" — Code's design call, but never two unlabeled times). Careful: server-rendered pages must not flash wrong times (client-convert or hydrate cleanly); tutors keep THEIR times as-is (their zone is their working zone). View-as note: staff viewing as a family will see the STAFF device's zone — banner the view-as chrome accordingly so nobody files a bug. **B. Emails (no browser):** add a family timezone: derived without asking where possible (school family → the school's timezone; open-class family → the class's display-city machinery can't tell WHICH city — capture country/city at registration or infer from the shipping address when one exists; tutoring families → ask/derive at intake), stored on the family, staff-editable (PL-386 pattern), shown in the admin family hub. Family-facing emails render session/class times in the FAMILY's zone when known ("7:00 PM in Rome"), with the class-city time secondary where ambiguity could hurt (schedule-change emails); unknown zone → today's behavior exactly (labeled class time — never guess). The zoned deadline/expiry composers join the same rule. **C. Scope fences:** admin + tutor surfaces unchanged (SLC/their-own per PL-398); public /c pages keep the feeder-city time groups (they already speak each school's local time — that machinery is the multi-city answer and stays); ICS calendar files are zone-correct by construction (verify, don't touch). Migration for the family timezone column; imports (PL-363) map school timezone automatically. Verify E2E: Rome-family fixture — portal shows Rome times on a Rome-zone browser, T3 schedule-change email says Rome times with SLC secondary; unknown-zone family renders exactly today's output; tutor view unchanged; ICS opens correct in Google Calendar.

## PL-420 — Deletion drift: the banner offers only Revert, but the email promises Adopt-or-revert (Scarlett, Aug 30)
For a MOVED event the drift banner offers adopt or revert; for a DELETED event it renders only "Revert Billy's calendar" — while the alert email promises both. Adopting a deletion has an obvious meaning: the tutor deleted the event because the session isn't happening — adopt = CANCEL the session through the normal cancellation machinery (family notice, the <24h reserved-time/fee rules, timecard consequences), with the consequence stated inline before confirming ("cancelling inside 24 hours: the family is still billed / Billy is still paid — reserved time" vs the free ≥24h case, whichever applies). Add "Adopt — cancel this session" beside Revert on deletion banners; the past-session variant (PL-393) already has its what-actually-happened options — confirm deletion drifts reach it too. Email copy stays truthful per change type (it already says adopt-or-revert; now the banner delivers). Verify E2E: delete-drift ≥24h out → adopt cancels free w/ family notice; delete-drift <24h → fee/reserved-time consequence stated and applied; revert restores the event; past-deletion flows to the PL-393 options.

## PL-421 — Timecards: drop "Prep Time" from the work-type dropdown (Scarlett, Aug 30)
The PL-412 prep-time checkbox is now the one way to record prep, but "Prep Time" still sits in the per-session work-type dropdown — two paths to the same pay type, and selecting it on a session row misclassifies the SESSION as prep. Remove it from the dropdown (sessions are Test Prep / 1-on-1 / 2-on-1 / Class-Workshop / Other; prep rides the checkbox). Handle existing data honestly: any session row already carrying work_type='Prep Time' (Scarlett's screenshot shows several) needs a migration verdict — convert to the correct session type + move the hours into the prep-time field where that's clearly what was meant, or list them for staff review if intent is ambiguous (report the count + disposition in ship notes; never silently rewrite confirmed timecards — confirmed rows get flagged for admin review, not mutated). The pay-scale/payroll CSV "Prep Time" TYPE itself stays (the checkbox feeds it); this is UI-path consolidation only. Verify: dropdown options correct, prep checkbox still flows to the CSV, legacy rows dispositioned per the ship note.

## PL-422 — Family portal: side menu + self-service "Your information" (Scarlett, Aug 30)
**A. Menu:** the family portal gets the tutor treatment (PL-404/413's reconciled nav): a side menu — **Classes · Tutoring · Billing · Email preferences · Your information** — content-gated as always (a section with nothing renders no menu item; a single-enrollment family may still see the minimal chrome only if the menu earns its place — reconcile with PL-404's "zero chrome when unnecessary" rule and note the threshold). Mobile = the same collapse pattern as the tutor nav. **B. "Your information" — self-service editing that updates EVERYTHING:** parents edit their own facts (name, phone, contact preference, emergency contact, arrival instruction) and their student's (phone, pronouns, grade, learning notes) — one write path shared with the staff editors (PL-386's API) so every surface updates identically; recorded who/when as parent-edited. **Guardrails:** the parent EMAIL is the sign-in identity — self-service email change requires a verify loop (link to the new address confirms; old address notified; until confirmed nothing changes) or, simpler v1, email shows with "contact us to change" (Code picks, notes the choice; never an unverified self-serve swap). Billing stays staff-side per the standing rule (the Billing menu item is the existing view/pay surface, not billing-fact editing). Students never get their own edit surface — this is the parent's login. **C. Admin visibility:** every self-service edit lands in Recent parent activity + the family hub's Recent-activity pane (PL-405) — but NOT Needs Attention, UNLESS it needs attention: an edit that trips a real condition raises a row (attempted email change colliding with another family per the PL-386 dedupe refusal; an edit while an invoice/agreement is pending against the old facts if any such dependency exists — Code audits which edits have consequences and gates accordingly, listing the conditions in ship notes; plain edits stay quiet). **D.** View-as renders identically (and staff view-as can SEE but not submit the family's edit forms — pointer-events rule). Verify: parent round-trips a phone + learning-notes edit → family page, admin hub, and comms all reflect it; activity pane logs it; no Needs Attention row; a colliding email attempt → refused politely family-side + Needs Attention row admin-side; mobile menu.

## PL-423 — Family portal Tutoring pane: gentle 1-on-1 sell for class-only families (Scarlett, Aug 30)
When a family has a class enrollment but NO 1-on-1 tutoring, the Tutoring menu item/pane (PL-422) doesn't vanish — it becomes a gentle pitch for the service: **the SAME sells already in the system** (the one-on-one-pitch flow block / registration-page copy — compose from the existing copy source, no new hand-written pitch, PL-96 drift rule) plus the family's real purchase path: the existing per-enrollment add-on hours page (their tokenized link), priced by the class-relative tier that already exists — **pre-class / during-class / post-class pricing from the price list, composed, never hand-typed** — showing the CURRENT tier plainly ("1-on-1 hours are $X/hr while your class is running") and, where honest and helpful, the tier context ("goes to $Y/hr after the class ends" — only if Scarlett's existing pricing copy already frames it that way; check and match, don't invent urgency). Autopay/consent machinery untouched — this is placement of existing copy + the existing purchase link, exactly like PL-362's approach. Gating: class-enrolled AND zero active/paused tutoring engagements; families with tutoring see today's real pane; families with neither see no Tutoring item at all (content-gated). One pitch, not a nag: no badges/counters, quiet styling consistent with the portal. Verify: three fixture shapes (class-only → pitch w/ correct tier price per class phase; tutoring family → real pane; neither → no item); price matches the price list in all three class phases; link lands on their add-on page.

## PL-424 — Availability UPDATES are treated like first-time shares, end to end (Scarlett, Aug 30)
An update to already-shared availability behaves identically to the first share on every surface. Fix the whole chain — the system knows whether prior windows existed, so compose accordingly: **A. Family confirmation:** an update says update ("Thanks — Roman's availability is updated. If this changes the schedule we're building, we'll be in touch."), not the first-time "we propose → you confirm → sessions begin" walkthrough (that copy stays for genuine first shares). **B. Family flow:** after saving an update, redirect back to the portal page they came from (return-to carried through the availability page; a settled inline "availability updated" note on arrival) instead of stranding them on the availability form. First-time shares keep their current landing if it reads right — decide + note. **C. Admin email:** an update email SAYS it's an update — subject "…updated Roman's availability" — and its body shows WHAT CHANGED (old → new windows, composed diff: "Monday 4–6 PM unchanged · Tuesday ADDED 4–6 PM · Sunday REMOVED"), not the generic ready-to-schedule framing; the 3-business-day expectation line only applies where that promise was actually made (first share) — an update against an ALREADY-RUNNING schedule instead says what it may affect. **D. The click-through lands on a resolution surface** (standing rule): deep-link to the family record's shared-windows view showing the SAME diff + what's downstream — flag any EXISTING scheduled sessions/proposals now OUTSIDE the new windows ("Wednesday 4:30 PM session now falls outside Roman's shared availability") with links into the PL-387 edit machinery; nothing auto-changes, staff decides. If nothing downstream is affected, say so plainly ("no scheduled sessions conflict with the new windows"). Verify E2E: first share unchanged; update → family sees update copy + lands back where they were; admin email carries the diff; click-through shows diff + a fixture conflict flagged; no-conflict update says so.

## PL-425 — Admin dashboard loads in 6–10s (Scarlett, Aug 30) — diagnosed: inline Google audit + serial queries
Causes found in /api/admin/dashboard/route.ts: **(1)** line ~361 runs `auditXclDrift()` INLINE on every load — live Google API calls across every connected tutor calendar, in the request path. Since PL-410 (push + hourly poll keep drift current), the dashboard should READ the calendar_drift table, never re-audit; remove the inline call (the table + push/poll are the source of truth; if a staleness guard feels needed, a cheap "last audit ran Xm ago" readout beats re-running it). **(2)** ~a dozen sequential awaits after the initial Promise.all (locEngs, missedCalls, outbound, calDrift, genFailures, manualNotes, scheduleDrafts, health, activity, week counts…) — batch the independent ones into Promise.all groups. **(3)** `computeSystemHealth` in the same path — check what it probes; anything remote gets cached (short TTL) or moved behind the health card's own fetch. Target: dashboard API under ~1.5s warm; measure before/after and state both in the ship note. No behavioral changes — same data, same Needs Attention rows (drift rows now from the table must match what the inline audit produced; verify parity on a fixture drift). Sweep siblings while in there: any OTHER page-load path calling a Google/QBO/Printful API inline (grep the GET routes) gets the same treatment — report findings.

## PL-426 — Class drafts: multiple drafts + clear "which draft am I editing" state (Scarlett, Aug 31)
Two fixes to the PL-370 drafts: **A. Resuming must look resumed:** clicking "resume" leaves the Draft-classes row (with its resume/delete buttons) sitting there — ambiguous whether you're editing that draft or starting fresh. On resume, the drafts list gives way to a clear editing banner at the top of the wizard ("Editing draft: ASF — saved August 31 · discard this draft") and every subsequent "Save draft" UPDATES that same row (never mints a sibling); "Start blank"/"Copy a previous class" explicitly exits draft-editing (with the banner's state cleared). **B. Multiple drafts:** currently it appears only one draft can exist — support several: each Save-draft from a fresh wizard creates its own row, the Draft-classes list shows ALL (name, saved date, who, resume/delete each), and resume loads exactly the clicked one. Diagnose whether the single-draft behavior is a hard constraint (upsert key?) or a list rendering limit, and note it. **C. Sweep the sibling:** tutoring schedule drafts (schedule-drafts-card) — confirm they already support multiple + the same clear editing state; if they have either gap, fix identically (one drafts pattern portal-wide); report the verdict either way. Verify: two class drafts side by side, resume A → edit → save updates A only, B untouched; the editing banner shows/clears correctly; schedule-drafts verdict in ship notes.

**PL-426 amendment (Scarlett, Aug 31) — auto-save:** while editing a draft (or once a fresh wizard has been saved as one), the wizard auto-saves on every step transition (and debounced after idle edits within a step — Code picks the interval), so "Update draft" clicks stop being the tutor's job; the banner shows quiet save state ("saved just now" / "saving…" — never a blocking spinner, never a toast per keystroke). The explicit Save-draft button stays for the FIRST save of a fresh wizard (that's the deliberate "keep this" moment — don't auto-mint drafts for every abandoned peek at the wizard) and as a manual reassurance afterward. Failures surface honestly (inline "couldn't save — retrying", the wizard keeps state). Auto-save writes the same row per the amendment above — never siblings. Verify: step through a draft with edits, close the tab with no explicit save → resume shows everything; a fresh wizard abandoned WITHOUT ever saving leaves no draft row.

## PL-427 — Class wizard: Next/Back land at the top of the step (Scarlett, Aug 31)
Advancing a wizard step keeps the scroll position at the bottom (where the Next button was) — every step starts with a scroll-up. On step change (Next, Back, and resuming a draft into a mid-flow step), scroll to the top of the step content (the step heading focused for keyboard/screen-reader users; instant or gentle per prefers-reduced-motion). Applies to the greyed-Next validation case too — when Next refuses, the "Next needs: …" explainer should be visible where the user is, not off-screen (scroll to it or keep it beside the button; whichever, no hunting). Sweep other multi-step flows for the same nit (registration flow's pages, engagement wizard) — fix where present, note where already correct. Verify at phone height where it bites hardest.

## PL-428 — Wizard branding step: SHOW the current logo (Scarlett, Aug 31)
The school-branding fieldset (wizard step 4, MIS example) offers "upload / replace logo" without showing the logo it has — impossible to know whether replacing is needed. Render the current logo as a thumbnail beside the control (height-capped, on a neutral checker/white chip so light logos read; alt "{school} logo"); no logo on record → an honest empty state ("no logo yet — upload one"), and the link text matches the state ("upload logo" vs "replace logo"). The accent swatch already shows itself — same principle, applied to the logo. Sweep every other upload-without-preview control (Schools card editor, class hero image, block image controls, instructor headshots — most already preview; report any that don't and fix identically). Verify: MIS shows its real logo in the wizard; a logo-less fixture school shows the empty state; replacing updates the thumbnail immediately.

## PL-429 — Skipped collateral: email reminder with real urgency + a fail-closed audit (Scarlett, Aug 31)
Today a skipped-collateral class gets only the dashboard reminder (state-driven, self-clears — good but passive). **A. Email nudge, urgency-aware:** remind by email when it starts to MATTER, not on a dumb timer — tied to the first moment collateral would actually be used (the CS welcome becoming sendable / the class's comms schedule approaching whichever step consumes collateral): one reminder at a sensible lead ("MIS SAT Prep's collateral isn't set up — the counselor welcome goes out plain without it; set it up under Classes → Branding & collateral"), deep-linked, once per class per the alerted-once pattern (PL-402 style — the dashboard row stays the persistent reminder), self-cancelling when collateral is completed. Recipient = admin (billy@ in test). **B. Fail-closed AUDIT:** the CS welcome is verified fail-closed (batch 24); sweep EVERY other collateral consumer — counselor follow-ups, flyer/PDF generation, any template bodying collateral fields, portal surfaces — and verify each either composes cleanly without collateral or refuses with a plain reason; NOTHING may render empty placeholders or broken images to a counselor/family. List each consumer + verdict in ship notes; fix any that fail open. Verify: fixture class with skipped collateral → reminder email at the right moment, once; completing collateral cancels it; every consumer's no-collateral render inspected.

## PL-430 — Class-created banner names the FULL class (Scarlett, Aug 31)
The success banner says "SAT Prep is created 🎉" — no school, so the creator can't be sure which class just landed. Use the same display-name composition the rest of the portal uses (PL-274 rule: school class → "MIS SAT Prep", open class → its full name, "HGL ACT Prep" via course_meta) — one existing helper, not a new string. Quick sweep: any other confirmation banner/toast naming a class bare (cancel-class confirm, draft-saved notes, duplicate-class prefill banner) gets the same composed name; list in ship note.

**PL-427 amendment (Scarlett, Aug 31):** confirmed in the wild on the FAMILY registration flow too — advancing to the second page lands below the "Add 1-on-1 tutoring" title (a small scroll to even see the step heading). The registration flow is explicitly IN scope for the fix, family-facing and highest-traffic — verify it specifically at phone height, not just the admin wizards.
