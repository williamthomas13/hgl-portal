# Portal fixes — batch 44 (ACCUMULATING — opened Aug 30, 2026)

**Standing rules:** all prior (see batch 41/42/43 headers).

(CLOSED for batch 44 hand-off, Aug 31. Next PL: PL-431. Batch 43 [PL-416–417] shipped separately.)

**Batch closed Aug 31 — all 13 items (PL-418–430, incl. the PL-426/427 amendments) shipped**, per-item ✅ notes below. Gate battery green at close: `tsc` · `next build` · `smoke:public` · `regress:links` · `regress:pronouns` · `regress:client-imports` · `regress:alert-pins` · `regress:alert-categories` · `regress-cancel-class` · `regress-resume-addon` (smoke + the two E2E gates run against the PROD build on :3100). **Migrations `20260913000001` (PL-425 drift kind/title) · `…02` (PL-422 family_fact_edits) · `…03` (PL-424 availability_changes) — all APPLIED via the Management API; nothing pending for Scarlett.** Premise corrections flagged inline: PL-419 (families.timezone existed since phase 7a — no migration; the work was write paths + consumption), PL-421 (2 legacy rows in the whole DB, both open-card QA fixtures → flagged not migrated, [docs/pl421-flagged-prep-rows.json](pl421-flagged-prep-rows.json)), PL-423 (no during-class price row exists — nothing invented), PL-425 (computeSystemHealth probes nothing remote). New DRAFT templates awaiting Scarlett's ramp: `AL_AVAILABILITY_UPDATED` v1 · `AL_COLLATERAL_NUDGE` v1 (code twins send identical copy meanwhile). Real-data fixes in passing: the collateral save now clears the skip stamp (the post-PL-384 evergreen completion-proxy bug), the first-share availability alert's false "family record shows the windows" claim now points at the real review surface, and the tutoring card's raw-IANA zone label hack is retired.

## ✅ PL-418 — SHIPPED (Aug 31)
SessionCalendar's internal fallback now IS the one resolver (`publicTimeCityLabel({location, timezone})` — `friendlyZoneCity` retired from the component; identical output by construction for label-less callers), and every portal call site passes the class's FULL facts: parent-view, instructor-view (tutor), and CounselorClassCard (both its callers — the signed-in counselor view AND the tokenized /class-roster page — queries widened with `schools.city` + `display_cities` + timezones). A "Room 204" HGL in-person class now says "Salt Lake City", never the zone city "Denver" (`hglInPerson` fact wired). Tutor + counselor calendars previously rendered NO zone line at all (no timezone passed) — they now carry the honest resolver label (verified live: "(times shown in Cape Town and Düsseldorf time)" on the PSAT open class in the tutor view). Admin SessionCalendar call sites (admin page, wizard preview) unchanged — they pass no timezone and render no line, as before. Coordinated with PL-419: the resolver label is the family view's secondary/fallback exactly as specified.

## PL-418 (original) — SessionCalendar joins the one time-label resolver (Scarlett, Aug 30 — the batch-39 residual, promoted)
The portal SessionCalendar (family + tutor views) still labels times via the old location-heuristic fallback — a "Room 204" HGL class can read "Denver time" on the page families visit most, while every email and public page says "Salt Lake City time". Point it at `publicTimeCityLabel` with the class's full facts (school city, display_cities, location, hglInPerson), like the six call sites PL-382 converged. NOTE: PL-419 may supersede the family-facing label with device-local times — coordinate: the resolver label remains the fallback/secondary either way, so do this convergence regardless.

## ✅ PL-419 — SHIPPED (Aug 31) — **PREMISE CORRECTION: `families.timezone` has existed since phase 7a (20260713000001) — NO migration needed.** The column was read by the whole tutoring email path but NEVER WRITTEN; the real work was the write paths + class-side consumption.
**A. Portal:** SessionCalendar is now a client component with a `localTimes` mode (family view only) — post-mount, when the device zone differs from the class's, each row converts date chip + weekday + times to the BROWSER's zone with the class-city time labeled alongside ("1:00–3:00 AM your time · 10:00 AM–12:00 PM in Düsseldorf", footer "(times in your local time — the class itself runs on Düsseldorf time)"); SSR and the hydration paint keep the class-zone render exactly, so nothing flashes wrong and matching zones change nothing (verified live: Berlin ISD class on a Vancouver-zone browser). Tutoring card: new `local-time.tsx` client leaf (`LocalDay`/`LocalTimeRange`/`LocalZoneNote` + `useDeviceTimezone`) converts the Next-session line, the Upcoming list, and the reschedule drawer to the device zone; weekly-pattern chips keep the tutor's wall clock and the header note says so honestly ("Session times are in your local time. Weekly patterns are listed in X time.") — DESIGN NOTE: weekly wall-clock patterns don't convert across zones under DST, so they stay labeled instead (also retired the raw `tz.split('/')` label hack, PL-398 rule). Tutor/admin/counselor surfaces: UNCHANGED (verified — tutor view keeps wall-clock class times). View-as banner now says "times render in YOUR device's timezone, not the family's" for parent previews.
**B. Emails:** write paths added — registration derives silently (school class → school's timezone; open class → the registering device's validated IANA zone via a new `deviceTimezone` field; fill-only-when-null, an existing/staff-set zone always stands), intake derives from the device's validated zone (the Denver availability-math fallback is NEVER stored — that would be a guess), the PL-363 import script maps the school's timezone, and the admin family hub subheader got an inline `TimezoneEdit` (searchable TimezoneSelect) through the PL-386 route (`timezone` joined PARENT_FIELDS with IANA validation; "timezone not known" shown honestly when null). Rendering: `familyTimezone` rides the class bundle → `EnrollmentEmailContext`; `sessionScheduleBlock` converts each session (dates too) to the family's zone when known-and-different with a labeled note; NEW `contextZonedDeadline` replaces all four `zonedDeadline` composer call sites (expiryDate variable + twin, W2 claim deadline, waitlist offer) — family zone when known, today's labeled class-zone render byte-for-byte otherwise (composed-var change, live templates need no new version; twins share the same consts by construction); T3 reschedule-ack + schedule-change quote via NEW `familyWhenPhrase` — family clock leads with the tutor-city time secondary ("Wed, Sep 2, 8:00–9:30 PM in Rome (12:00–1:30 PM in Salt Lake City)", secondary carries its own date when it differs), unknown/same zone = today's output exactly. 14/14 compile-and-call assertions green (unknown-zone byte-identity proven for schedule block + deadlines).
**C. Fences:** admin/tutor unchanged; /c feeder-city groups untouched; ICS verified zone-correct and NOT touched (`DTSTART;TZID=Europe/Berlin:…T100000` matches the ISD wall clock; tutoring feed is UTC-stamped).

## PL-419 (original) — Families see THEIR OWN local time (Scarlett, Aug 30)
A Rome family tutoring with an SLC tutor, or a Munich family in an online class, currently sees the CLASS/tutor time with an honest city label — and does the math themselves. Goal: their own local time. **A. Portal (the device knows):** family-portal time renders (SessionCalendar, schedule cards, session lists) convert to the BROWSER's timezone and say so plainly ("times in your local time"); when the device zone differs from the class's zone, optionally show the class-city time secondary ("7:00 PM your time · 11:00 AM in Salt Lake City" — Code's design call, but never two unlabeled times). Careful: server-rendered pages must not flash wrong times (client-convert or hydrate cleanly); tutors keep THEIR times as-is (their zone is their working zone). View-as note: staff viewing as a family will see the STAFF device's zone — banner the view-as chrome accordingly so nobody files a bug. **B. Emails (no browser):** add a family timezone: derived without asking where possible (school family → the school's timezone; open-class family → the class's display-city machinery can't tell WHICH city — capture country/city at registration or infer from the shipping address when one exists; tutoring families → ask/derive at intake), stored on the family, staff-editable (PL-386 pattern), shown in the admin family hub. Family-facing emails render session/class times in the FAMILY's zone when known ("7:00 PM in Rome"), with the class-city time secondary where ambiguity could hurt (schedule-change emails); unknown zone → today's behavior exactly (labeled class time — never guess). The zoned deadline/expiry composers join the same rule. **C. Scope fences:** admin + tutor surfaces unchanged (SLC/their-own per PL-398); public /c pages keep the feeder-city time groups (they already speak each school's local time — that machinery is the multi-city answer and stays); ICS calendar files are zone-correct by construction (verify, don't touch). Migration for the family timezone column; imports (PL-363) map school timezone automatically. Verify E2E: Rome-family fixture — portal shows Rome times on a Rome-zone browser, T3 schedule-change email says Rome times with SLC secondary; unknown-zone family renders exactly today's output; tutor view unchanged; ICS opens correct in Google Calendar.

## ✅ PL-420 — SHIPPED (Aug 31)
Deletion drift now offers **"Adopt — cancel this session"** beside Revert (future deletions; past ones keep the PL-393 what-actually-happened buttons — `past` was already independent of the deleted state, confirmed, and the route now refuses a stale adopt-click on a passed session with a plain pointer to those options). Adopt-a-deletion cancels through the NORMAL machinery with the consequence stated INLINE before the committing click (standing inline-confirm rule): **<24h** → `forfeited` (reserved time — "the family is still billed for this session and {tutor} is still paid"), **≥24h** → a FREE cancellation (status `'cancelled'`, the PL-62 tombstone — never billed, blocks re-materialization; NOTE: the machinery had no free-cancel outcome on the cancel action — 'cancelled' is the existing free state, now reachable here). Family notice both ways: T3_SCHEDULE_CHANGE gained a `'cancelled'` kind ("…was cancelled. With more than 24 hours' notice there's no charge for it. Want to rebook the time? Just reply…") — composed `changeListBlock` var, so the live template needs no new version; twin shares the same lines by construction. DESIGN CALL: the free-cancel skips the coalesced tutor-side notice (the change came FROM the tutor's own calendar; the forfeited path keeps it — the pay implication is worth telling). `cancelled_by='tutor'` (attributionally honest) + a `calendar-drift banner (PL-420)` cancel_note (the activity feed's existing matcher reads it). The drift alert email's adopt sentence now covers both change types truthfully ("a moved event runs the normal reschedule; a deleted event cancels the session under the 24-hour rules"). **E2E on REAL calendar deletions (Billy's live calendar, Janeson fixture sessions, restored after):** ≥24h delete → banner shows both buttons, inline free-case consequence, adopt → status cancelled + attribution + T3 'cancelled' composed and reached the send gate (PL-60 dev guard suppressed the non-QA recipient — the path proven to the wire); <24h delete → inline reserved-time consequence rendered; Revert → event restored on the calendar (200, confirmed), drift cleared; all sessions/events restored to their pre-test state.

## PL-420 (original) — Deletion drift: the banner offers only Revert, but the email promises Adopt-or-revert (Scarlett, Aug 30)
For a MOVED event the drift banner offers adopt or revert; for a DELETED event it renders only "Revert Billy's calendar" — while the alert email promises both. Adopting a deletion has an obvious meaning: the tutor deleted the event because the session isn't happening — adopt = CANCEL the session through the normal cancellation machinery (family notice, the <24h reserved-time/fee rules, timecard consequences), with the consequence stated inline before confirming ("cancelling inside 24 hours: the family is still billed / Billy is still paid — reserved time" vs the free ≥24h case, whichever applies). Add "Adopt — cancel this session" beside Revert on deletion banners; the past-session variant (PL-393) already has its what-actually-happened options — confirm deletion drifts reach it too. Email copy stays truthful per change type (it already says adopt-or-revert; now the banner delivers). Verify E2E: delete-drift ≥24h out → adopt cancels free w/ family notice; delete-drift <24h → fee/reserved-time consequence stated and applied; revert restores the event; past-deletion flows to the PL-393 options.

## ✅ PL-421 — SHIPPED (Aug 31)
"Prep Time" left `STANDARD_WORK_TYPES` (the ONE source — dropdown and the server allowlist both tighten from it; verified: the API now refuses it with "pick one of: Test Prep, 1-on-1, 2-on-1, Class/Workshop, Other" — note the real name is `Class/Workshop`, not "Class-Workshop" as this item wrote it). The PAY TYPE stays: `PREP_WORK_TYPE` still rides `prepRows()` → `hoursByWorkType` (a new `DISPLAY_ORDER` keeps Prep Time in its old column position — verified live: "Test Prep 4.5 · 1-on-1 12 · 2-on-1 1.5 · Prep Time 2.95 · Class/Workshop 1.5", with a checkbox-set 30 min flowing straight into that number), and a literal "Prep Time" in a tutor's `pay_type_titles` can never resurface as an extra option (explicit guard — the dedupe used to let it back in). **Legacy rows: 2 total in the whole database** (queried) — both `work_type='Prep Time'` session rows are the SAME instant (Aug 3, Fakey McFakerson/ASVAB — QA fixture data, Billy testing the dropdown; one is a plain duplicate), both on an OPEN timecard, **zero on confirmed/approved/exported cards**. Disposition: FLAGGED for staff review in [docs/pl421-flagged-prep-rows.json](pl421-flagged-prep-rows.json), NOT auto-converted — converting a session row typed 'Prep Time' either loses the prep classification (retype to 1-on-1) or double-pays the hour (retype + move minutes into `prep_minutes`), so intent is ambiguous by construction; confirmed rows untouched by definition (there are none). The open rows stay correctable in the panel: a legacy stored value renders as **"Prep Time (legacy)"** in its own row's dropdown (the select tells the truth) and can be changed to any real type — it just can't be picked anywhere. NO migration needed (premise correction: nothing mutates). Known pre-existing quirk left as-is and noted: the payroll CSV computes `prep_hours` from `prep_minutes` only, so the two flagged rows report as session_hours there until staff retype them — with zero new rows possible, the disagreement dies with the flags.

## PL-421 (original) — Timecards: drop "Prep Time" from the work-type dropdown (Scarlett, Aug 30)
The PL-412 prep-time checkbox is now the one way to record prep, but "Prep Time" still sits in the per-session work-type dropdown — two paths to the same pay type, and selecting it on a session row misclassifies the SESSION as prep. Remove it from the dropdown (sessions are Test Prep / 1-on-1 / 2-on-1 / Class-Workshop / Other; prep rides the checkbox). Handle existing data honestly: any session row already carrying work_type='Prep Time' (Scarlett's screenshot shows several) needs a migration verdict — convert to the correct session type + move the hours into the prep-time field where that's clearly what was meant, or list them for staff review if intent is ambiguous (report the count + disposition in ship notes; never silently rewrite confirmed timecards — confirmed rows get flagged for admin review, not mutated). The pay-scale/payroll CSV "Prep Time" TYPE itself stays (the checkbox feeds it); this is UI-path consolidation only. Verify: dropdown options correct, prep checkbox still flows to the CSV, legacy rows dispositioned per the ship note.

## ✅ PL-422 — SHIPPED (Aug 31)
**A. Menu:** the family portal has the tutor-treatment side menu (PortalNav reuse — desktop sticky rail, mobile scrolling chip row, verified at phone width): **Classes · Tutoring · Billing · Email preferences · Your information**, content-gated (Billing only with tutoring invoices; Tutoring per the PL-423 gate; the per-student jump links retired — students live under Classes, one nav shape portal-wide). THRESHOLD RECONCILIATION (noted per the item): "Email preferences" and "Your information" are account-level destinations every family has, so the menu always carries ≥2 items and PortalNav's <2 zero-chrome rule stops biting for families — deliberate; those destinations must be findable. **Email preferences is a NEW surface** (none existed): one honest switch (class announcements & offers) + the "these always send" transactional truth, backed by `/api/portal/family-prefs` (family-gated) speaking the machinery's own semantics — OFF = family flag + a suppression row for the PARENT's address (the legacy family-token path's semantics), ON clears the flag + the parent's OWN suppression only (PL-280: a parent re-subscribing never re-arms a student's leg) — this is also the system's first resubscribe surface. Round-tripped live.
**B. Self-service through the ONE write path:** `/api/admin/family-facts` (PL-386's API) now accepts parent sessions (`sessionFamily`), scoped hard — own family/student/lead rows only (403 across families, verified), self-service field set only: parent name + phone; student phone, pronouns, grade, learning notes; intake contact-preference / arrival instruction / emergency contact. **Email: v1 "contact us to change"** (the Code-picks decision — the panel shows the address read-only with the sign-in explanation; the API refuses a parent email edit with the same copy; never an unverified swap). Timezone + student email stay staff-side (refusals verified). Who/when: `contact_updated_by='parent:{email}'` + `contact_updated_at` stamped on every parent action, and every edit appends to the NEW `family_fact_edits` log (migration `20260913000002` — APPLIED; append-only, staff-read policy). New portal panel `family-info-panel.tsx` (Your information) with inline row editors; intake-less families get the honest "captured at intake — reply to add them" note.
**C. Admin visibility:** parent edits land in the PL-405 builder as a new 'Family edits' source ("Testy QAParent updated Reggie's learning notes from the family portal." — verified in the family hub's activity API) AND in the tutoring page's "Recent parent activity" pane (client-read via the log's staff policy — verified live alongside the reschedule rows). **NOT Needs Attention — consequence audit (the item asked for the list):** the v1 parent-editable fields are phone/contact-pref/emergency/arrival/pronouns/grade/learning-notes — none is load-bearing for sign-in, invoices, or agreements, and the one consequence-bearing edit (email → the PL-386 dedupe collision) is excluded from self-serve by design, so **zero edit conditions can raise a row in v1**; plain edits stay quiet. If email self-serve ever ships (verify-loop v2), the 409-collision condition becomes the NA row to add — noted for then.
**D. View-as:** renders identically and needed NO new work — the PL-325 capture-phase interceptor already blocks the new edit buttons/forms (verified: the edit affordance is visible in view-as, the click is swallowed, no editor opens).
E2E: parent round-tripped phone + learning-notes edits from the real family login → portal, admin hub, and both activity feeds reflect them; guards verified (email/timezone/student-email/other-family all refuse with plain copy); prefs toggle round-trips including resubscribe; mobile chip menu verified at 375px.

## PL-422 (original) — Family portal: side menu + self-service "Your information" (Scarlett, Aug 30)
**A. Menu:** the family portal gets the tutor treatment (PL-404/413's reconciled nav): a side menu — **Classes · Tutoring · Billing · Email preferences · Your information** — content-gated as always (a section with nothing renders no menu item; a single-enrollment family may still see the minimal chrome only if the menu earns its place — reconcile with PL-404's "zero chrome when unnecessary" rule and note the threshold). Mobile = the same collapse pattern as the tutor nav. **B. "Your information" — self-service editing that updates EVERYTHING:** parents edit their own facts (name, phone, contact preference, emergency contact, arrival instruction) and their student's (phone, pronouns, grade, learning notes) — one write path shared with the staff editors (PL-386's API) so every surface updates identically; recorded who/when as parent-edited. **Guardrails:** the parent EMAIL is the sign-in identity — self-service email change requires a verify loop (link to the new address confirms; old address notified; until confirmed nothing changes) or, simpler v1, email shows with "contact us to change" (Code picks, notes the choice; never an unverified self-serve swap). Billing stays staff-side per the standing rule (the Billing menu item is the existing view/pay surface, not billing-fact editing). Students never get their own edit surface — this is the parent's login. **C. Admin visibility:** every self-service edit lands in Recent parent activity + the family hub's Recent-activity pane (PL-405) — but NOT Needs Attention, UNLESS it needs attention: an edit that trips a real condition raises a row (attempted email change colliding with another family per the PL-386 dedupe refusal; an edit while an invoice/agreement is pending against the old facts if any such dependency exists — Code audits which edits have consequences and gates accordingly, listing the conditions in ship notes; plain edits stay quiet). **D.** View-as renders identically (and staff view-as can SEE but not submit the family's edit forms — pointer-events rule). Verify: parent round-trips a phone + learning-notes edit → family page, admin hub, and comms all reflect it; activity pane logs it; no Needs Attention row; a colliding email attempt → refused politely family-side + Needs Attention row admin-side; mobile menu.

## ✅ PL-423 — SHIPPED (Aug 31)
Class-only families keep the Tutoring menu item and get `tutoring-pitch-section.tsx`: the **`one-on-one-pitch` site block — the SAME source the registration flow renders** (PL-96: zero new hand-written pitch; Scarlett's edits on Settings → Class pages reach both surfaces) plus, per student, the real purchase path matching the price list's own framing per class phase: **prestart** → "1-on-1 hours are from $X/hr with a package (regular $Y/hr) — these rates are only available before the class starts, {date}" (composed from the pre-package rows — the addon page's own tier-context framing, matched not invented) + per-package buttons to their tokenized add-on page; **running** → **PREMISE CORRECTION: there is NO during-class purchase row in the price list and the tokenized addon page closes at session 1** — so the doc's "$X/hr while your class is running" line cannot be composed and was NOT invented (the item's own rule); the pane speaks the existing honest copy (discounted hours after it ends — watch for the email, or get in touch); **finished** → post-package savings + the discount-page CTA (email #8's path). Gate: paid class enrollment AND zero tutoring anywhere — no active/paused engagement, **no invoice** (a refinement: TutoringSection renders for invoice-holding families, so the pitch yields to it — never both), and **no purchased add-on hours** (those families already said yes; their PL-207 card works the redemption). One pitch, quiet styling, no badges. Verified on all three fixture shapes: class-only (Testy QAParent → pitch with "from $95/hr (regular $130/hr)… before October 13", ISD international tier, real registration copy verbatim), tutoring family (wthomas → real pane + Billing item, no pitch), neither (f@f.com → no Tutoring item, no pitch).

## PL-423 (original) — Family portal Tutoring pane: gentle 1-on-1 sell for class-only families (Scarlett, Aug 30)
When a family has a class enrollment but NO 1-on-1 tutoring, the Tutoring menu item/pane (PL-422) doesn't vanish — it becomes a gentle pitch for the service: **the SAME sells already in the system** (the one-on-one-pitch flow block / registration-page copy — compose from the existing copy source, no new hand-written pitch, PL-96 drift rule) plus the family's real purchase path: the existing per-enrollment add-on hours page (their tokenized link), priced by the class-relative tier that already exists — **pre-class / during-class / post-class pricing from the price list, composed, never hand-typed** — showing the CURRENT tier plainly ("1-on-1 hours are $X/hr while your class is running") and, where honest and helpful, the tier context ("goes to $Y/hr after the class ends" — only if Scarlett's existing pricing copy already frames it that way; check and match, don't invent urgency). Autopay/consent machinery untouched — this is placement of existing copy + the existing purchase link, exactly like PL-362's approach. Gating: class-enrolled AND zero active/paused tutoring engagements; families with tutoring see today's real pane; families with neither see no Tutoring item at all (content-gated). One pitch, not a nag: no badges/counters, quiet styling consistent with the portal. Verify: three fixture shapes (class-only → pitch w/ correct tier price per class phase; tutoring family → real pane; neither → no item); price matches the price list in all three class phases; link lands on their add-on page.

## ✅ PL-424 — SHIPPED (Aug 31)
The system now KNOWS whether prior windows existed — the save route reads the grid BEFORE the delete-replace (the linchpin) and records every save in the NEW `availability_changes` table (migration `20260913000003` — APPLIED; before/after grids + kind, because freebusy keeps no history). **Update = prior PARENT-source rows existed** (intake-only rows still count as a first share — that promise was never made against them; verified: Janeson's intake-only grid → first save kind='first', second → 'update'). **A. Family confirmation:** an update says update ("Thanks — Janeson's availability is updated. If this changes the schedule we're building, we'll be in touch." — verified live); genuine first shares keep the propose-within-3-business-days walkthrough exactly. **B. Flow:** the portal tutoring card's link now carries `return=/portal`; an update save redirects back with a settled "Availability updated — thanks!" note on arrival (verified end-to-end); DECISION (noted): first shares keep their current landing — the 3-day promise message reads right there. **C. Admin email:** a NEW `AL_AVAILABILITY_UPDATED` alert (registry-seeded as a DRAFT v1 via the house seed script — code twin sends identical copy until ramped; label/category/sample pin registered, the pin COMPUTED from the same diff leaf) — subject "{parent} updated {student}'s availability", body carries the composed old→new diff ("Monday 4:00 PM–6:00 PM unchanged · Tuesday ADDED · Sunday REMOVED") from the new **`availability-diff.ts` leaf (ONE composer: email, review card, sample pin)**, says what it may affect, and NEVER repeats the 3-business-day line; dedupe keyed per change row (every distinct update rings once — the old dated key stays on first shares). Verified: both sends composed with correct keys (PL-60 suppressed the non-QA subscriber in dev — path proven to the wire). **D. Resolution surface:** the click-through lands on `/admin/tutoring?availability={studentId}` — a NEW review card showing the SAME diff + downstream: every future proposed/confirmed session checked against the new windows on each range's OWN zone (`sessionOutsideWindows` — empty availability is never "unavailable"), conflicts flagged with links into the PL-387 session-edit machinery, the no-conflict case said plainly, nothing auto-changes (verified live: Tue 11 AM and Mon 1 PM sessions correctly flagged outside Mon/Tue 4–6 PM windows, each with a review/move link). Also fixed in passing: the first-share alert's "the family record shows the shared windows" claim was FALSE (no such view existed) — it now links this review surface. Fixtures cleaned + Janeson's intake grid restored.

## PL-424 (original) — Availability UPDATES are treated like first-time shares, end to end (Scarlett, Aug 30)
An update to already-shared availability behaves identically to the first share on every surface. Fix the whole chain — the system knows whether prior windows existed, so compose accordingly: **A. Family confirmation:** an update says update ("Thanks — Roman's availability is updated. If this changes the schedule we're building, we'll be in touch."), not the first-time "we propose → you confirm → sessions begin" walkthrough (that copy stays for genuine first shares). **B. Family flow:** after saving an update, redirect back to the portal page they came from (return-to carried through the availability page; a settled inline "availability updated" note on arrival) instead of stranding them on the availability form. First-time shares keep their current landing if it reads right — decide + note. **C. Admin email:** an update email SAYS it's an update — subject "…updated Roman's availability" — and its body shows WHAT CHANGED (old → new windows, composed diff: "Monday 4–6 PM unchanged · Tuesday ADDED 4–6 PM · Sunday REMOVED"), not the generic ready-to-schedule framing; the 3-business-day expectation line only applies where that promise was actually made (first share) — an update against an ALREADY-RUNNING schedule instead says what it may affect. **D. The click-through lands on a resolution surface** (standing rule): deep-link to the family record's shared-windows view showing the SAME diff + what's downstream — flag any EXISTING scheduled sessions/proposals now OUTSIDE the new windows ("Wednesday 4:30 PM session now falls outside Roman's shared availability") with links into the PL-387 edit machinery; nothing auto-changes, staff decides. If nothing downstream is affected, say so plainly ("no scheduled sessions conflict with the new windows"). Verify E2E: first share unchanged; update → family sees update copy + lands back where they were; admin email carries the diff; click-through shows diff + a fixture conflict flagged; no-conflict update says so.

## ✅ PL-425 — SHIPPED (Aug 31) — dashboard API **3.7–5.3s → ~460ms warm** (measured 3 runs before/after on dev; the request path now makes ZERO third-party calls)
**(1) The inline `auditXclDrift()` is GONE from the request path** — and its detection didn't just move to a timer: it FOLDED into the PL-180/410 audit (`auditTutoringTimeDrift` already lists every tutor's events — the XCL-retitle check reads the same list, zero extra Google calls). XCL drift now persists in `calendar_drift` with `kind='xcl'` + `cal_title` (migration `20260913000001` — APPLIED via the Management API); push + hourly poll keep it current. The dashboard renders both kinds from ONE table read: `kind='xcl'` → the original PL-154 row (same wording, same session-dialog href, urgent when past); `kind='time'` → the existing moved/deleted rows. The banner and its ⚠ chips stay exactly the moved/deleted set (`syncTutoringDriftTable` returns time-rows only; schedule-view filters `kind='time'`), and XCL rows never ring the drift-alert doorbell (parity: PL-154 was dashboard-only). **Parity verified on a REAL fixture drift:** retitled Janeson's Sep 1 event `XCL-` on Billy's live calendar → scan → `kind='xcl'` row landed, dashboard row byte-matches the old copy shape ("Billy Thomas marked Janeson Foney Fony's Sep 1 session \"XCL- …\" in Google…"), banner feed empty, zero alert emails; restored the title → row self-cleared. Behavior deltas, both deliberate: a hand-DELETED event now yields ONE row (the actionable banner row) instead of the old double (XCL row + banner row); an event both moved AND XCL-retitled reads as XCL (the cancel signal is the story).
**(2) Serial awaits batched:** 19 sequential top-level awaits + a per-engagement N+1 (the block-continue count at ~:713) collapsed into TWO `Promise.all` phases — Phase B (16 independent reads incl. health/activity/week counts, right after the existing head batch) and Phase C (the 7 reads that need Phase B's results: availability trio, package drawdown + family renewals, missed-call callbacks, and the block-continue check as ONE query filtered in JS). Same data, same rows, same order (undated rows keep stable relative order — processing loops untouched).
**(3) `computeSystemHealth` probes NOTHING remote** (six DB reads + arithmetic — premise note) — it joined Phase B; no cache needed.
**Payload parity:** identical attention ids/health/counts before vs after on live data.
**Sibling sweep (inline third-party calls in read paths):** `instructor-fit` (nested freeBusy loops) — user-triggered suggester with a loading state, needs LIVE freebusy for assignment decisions, not a page-load path → kept, reported; `qbo/catalog` — settings-panel item mapping, live-by-intent → kept; `class-info` `printfulCountries()` — already behind a 24h cache; `attach-payment` GET — a live Stripe lookup mid-flow, intentional. The tutoring drift banner's on-open scan stays too (it's the adopt/revert decision surface — stale reads there could act on resolved drift; it loads async and doesn't block the page). **Zero page-load offenders remain.**

## PL-425 (original) — Admin dashboard loads in 6–10s (Scarlett, Aug 30) — diagnosed: inline Google audit + serial queries
Causes found in /api/admin/dashboard/route.ts: **(1)** line ~361 runs `auditXclDrift()` INLINE on every load — live Google API calls across every connected tutor calendar, in the request path. Since PL-410 (push + hourly poll keep drift current), the dashboard should READ the calendar_drift table, never re-audit; remove the inline call (the table + push/poll are the source of truth; if a staleness guard feels needed, a cheap "last audit ran Xm ago" readout beats re-running it). **(2)** ~a dozen sequential awaits after the initial Promise.all (locEngs, missedCalls, outbound, calDrift, genFailures, manualNotes, scheduleDrafts, health, activity, week counts…) — batch the independent ones into Promise.all groups. **(3)** `computeSystemHealth` in the same path — check what it probes; anything remote gets cached (short TTL) or moved behind the health card's own fetch. Target: dashboard API under ~1.5s warm; measure before/after and state both in the ship note. No behavioral changes — same data, same Needs Attention rows (drift rows now from the table must match what the inline audit produced; verify parity on a fixture drift). Sweep siblings while in there: any OTHER page-load path calling a Google/QBO/Printful API inline (grep the GET routes) gets the same treatment — report findings.

## ✅ PL-426 (+ amendment) — SHIPPED (Aug 31)
**Diagnosis (the item asked):** single-draft behavior was NEITHER a hard constraint NOR a list limit — the table/API/list all supported many rows; the wizard just held one `draftId` per mount with no way to say which row it owned and no affordance to start a sibling (plus `draftName()` made same-school drafts read identically). **A. Editing state:** the wizard now carries an **editing banner** at its top whenever a draft row is owned — "Editing draft: **MIS QA PL426 Draft A** — saved August 31" — with the amendment's quiet save state ("saving…" / "saved just now" / "couldn't save — retrying" — never a spinner, never a toast), an inline-confirm **discard this draft** (removes the saved row, keeps what's on screen), and **start blank** (explicitly exits draft-editing via the page remount, banner state cleared — Copy-a-previous-class exits the same way by remount). On resume, the Draft-classes list **gives way** to the banner (hidden while editing — no ambiguity about which row the wizard owns), and drafts now carry the composed display name (PL-430's helper — two schools' drafts stop reading alike). **B. Multiple drafts:** verified live — draft A (MIS) and draft B (ISD) side by side; resume A → edit → saves update A ONLY (B untouched, id-scoped update; discard removed only B). **Amendment — auto-save:** once a draft row exists, the wizard auto-saves into the SAME row (ONE row-writer shared with the button) — step transitions and idle edits both land within a 1.5s debounce (Code's interval pick; a step change saves inside it), an in-flight guard prevents racing the explicit button, failures show the honest inline state and retry after 5s with the wizard keeping its state. The FIRST save stays the explicit button (auto-save is gated on `draftId` — an abandoned peek can never mint a draft; structurally guaranteed), which remains as manual reassurance ("Update draft"). Verified: stepped through with edits + a step-3 session, NO explicit save, resumed → everything there at the step I left (blob confirmed in DB). **C. Schedule-drafts sweep verdict:** multiple drafts — ALREADY correct (the card lists every row, resume/discard each); clear editing state — HAD the same gap (`serverDraftId` was invisible) → the engagement wizard now shows the same-shape banner ("Editing {student}'s saved schedule draft — Save as draft updates it, Create retires it"); its card row stays listed below the wizard (the banner disambiguates — the card sits under the open wizard, unlike the class list which sat beside a fresh-looking one; noted as the one deliberate asymmetry). Schedule drafts already had their own localStorage auto-draft timer (PL-338) — auto-save parity noted as pre-existing.

## PL-426 (original) — Class drafts: multiple drafts + clear "which draft am I editing" state (Scarlett, Aug 31)
Two fixes to the PL-370 drafts: **A. Resuming must look resumed:** clicking "resume" leaves the Draft-classes row (with its resume/delete buttons) sitting there — ambiguous whether you're editing that draft or starting fresh. On resume, the drafts list gives way to a clear editing banner at the top of the wizard ("Editing draft: ASF — saved August 31 · discard this draft") and every subsequent "Save draft" UPDATES that same row (never mints a sibling); "Start blank"/"Copy a previous class" explicitly exits draft-editing (with the banner's state cleared). **B. Multiple drafts:** currently it appears only one draft can exist — support several: each Save-draft from a fresh wizard creates its own row, the Draft-classes list shows ALL (name, saved date, who, resume/delete each), and resume loads exactly the clicked one. Diagnose whether the single-draft behavior is a hard constraint (upsert key?) or a list rendering limit, and note it. **C. Sweep the sibling:** tutoring schedule drafts (schedule-drafts-card) — confirm they already support multiple + the same clear editing state; if they have either gap, fix identically (one drafts pattern portal-wide); report the verdict either way. Verify: two class drafts side by side, resume A → edit → save updates A only, B untouched; the editing banner shows/clears correctly; schedule-drafts verdict in ship notes.

**PL-426 amendment (Scarlett, Aug 31) — auto-save:** while editing a draft (or once a fresh wizard has been saved as one), the wizard auto-saves on every step transition (and debounced after idle edits within a step — Code picks the interval), so "Update draft" clicks stop being the tutor's job; the banner shows quiet save state ("saved just now" / "saving…" — never a blocking spinner, never a toast per keystroke). The explicit Save-draft button stays for the FIRST save of a fresh wizard (that's the deliberate "keep this" moment — don't auto-mint drafts for every abandoned peek at the wizard) and as a manual reassurance afterward. Failures surface honestly (inline "couldn't save — retrying", the wizard keeps state). Auto-save writes the same row per the amendment above — never siblings. Verify: step through a draft with edits, close the tab with no explicit save → resume shows everything; a fresh wizard abandoned WITHOUT ever saving leaves no draft row.

## ✅ PL-427 (+ amendment) — SHIPPED (Aug 31)
**Class wizard:** ONE effect on step change scrolls the wizard root into view and focuses it (`tabIndex={-1}`, `preventScroll` — keyboard/screen-reader users land at the step top), instant under `prefers-reduced-motion`, smooth otherwise; covers Next, Back, the collateral Skip jumps, the error deep-link jump, AND resuming a draft into a mid-flow step (the effect fires on the initial mount when the restored step > 1; a fresh wizard's first render never scrolls — opening the Classes tab must not yank the page). Verified at phone height (375px): clicking Next from the page bottom moved scrollY 427→0 with the new step's top in view, wizard root focused. **Greyed-Next explainer verdict:** already correct structurally — "Next needs: …" renders directly under the button (PL-239), and a refused Next means the user is AT the button, so the explainer is where they are (verified visible at phone height: "Next needs: price · capacity"); no change needed. **Amendment — registration flow:** page 2 is a sibling return, not a route change — added the same treatment (scroll to top + the "Add 1-on-1 tutoring?" H1 focused, reduced-motion aware); verified live at phone height with a QA registration (scrollY 1196→0, H1 focused; QA rows cleaned after). **Sweep:** engagement wizard — N/A, no step paging exists (numbered SECTIONS on one form; the `step={5}` in there is a number-input attribute, not a wizard step); tutoring-page draft resume already scrolls (PL-338); intake form already scrolls post-submit. VERIFICATION NOTE: the batch-42 real-Chrome rule targets scroll-EVENT-driven UI (the pane suppresses scroll events); these are programmatic scrolls whose effect is directly observable in the pane (scrollY deltas above), so the pane verification stands.

## PL-427 (original) — Class wizard: Next/Back land at the top of the step (Scarlett, Aug 31)
Advancing a wizard step keeps the scroll position at the bottom (where the Next button was) — every step starts with a scroll-up. On step change (Next, Back, and resuming a draft into a mid-flow step), scroll to the top of the step content (the step heading focused for keyboard/screen-reader users; instant or gentle per prefers-reduced-motion). Applies to the greyed-Next validation case too — when Next refuses, the "Next needs: …" explainer should be visible where the user is, not off-screen (scroll to it or keep it beside the button; whichever, no hunting). Sweep other multi-step flows for the same nit (registration flow's pages, engagement wizard) — fix where present, note where already correct. Verify at phone height where it bites hardest.

## ✅ PL-428 — SHIPPED (Aug 31)
The wizard's step-4 school-branding fieldset now renders the current logo as a thumbnail (`h-9` capped, white chip + border so light logos read, alt "{school} logo") beside the control; no logo → the honest empty state ("no logo yet — upload one") and the link text matches the state ("upload logo" vs "replace logo") — the accent swatch's own principle, applied. Replacing updates immediately (the upload route's timestamped filename + the existing `onSchoolsChange()` refresh — no cache-busting needed). Verified live: MIS shows its real logo in the wizard with "replace logo"; the empty state is the same ternary verified in the panels below (every real school currently has a logo — a logo-less fixture wasn't manufactured on live data; branch noted as code-verified). **Sweep (upload-without-preview):** the wizard fieldset was the ONLY offender — Schools contacts card, School branding panel, class hero image, block image controls, and instructor headshots ALL already preview with empty states and state-matched link text (listed verdicts, nothing else to fix); portal materials is documents, N/A. **Also retired in passing:** the unreachable step-1 add-a-school logo branch (`newSchoolLogo` — orphaned when PL-237 moved branding to step 4; the new-school path takes its logo on step 4 where the thumbnail now shows it).

## PL-428 (original) — Wizard branding step: SHOW the current logo (Scarlett, Aug 31)
The school-branding fieldset (wizard step 4, MIS example) offers "upload / replace logo" without showing the logo it has — impossible to know whether replacing is needed. Render the current logo as a thumbnail beside the control (height-capped, on a neutral checker/white chip so light logos read; alt "{school} logo"); no logo on record → an honest empty state ("no logo yet — upload one"), and the link text matches the state ("upload logo" vs "replace logo"). The accent swatch already shows itself — same principle, applied to the logo. Sweep every other upload-without-preview control (Schools card editor, class hero image, block image controls, instructor headshots — most already preview; report any that don't and fix identically). Verify: MIS shows its real logo in the wizard; a logo-less fixture school shows the empty state; replacing updates the thumbnail immediately.

## ✅ PL-429 — SHIPPED (Aug 31)
**A. The urgency-keyed nudge:** `sweepCollateralNudges()` (new util, hourly-sweep hooked with the house try/catch + counter) rings when collateral would actually be USED — the CS welcome is otherwise sendable (sessions on the calendar + enrollment deadline set, the welcome gate's own facts) while the wizard's skip stamp still stands and the class is still live. Once per class EVER (`collateral_nudge:{classId}` sendOnce dedupe — the dashboard row stays the persistent reminder), deep-linked to `/admin?collateral={id}`, composed class name, honest copy ("its default is the PLAIN version — no flyer or parent letter attached"). Self-cancelling for real now: **the collateral panel's save clears `collateral_reminder_at`** — which also FIXES the stale post-PL-384 completion proxy (the dashboard row keyed on `!short_link`, but evergreen links made that column stay null forever: an evergreen school's completed collateral could never clear the row; saving now does). Registered as `AL_COLLATERAL_NUDGE` (draft v1 seeded, label/category class_ops/`{alertClassName}` var + sample pin — twin sends identical copy until ramped). **E2E (compile-and-call):** not-ready-but-stamped class → NO premature nudge (urgency gate holds); ready-but-skipped → the nudge composed and reached the send gate with the right key/recipient resolution (PL-60 suppressed the non-QA class_ops subscriber in dev — path proven to the wire; once-only is the sendOnce dedupe row a real send creates, the PL-402-proven primitive); stamp cleared → nothing fires. All fixtures restored.
**B. Fail-closed audit — every collateral consumer, with verdicts:**
· **CS welcome** (class-confirmed route) — already fail-closed (batch 24); now reads the NEW shared `collateralMissing()` helper (ONE readiness source; the follow-up mode keeps its sessions-only requirement).
· **Collateral artifact endpoint** (`/api/classes/{id}/collateral/*`) — **WAS FAIL-OPEN** (auth-gated but no readiness gate: it would render a flyer with a raw registration URL where the printed link belongs). NOW refuses with the plain reason (verified live: ASF → 400 "still missing: the session calendar · the enrollment deadline"; ready ISD → 200 PDF).
· **Counselor "Class materials" block** (counselor-class-card — BOTH callers: the portal view and the tokenized /class-roster page) — **WAS FAIL-OPEN** (buttons rendered unconditionally). NOW gates on the same facts and renders an honest wait note ("still being finished — they'll appear here automatically") instead of buttons; queries widened with `short_link` + `schools.evergreen_code`.
· **Flyer/letter template bodies** — compose cleanly BY DESIGN: blurbs fall back to computed defaults, the letter paragraph omits when absent, the logo element isn't emitted when missing (never a broken image), accent falls to HGL blue (`usableAccent`), the link resolves evergreen-first. Verdict: clean degrade — the gates above stop the one dishonest case (raw-URL/no-calendar materials reaching a counselor).
· **Public /classes + /c pages + embed** — logo guarded with fallbacks (embed falls to the HGL logo). Clean.
· **collateral-render logo fetch** — null-safe with a logged fallback. Clean.
· class-report/agreement-pdf/survey-qr — borrow renderer utilities only, not collateral-field consumers. N/A.
**NOTHING renders empty placeholders or broken images to a counselor or family.** Recipient note: the alert resolves through the class_ops subscription (prod config); dev sends are PL-60-suppressed per the standing guard.

## PL-429 (original) — Skipped collateral: email reminder with real urgency + a fail-closed audit (Scarlett, Aug 31)
Today a skipped-collateral class gets only the dashboard reminder (state-driven, self-clears — good but passive). **A. Email nudge, urgency-aware:** remind by email when it starts to MATTER, not on a dumb timer — tied to the first moment collateral would actually be used (the CS welcome becoming sendable / the class's comms schedule approaching whichever step consumes collateral): one reminder at a sensible lead ("MIS SAT Prep's collateral isn't set up — the counselor welcome goes out plain without it; set it up under Classes → Branding & collateral"), deep-linked, once per class per the alerted-once pattern (PL-402 style — the dashboard row stays the persistent reminder), self-cancelling when collateral is completed. Recipient = admin (billy@ in test). **B. Fail-closed AUDIT:** the CS welcome is verified fail-closed (batch 24); sweep EVERY other collateral consumer — counselor follow-ups, flyer/PDF generation, any template bodying collateral fields, portal surfaces — and verify each either composes cleanly without collateral or refuses with a plain reason; NOTHING may render empty placeholders or broken images to a counselor/family. List each consumer + verdict in ship notes; fix any that fail open. Verify: fixture class with skipped collateral → reminder email at the right moment, once; completing collateral cancels it; every consumer's no-collateral render inspected.

## ✅ PL-430 — SHIPPED (Aug 31)
The success banner composes through the ONE existing helper (`classDisplayLabel`, PL-274/328 rule): "MIS SAT Prep is created 🎉", "Online SAT Math Deep Dive…", "HGL ACT Prep…" — never a bare type (shortName is null at creation by construction; `course_meta.display_name` remains the /c sample-page naming source, not wired here — same as everywhere else the helper is used). **Sweep (banners/confirmations naming a class bare):** draft-saved names (`draftName()` — now composed, verified live: "MIS QA PL426 Draft A" in the banner and the drafts list), the cancel-class confirm (`classLabel` now the composed label), the run-anyway confirm message, and the staff-enroll panel header — all fed from one `composedLabel` per class row in admin/page.tsx. The duplicate-class prefill banner was already composed (verified pre-existing). Wider non-banner surfaces that hand-roll `nickname + class_type` (class card headers, tutoring/timecards/QBO/family-page labels) produce identical output for school classes and were left as-is — headers, not confirmations; listed here per the sweep rule.

## PL-430 (original) — Class-created banner names the FULL class (Scarlett, Aug 31)
The success banner says "SAT Prep is created 🎉" — no school, so the creator can't be sure which class just landed. Use the same display-name composition the rest of the portal uses (PL-274 rule: school class → "MIS SAT Prep", open class → its full name, "HGL ACT Prep" via course_meta) — one existing helper, not a new string. Quick sweep: any other confirmation banner/toast naming a class bare (cancel-class confirm, draft-saved notes, duplicate-class prefill banner) gets the same composed name; list in ship note.

**PL-427 amendment (Scarlett, Aug 31):** confirmed in the wild on the FAMILY registration flow too — advancing to the second page lands below the "Add 1-on-1 tutoring" title (a small scroll to even see the step heading). The registration flow is explicitly IN scope for the fix, family-facing and highest-traffic — verify it specifically at phone height, not just the admin wizards.
