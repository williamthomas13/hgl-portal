# Portal fixes — batch 48 (CLOSED for hand-off Sep 2, 2026 — 3 items, PL-453…455)

**Standing rules:** all prior (batch 41/42/44/45/46 lists).

(Closed Sep 2. Next PL: PL-456. PL-452 moved into batch 47 pre-hand-off.)

**Batch closed Sep 6 — 3 of 3 shipped.** Per-item ✅ notes below. Gate battery green at close vs the PROD build on :3100, pass COUNTS confirmed: `tsc` · `next build` · `smoke:public` (11) · `regress:links` (133) · `regress:pronouns` (61) · `regress:client-imports` (0 violations) · `regress:alert-pins` (23) · `regress:alert-categories` (25 keys) · **`regress:billing-recipient` (NEW, 25)** · `regress-cancel-class` (11) · `regress-resume-addon` (13) · `pl402-drift-alert-e2e` (ALL PASS). **Gate maintenance in this batch:** `regress-cancel-class` had hardcoded Sep 1/8 2026 class dates that drifted into the past on Sep 6 — /api/register refused the closed class and the gate failed for the harness's own reason (2 FAIL + a crash); dates are now relative (today+25/+32) and it passes 11/11 — a lesson for the standing list: harness fixtures with literal dates rot. **Migration `20260917000001` (PL-454, `families.billing_name`, additive) APPLIED via the Management API** — applied BEFORE the deploy on purpose: the two-phase rule exists so running code never references a column that isn't there, and for an ADD that ordering is column-first (a drop is code-first); nothing pending for Scarlett. PL-453's /70 re-sampled against the prod build: identical numbers to the hundredth.

**QuickBooks environment — the read-only ask (recorded, NOT flipped):** the deployed app's own status endpoint (`/api/qbo/status` on hgl-portal.vercel.app, read in Scarlett's signed-in Chrome) reports **`environment: "sandbox"`**, `status: connected`, realm **"Sandbox Company US 6f1e"**, connected Jul 16 by williamraymondthomas@gmail.com, item map fully set (Group Class 19 · 1-on-1 Tutoring 20 · 1-on-1 Test Prep 21 · 1-on-1 Subject Tutoring 22 · deposit → Stripe Clearing 1150040000), queue 0 waiting · 1 failed — so **the QuickBooks panel's SANDBOX pill IS showing** (the pill renders exactly when that field reads sandbox). Vercel: a `QBO_ENVIRONMENT` variable EXISTS on the project, Production scope, added Jul 8, but it is marked **Sensitive** (lock icon; "Copy to Clipboard" disabled), so its literal value cannot be read back from the dashboard by anyone — the runtime reading above is the truth: whatever the stored string is, it is not `production` (`qboEnvironment()` treats anything else as sandbox). **Scarlett's belief that the production keys are already in Vercel is not confirmed by anything readable:** the four QBO variables present (CLIENT_ID, CLIENT_SECRET, REDIRECT_URI, ENVIRONMENT) all date from Jul 8, the sandbox connection day. Cutover step 3c therefore starts from: sandbox environment, sandbox realm connected, production keys unverified — flipping means set `QBO_ENVIRONMENT=production` (+ production client id/secret if the Jul 8 ones are sandbox keys) → redeploy → reconnect → re-map items, in that order, and NOT in this batch.

**Awaiting Scarlett:** the PL-453 scrim veto (`docs/pl453-hero-scrim/`, before-75 · after-70 · rejected-65) · whether the templates page should grow a "billing" chip (PL-454 scope note) · everything still open from batch 47 (SLS crest, phone list, Deep Dive/PSAT codes, MIS announce).

**Also in this hand-off, docs only:** `docs/cutover-decommission-checklist.md` gained step 3b (QA purge discipline — dry run → review together → amend lists → apply; gcal sweep by hand; the PL-48 lists predate Janeson/ISD QA counselor/Willie Tomás sentinel/MIS QA conflicts) and step 3c (QBO out of sandbox BEFORE Stripe live: Intuit production-keys questionnaire 1–2 weeks early, bookkeeper Items 408-3/408-5 + Stripe Clearing, `QBO_ENVIRONMENT=production` → reconnect → re-map). Scarlett believes the production keys are already in Vercel — **Code: read the QuickBooks panel's SANDBOX pill + Vercel `QBO_ENVIRONMENT` and record the true starting state in the ship notes; do NOT flip the environment in this batch.**

## ✅ PL-453 — SHIPPED (Sep 6) — **/70 ships; /65 measured and REJECTED. Before/afters for Scarlett's veto: `docs/pl453-hero-scrim/` (before-75 · after-70 · rejected-65, × classes/class-page × desktop/375px)**
Both mural-hero surfaces (`app/classes/page.tsx` + `app/c/[slug]/view.tsx`, one class each) go `bg-hgl-slate/75` → **`/70`**. **Measured, not eyeballed** (`scripts/.tmp-pl453-sample.mjs`, puppeteer + pngjs): each surface × viewport was rendered twice — once for the screenshot, once with the TEXT LAYER HIDDEN so the composite pixels (mural + scrim) under the real headline box and the real bullet/body boxes could be sampled and scored against white. Contrast vs white, worst pixel / 99th percentile:

| scrim | classes desktop (headline worst · bullets p99) | classes 375px | class page desktop | class page 375px |
|---|---|---|---|---|
| /75 (before) | 4.12 · 4.10 | 3.96 · 5.11 | 4.00 · 4.14 | 3.58 · 4.14 |
| **/70 (shipped)** | **3.79 · 3.80** | **3.65 · 4.90** | **3.67 · 3.83** | **3.26 · 3.83** |
| /65 (rejected) | 3.49 · **3.48** | 3.32 · 4.67 | 3.38 · 3.54 | **2.94** · 3.54 |

Rule from the item: headline ≥3:1 at the brightest pixel (large-text AA) and bullets ≥3.5:1 at p99. **/70 passes everywhere** (headline never below 3.26:1; bullets never below 3.80:1 at p99, ≥3.99 at p95). **/65 fails both** — the extrabold headline drops to 2.94:1 at its worst pixel on the class page at 375px (below large-text AA), and /classes' body line falls to 3.48:1 at p99 — so per the item's own instruction the step stops at /70, the last passing value; the /65 shots are in the folder anyway so Scarlett can see what she'd be trading. The scrim never touches uploaded per-class hero photos (PL-351 frames them below the text — unchanged premise from PL-451). One Tailwind class per surface; revertible in one line each if vetoed. The PL-451 folder is left as the previous step's record.

## PL-453 — Hero scrim: go one more step brighter (Scarlett, Sep 2)
Scarlett saw the PL-451 before/afters and wants it brighter still — the /80 → /75 step was in the right direction but not enough. Ease the scrim one more measured step on BOTH mural-hero surfaces (`app/classes/page.tsx` + `app/c/[slug]/view.tsx` — one design, one class per surface): try `bg-hgl-slate/70` first, and if the headline still holds large-text AA (≥3:1 against the sampled brightest pixel) with bullets ≥3.5:1 at p99 over the mural, consider /65 as the candidate and show both. Same discipline as PL-451: sample the text column's lightest pixels rather than eyeballing, keep the white headline + bullets comfortably readable, and if a step drops bullets below ~3.5:1 at p99 say so and stop at the last passing value rather than pushing through. Uploaded per-class hero photos stay out of scope (PL-351 frames them below the text; the scrim never covers them). Refresh `docs/pl451-hero-scrim/` (or a `pl453-` sibling) with before/after × desktop/375px so Scarlett can pick — she keeps the veto.

## ✅ PL-454 — SHIPPED (Sep 6) — migration `20260917000001` (families.billing_name) **APPLIED** (additive column, applied BEFORE deploy — the two-phase rule's intent is that running code never references a missing column; for an ADD that means column first, code second)
**Doc-premise corrections, loudly:** the family did NOT have only one address — `families.billing_email` (+ `billing_cc_emails`) has existed since the July 13 phase-7a migration ("the mom's assistant") and FIVE tutoring sends were already routing to it **per call site** (`billing_email ?? parent_email` copy-pasted in T1, T1B, T2, T2B, T4), but **no UI anywhere could set it** (zero families had it set; only a direct DB edit could). So the real gap was: no name, no editor, no ONE rule, and PR1–4 left out. **What shipped:** ① `families.billing_name` (nullable; the email column keeps its delivery-only meaning, comments added). ② **The categorization lives in the registry key list and nowhere else:** `TemplateSeed.billing?: 'replaces' | 'copies-parent'` on `comms-template-seed.ts`, exported as `BILLING_TEMPLATE_KEYS`; ③ **ONE routing helper** `familyRecipients(family, templateKey)` in `app/utils/billing-recipient.ts` reads the flag — every billing send now spreads `...familyRecipients(...)` instead of its own expression (T1/T1B in tutoring-billing.ts, T2/T4/T2B in tutoring-stripe.ts, PR1–4 in the reminders cron via the class bundle's new `billingEmail`), and `billingAddress()` covers the two non-template spots (the Stripe customer's email at creation, the admin "send a manual email" mailto). A billing email equal to the parent's counts as unset. **Per-template verdict table:**

| template | verdict | why |
|---|---|---|
| T2_INVOICE, T2B_PAYMENT_REMINDER, T4_PAYMENT_FAILED | **replaces** the parent | money only — invoice / dunning / failed card |
| T1_MONTHLY_PROPOSAL, T1B_PROPOSAL_NUDGE | billing contact **to**, parent **cc** | the proposal also carries the month's schedule (the parent's facts); confirm link is family-tokenized so either can act |
| PR1–PR4 (class balance) | billing contact **to**, parent **cc** | carries the class facts; the enrollment is the parent's record |
| E0/SR receipts & confirmations, T8 welcome, T3/T_SCHEDULE_*, CX_FAMILY (cancellation + refund options), everything else | **never rerouted** | logistics / enrollment record; the parent's |
| autopay consent | not a separate email — the PL-362 nudge block rides T1/T1B/T2/T2B/T8; its `/tutoring/autopay/{token}` link resolves the FAMILY by id (no login) | unaffected, verified |
| "refund notices" | no family-facing refund template exists beyond CX_FAMILY's options block (AL_REFUND_REQUEST is a staff alert) | nothing to route |

**Guardrails:** the billing email is never a login (no auth user; the family-facts route rejects a bad address and rejects the parent's own address with a plain message; no dedupe against other families — a shared accountant is legitimate); unsubscribe/suppression untouched (all transactional); the send log records `to[0]` as `recipient_email` and the cc list as `cc` (the REAL addresses — the family comms timeline already matched on billing_email); the family's `billing_cc_emails` keeps riding exactly where it rode before (PR sends never cc'd and still don't). **Editors:** portal → Your information gains "Send billing emails to someone else — optional" (name + email rows, clearable, same `/api/admin/family-facts` write path, parent-scoped, logged in the PL-422C trail as "updated the billing contact"); admin family profile → the Billing card shows "Billing contact: {name} · {email}" with FactEdit on both (warning copy on the email) and the contact-facts list shows "Billing contact"; portal Billing section shows "Invoices go to {name} · {email} (change this under Your information)" when set. **Verified:** `regress:billing-recipient` (NEW gate, 26 checks) — the flag table equals this one; **291 key×family cases prove a no-billing family routes byte-identically to the legacy expression for EVERY registry key** (incl. billing==parent and whitespace); replaces/copies-parent/dedupe semantics; six non-billing keys stay with the parent even when a contact is set. E2E on Testy QAParent: set name + email from the family portal (real UI clicks), guards returned 400s (bad address, parent's own address), a REAL T2B reminder (compile-and-call `sendPaymentReminder`) logged `recipient_email = billy+billing@…`, `cc = null` and was delivered (row removed after); the ISD class bundle's enrollment row carries `billingEmail` for PR1–4; `/tutoring/autopay/{token}` rendered the consent page (200) with no login; a temporary QA invoice showed the portal "Invoices go to Pat Accountant · billy+billing@…" line (invoice deleted); admin profile round trip set → clear → clear name; family ends null/null, `families with billing_email set` = 0 (nothing live changed routing). **Scope note:** the templates admin page (a client component) does not yet show a "billing" chip beside audience — the categorization is readable in the seed and in this table; adding the chip means shipping the 150 KB seed to the browser or a new endpoint, deferred (say the word and it's PL-456-sized).

## PL-454 — Optional separate billing contact per family (Scarlett, Sep 2)
Parents ask for billing mail to go to a different address (a spouse, an office, an accountant) than the class-logistics mail. Today a family has ONE address — `families.parent_email`, which is also the sign-in identity and receives everything — and the Email preferences page is honestly a single marketing switch. Add an OPTIONAL billing contact: `families.billing_name` + `families.billing_email` (nullable; migration), editable by the family in the portal's **Your information** section ("Send billing emails to someone else — optional") and by staff on the family profile. **Routing rule, ONE place:** the billing-category sends — registration receipts/confirmations' money lines stay with the parent (they're the enrollment record), but the TUTORING billing set (T1 monthly proposal, T1b, T2 invoice, T4 payment failed, the autopay consent + dunning/overdue family-facing mail, refund notices, payment-reminder PR1–4 for class balances) go to the billing contact **when set** — decide per template whether the parent is ALSO copied (default: billing contact replaces the parent on invoice/receipt/dunning; parent stays on anything that also carries schedule or class facts). Record the per-template verdict table in the ship note; the categorization lives in the registry (a `billing: true` flag on the template key list), never per-callsite. Guardrails: the billing email is NOT a login (no auth user minted; the tokenized links in those emails keep working as they do now — verify the autopay consent token path still resolves the family), unsubscribe/suppression logic is unaffected (transactional), the send log shows the actual recipient, and clearing the field returns everything to the parent. Portal "Billing" section shows "Invoices go to {billing_email}" when set. Verify: set/clear round trip from the family portal + admin; a T2 invoice render + a PR reminder + the autopay flow each addressed correctly; a family with no billing contact is byte-identical to today (regress:links).

## ✅ PL-455 — SHIPPED (Sep 6) — one surface, four states verified
The family class card (`app/portal/parent-view.tsx`, the ONE family card — no other family surface rendered "Take the diagnostic test") now keeps the Synap link for the whole run. **Pre-start: byte-for-byte today's copy** ("first up: the diagnostic test, due {date} (the day before class)" + the blue "Take the diagnostic test" button). **From the first session on:** the reminder line becomes "Your practice tests live in the class's Synap group." (no due-date claim — the second diagnostic's timing is the instructor's call) and the button relabels to a calm "Diagnostic tests (Synap)" in the same quiet style as "Add to calendar". Same URL normalization as before (`synap.ac` → `https://synap.ac`). **Gone after the run by construction:** the card only renders for a FUTURE session and never for cancelled classes, so the day after the last session the card (and the link) is gone — no new state, nothing to forget. **No Synap group → nothing at all** (no line, no button — the batch-46 never-a-dead-button rule). E0/welcome + the diagnostic nudge emails untouched. **Verified via /admin/view-as on Testy QAParent (Reggie):** pre-start (ISD SAT Prep, Oct 13) unchanged; mid-run (a self-cleaning QA class with sessions −2/+3/+6 days + synap) shows the calm line + "Diagnostic tests (Synap)" → https://synap.ac; post-completion (sessions −9/−6/−2) shows no card and the page's only Synap link is the ISD pre-start button; no-synap (mid-run, `synap_group` null) shows the next-class card with no Synap line and no button. View-as renders the real ParentView component (PL-404), so the family's own portal is the same render by construction; the QA class was deleted after (enrollment + sessions + send rows).

## PL-455 — Synap link stays visible on the family class card for the whole class run (Scarlett, Sep 2)
"Where's the Synap link?" is a question staff get constantly. The family portal's class card shows the "Take the diagnostic test" button ONLY while `today < firstSession`; after day one it vanishes, though the SAME Synap group carries the second diagnostic near the end of the class and any catch-up/retake. Keep the link on the card for the entire run (pre-start through the last session; drop it once the class is completed/cancelled): pre-start keeps today's copy ("first up: the diagnostic test, due {date}" + button); from the first session on, relabel to a calm persistent link — "Diagnostic tests (Synap)" — with the reminder line replaced by "Your practice tests live in the class's Synap group" (no due-date claim after start; the second diagnostic's timing is the instructor's call). Same URL normalization the card already does. When the class has NO synap group (diagnostics off or unset), render nothing — never a dead button (the batch-46 audit rule). Also: the E0/welcome + diagnostic nudge emails already carry the link — unchanged. Verify: family portal render pre-start, mid-run, post-completion, and a no-synap class; view-as parity from admin.
