# Portal fixes — batch 54 (OPEN — Sep 22, 2026, Scarlett's review of the public pages on prod `4e0153f`)

**Standing rules:** all prior, incl. the PL-460 CTA rule and the PL-478 "nobody stranded" rule. Next PL after this file: PL-498. Everything below is public-page polish before the DNS cutover; no schema change expected except the nav JSON shape (PL-492). Measurements are from highergroundlearning.com at 1428px wide (Claude, Sep 22) — re-measure at 375px yourself.

## PL-492 — Public header: make it look like highergroundlearning.com's (Scarlett: "noticeably different")

The portal header mirrors the main site's *items* (PL-478) but not its look. Match the main site's CSS, not just its links:
- **Typography:** nav items are Pontano Sans 17px, weight 400, `text-transform: none`, no letter-spacing, ~26px apart; ours are 13px bold slate. Wordmark ~74×60 (`h-14`-ish, not `h-9`); the bar is ~104px tall with the wordmark vertically centred; max-width and side gutters like the main site (~57px at desktop), not `max-w-6xl`.
- **"Free Consultation" button:** `#00AEEE` (`hgl-blue`) background, white, Pontano/proxima 17px, ~7px radius, ~20px padding (61px tall on the main site — ours is a 13px pill). Keep it pointing at `/inquire?source=header`. "Sign in" stays (main site has a cart icon there instead) but styled quietly in the same face.
- **Over-hero variant:** on the main site the header is transparent and sits ON the hero with white text (see /team, /classes); on plain pages it's white with black text. Do the same: pages that already have a hero (/classes, /team, the class page, EvergreenCapture) get the transparent/white-text header over the image; /inquire, /compass, /partner, link-help and the notice cards keep a white bar with black text. The hero's scrim (PL-451/453) must still pass the measured contrast with the nav text on top of it — extend the existing check to the nav links.
- **Tutoring folder:** the seven service links (Buy 1-on-1 tutoring hours · Academic Support · SAT · ACT · AP/IB · University Applications · GRE/GMAT) nest under a **"Tutoring"** parent exactly as on the main site, so the bar reads *Tutoring · Classes · About · Team · Pricing · Contact*. Desktop: hover/focus-open dropdown (also keyboard-openable; `Escape` closes). Mobile sheet: "Tutoring" is an expandable group with the seven under it. Nav model: `NavItem` gets an optional `children: NavItem[]` (one level only); `parseNav` accepts it; `DEFAULT_SITE_NAV` re-seeds with the folder; a saved `site_nav` in `app_settings` that still has the seven flat items is migrated in place on first load (don't make Scarlett re-enter the menu). Settings → Public site menu shows/edits the nesting.
- Gate: smoke asserts the six top-level labels in order, the seven children under "Tutoring", and that every child is reachable without JS (the `<details>`/no-JS path). `regress:canonical-hosts` unchanged.

## PL-493 — Public footer: match the main site's footer; public email is info@, never kelsie@

Main site footer (white background, black text, blue `#00AEEE` links, Pontano 17px; © line "Some rights reserved."): a **newsletter block** on the left — H2 in the serif display face *"Finally get something useful in your inbox"*, body *"Sign up to receive occasional discounts, SAT/ACT info, strategy tips, college prep advice, and more."*, email field + blue **Sign Up** button, small print *"We won't share your info, and if our messages stop being useful to you later, it will be super easy to unsubscribe."* — then link columns **Questions?** (FAQs · Contact us) · **Support** (Terms and Conditions · Privacy Policy) · (About · Services · Team) · (Our Approach · Get Started · Bring HGL to your school), © line bottom right. Ours is a dark slate three-column footer with a tagline and contact lines.
- Restyle `SiteFooter` to that layout and palette. The newsletter block IS the College Prep Compass capture (PL-477) — post to the same endpoint the `/compass` page uses, same success copy; keep the `footer-compass` test id on the form. Portal-internal columns keep site-relative links (Team → `/team`, Get Started → `/inquire?source=footer`, Bring HGL to your school → `/partner`); the rest stay same-tab absolute links to the main site. `DEFAULT_FOOTER_NAV` grows group headings (`children` again) and re-seeds; migrate a saved `site_footer_nav` the same way as PL-492.
- **Email:** the main-site footer has no email/phone at all. If ours keeps them (Scarlett's call: fine either way), the address is **info@highergroundlearning.com**, not `contact_email`. `contact_email` is the *tutoring contact* (Kelsie — right for the T-series tutoring emails, leave that setting alone). Add `PUBLIC_CONTACT_EMAIL = 'info@highergroundlearning.com'` (constant, or a `public_contact_email` app_setting defaulting to it) and use it on every public surface: the footer and the `/inquire` "Rather just talk to a person? Email …" line. Phone stays `contact_phone`. Grep for any other public page reading `loadContactInfo().email`.
- Gate: smoke asserts no public page (`/`, `/classes`, `/team`, `/inquire`, `/compass`, `/partner`, one `/{code}`) contains `kelsie@`.

## PL-494 — /inquire form: drop the school helper text, move "How do you prefer to connect?" up, phone/email required by the chosen channel

1. **Remove** the placeholder *"Homeschooled or graduated? Just say so"* on "Student's school" (unreadable in the field). No replacement text.
2. **Order:** "How do you prefer to connect?" moves up so it comes BEFORE Email and Phone — row 1 First name · Last name, row 2 How do you prefer to connect? (full width), row 3 Email · Phone, then Student first · Student last, Student's school, help-with, anything-else. (Claude's reading of "up a line" — the channel choice now drives which of the two is required, so it has to be asked first.)
3. **Conditional required (supersedes the fixed nine of PL-488):** connectPref ∈ {call, text, whatsapp} → **Phone required**, Email optional; connectPref = email → **Email required**, Phone optional. Whatever is typed in the optional one is still saved. The `*` marker on Email/Phone follows the selection live; before a channel is picked both show as required. The honest error ("Please fill in: …") and **`/api/inquiry` server-side validation** use the same rule (the 400 names the field the chosen channel needs). The embed (`INQUIRE_SPEC` / `/embed/inquire.js`) follows the same rule — one rule, three places, as PL-488.
4. Gate: replace PL-488's nine blank-field probes with: each channel × missing its required field → 400; call/text/whatsapp with phone and no email → 200; email with email and no phone → 200; no channel + nothing → 400 naming the channel.
- Note for Scarlett (not for Code): a lead who picks "Phone call" and leaves Email blank gets no `IQ_INQUIRY_ACK` — staff reply by phone. Acceptable per her instruction; the pipeline row must not error on a null email.

## PL-495 — /team: hero photo full-bleed like /classes; member photos match the main site's

- **Hero:** the team photo (site-content block `team-hero`) spans the whole header band exactly as `PAGE_HERO` does on /classes — `absolute inset-0 object-cover` behind the heading with the same measured scrim, no slate frame or rounded inset image below the text (today: a slate band with the 856px photo sitting under the heading and grey either side). Heading + tagline centred over the image like the main site (*"Where know-how meets dynamism."* in the serif display face). Height in line with /classes' hero at the same viewport (main site's is ~740px at 1428 wide; ours needn't be that tall, but /team and /classes should match each other). Keep PL-481's editable slot and alt text; the contrast gate (PL-453) now applies to /team too.
- **Member photos:** the main site shows each person as a **1:1 square, ~278px wide at desktop (fills its column in a 3-up grid), `object-cover`, square corners, name centred beneath (serif, 20px), role in small caps beneath that, bio centred**. Ours are 112px circles beside the text. Match: square tiles filling the column (3 columns ≥1024px, 2 at tablet, 1 at phone), same aspect on every card, name/role/bio centred under. Uploaded portraits that aren't square are cropped centre by `object-cover`.
- Gate: smoke asserts every `/team` portrait renders with equal width and height and ≥ 240px at 1280 wide; the hero `img` covers the section's full width.

## PL-496 — Admin → Classes → Short links: past classes shown as "now showing" (Scarlett, Sep 22)

`/api/admin/evergreen` computes `serving` and `candidates` from `classes.status = 'open'` only, so the 13 backfilled ended classes (status open, last session in the past) read *"now showing AISJ SAT (starts Jan 16)"* while the public `/aisj` correctly falls through to the interest page because `resolveEvergreen` applies `classEnded()` (PL-470). One resolver: the admin route uses the same rule (`classEnded` from `app/utils/evergreen.ts`, or better, calls the same serving function the public route uses) for the serving line, the pin validation and the candidates list; an ended class can't be pinned. Gate: smoke seeds one open-but-ended class under a coded school and asserts the admin payload says between-classes for it while an open future class is served.

## PL-497 — /classes "Recent classes": show 16 before "more"

`RECENT_CAP` 12 → **16** in `app/classes/page.tsx` (13 recent today → all show, SIS included, no "more (1)"). "more" still appears only when there are more than 16; the `?recent=all` fallback and the PL-491 smoke stay (the smoke's "more" check skips honestly under the cap, as it already does). Homepage embed cap unchanged unless it shares the constant.
