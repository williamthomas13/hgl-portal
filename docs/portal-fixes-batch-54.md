# Portal fixes — batch 54 (OPEN — Sep 22, 2026, Scarlett's review of the public pages on prod `4e0153f`)

**Batch closed Sep 22 — 6 of 6 shipped (PL-492 · 493 · 494 · 495 · 496 · 497).** Gate battery vs the PROD build on :3100: `tsc` · `next build` · `smoke:public` **114** (was 69 — the nav shape on four pages, the no-JS sheet, the tones, the footer, the kelsie@ sweep across eight pages, the admin registry with a staff session, the channel matrix, the keyboard folder, the team tiles/hero, and the **contrast sampler**: at 1280 the h1 over the /70 scrim holds worst **4.11**:1 on /classes · **3.79** on /team · **3.83** on the class page; the overlay nav links worst **7.51 / p99 7.76** on /classes · **7.76 / 7.80** on /team · **6.80 / 7.60** on the class page — the top band earns those) · `regress:cta-landings` (232) · `regress:canonical-hosts` (49). No migration (the nav JSON shape is code-side; prod holds no saved `site_nav` / `site_footer_nav`, so the defaults seed). QA fixtures self-cleaned. **One deliberate departure, flagged:** EvergreenCapture keeps the white header bar — it has no hero image to sit on (see PL-492). **Recorded consequence:** the overlay header carries the HGL mark on the hero, so the /classes hero's own mark and the class-page lockup's HGL mark are retired (school logo only) — one mark per viewport, per PL-483.

**Standing rules:** all prior, incl. the PL-460 CTA rule and the PL-478 "nobody stranded" rule. Next PL after this file: PL-498. Everything below is public-page polish before the DNS cutover; no schema change expected except the nav JSON shape (PL-492). Measurements are from highergroundlearning.com at 1428px wide (Claude, Sep 22) — re-measure at 375px yourself.

## PL-492 — Public header: make it look like highergroundlearning.com's (Scarlett: "noticeably different")

### ✅ SHIPPED (Sep 22) — measured on the dev server at 1024/1280 and 375px, then the prod build in the gate battery

- **Look:** `SiteHeader` wears the public skin itself (Pontano Sans) so /inquire, /compass, /partner and the notice cards get it too. Measured at desktop: bar **104px**, wordmark **60px** tall (107 wide — our mark's own ratio), items **17px / weight 400 / no transform / 26px apart**, "Free Consultation" **61px tall, #00AEEE, 17px, 7px radius**, "Sign in" the same face in grey. Max width 1500px with 57px side gutters from `lg`, 20/32px below. The colour wordmark on the white bar, the white one on the overlay.
- **Two tones** (`tone="white" | "overlay"`, `data-tone` on the header): the overlay is `absolute` over the hero with white text; the hero clears it with `HEADER_CLEARANCE` (the same 64/80/104 heights) and a soft top band (`from-black/60 via-black/30 to-transparent`, 150% of the bar) sits behind the nav so 17px regular links MEASURE — the /70 scrim alone gives ~3.3:1, fine for a 36px bold headline, not for body-size links. Overlay on **/classes, /team, the class page** (in-progress and open alike; the sample-page variant keeps the white bar so its amber banner stays readable). White on **/inquire, /compass, /partner, link-help, the notice/state cards**. **EvergreenCapture stays on the white bar** — the doc lists it among "pages that already have a hero", but the no-upcoming card has no hero image (it is a card on the grey page); putting a transparent header over grey would be a white-on-grey header. A hero for that card is a separate decision — flagged for Scarlett.
- **Contrast is a gate now** (PL-453's sampler, in `smoke:public`): with the text layer hidden, every pixel under the h1 box and under each nav link/button box is sampled at 1280 on /classes, /team and the class page; the h1 must hold ≥3:1 at its worst pixel, the nav links ≥4.5:1 at the 99th percentile and ≥3:1 worst. Numbers in the close line.
- **Tutoring folder:** `NavItem.children` (one level; `parseNav` accepts it, a folder's URL may be blank), `DEFAULT_SITE_NAV` re-seeds *Tutoring · Classes · About · Team · Pricing · Contact* with the seven under Tutoring. Desktop = `NavFolder` (client): hover/focus open via CSS (`group-hover` / `group-focus-within`, so it works with JS off), click/Enter/Space toggles, **Escape closes** (and suppresses the hover re-open until the pointer/focus leaves). Mobile sheet: a nested `<details>` group. Every child is server-rendered in BOTH places, so nothing needs JS to reach. `migrateNav('header')`: a saved flat `site_nav` still carrying the seven service links folds them into a Tutoring folder at the position of the first, keeping order and show flags, and the JSON is rewritten once (prod has NO saved `site_nav` today — it seeds from the default). Settings → Public site menu edits the nesting ("make a folder" / "+ link inside", sub-rows indented).
- **Consequence recorded:** the overlay header carries the HGL mark ON the hero at every width, so the /classes hero's own mark and the class-page lockup's HGL mark are gone (`BrandLockup hglMark={false}` → school logo only) — PL-483's "one HGL mark per viewport" now holds via the header.
- Gate: smoke asserts the six labels in order (`data-nav-top`), the seven children under "Tutoring" on desktop AND in the `<details>` sheet (`data-nav-child`), the sheet expanding at 375px in a real browser, the keyboard open + Escape close, the tones per page, the contrast numbers. `regress:canonical-hosts` unchanged.


The portal header mirrors the main site's *items* (PL-478) but not its look. Match the main site's CSS, not just its links:
- **Typography:** nav items are Pontano Sans 17px, weight 400, `text-transform: none`, no letter-spacing, ~26px apart; ours are 13px bold slate. Wordmark ~74×60 (`h-14`-ish, not `h-9`); the bar is ~104px tall with the wordmark vertically centred; max-width and side gutters like the main site (~57px at desktop), not `max-w-6xl`.
- **"Free Consultation" button:** `#00AEEE` (`hgl-blue`) background, white, Pontano/proxima 17px, ~7px radius, ~20px padding (61px tall on the main site — ours is a 13px pill). Keep it pointing at `/inquire?source=header`. "Sign in" stays (main site has a cart icon there instead) but styled quietly in the same face.
- **Over-hero variant:** on the main site the header is transparent and sits ON the hero with white text (see /team, /classes); on plain pages it's white with black text. Do the same: pages that already have a hero (/classes, /team, the class page, EvergreenCapture) get the transparent/white-text header over the image; /inquire, /compass, /partner, link-help and the notice cards keep a white bar with black text. The hero's scrim (PL-451/453) must still pass the measured contrast with the nav text on top of it — extend the existing check to the nav links.
- **Tutoring folder:** the seven service links (Buy 1-on-1 tutoring hours · Academic Support · SAT · ACT · AP/IB · University Applications · GRE/GMAT) nest under a **"Tutoring"** parent exactly as on the main site, so the bar reads *Tutoring · Classes · About · Team · Pricing · Contact*. Desktop: hover/focus-open dropdown (also keyboard-openable; `Escape` closes). Mobile sheet: "Tutoring" is an expandable group with the seven under it. Nav model: `NavItem` gets an optional `children: NavItem[]` (one level only); `parseNav` accepts it; `DEFAULT_SITE_NAV` re-seeds with the folder; a saved `site_nav` in `app_settings` that still has the seven flat items is migrated in place on first load (don't make Scarlett re-enter the menu). Settings → Public site menu shows/edits the nesting.
- Gate: smoke asserts the six top-level labels in order, the seven children under "Tutoring", and that every child is reachable without JS (the `<details>`/no-JS path). `regress:canonical-hosts` unchanged.

## PL-493 — Public footer: match the main site's footer; public email is info@, never kelsie@

### ✅ SHIPPED (Sep 22) — with PL-492 (they share the nav model)

- **Layout/palette:** white, black text, `#00AEEE` links, Pontano 17px (measured), the newsletter block left (H2 in Source Serif 4 — the approved stand-in for the site's adonis-web — *"Finally get something useful in your inbox"*, the body line, email + blue **Sign Up**, the small print verbatim), the link columns right, contact line + *"© 2026 Higher Ground Learning. Some rights reserved."* along the bottom. Columns from the main site verbatim: **Questions?** (FAQs · Contact us) · **Support** (Terms and Conditions · Privacy Policy) · About · Services · Team · Our Approach · Get Started · Bring HGL to your school. Team → `/team`, Get Started → `/inquire?source=footer`, Bring HGL to your school → `/partner`; the rest same-tab absolute links (`/terms-of-service`, `/services`, `/our-approach` are the main site's real paths, read from its footer).
- **The newsletter block IS the Compass capture:** `FooterCompassForm` (client) posts to `COMPASS_SPEC.apiPath` (`/api/compass`, source `portal:footer`, honeypot included) and shows `COMPASS_SPEC.thankYou` on success; `data-testid="footer-compass"` stays on the form.
- **`DEFAULT_FOOTER_NAV`** = four groups (two headed, two heading-less — a group's label may be blank); `migrateNav('footer')` reshapes a saved flat list into the columns, carries a saved show flag over by link, and appends any saved link the columns don't know to the last group. Prod has no saved footer JSON today.
- **Email:** `PUBLIC_CONTACT_EMAIL = 'info@highergroundlearning.com'` (`app/utils/public-contact.ts`) on the footer, the /inquire "rather just talk to a person?" line and the /partner no-JS note. `contact_email` (Kelsie) is untouched and still feeds the T-series contact block; every other `contact.email` reader is a tokenized family surface (tutoring/confirm/schedule/autopay, agreements, intake, survey, waitlist, refund, coverage, convert, availability) — those are the tutoring contact by design, left alone. Phone stays `contact_phone`; the footer keeps email · phone · WhatsApp on its bottom line (Scarlett's call either way — easy to drop).
- Gate: smoke asserts no public page (`/`, `/classes`, `/team`, `/inquire`, `/compass`, `/partner`, `/qasmoke`, `/qasmoke2`) contains `kelsie@`, plus the newsletter block, the Compass form, the two headed columns, the © line and the info@ address on every audited page.


Main site footer (white background, black text, blue `#00AEEE` links, Pontano 17px; © line "Some rights reserved."): a **newsletter block** on the left — H2 in the serif display face *"Finally get something useful in your inbox"*, body *"Sign up to receive occasional discounts, SAT/ACT info, strategy tips, college prep advice, and more."*, email field + blue **Sign Up** button, small print *"We won't share your info, and if our messages stop being useful to you later, it will be super easy to unsubscribe."* — then link columns **Questions?** (FAQs · Contact us) · **Support** (Terms and Conditions · Privacy Policy) · (About · Services · Team) · (Our Approach · Get Started · Bring HGL to your school), © line bottom right. Ours is a dark slate three-column footer with a tagline and contact lines.
- Restyle `SiteFooter` to that layout and palette. The newsletter block IS the College Prep Compass capture (PL-477) — post to the same endpoint the `/compass` page uses, same success copy; keep the `footer-compass` test id on the form. Portal-internal columns keep site-relative links (Team → `/team`, Get Started → `/inquire?source=footer`, Bring HGL to your school → `/partner`); the rest stay same-tab absolute links to the main site. `DEFAULT_FOOTER_NAV` grows group headings (`children` again) and re-seeds; migrate a saved `site_footer_nav` the same way as PL-492.
- **Email:** the main-site footer has no email/phone at all. If ours keeps them (Scarlett's call: fine either way), the address is **info@highergroundlearning.com**, not `contact_email`. `contact_email` is the *tutoring contact* (Kelsie — right for the T-series tutoring emails, leave that setting alone). Add `PUBLIC_CONTACT_EMAIL = 'info@highergroundlearning.com'` (constant, or a `public_contact_email` app_setting defaulting to it) and use it on every public surface: the footer and the `/inquire` "Rather just talk to a person? Email …" line. Phone stays `contact_phone`. Grep for any other public page reading `loadContactInfo().email`.
- Gate: smoke asserts no public page (`/`, `/classes`, `/team`, `/inquire`, `/compass`, `/partner`, one `/{code}`) contains `kelsie@`.

## PL-494 — /inquire form: drop the school helper text, move "How do you prefer to connect?" up, phone/email required by the chosen channel

### ✅ SHIPPED (Sep 22) — page + embed + API, one rule

- Placeholder gone (page + `INQUIRE_SPEC`). Order: First · Last / **How do you prefer to connect?** (full width) / Email · Phone / Student first · last / Student's school (full width) / help-with / anything-else — the same order in the embed spec.
- **The rule lives once:** `normalizeConnectPref()` + `channelNeeds()` in `embed-forms.ts` — call · text · WhatsApp → Phone required, email → Email required, nothing chosen → both read as required and the error names the channel. The /inquire form computes its required list + live `*` markers from it; `/api/inquiry` builds its `REQUIRED` list from it (the 400 names the field the chosen channel needs); the embed script carries the SAME two functions serialised (`normalizeConnectPref.toString()`), with `requiredBy: 'channel'` on Email/Phone and markers that follow the select live.
- The optional one is still saved; a phone-channel lead with no email lands with `contact_email` null (the column is nullable), the per-email duplicate throttle and `IQ_INQUIRY_ACK` are skipped for it (staff reply by phone — Scarlett's note), and the admin alert prints whichever contact facts exist.
- Gate: PL-488's nine blank-field probes are replaced by the matrix — each of Phone call / Text / WhatsApp with a phone and no email → 200 and without a phone → 400 naming Phone only; Email with an email and no phone → 200 and without → 400 naming Email only; no channel + nothing → 400 naming the channel; a blank fixed field still names itself; the embed carries 7 fixed `required` + 2 `requiredBy: channel`, the channel field before Email, no placeholder, and the serialised rule.


1. **Remove** the placeholder *"Homeschooled or graduated? Just say so"* on "Student's school" (unreadable in the field). No replacement text.
2. **Order:** "How do you prefer to connect?" moves up so it comes BEFORE Email and Phone — row 1 First name · Last name, row 2 How do you prefer to connect? (full width), row 3 Email · Phone, then Student first · Student last, Student's school, help-with, anything-else. (Claude's reading of "up a line" — the channel choice now drives which of the two is required, so it has to be asked first.)
3. **Conditional required (supersedes the fixed nine of PL-488):** connectPref ∈ {call, text, whatsapp} → **Phone required**, Email optional; connectPref = email → **Email required**, Phone optional. Whatever is typed in the optional one is still saved. The `*` marker on Email/Phone follows the selection live; before a channel is picked both show as required. The honest error ("Please fill in: …") and **`/api/inquiry` server-side validation** use the same rule (the 400 names the field the chosen channel needs). The embed (`INQUIRE_SPEC` / `/embed/inquire.js`) follows the same rule — one rule, three places, as PL-488.
4. Gate: replace PL-488's nine blank-field probes with: each channel × missing its required field → 400; call/text/whatsapp with phone and no email → 200; email with email and no phone → 200; no channel + nothing → 400 naming the channel.
- Note for Scarlett (not for Code): a lead who picks "Phone call" and leaves Email blank gets no `IQ_INQUIRY_ACK` — staff reply by phone. Acceptable per her instruction; the pipeline row must not error on a null email.

## PL-495 — /team: hero photo full-bleed like /classes; member photos match the main site's

### ✅ SHIPPED (Sep 22) — with the PL-492 overlay header

- **Hero:** the `team-hero` block's photo is `absolute inset-0 object-cover` behind the heading with the same /70 scrim as /classes (the brand hero stands in if no photo is uploaded); heading + tagline centred, the tagline in the serif display face; `HERO_MIN_H` (300/380/460px) is shared by /team and /classes so the two match (the gate asserts equal heights at 1280). PL-481's editable slot + alt text unchanged. The h1 now sits over the image, so the PL-453 contrast sampler runs on /team too.
- **Portraits:** `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`, each tile `w-full aspect-square object-cover`, square corners; name centred (serif, 20px), role in small caps beneath, bio centred (still behind the tap-to-expand). Non-square uploads crop centre. No photo → an initials tile of the same shape. Measured at 1280: 16 tiles, all 307×307.
- Gate: every portrait renders with equal width and height and ≥240px at 1280; the hero `img` covers the section's full width and height; the two heroes match.


- **Hero:** the team photo (site-content block `team-hero`) spans the whole header band exactly as `PAGE_HERO` does on /classes — `absolute inset-0 object-cover` behind the heading with the same measured scrim, no slate frame or rounded inset image below the text (today: a slate band with the 856px photo sitting under the heading and grey either side). Heading + tagline centred over the image like the main site (*"Where know-how meets dynamism."* in the serif display face). Height in line with /classes' hero at the same viewport (main site's is ~740px at 1428 wide; ours needn't be that tall, but /team and /classes should match each other). Keep PL-481's editable slot and alt text; the contrast gate (PL-453) now applies to /team too.
- **Member photos:** the main site shows each person as a **1:1 square, ~278px wide at desktop (fills its column in a 3-up grid), `object-cover`, square corners, name centred beneath (serif, 20px), role in small caps beneath that, bio centred**. Ours are 112px circles beside the text. Match: square tiles filling the column (3 columns ≥1024px, 2 at tablet, 1 at phone), same aspect on every card, name/role/bio centred under. Uploaded portraits that aren't square are cropped centre by `object-cover`.
- Gate: smoke asserts every `/team` portrait renders with equal width and height and ≥ 240px at 1280 wide; the hero `img` covers the section's full width.

## PL-496 — Admin → Classes → Short links: past classes shown as "now showing" (Scarlett, Sep 22)

### ✅ SHIPPED (Sep 22)

- `resolveClass()` in `evergreen.ts` is exported (it also returns the serving class's id/type/start) and the admin GET calls it per coded school/course for the "now showing" line — pin wins while open AND not ended, else newest open not-ended, else nothing — exactly what `/{code}` serves. Candidates = open classes with `classEnded()` false (the query now carries sessions + timezone). The pin POST refuses an ended class with a plain-English reason.
- Gate: smoke seeds an open class whose last session was 33 days ago under the coded QASMOKE2 school and, with a staff session, asserts the admin payload says between-classes for it (serving null, no candidates), that the ended class can't be pinned (400), and that QASMOKE (open future cohorts) still reads "now showing" one of them. The public `/qasmoke2` no-upcoming check doubles as the other half.


`/api/admin/evergreen` computes `serving` and `candidates` from `classes.status = 'open'` only, so the 13 backfilled ended classes (status open, last session in the past) read *"now showing AISJ SAT (starts Jan 16)"* while the public `/aisj` correctly falls through to the interest page because `resolveEvergreen` applies `classEnded()` (PL-470). One resolver: the admin route uses the same rule (`classEnded` from `app/utils/evergreen.ts`, or better, calls the same serving function the public route uses) for the serving line, the pin validation and the candidates list; an ended class can't be pinned. Gate: smoke seeds one open-but-ended class under a coded school and asserts the admin payload says between-classes for it while an open future class is served.

## PL-497 — /classes "Recent classes": show 16 before "more"

### ✅ SHIPPED (Sep 22) — `RECENT_CAP = 16`; nothing else touched (the embed has its own cap). The PL-491 "more" smoke skips honestly under the cap.


`RECENT_CAP` 12 → **16** in `app/classes/page.tsx` (13 recent today → all show, SIS included, no "more (1)"). "more" still appears only when there are more than 16; the `?recent=all` fallback and the PL-491 smoke stay (the smoke's "more" check skips honestly under the cap, as it already does). Homepage embed cap unchanged unless it shares the constant.
