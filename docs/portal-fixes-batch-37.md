# Portal fixes — batch 37 (ACCUMULATING — opened Aug 15, 2026)

**Standing rules:** plain-English statuses · no internal shorthand · every alert deep-links its record + click-through to a resolution surface · samples from composers · `git push` after committing · PL-x IDs in commits · check items off here when shipped · registry template edits ship as new versions via anchor guards, twins+seeds lockstep · verify composed blocks via the composer path · inline confirm banners only · NO native browser dialogs · schedule-change baseline = what the family SAW at registration · financial facts never render into marketing copy (incl. /c pages) · two-phase column drops incl. post-drop grep · **test/QA email → billy@ / billy+*@ ONLY** · crashed gate ≠ green gate · public time labels = city names, never IANA zone cities.

## ✅ PL-354 — Scarlett's batch-36 verdicts (Aug 15): attach the two flagged images + confirm SLS = Milan
Both flagged stock composites are CLEARED — attach them to their blocks (alt text per the PL-351 rule). SLS = "Milan" CONFIRMED — clear its flag; the seed stands. Scarlett will do her copy walkthrough via Settings → Class pages separately.

**Shipped:** the "30-minute strategy sessions" and "1-on-1 tutoring" composites are attached to their what's-included cards with descriptive alt text ("An instructor working one-on-one with a student on test strategy" / "A student in an online 1-on-1 tutoring session at home"), same variant scheme as the PL-351 port (480/960/full-width webp renditions) — all four cards now carry images, verified rendering live. SLS = Milan: the seed stands as-is (it filled a NULL, so nothing to change); the confirmation is recorded here and the flag language cleared from the batch-36 notes. Editable any time: images under Settings → Class pages, the city on the Schools card.

## ✅ PL-355 — Class-page variant machinery (the batch-36 PL-348 amendment, now built)
Build the documented-but-unbuilt amendment in full (see batch-36 doc for the resolved details):
**A. Course keys + course-type block scope.** Model a course key on classes (how Deep Dive re-runs share identity — pick the simplest thing that works with clone/duplicate flows and say what you chose); content blocks gain scope shared-evergreen / course-type(key) / per-class; a new class of a course inherits its course-type blocks automatically, editable; "mint a new course block set" is a first-class blocks-admin action.
**B. Feeder-city automation.** Follow-up classes derive display cities from their feeder classes' schools (feeder linkage exists), each contributing city+timezone via `publicTimeCityLabel` machinery, same-offset cities collapsed onto one line; `display_cities` remains the manual override; fallback = own city, plainly labeled.
**C. Sibling-run cross-links.** Live classes sharing a course key cross-link ("Can't make these times? There's also a section at 6:30–8:00 pm Salt Lake City →"); no section object — sections are separate classes.
**D. Follow-up prerequisite line.** Per-class field, renders near the bullets.
**E. At-HGL variant.** HGL branding, location/directions block from the record, Salt-Lake-City-labeled times, full template shape; seed the "ACT Prep @ HGL" course-type block set from the DRAFT copy in the batch-36 doc, marked unreviewed for Scarlett's walkthrough; exam-name as a variable in the shared exam-registration fine-print block rather than an SAT/ACT fork (if simple).
Everything ships dark behind the same closing gates as batch 36.

**Shipped (migration `20260908000007`, idempotent, APPLIED via the Management API):**
**A — course keys, THE MODELING CHOICE:** `classes.course_key` is a stored slug DERIVED from class_type at creation, open-enrollment classes only (school classes carry NULL — their pages share the evergreen set and must never cross-link other schools' runs). Derived-from-type is the simplest thing that survives clone/duplicate: duplicates copy class_type, so re-runs share the key with nothing extra to remember. Documented trade-off: renaming the class type for a re-run changes which block set it inherits — the mint/copy action covers that deliberately. Existing opens backfilled (sat-deep-dive-…×2, psat-prep). Blocks gained scope shared / course(key) / class(id) with a DB consistency constraint; inheritance IS the render rule (course rows render on every matching class — nothing is copied); the blocks admin groups each course's set under its own heading ("every class of this course inherits these") and **"Mint a new course block set" is a first-class action** — key-shape validated, copy-from-an-existing-course or blank skeleton, refuses a course that already has blocks, who/when stamped. Verified live end-to-end: bad key refused, copy of act-prep minted 5 correctly re-keyed blocks, duplicate mint refused, rows cleaned.
**B — feeder cities:** a follow-up class derives its schedule display from its feeder classes' schools (feeder linkage = feeder.follow_on_class_id): each school contributes city+timezone (city via publicTimeCityLabel), each session row renders every city's OWN local time via zonedToUtc→formatTimeRange, same-rendered-time (same offset) cities collapse onto one line, and the schedule header says "Times are shown in each city's local time" instead of naming any zone city. `display_cities` stays the manual override (label list on the class's own times); no feeders → the PL-353 behavior. Verified on the REAL winter Deep Dive: its two real feeders (AISCT + ISD) render "11:00 PM–1:00 AM Cape Town · 10:00 PM–12:00 AM Düsseldorf" — genuinely different November offsets, correctly NOT collapsed.
**C — sibling cross-links:** live same-course sections (status open, registration not past) cross-link under the schedule — verified rendering exactly the resolved shape: "Can't make these times? There's also a section at 6:00–8:00 PM Salt Lake City →". No section object anywhere.
**D — prerequisite line:** new `classes.prerequisite_note`, editable on the wizard's Branding & Collateral step AND the Collateral card (duplicate-class prefill carries it), renders as "Prerequisite: …" under the hero bullets.
**E — at-HGL variant + ACT seed:** the "ACT Prep @ HGL" course-type set is seeded from the batch-36 draft copy with updated_by NULL, so every block shows "seeded copy — not yet reviewed" for Scarlett's walkthrough (hero blurb · Built Around How the ACT Actually Works · Sample of Topics Covered · Practice Tests That Mean Something · Where we meet). A wizard-created class with class_type "ACT Prep" inherits it automatically (course_key 'act-prep'). The location block renders for in-person open classes: frame copy from the block with `{address}` filled from the record + an always-from-the-record "Open in Google Maps →" link; times label "Salt Lake City" via the existing location-city rule; HGL-blue branding (no school identity) was already the school-less rendering. **Exam name is a VARIABLE, no fork:** the shared exam-registration fine print (heading now "Exam registration") uses `{examName}`/`{examRegistrationLink}`, substituted per class (ACT → ACT + ACT.org link, else SAT + College Board link) — the migration only rewrote the never-edited seed (updated_by null), so Scarlett's edits would have won. Substitution happens before markdown rendering, page-side; never money.
**Verified live:** ACT fixture page carried every probe (blurb, all three sections, prerequisite, location + Maps, Salt Lake City labels, ACT exam copy, cross-link both ways with a second section); ISD still substitutes SAT; the fixture pair was deleted after the walkthrough captures (the real ACT class gets created in the wizard when Scarlett's ready and inherits everything). PL-350 grew 'course' + 'location' section metrics (beacon whitelist + report labels). Gates: smoke:public 11/11, client-imports green, tsc clean, production build clean. **Walkthrough screenshots delivered:** ACT @ HGL desktop+mobile (from the temporary preview class) and the real Deep Dive desktop+mobile (live feeder cities). Still dark behind the batch-36 closing gates (Scarlett's walkthrough incl. the DRAFT ACT copy, then DNS).

## PL-356 — Class-page block links open in new tabs (Scarlett, Aug 15)
`site-md.ts` renders block links as same-tab anchors — a family clicking "Preview our curriculum" mid-registration navigates away and loses their flow. External/document links (http(s) absolute URLs, PDFs) get `target="_blank" rel="noopener noreferrer"`. Keep SAME-tab for true in-page/in-flow links: anchors (#…) and relative portal paths that ARE the flow (e.g. the register CTA itself). mailto:/tel: untouched. Applies wherever site-md renders (class pages + state cards).

## PL-357 — Wire the flow-only pitch block to the registration flow, or it lies (Scarlett, Aug 15)
Scarlett asked what editing the "1-on-1 tutoring pitch — NOT shown on class pages" block actually does. Answer today: NOTHING — the registration page's upsell copy is HARDCODED in `app/register/[id]/page.tsx` (~line 422); the block is an orphan whose edits go nowhere, primed to silently diverge from what families see (the PL-234 sample-vs-composer trap, in page form). Fix: the registration flow's second-page upsell section renders FROM the flow-only block (site-md, same one source; hardcoded copy deleted after wiring — verify rendered output matches current copy byte-for-byte before deleting). The admin label then tells the truth ("shown on the registration flow's second page — edit here"). If wiring is genuinely awkward, DELETE the block instead and say so — an editable surface that edits nothing is worse than no surface.

## PL-358 — Instructor profiles = ONE source; team page + class-page cards generated from it (Scarlett, Aug 15 — supersedes the earlier hand-curated-gallery version of this item)
**A. Profile fields on instructors** (the one instructors table): headshot (PL-351 image descriptor — alt required, webp renditions, upload in the profile editor), one-line credential ("Princeton", "International SAT, Math"), bio (site-md), plus `show_on_team` toggle and a display order. Editing happens in ONE place — the instructor profile editor in the Tutors panel. Adding a new instructor there populates everything below automatically.
**B. Public team page** `/team` on the portal, generated from profiles (show_on_team, ordered): circular headshots, name, credential, bio — same visual family as the /c pages (mobile-first; feel free to improve on the Squarespace formatting: e.g. tap-to-expand bios instead of a wall of text). Honest degrade for missing photos. Ships dark like /c.
**C. Class-page instructors block** stops being hand-curated copy: it renders featured profiles FROM the same source (a featured flag or per-block selection of instructor ids + the intro text), "Meet the whole team" links to /team (portal page once live; sqsp page meanwhile).
**D. One-time port from https://highergroundlearning.com/team:** scrape the current entries (William Thomas/President, Eric Brown/Executive Director, Jason Topa, Kelsie Rank, Gwendolyn DeSilva, Kevin Marren, Kaile Cota, Janet Amaya Pisco, Alexa Jordan, Charlotte Thayer, Toby Frazer, Trevor Smith, + any others) — headshots at full res (HGL's own staff photos), credentials, bios — and fill the matching instructors' profiles (name-match carefully; flag non-matches rather than creating duplicate instructor rows; non-teaching staff like William/Kelsie exist in the one instructors table already — PL-91 two-flags rule).
**E. Squarespace, meanwhile:** do NOT build code injection as the destination. Interim options in order of preference: (1) leave the sqsp team page as-is until the domain-era /team goes live, then swap the nav link (one-line sqsp edit, no injection); (2) if Scarlett wants the generated page inside sqsp sooner, a minimal script/iframe embed of /team is acceptable as a STOPGAP — but it's the Option-B pattern from the class-page decision: more moving parts, same content, retired at cutover. Recommend (1).
