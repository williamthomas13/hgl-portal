# Portal fixes — batch 39 (ACCUMULATING — opened Aug 16, 2026)

**Standing rules:** plain-English statuses · no internal shorthand · every alert deep-links its record + click-through to a resolution surface · samples from composers · `git push` after committing · PL-x IDs in commits · check items off here when shipped · registry template edits ship as new versions via anchor guards, twins+seeds lockstep · verify composed blocks via the composer path · inline confirm banners only · NO native browser dialogs · schedule-change baseline = what the family SAW at THEIR registration (incl. imports) · financial facts never render into marketing copy or structured markup (incl. /c pages) · two-phase column drops incl. post-drop grep · **test/QA email → billy@ / billy+*@ ONLY** · crashed gate ≠ green gate · public time labels = city names, never IANA zone cities · imports fire NO emails · previews use the REAL renderer, never approximations.

(Accumulating. Next PL: PL-383.)

## ✅ PL-368 — {examName} is wrong for PSAT classes, in BOTH switches (Scarlett, Aug 16)
**Shipped:** ONE shared switch — new client-safe leaf `app/utils/exam-family.ts` (`examFamilyFor`): **PSAT checked first** (\bpsat\b — "PSAT Prep" contains "sat"), then ACT, then SAT; PSAT returns `schoolBased: true` with NO links. Both former switches now delegate: `examInfoFor()` (email side) wraps it, and `/c/[slug]` substitutes from it — they structurally cannot disagree (plus a 10-assert agreement check ran across PSAT/SAT/ACT/Deep-Dive/Intensive-PSAT inputs). **PSAT guidance is school-based, honestly:** email `{examRegistrationLink}` and the VFAQ twin compose "the student's school — PSAT registration is handled by the school, so ask the school's counseling office about dates and sign-up" (never a College Board link); page-side, a markdown link wrapping `{examRegistrationLink}` COLLAPSES to the same plain wording (no dead/wrong link), so the shared fine-print reads "Visit the student's school — …" for PSAT classes. `{examName}` resolves "PSAT" on both sides. Admin blurb corrected: `{address}`/`{examName}`/`{examRegistrationLink}` fill in for SHARED and course blocks alike (they always did — the blurb lied), so Scarlett can rely on {examName} in shared cards (PL-371 uses this). Verified live: /c/psat-prep-fall26 renders PSAT + counseling-office wording + zero collegeboard.org; ISD unchanged (SAT + College Board link); composer-path E3_VFAQ both families 10/10.
Two separate exam-family switches exist and both misclassify PSAT: (1) email side — `examInfoFor()` (lifecycle.ts) matches `/sat/i`, so "PSAT Prep" → examName "SAT" + College Board registration link; (2) class-page side — `/c/[slug]/page.tsx` block substitution has NO fallback: anything without the word "ACT" renders "SAT". PSAT registration actually goes through the student's SCHOOL, not collegeboard.org. Fix both switches (PSAT checked BEFORE the sat pattern): examName "PSAT", and the registration guidance says school-based registration honestly (email `{examRegistrationLink}` falls back to plain wording, not a wrong link; page-side likewise). Keep the two switches from drifting — either share one helper or add a test asserting they agree on SAT/ACT/PSAT inputs. Also: the Class-pages admin blurb says {address}/{examName} fill in "in course blocks" — they substitute in SHARED blocks too; fix the wording so Scarlett can rely on it (this enables putting {examName} in the shared "Live class instruction" card).

## PL-369 — "What's included" cards must respect the class record (Scarlett, Aug 16)
The four shared `included-*` cards on /c pages currently render unconditionally on every class; make them class-fact-aware (facts from the class record, never from copy — standing rule):
**A. `included-instruction` title is mode-aware:** "Live class instruction" for in-person, **"Live online class instruction"** when the class's Delivery Mode (the Add-a-new-class checkbox → `delivery_mode='online'`) is online. Title variant composed from the record — Scarlett should not need two cards. Card renders for all classes.
**B. `included-strategy` (30-minute strategy sessions) renders ONLY for school classes.** Open-enrollment / "no school" classes — the Deep Dive follow-ups AND classes taught at Higher Ground — do NOT include strategy sessions; hide the card there. (Both current cases are the same `isOpenEnrollment` / no-school condition; if HGL-taught vs Deep Dive ever need to differ, that's a later distinction — for now no-school = no card.)
**C. `included-exams` (Full-length practice exams) gates on the diagnostics checkbox** (`has_diagnostics` from Add a new class): Deep Dives don't have it → no card; HGL-taught classes usually do → card shows. Fact-driven, not course-key-driven.
**D. Consistency sweep:** the shared FAQ copy also references strategy sessions and practice exams (e.g. "What is the 30-minute strategy session?", schedule FAQ's "full-length practice exams included" line) — those FAQ entries must follow the same conditions or a Deep Dive page will promise in the FAQ what the cards rightly hid. Report every shared block mentioning either feature and gate or reword each (report in ship notes which and how). Admin: the Class pages panel should label each conditional card with WHEN it shows ("shows only when the class has diagnostic tests", etc.) so the walkthrough reads honestly; count badges unaffected. Verify on the real winter Deep Dive page (no strategy/exams cards, correct FAQ) + ISD (all four) + an online class or sample (title variant), via the real renderer.

## PL-370 — Save a class as a draft and come back to it (Scarlett, Aug 16)
Add a new class currently keeps all wizard state in memory — leave the page and the work is lost; a class row only exists after Review completes (`classes.status` is only open/cancelled). Give it the same come-back-later ability as tutoring schedule drafts: **"Save draft"** available from any wizard step (only minimal validity required — e.g. a working name; NOT the full Next-gating), persisted server-side (not browser storage), resumable from a drafts list on the Classes tab (plain-English "Draft — not visible to families" status chip). **A draft is inert everywhere:** no public /c page (404/no sitemap/no shortlink), invisible to registration, sends nothing, excluded from min-enrollment/term reports/calendar/comms scheduling and from every "open classes" query — sweep the codebase for `status='open'` consumers and decide each explicitly (most want open only; the class-pages admin preview may reasonably target a draft for walkthrough purposes — decide and note). Implementation shape is Code's call (a `draft` status on the real row vs a stored wizard-state blob), but whichever way: finishing the wizard from a draft goes through the SAME final create/validate path as a fresh run (no half-validated classes leaking to open), slugs mint at completion (not at draft save — slugs are never rewritten, so don't mint from a working title), and deleting a draft is a plain inline-confirmed action with nothing downstream to clean. Verify: draft → leave → resume → finish = identical class to a straight-through run; draft invisible on /c, sitemap, registration, reports.

## PL-371 — Shared class-page blocks: literal "SAT" → {examName} sweep (Scarlett, Aug 16)
The SHARED blocks (they render on every class page, ACT and PSAT included) still hand-write "SAT" throughout — ported verbatim from the sqsp SAT-era copy. Sweep every shared block (cards, sections, FAQ bodies) and replace literal SAT mentions with `{examName}` — WITH judgment, not blind find-replace: (a) genuine comparisons stay literal (the improvement FAQ's "100 point increase on the SAT or 3 points on the ACT" — either recast exam-neutrally or leave both names; don't produce "{examName} or 3 points on the ACT"); (b) the PSAT FAQ ("Do you help students prep for the PSAT?") intentionally names both tests — leave or recast once PL-368 makes PSAT a real examName; (c) grammar must survive substitution for every value ("maximize SAT success" → "maximize {examName} success" is fine; check each); (d) score-scale-specific claims (point figures) must not silently apply to the wrong exam's scale. COURSE-set blocks are exam-specific by nature (act-prep says ACT, deep-dive says SAT) — leave literal, out of scope. Ship notes list every occurrence and its disposition (replaced / left literal + why). Preserve the unreviewed badges: this sweep must NOT stamp `updated_by` as if Scarlett reviewed the copy — her walkthrough is still pending. Verify via the real renderer: an ACT class/sample page shows zero stray "SAT" in shared sections; ISD still reads correctly.

## PL-372 — Featured-instructor cards: schools under the name on /c, job title stays on /team (Scarlett, Aug 16)
The class-page featured cards currently render the profile's `credential` field under the name — the same field /team shows (and feeds its Person JSON-LD jobTitle). Scarlett wants DIFFERENT text per surface: on /c pages, the schools the instructor teaches at ("ASF · ISD"); on /team, the job title as today. Add a class-page display field on the instructor profile (e.g. `classes_display_line`, edited in the profile editor's Public profile fieldset beside "Featured on class pages", plain label like "Shown under the name on class pages"): /c cards render it when set, falling back to `credential` when blank so nothing regresses; /team and its JSON-LD untouched. Free text, Scarlett-editable — do NOT auto-derive from feeder-school assignments (she wants to control it, and the display list may not equal the assignment list). Verify: ISD page cards show the new line, /team unchanged (page + JSON-LD), blank field = today's rendering.

## PL-373 — Shared-block images are in-person shots; online classes need online variants (Scarlett, Aug 16)
Online class pages currently render the shared sections' IN-PERSON photos (the whiteboard-lesson shot, the in-person 1-on-1 shot — alts "…teaching an SAT math lesson at the whiteboard", "…working one-on-one with a student on test strategy"). Wrong imagery for a class that happens on a screen. **A.** Give shared blocks a per-block ONLINE image variant beside the existing image control (label plainly, e.g. "Image shown for online classes"; layout hint shared with the main image); the /c renderer picks by the class's `delivery_mode` — online variant when the class is online AND a variant exists, else today's image (fail to the existing image, never to broken). **B.** Port the online-appropriate photos from the sqsp SLS page (https://highergroundlearning.com/sls — the St. Louis School live-online class): the markdown fetch exposes no img URLs, so pull them via the browser/sqsp admin per the PL-351 pipeline (480/960/full webp, honest descriptive alts for what the ONLINE photos actually show — never reuse the whiteboard alt), and set them as the online variants on the blocks whose in-person photos they replace. Ship notes: which blocks got variants, which sqsp images were used, and which shared blocks still have only an in-person image (report, don't invent). Verify via the real renderer on an online class (or `/c/sample--` with an online synthetic — confirm the sample machinery can exercise delivery_mode online; if not, that's part of this PL): online page shows the online photos with correct alts; ISD (in-person) unchanged.

## PL-374 — /c page top: hero image instead of the school-accent bar; fonts match the sqsp brand site (Scarlett, Aug 16)
**A. Hero.** The /c page top currently defaults to a big bar of the school's accent color — too jarring. Replace with the brand-site homepage's main image (found: https://images.squarespace-cdn.com/content/v1/626708ddb71d2605ec7f1693/8bc36428-9666-436c-9cb2-8178eb745ad2/hgl-bg-1.jpg — the SLC-headquarters shot; confirm in the browser it's the one Scarlett means and pull the full-res original via sqsp), ported through the PL-351 pipeline (responsive webp, honest alt) and served from OUR bucket — never hotlink squarespace-cdn (it dies at decommission). Text over the image must stay readable (overlay/scrim as needed, mobile checked). The accent color may still play a SMALL role (e.g. a thin rule, link/button tint on that page) — Code's design call, but no more large accent fields. Classes without a school keep whatever neutral treatment they have today unless the hero image reads right there too (probably it does — one hero everywhere is simpler; decide and note). **B. Fonts.** Match the /c (and /team, sample) public pages' typography to the current Squarespace site. The fonts aren't exposed in a markdown fetch — inspect the live site in the real browser (computed font-family on headings + body, weights, sizes/letter-spacing where load-bearing) and record them in ship notes. Licensing honestly: if they're Squarespace-licensed commercial fonts we can't self-host, say so and propose the closest Google-font match for Scarlett's sign-off rather than shipping a lookalike silently. Admin pages unchanged — this is the public-page skin. Verify: real winter Deep Dive + ISD + an online class page top-to-bottom in the browser (desktop + 1280px + mobile width), rich-results tester still green (hero/JSON-LD untouched or updated coherently).

## PL-375 — No-school class pages show the Higher Ground logo where school pages show the school's (Scarlett, Aug 16)
The /c page top renders the school's logo (`school.logo_url`) — school classes like ISD get theirs, but no-school/open-enrollment classes (Deep Dive, HGL-taught) currently get NOTHING in that slot. Render the Higher Ground logo there instead for schoolless classes. Assets already exist in the repo (`public/collateral/hgl-logo-color.png` / `hgl-logo-white.png` — pick per the PL-374 hero treatment's contrast; on the new hero image that likely means the white one over the scrim, Code's design call). Alt "Higher Ground Learning logo"; same size/placement as school logos so the two page variants read as one design. Applies to `/c/sample--` synthetics without a school too. Verify: winter Deep Dive page shows the HGL logo, ISD still shows the ISD logo, sample pages correct.

## PL-376 — ACT Prep course set: relabel, in-person imagery, facts composed from the record, tests block conditional (Scarlett, Aug 16)
**A. Relabel:** the course set currently labeled "Act Prep" in Settings → Class pages reads **"HGL ACT Prep"** (course display name wherever that label composes from — group header, sample-page naming; the course_key/slug does NOT change).
**B. Imagery:** the set's block images should match an in-person SAT class — use the in-person classroom photography (the shared whiteboard/1-on-1 shots' family, per the PL-351 pipeline), not whatever it carries now; alts honest.
**C. Hand-written class facts → composed.** The act-prep hero-blurb hand-writes "Eight hours of expert instruction, two full practice tests…" — class facts in copy, violating the standing rule, and wrong the moment a class is set up as 12 hours / 1 test. Add class-fact tokens to the /c substitution (alongside {examName}/{address}): **{instructionHours}** (computed from the class's real sessions — sum of session durations, formatted plainly, e.g. "eight hours"/"8 hours" — pick one style and note it) and **{practiceTestCount}** (from the record's `practice_test_count`, word-or-digit style matching). Recast the hero-blurb (and any other act-prep block hand-writing these numbers — sweep the set) to use the tokens; grammar must survive any value incl. 1 ("1 full practice tests" is not acceptable — pluralize properly). Tokens documented in the admin blurb. Sample--{key} synthetics need sensible values.
**D. Conditional block:** "Practice Tests That Mean Something" renders ONLY when the class actually has practice tests (`has_diagnostics` true / `practice_test_count` > 0 — align with PL-369's `included-exams` gate so the two never disagree). Its copy also hard-writes "Two full-length…" — same token treatment when it does render.
Verify via the real renderer on the sample page + a real ACT class when one exists: label, images, composed numbers matching the wizard values, block hidden when tests are zero.

## ✅ PL-377 — Class-page blocks: Scarlett approves ALL current copy — clear every "not yet reviewed" badge (Scarlett, Aug 16)
**Shipped FIRST in the batch** (deliberately before the copy-rewording PLs, so the approval covers exactly the copy Scarlett saw). A review is not an edit: migration `20260909000001` (APPLIED) adds `reviewed_by/reviewed_at` — the badges now key on "updated_by NULL **and** reviewed_by NULL", `updated_by` stays honest, and the block footer reads "**approved as-is by Scarlett, 16 August 2026**" instead of faking a change. One-time stamp applied to all 26 badge-carrying blocks → zero amber badges. Follow-ons: per-block **"mark reviewed"** link (on unreviewed blocks only) + per-group **"mark all reviewed"** beside the badge — both record the actual caller, so the next seeding clears without a Code round-trip. Rewording scripts in this batch (PL-369/371/376) CLEAR `reviewed_by` on the blocks they touch, re-raising badges only there. Verified: staff-cookie API 5/5 (zero unreviewed after stamp; a fresh seeded block raises the badge; mark_reviewed records the reviewer with `updated_by` still NULL).
Scarlett has reviewed and approved the class-page blocks as they stand (Shared 16 · act-prep · deep-dive · flow — all groups). One-time: mark every current block reviewed (stamp the review marker the badges key on — `updated_by` or equivalent — recording SCARLETT as the reviewer with today's date, honestly "approved by Scarlett, Aug 16", not a fake edit), so all amber badges clear. Two follow-ons: (1) add a per-block **"Mark reviewed"** (+ per-group "mark all reviewed") action in the admin so the next seeding doesn't need a Code round-trip to clear; (2) batch-39 PLs that RESEED or reword block copy (PL-371 sweep, PL-376 recasts, PL-369 FAQ gating) land those NEW versions as unreviewed again — approval covers today's copy, not future edits. Verify: zero amber badges after the stamp; a subsequent copy-editing PL re-raises badges only on the blocks it touched.

## PL-378 — Public class discovery: /classes browse page + evergreen school & course links (Scarlett, Aug 16)
Today the /c landing pages are reachable only by shortlink — fine for school classes (counselors share them), but follow-up (Deep Dive) and HGL-taught classes have no sharer, and the sqsp classes page won't exist post-cutover. Three pieces:
**A. `/classes` browse page** (public, portal-rendered, replaces the sqsp classes grid): every OPEN class from the DB — school classes with school logo, no-school classes with the HGL logo (PL-375 consistency) — location/country filtering like the sqsp page, capacity states composed from the record ("Class full" / "Only N available" — same computations the /c pages use, never hand-typed). Cards link to /c/{slug}. Consider a modest past-classes section (sqsp shows one). Draft classes (PL-370) never appear. Sitemap + the PL-374 skin (hero/fonts). The sqsp "Classes" nav repoints here at cutover (add to the PL-363 checklist).
**B. Evergreen per-SCHOOL links** — permanent `/{school-shortcode}` (e.g. hgl.co/aisct) resolving to the school's NEWEST OPEN class's /c page; when none is open → a friendly capture page ("No upcoming class at {school} right now — leave your email…") feeding the existing leads machinery, school-branded. These are PERMANENT (logos, counselor bookmarks, old emails) — never 404. Clarify vs the existing per-CLASS shortcodes (isd/mis/nido/sls): class shortcodes stay as-is for point-in-time shares; school links are the durable layer. If an existing class shortcode equals the natural school code, resolve the collision deliberately and note it.
**C. Evergreen per-COURSE links** — same pattern for no-school courses: `/act`, `/deep-dive` (Scarlett picks the codes in admin, editable) → newest open class of that course_key → interest-list fallback naming the course. This is how HGL/follow-up classes stay findable with no sharer: on /classes for browsers + one permanent URL per course.
Admin: school editor + course sets each show/edit their evergreen code with plain copy ("this link always works, even between classes"). Reserved-path collisions (team, classes, c, admin…) refused at edit. Verify: browse page against real+fixture classes at all capacity states; evergreen links live class → fallback flip when the class closes; drafts absent; rich-results/sitemap green.

**PL-378 amendment — hgl.co legacy forwards (Scarlett, Aug 16):** hgl.co/act ALREADY forwards (registrar-level) to the sqsp 1-on-1 ACT tutoring page (highergroundlearning.com/act) — and after the PL-349 DNS cutover the portal answers ALL of hgl.co, so every such forward dies unless preserved. (1) Add to the cutover checklist: Scarlett inventories every live hgl.co registrar forward; the portal serves each as a 301 to its highergroundlearning.com destination (a small legacy-redirects map, admin-editable so she can retire them later). (2) Evergreen course codes must not collide with the legacy map OR shortcodes OR reserved paths — `/act` is TAKEN by tutoring; the ACT class code is Scarlett's pick (e.g. /actprep). The code editor refuses collisions across all three namespaces with a plain-English reason.

## PL-379 — Email sweep: no diagnostic-test or strategy-session talk for classes that don't have them (Scarlett, Aug 16)
The step-level skips (synap_access, second_diagnostic) and the PL-274 composed variables (diagnosticDueLine, diagnosticPsE4Block, the what-you'll-get lists…) already guard most surfaces — but a seed sweep found hand-written survivors: **(1) LR_WELCOME** hand-writes the whole "1. The diagnostic test — this one's time-sensitive" numbered section + [Take the diagnostic test] button, unguarded — a late registrant to a no-diagnostics class (Deep Dive) is told to take a test that doesn't exist. Convert to a guarded composed block; the numbered list must renumber cleanly when the section drops (no "2." leading). **(2) The VFAQ email's strategy-session answer** — hand-written "understand your diagnostic score report" AND the strategy-session promise itself, which no-school classes (Deep Dive, HGL-taught) don't include at all (the email-side twin of PL-369-B): gate the whole Q&A on the same condition, and make the score-report phrase diagnostics-aware within it. **(3) Full sweep, not just these two:** grep every template seed + code twin + live registry body for diagnostic/Synap/strategy-session mentions outside guarded variables; every hit either composes from a guarded block or is skipped with the email; ship notes list each hit + disposition. Registry rule: live templates gaining guards ship as new versions, twins+seeds lockstep, anchor guards. Verify composer-path both-ways renders (diagnostics class vs Deep-Dive-like class: zero diagnostic/strategy mentions in the latter, incl. LR_WELCOME renumbering) + test sends → billy@ both cases.

## PL-380 — Raw ISO dates ("2026-10-13") in the admin roster report; sweep every email for them (Scarlett, Aug 16)
The roster report renders bare ISO dates throughout ("starts 2026-10-13" in the under-minimum list and every class card). Plain-English statuses is a STANDING rule — dates included: render "October 13, 2026" (match the family-email date formatter's style — ONE formatter, not a new hand-rolled one; timezone handling per the class as elsewhere). **Sweep:** grep every email composer — admin reports/alerts/digests AND family templates+twins+seeds — for raw ISO date rendering (unformatted date columns, toISOString slices, YYYY-MM-DD string interpolation); fix each through the shared formatter; ship notes list every surface caught. Subjects and preheaders count too. Verify: fresh roster-report render → billy@ with zero ISO dates, plus composer-path spot renders of any other fixed surfaces.

**PL-380 note:** the T_TIMECARD email is a confirmed offender — ISO dates in the SUBJECT, heading, and body ("Your timecard for 2026-08-01 – 2026-08-15"); render the pay period plainly ("August 1 – 15, 2026").

## PL-381 — Timecard email copy: explain what "correcting an exception" actually means (Scarlett, Aug 16)
The timecard-ready email says "correct any exception (a no-show)" and separately "no-shows are on the card on purpose — you're paid for reserved time," which reads as a contradiction: why correct something that's listed automatically? Reality: the card is built from the SCHEDULE — the portal can't know a student no-showed; the TUTOR marks it (or adjusts an actual duration), and the marking changes the record, not the pay. Rewrite the email (and the portal timecard panel's matching intro line — same words both places) to say that plainly, e.g.: the card assumes every scheduled session happened as planned; if a student didn't show or a session ran a different length, mark it so our records are right — your pay doesn't change for no-shows, you're paid for the reserved time either way. Registry rule if T_TIMECARD is a registered template (new version, twin+seed lockstep); composer-path render + test send → billy@.

## PL-382 — Emails say "Denver time" where pages say "Salt Lake City time" — converge on the ONE public label source (Scarlett, Aug 16; the standing residual, now promoted)
Confirmed cause: email composers' "(times shown in … time)" + zoned deadlines go through `friendlyZoneCity(timezone, location)` (comms-variables.ts:53), which only reads a city from a postal-address-shaped location — "Room 204" yields nothing → falls back to the IANA zone city ("Denver"). Public pages already resolve properly via `publicTimeCityLabel` (school city → display_cities → address city → zone city). Fix: EVERY email time label resolves through `publicTimeCityLabel` with the class's school city + display_cities plumbed into the comms context (bundle carries school already; add what's missing) — one source, per the standing rule (public time labels = city names, never IANA zone cities; this was always the intent, emails just lagged). Sweep: every "(times shown in …)" render, zoned deadline/expiry label, calendar-related copy, and any other composer time label — plus admin-facing emails IF they show family-facing strings (admin surfaces may keep zone ids per the PL-353 rule). Verify composer-path: a Room-204 HGL class renders "Salt Lake City time" (school-less → display_cities/location; decide what an HGL no-school class carries so it resolves to Salt Lake City, and note it), ISD renders Düsseldorf, an online class its display cities; test sends → billy@.
