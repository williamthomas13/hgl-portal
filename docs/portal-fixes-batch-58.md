# Portal fixes — batch 58 (OPEN — Sep 24, 2026): PL-507 homepage team embed · PL-508 strip fallbacks

**Sep 24 — PL-507 + PL-508 SHIPPED; PL-509 + PL-510 stay OPEN in this file (next round).** Pre-flight: `NODE_USE_ENV_PROXY=1 node scripts/project-sends.mjs --hours 48` → **0 projected actions**. Gate battery vs the PROD build on :3100: `tsc` · `next build` · `smoke:public` **136** (was 129 — the team embed's h2 / button / seeded four as 82px circles with credentials / zero hgl.co / team-empty preview, the PL-508 real-logo preview tiles, the QA page's second block) · `regress:cta-landings` (232) · `regress:canonical-hosts` (**79**, unchanged) · `regress:embed-order` (21) · **`regress:embed-team` (12, NEW)** · `regress:client-imports` (0). No migration, no env var, no DNS / Vercel domain touched. **Decisions recorded:** `embed_team_members` seeds with the first four in /team order (William · Eric · Jason · Kelsie) on first read — Scarlett changes it in Settings without a deploy; the button reads "Meet the team" per the doc (the site's own says "See more"). **Departures:** the homepage portraits re-measured at 1280 are 81×82 in a 163px tile on a 203px pitch (not the doc's 90 — that was the 1390px viewport), so the embed uses 82px circles / 163px tiles / 40px gaps; the PL-508 nickname rule is asserted in `regress:embed-team` (it compiles the pure monogram module alongside) rather than the embed-order fixture, since it is not an ordering rule; the person placeholder keeps initials (a person's initials ARE their name). QA fixtures self-cleaned.

**Standing rules:** all prior. Next PL after this file: PL-511. Cutover state: rows 1–16 + 8 done; row 19 (classes strip) is being pasted by Scarlett now; /team half of row 20 done (URL mapping + menu link); /classes half waits for the four running classes to end (~Oct 1). **Do not touch DNS / Vercel domains / env.** Pre-flight before any prod data change: `NODE_USE_ENV_PROXY=1 node scripts/project-sends.mjs --hours 48`.

## PL-507 — `/embed/team.js`: the homepage "Meet our team" section, fed by the portal (Scarlett, Sep 24)

### ✅ SHIPPED (Sep 24) — `/embed/team.js` + the `embed_team_members` setting + the QA page's second block

- **Measured on the homepage by DOM at 1280 (Sep 24, re-measured — not the doc's 90px):** "Meet our team" is a 63.08px adonis-web/400 display line (a `<p>`, centred); the portraits are **81×82 images inside a 50%-radius wrapper**, object-fit cover, on a **203px column pitch (163px tile + 40px gap, six across in 1178px)**, row pitch 305px; the name an adonis-web **26px/400** h2 **13px** under the circle; the role **17px Pontano Sans** (line-height 30.6); "See more" = the site's CTA (proxima-nova 17px/500, #00AEEE, 6.8px radius, 20.4px padding, 122×62) → /team. Ours: the same h2 scale as PL-506 (clamped for phones), 82px circles, 163px tiles at a 40px gap (56px between rows), 26px serif name 13px under, 17px role, the button reads **"Meet the team"** (the doc's wording) → `${publicSiteOrigin()}/team`. Round initials placeholder for a missing photo. Same 5-minute cache; zero hgl.co.
- **Who shows:** `app/utils/embed-team.ts` (pure) `selectTeam()` honours the setting's order, drops anyone with `show_on_team` false, ignores unknown ids, collapses duplicates, caps at **12**; `defaultTeamSeed()` = the first N in /team order. **Decision recorded (Scarlett's default):** `embed_team_members` seeds with the **first four in /team order = William · Eric · Jason · Kelsie** (the leadership entries) the first time it is read; edited under **Settings → Site content → "Homepage strip: team members"** (next to the priority schools; 1–12; only /team-visible people are offered). The credential line is the same field /team renders (`credential` — a title for leadership, subjects for tutors), the photo the same `headshot` image.
- **QA page:** a second block (`#hgl-team` + its own `?preview=` forwarder) under the classes strip; `?preview=team-empty` renders heading + button only.
- **Docs:** checklist **9c** (the paste-once snippet; the Squarespace tiles AND heading go); host-table row 19 lists both embeds.
- **Gate:** `regress:embed-team` (12 checks: order, hidden excluded, unknown ignored, duplicates, cap 12, empty, the seed, credential passthrough, + the PL-508 monogram rule); smoke asserts the h2, the button, the seeded four as 82px circles with credentials, zero hgl.co, the team-empty preview, the QA page's second block.


**Why:** the homepage's "Meet our team" section is twelve hand-placed Squarespace tiles (measured Sep 24: "Meet our team" as a 67px display line, 90×90 circular portraits with a 50% wrapper radius, name + role/subjects under each, "See more → /team"). Kelsie will maintain instructors in the portal from now on, so the homepage should read from the same records the portal /team page does — one edit, both places.

1. **The embed**, same pattern as `upcoming-classes.js` (PL-385/506): one Code-block snippet (`<div id="hgl-team">` + noscript link + `<script src=".../embed/team.js" defer>`), inline styles only, renders its own headline **"Meet our team"** in the measured homepage heading scale (same clamp as PL-506), then a row of tiles: circular portrait (same source images as /team, `rounded-full`, 90px on the homepage measure — verify via DOM again, don't reuse), name, and the credential line exactly as /team renders it (title for leadership, subjects for tutors — whatever field /team uses). Placeholder-initials tile for a missing photo, round. Below: a **"Meet the team" button** in the site's CTA style (PL-506's measured button) → `${publicSiteOrigin()}/team`. Same 5-minute edge cache; zero hgl.co (canonical-hosts stays 79).
2. **Who shows:** an admin setting `embed_team_members` (ordered list of profile/instructor ids, edited under Settings → Site content next to the priority schools, "Homepage strip: team members") — default **four**, seeded with the four leadership entries in /team order (Billy, Eric, Jason, Kelsie) so the first paste matches the top of today's section. The setting accepts 1–12; the strip lays out whatever count is chosen (4 across on desktop, 2 across on phones). Only `show_on_team` people are selectable; someone hidden from /team drops out of the homepage automatically. **DECISION for Scarlett (default = 4 leadership):** she may prefer four tutors or a mix — the setting makes it her call without a deploy.
3. **Preview page:** `public/sqsp-embed-test.html` gains a second block below the classes strip so both embeds are eyeballed together; `?preview=team-empty` (no members chosen → renders just the headline + button, never a hole).
4. **Docs:** checklist 9c = the paste-once team snippet + where it goes (replace the Squarespace "Meet our team" section; the Squarespace heading goes, the embed carries it); host-table row 19 lists both embeds.

Gate: `regress:embed-team` (new, pure ordering/selection module): setting order honored, hidden-from-team excluded, cap 12, empty state; `smoke:public` asserts the embed's h2 and button; canonical-hosts 79.

## PL-508 — strip fallbacks: nickname initials + real logos in previews (Scarlett, Sep 24)

### ✅ SHIPPED (Sep 24)

- **Nickname, not initials:** `app/utils/school-monogram.ts` `schoolMonogram(nickname, name)` — the nickname ("Nido", "SAS") when the school has one (clipped at 8 characters so a tile never overflows), full-name initials only without one, "HGL" for blank. ONE rule: the classes strip's inline tile (now `min-width:72px` with padding so "Leone" fits) and the /classes `SchoolTile` (which now takes `nickname`; the four /classes call sites pass it). The team tile has no school-style fallback — a person's placeholder is their initials, which IS their name.
- **Real logos in previews:** at preview time the route reads `schools.name → logo_url, nickname` (a read, never a write) and the synthetic schools named after real ones (Nido, SAS, ASM, MIS, Leone, SLS, ASF, ISD, SIS, AISJ, CAC) borrow that logo and nickname; everything else stays invented. 20/20 prod schools carry a logo (Sep 24), so the live strip never shows a monogram today.
- **Gate:** the monogram rule is asserted in `regress:embed-team` (it compiles the pure module alongside — the doc said "embed-order fixture"; the rule is not an ordering rule, so it rides the team gate's compile step instead — recorded); smoke asserts the `upcoming2` preview tiles carry a real `<img>` for Nido and SAS.

1. The initials tile in `upcoming-classes.js` (and PL-507's team tile when a school-style fallback applies) uses the school **nickname** ("Nido", "SAS"), not initials of the full name ("CNA" for Colegio Nido de Aguilas is not a name anyone uses). Every school in prod has a logo today (checked Sep 24: 20/20), so this only guards future schools — but the QA page showed it and it read wrong.
2. `?preview=…` synthetic rows carry **real logo URLs** (the actual Nido/SAS/… `logo_url`s read from the schools table at preview time; synthetic everything else) so the QA page looks like the site. Never data writes.
Gate: embed-order fixture asserts the nickname fallback; smoke asserts a preview tile carries an `<img>` not an initials box.

## PL-509 — homepage classes strip: fill the section (Scarlett, Sep 24, after the first live paste)
**Measured live on the homepage (1390px viewport, Sep 24):** the Code block's mount is **1279px** wide, but the four tiles render at **169px** each with a **54px** logo — the row fills ~700px and floats in the middle of a full-width section whose neighbours run edge to edge. The headline (63px adonis-web, 34px below) and the button (167×62, 17px) are right; the tiles are the problem.
1. **Tiles scale to the mount:** four across, `grid-template-columns: repeat(4, 1fr)` with a ~32px gap across the full mount width (so ~290px each at 1279px); logo box ~160px square (object-contain, the school-tile treatment), school name at the site's card-title size (measure the homepage's "Our Tutoring Services" card titles via DOM — those are the sibling cards), meta line ~17px. 2 across at ≤768px, 1 across at ≤420px, logo ~120px on phones. The large-card mode (2–3 upcoming) scales the same way (2–3 across the full mount).
2. Vertical rhythm: match the padding of the neighbouring sections above and below the block (measure) so the strip reads as one of them, not an inset widget.
3. Re-measure, don't reuse: PL-506's numbers were taken on the QA page; take these on the live homepage with the block in place.
Gate: smoke asserts tile width ≥ 20% of mount at 1280 and the 4/2/1 column breakpoints on the QA page (which should size its mount like the homepage: max-width 1279px).

## PL-510 — `/classes` headings: the site's display scale (Scarlett, Sep 24)
Measured on prod: `/classes` h1 "Classes" = 36px/800, section h2s "In progress" / "Recent classes" = 18px/700 — far below the site's display scale (homepage headings 63px/400 adonis-web → Source Serif 4 stand-in; the strip's h2 already uses it). Bring the page into the same family: **h1 "Classes"** at the site's page-title scale (measure a Squarespace inner page's h1, e.g. /about, /sat — not the homepage hero which is an image), weight 400 like the site, and **section headings** ("In progress", "Recent classes", "Open for registration" …) at the site's h2 scale (measure the same pages' h2s), also 400. Card titles stay as they are. Keep the phone clamp pattern from PL-506. Apply the same two levels to /team's h1 and any section headings, so the public pages agree. Gate: smoke asserts the computed sizes on /classes and /team at 1280 and 375.
