# Portal fixes — batch 52 (CLOSED for hand-off Sep 21, 2026 — Scarlett's public-page walkthrough, 10 items, PL-478…487)

**Standing rules:** all prior, incl. the PL-460 CTA rule.

(Opened + closed Sep 21, 2026 while Code was still finishing batch 51. Next PL: PL-488. **This file is UNTRACKED — Claude did not commit because Code's tree was dirty; Code: commit it as-is as your first step, before any edits.**)

**ORDER: finish + close batch 51 FIRST (the DNS flip waits on PL-471 / 470 / 472 / 474).** Then this batch. Several items here touch surfaces batch 51 just built — PL-470's in-progress page, PL-472's copy, PL-473/477's embeds: apply these changes ON TOP of them, don't fork. Within this batch: PL-485 + PL-480 + PL-482 (small copy/state fixes) → PL-484 → PL-479 → PL-483 → PL-481 → PL-478 (largest). Scarlett's walkthrough verdict otherwise: /login fine, pipeline fine, pages look good.

**Timecard stop-gap RETIRED Sep 21 (evening):** batch 51 verified by Claude on prod — `project-sends.mjs` shows 0 projected actions for the next 30 days with all four records-only classes named quiet — so Claude REMOVED the six pre-claimed `t5_timecard` rows (Gwen / Kevin / Rebecca × 2026-09-16, 2026-10-01; snapshot in `scripts/.tmp-removed-t5-preclaims-2026-09-21.json`). Tutoring can be tested with any tutor; timecard emails behave normally.

## PL-478 — Public pages get the main site's header + footer, so nobody is stranded (Scarlett, walkthrough Sep 21)

### ✅ SHIPPED (Sep 21) — E2E on the dev server at 375px + desktop (menu edited under Settings → rendered on /classes → reset to the defaults)
- **`SiteHeader`** (server component): HGL logo → highergroundlearning.com; the nav in the main site's order and wording; **Classes** and **Team** are portal links marked current (`aria-current="page"`) on their pages; right side "Free Consultation" → `/inquire?source=header` and a small "Sign in" → `/login`; mobile: the same items in a hamburger sheet (a no-JS `<details>`, so nothing shifts). **`SiteFooter`:** the footer links list, email · phone · WhatsApp (`wa.me/{digits}` from the existing contact settings — no new setting), the College Prep Compass sign-up link (PL-477), © line. Every link same-tab.
- **ONE source of truth:** `app/utils/site-nav.ts` — the lists live in `app_settings` (`site_nav`, `site_footer_nav`, JSON; defaults in code) and are edited under **Settings → Public site menu** (`site-nav-panel.tsx`: label · URL · order ↑↓ · show/hide · add/remove; API `set_site_nav` validates a site-relative or http(s) URL). Verified: an edit saved in the panel rendered on /classes on the next request; then reset.
- **Which pages got which variant:** FULL header + footer — `/classes`, `/{code}` + `/c/{slug}` (the live class page, its in-progress state, every closed / cancelled / no-class card, the between-classes capture), `/team`, `/inquire`, `/partner`, `/compass`, `/{code}/register` + `/register/{slug}` **step 1** (the form, and the ②③④⑦ state cards), `/waitlist/decline`, `/survey/{token}`, `/unsubscribe/{token}`, `/link-help`, `/interest/unsubscribe`. **COMPACT (logo + "← Back to class")** — the register flow's **add-on / checkout-focus step** (`viewPage === 'addons'`; the form picks between the two header nodes its page passes in), and `/success` (post-checkout, logo only). **NONE** — signed-in surfaces (family portal, instructor / counselor / tutor views, admin), `/login`, and every `/embed/*` (asserted in smoke). The `/register/{id}` page became a server page to render the header.
- **375px:** overflow 0 on every checked page; the mural hero's measured contrast is untouched (the header sits above it, not over it). **Same tab vs new tab:** recorded — the portal cannot set how a Squarespace link opens; the "Open in new tab" toggle on each Squarespace button (DNS runbook step 7) — recommendation SAME TAB; links from the portal to the main site are same-tab too.
"Should there be a top menu that mimics the main site so that they're not stranded?" Yes. Every PUBLIC portal page (`/classes`, `/{code}`, `/c/{slug}`, `/{code}/register` + the register flow, `/team` + bios, `/inquire`, `/partner`, `/compass`, the closed / in-progress / no-upcoming-class states, waitlist/survey/unsubscribe public pages) renders a shared site header and footer that mirror highergroundlearning.com:
- **Header:** HGL logo → `https://www.highergroundlearning.com`; nav in the main site's order and wording — Buy 1-on-1 tutoring hours (/1on1) · Academic Support · SAT · ACT · AP/IB · University Applications · GRE/GMAT · **Classes** (→ the portal's /classes, marked current when on a class page) · About · **Team** (→ portal /team) · Pricing · Contact; right side: "Free Consultation" button (→ portal /inquire once PL-473 lands; main-site /get-started until then) and a small "Sign in". Mobile: the same items in a hamburger sheet. Links to main-site pages are plain same-tab links.
- **Footer:** the main site's footer links (About, Team, Pricing, FAQs, Contact, Privacy Policy, Terms), WhatsApp + phone + email from the existing contact settings, © line. The College Prep Compass signup joins it when PL-477 ships.
- **ONE source of truth:** nav items live in a small editable list under Settings (label, URL, order, show/hide) seeded with the above — Scarlett changes the main site's menu a few times a year and must be able to mirror it without Code. NOT on signed-in surfaces (family portal, instructor/counselor views, admin) and NOT on the checkout-focus steps where a distraction-free header is deliberate (register step 2+ keeps logo-only + "Back to class"): say which pages got which variant.
- Embeds (`/embed/*`) never render it. Works at 375px; no layout shift; the mural hero keeps its measured contrast (PL-453).
- **Same tab vs new tab (Scarlett asked):** the portal cannot control how a Squarespace link opens — that's the "Open in new tab" toggle on each Squarespace link/button, set when the buttons are repointed (DNS runbook step 7). Recommendation recorded: keep SAME TAB (phones handle new tabs badly; back button keeps working) — the shared header is what prevents stranding. Links FROM the portal to the main site are same-tab too.

## PL-479 — /classes: new intro line, school cards with logos, "Talk to us" (Scarlett, walkthrough Sep 21)

### ✅ SHIPPED (Sep 21) — E2E on the dev server at 375px (QA school with an accent color and no logo → monogram tile; the four live classes render as In-progress cards with their real logos)
- **Intro line** verbatim (`data-testid="classes-intro"`). **Cards** (`data-testid="class-card"`, `data-state="open|in-progress|recent"`): `SchoolTile` — the logo contained on a white tile, or a **monogram tile in the school's accent color** (`usableAccent`, the flyer's same near-white safety) — school name, class type, "Live online / In person · city", dates. **Open:** "Registration open — closes {date}", "Only N spots left" only when ≤ 5 (the existing threshold), "Class full — waitlist open", button **View class**. **In progress:** "Under way — registration closed" (PL-470 state). **Recent:** "{city} · {Month YYYY} · {class type}", linking to the school's `/{code}` page, cap 12 + "and N more schools". Grid 1 col at 375px (overflow 0) → 2 → 3 on desktop. **Button** "Talk to us" → `/inquire?source=classes`.
- **Backfill JSON** gains optional `"logo": "path/to/file.png"` per school → `backfill-past-classes.mjs` runs the file through the SAME processing as the school-logo route (`processLogo`: background flood-fill + trim; `looksLikeImage` guard) and stores it at `school-assets/{schoolId}/logo-{ts}.png`, only when the school has no logo; a bad file warns and the school keeps the monogram (re-run with a fixed file). Claude supplies the files.
- **Homepage embed (APPROVED):** when NO class is open, `upcoming-classes.js` renders up to 4 recent-class school tiles (logo or monogram, name + city, linking to `/{code}`) + "See all classes →" instead of only the interest line; `?preview=empty` shows this state with the real recent classes. With classes open the compact strip is unchanged.
- **smoke:public** now asserts the card markup + tile, the intro line + button, and (PL-478) the header/footer on /classes, the in-progress code page, /team, /inquire, and that the embed never carries the header. JSON-LD / sitemap unchanged.
- **Intro line** under the "Classes" heading → **"Live test-prep classes at partner schools around the world, online, and at our HQ in Salt Lake City, USA."**
- **Cards, not a text list** — like the Squarespace store grid: one card per class with the SCHOOL LOGO (schools.logo_url; graceful monogram tile in the school's accent color when a logo is missing — never a broken image), school name, class type, city, and a status line. Open classes first (dates, "Registration open — closes {date}", seats-left only when ≤ the existing threshold, button "View class"); then **In progress** (PL-470 state: "Under way — registration closed"); then **Recent classes** (ended, not cancelled: "Milan · June 2026") — each card links to its `/{code}` page. Responsive grid (1 col at 375px, 2–3 on desktop), logos contained on a white tile so mixed aspect ratios look deliberate.
- **Button** "Not sure which class fits? Talk to us" → **"Talk to us"** (→ /inquire with `source=classes`).
- PL-476's backfill JSON gains an optional `logo` per school (a file path Claude supplies; uploaded through the same storage path as the school-logo route) so the 12 backfilled schools arrive with logos; schools without one get the monogram tile.
- The homepage embed (`upcoming-classes.js`) keeps its compact strip — but when NO class is open it may show up to 4 recent-class logo tiles + "See all classes →" instead of only the interest-list line (Scarlett: "it's a better look to show past schools compared to emptiness"). **APPROVED by Scarlett Sep 21 — build it.**
- JSON-LD / sitemap unchanged in meaning; update `smoke:public` for the card markup.

## PL-480 — Register-page states: "View our upcoming classes" + a FULL class reads differently from a CLOSED one (Scarlett, walkthrough Sep 21)

### ✅ SHIPPED (Sep 21) — migration `20260921000002` APPLIED (the block text update is inside it); E2E on the dev server at 375px with full-open / full-closed / open fixtures (cleaned)

**State matrix (`registration-form.tsx`), each state's copy deliberate:**

| # | State | Detect | What the page shows |
|---|---|---|---|
| ① | open | today ≤ close, not full | the registration form |
| ② | **full, deadline not passed** | today ≤ close, `isFull` | h1 "Join the Waitlist"; the ONE wording (`data-testid="state-full-open"`: *"This class is currently full. When a class is full, we'll try to teach an additional section. Leave us your email and we'll notify you if we're able to open up a place for you!"* + "Join the waitlist below (no payment now)…"); the waitlist join form; the interest capture (PL-484 fields + Compass opt-in) as the lighter door |
| ③ | **full, deadline passed ≤ 7 days ago** | today > close, `isFull`, ≤ 7 d | notice card titled *"This class is currently full"* with the same wording (`data-testid="state-full-closed"`) + the interest capture — NOT "you missed it". **Waitlist API after the deadline today:** `POST /api/waitlist` returns **410 "Registration for this class has closed."** when today > the close date (and 409 when spots are open), so the join form is honestly gone; the interest list is the door |
| ④ | closed (not full) | today > close, > 7 d or not full | "Registration for this class has closed" + PL-470's "See the class page — schedule, venue and calendar →" when the class is under way, else **"View our upcoming classes."** → `/classes` (never the main site) + the interest capture |
| ⑤ | in progress | (PL-470) | `/{code}` renders the in-progress page; `/register` = ④ with the back-link |
| ⑥ | ended | code falls through | PL-472 interest state |
| ⑦ | cancelled | `status='cancelled'` | reads as "This class is full" (PHASE4 §12) — "…not accepting new registrations. **View our upcoming classes.**" → `/classes` + interest capture |

**ONE wording source:** the `waitlist-note` site content block (Settings → Class pages → States; heading "This class is currently full" + Scarlett's sentence — the migration updated the seeded body only because it still read as seeded) — `class-info` returns it as `waitlist_note`, the register page's ② and ③ render it, the class page's full banner already read it; `app/utils/class-full-copy.ts` is the code fallback. **Every "class is full" / waitlist site, listed:** register page ② banner (converged) · register page ③ card (converged) · register page ⑦ cancelled card (title only, by design) · class page full banner (the block, already) · /classes card status "Class full — waitlist open" (short form, kept) · closed card (PL-470 — not a full state) · **W-series emails** `W1_WAITLIST` (confirmation), `W2_SPOT_OPEN`, `WR_WAITLIST_RELEASE` ("…the class stayed full, and we weren't able to open up a place…"), `CX_WAITLIST` — all LIVE registry copy Scarlett owns; listed, not rewritten. Staff side unchanged: ②/③ joins land on the roster's waitlist; the additional-section decision stays human.
- Closed state (`registration-form.tsx` ~437): "Upcoming classes are listed on our main site." → **"View our upcoming classes."** linking to `/classes` (the portal's page, PL-479) — never the main site.
- **State matrix — write it down in the ship note and make each state's copy deliberate:** ① open ② **full, deadline not passed** ③ **full, deadline passed ≤ 7 days ago** ④ closed (not full) ⑤ in progress (PL-470) ⑥ ended ⑦ cancelled. For ② and ③ the page must NOT read like "you missed it": use HGL's long-standing wording — **"When a class is full, we'll try to teach an additional section. Leave us your email and we'll notify you if we're able to open up a place for you!"** — above the existing waitlist join (②) or the interest capture (③, where the waitlist no longer accepts joins — say what the waitlist API does after the deadline today). Scarlett believes a similar sentence already exists elsewhere in the portal: find every "class is full" / waitlist message (register page, class page, /classes card, W-series emails, closed card) and converge them on ONE wording source; list the sites.
- Staff side: a join in ②/③ shows on the roster's waitlist as today; the "additional section" decision stays a human one (no automation).

## PL-481 — /team (Scarlett, walkthrough Sep 21)

### ✅ SHIPPED (Sep 21) — migration `20260921000003` APPLIED (the `team-hero` site block); the photo itself is Claude + Scarlett's upload
- **Editable image slot:** site content block `team-hero` (section **Team** under Settings → Class pages — the block's existing image control uploads through `/api/admin/site-content/image` with `target=block&key=team-hero`, variants generated, alt text required — no hot-linking to Squarespace's CDN). The page renders it when set. **Placement decision:** the photo sits BELOW the heading + tagline, full width inside the slate hero band — no text over it, so the PL-453 contrast question never arises (the alternative, a measured scrim over an unknown photo, would have to be re-measured every time the photo changes).
- **Visible tagline:** "Where know-how meets dynamism" (the block's body, editable; `data-testid="team-tagline"`) — the " — the instructors and staff behind Higher Ground Learning." sentence removed from the page and KEPT in `<meta name="description">`, OpenGraph and the JSON-LD (unchanged there).
- **Button** → "Schedule a free consultation" (`CONSULT_CTA`) → `/inquire?source=team`. Careers/onboarding untouched.
- Hero uses the SAME current team photo as highergroundlearning.com/team — an editable image slot in site content (Claude uploads the file from the main site with Scarlett; no hot-linking to Squarespace's CDN). Keep measured text contrast over it (PL-453 method) or place the heading beside/below the photo.
- Visible tagline: keep "Where know-how meets dynamism", REMOVE " — the instructors and staff behind Higher Ground Learning." from the page. KEEP that sentence in the `<meta name="description">` / OpenGraph / JSON-LD (that is what search engines and AI assistants read — removing it from the visible page costs nothing there).
- Button "Work with us — free consultation" → **"Schedule a free consultation"** ("work with us" reads as a job ad). → `/inquire?source=team`.
- (Careers + new-hire paperwork is filed separately for after Phase 8 — `claude/hgl-hiring-onboarding-roadmap.md`. Do not build here.)

## PL-482 — /inquire polish (Scarlett, walkthrough Sep 21)

### ✅ SHIPPED (Sep 21) — migration `20260921000002` APPLIED (additive: `leads.contact_first_name / contact_last_name / student_first_name / student_last_name / connect_pref`); E2E on the dev server
- **Split names** on `/inquire` (First name* · Last name* · Student first name · Student last name) and in the PL-473 embed spec (same fields + the "How do you prefer to connect?" select for parity). `/api/inquiry` stores the split columns AND the derived display names (`contact_name` / `student_name`) so every existing reader keeps working; legacy single-name callers still land. **Conversion:** `create_family` uses the split columns when present — no splitting logic (PL-466); the whitespace split survives only as the fallback for pre-PL-482 rows.
- **Intro copy** → *"Tell us a little about what you're looking for and we'll usually be able to reach out the same day. We'll get the rest of the details later when we connect!"* (page + embed).
- **Success bug:** the page's `<strong>thank you!</strong> We'll…` lost its boundary space to JSX (the PL-16 lesson) → `{' '}`. Checked every other success card: the embed strings carry their spaces; `InterestCapture` / `EvergreenCapture` use `{' '}`; the register/waitlist done card too.
- **Staff alert sentence:** "…asked about SAT Prep for Desmond **and wants us to get in touch via WhatsApp**." — omitted cleanly when no preference; stored as `connect_pref` (whatsapp | call | text | email) and shown on the Prospective Students card's first line ("· wants us to get in touch via phone call").
- Confirmed: the "Rather just talk to a person?" line reads `loadContactInfo()` — unchanged.
- **Split names:** "Your name" → First name* + Last name*; "Student's name" → Student first name + Student last name (optional). Stored in separate columns on the lead (migrate the existing single-name columns additively; keep a derived display name), carried into the family/student on conversion without any splitting logic (PL-466: both surnames live in the last-name field). Same fields in the PL-473 embed.
- Intro copy → **"Tell us a little about what you're looking for and we'll usually be able to reach out the same day. We'll get the rest of the details later when we connect!"**
- Success message bug: "Got it — thank you!We'll be in touch soon" → missing space/line break after "thank you!". Check the embed + every other success card for the same concatenation.
- **Staff alert sentence** gains the contact preference: "Billy Thomas (email, phone) asked about SAT Prep for Desmond **and wants us to get in touch via {WhatsApp | phone call | text | email}**." — omitted cleanly when they didn't pick one. Same fact on the Prospective Students card, first line.
- Confirmed, no change needed: the "Rather just talk to a person?" line reads the contact name/email/phone from the portal's contact settings (`loadContactInfo`) — not hard-coded.

## PL-483 — Co-branding on school class pages: HGL + the school (Scarlett, walkthrough Sep 21)

### ✅ SHIPPED (Sep 21) — `app/components/BrandLockup.tsx`, one component on the hero and every state card
- **The lockup:** HGL mark · thin divider · the school's logo on a white tile, equal optical height (`h-14` on the hero in the white treatment over the scrim, `h-10` on cards) — `data-lockup="hgl-x-school"`. No school logo → `hgl-only` (the school slot is simply absent — never two HGL logos). Wired into: the class-page hero (replacing the old school-logo-or-HGL-white slot), `ClassStateCard` (closed / cancelled / no-class, receives the school's logo), `PublicNoticeCard` (the register page's ③④⑦ cards, the class-info payload now carries `schools.logo_url`).
- **Viewport rule (the "three times at 375px" question) — decided and stated:** PL-478's site header carries the HGL mark on every public page. **At phone width (< 640px) the header IS the HGL mark: the lockup shows the SCHOOL logo only (or nothing), the /classes hero mark is hidden too.** From `sm` up the header + the full lockup both render (two HGL marks, never three). Leone XIII / SLS / ASF / new schools: when Scarlett uploads their logos the lockup fills in by itself.
Class/code pages and their state cards show the school's logo; when a school has none, the HGL logo is substituted (seen on Leone XIII). Scarlett will upload logos for Leone and the new schools — AND wants the HGL logo on these pages always, alongside the school's: a deliberate lockup — HGL mark · thin divider (or "×") · school logo, equal optical height, on the card header and the class-page hero, collapsing to HGL-only when the school has no logo (never two HGL logos). With PL-478's header carrying the HGL logo too, make sure the page doesn't show it three times at 375px — pick ONE of header/lockup per viewport and say which.

## PL-484 — Interest list: a name, a confirmation email, and the Compass opt-in (Scarlett, walkthrough Sep 21)

### ✅ SHIPPED (Sep 21) — migration `20260921000002` APPLIED (`class_interest.parent_first_name / parent_last_name / student_first_name / compass_opt_in / unsubscribed_at / confirmation_sent_at`); `CI_INTEREST_CONFIRM` seeded as DRAFT v1 (approved copy); E2E on the dev server at 375px
- **Fields:** ONE `InterestCapture` component everywhere (class states, register page ②③④⑦, the between-classes code page via `EvergreenCapture`): Email* · Your first name* · Last name · Student's first name (optional) — one compact row group, every input full width (one-handed at 375px, overflow 0). Split per PL-482; `parent_name` / `student_name` stay as derived display names. A fresh sign-up re-opts a previously unsubscribed address.
- **`CI_INTEREST_CONFIRM`** (transactional, from info@, DRAFT for the review send → ramp): subject / preheader / body exactly as approved; new registry variables `{studentFirstNameOrYourStudent}`, `{inquireLink}` (→ `/inquire?source=interest-confirm`), `{interestUnsubscribeLink}` (→ the NEW tokenized `/interest/unsubscribe?e=&t=`, which marks that address's interest rows unsubscribed — the Compass subscription untouched, so the two stay separately unsubscribable). Generic variant: the same body reads "…the next SAT Prep class at Higher Ground Learning" when there is no school (`{schoolName}` falls back) — stated, not a second template. Sent LIVE-ONLY (`renderDbEmail` → null while draft: **0 send rows after the E2E sign-up, verified**), dedupe `ci_confirm:{email}:{school}:{type}:{30-day bucket}` (one per address per school per 30 days), skipped when the address is in `marketing_suppressions`; `confirmation_sent_at` stamps the row.
- **"A class opened at your school" — VERIFIED it exists and sends:** `POST /api/admin/notify-interest` (the class card's "N families are waiting — notify them?" prompt, PL-54c) renders `NW_NEXT_CLASS_OPEN` — **LIVE on prod** (registry copy) with a code twin — per unnotified row, deduped `nw_open:{class}:{email}`, stamps `notified_at`. Two PL-484 touches: it now skips `unsubscribed_at` rows and greets by `parent_first_name` (fallback: the old whole-name split). No second half needed — no draft to write.
- **Compass opt-in:** one UNCHECKED checkbox (`data-testid="compass-opt-in"`, never pre-checked — asserted in the E2E) — *"Also send me the College Prep Compass — free test-prep and college-admissions tips by email."* on the interest capture (so also on register ②③④⑦). Checked → `subscribeCompass()` (`app/utils/compass.ts`, the ONE path the /compass form + embed use too; the API was refactored onto it): same consent record, source `interest-list`, same (future) welcome. E2E: sign-up with the box ticked → `class_interest` row (B52 · Parent · Kiddo · `compass_opt_in=true`) + `marketing_subscribers` row (source `interest-list`); the done card says "The College Prep Compass is on its way too."
- **Staff:** _the per-school count + list + export are NOT in this commit_ — see the honest note in the close: the admin class card already shows the unnotified count and the notify prompt; the school-card list + CSV export is the one PL-484 bullet still open (kept in scope, filed below).
- **Fields:** Email* · Your first name* · Your last name · Student's first name (optional) — so the first message HGL ever sends them can be personal. (`class_interest` already has `parent_name` + `student_name`; split per PL-482.) Keep it one compact row group; still usable one-handed at 375px.
- **Confirmation email `CI_INTEREST_CONFIRM`** (registry template, transactional, from info@, DRAFT for the usual review → ramp). **Copy APPROVED by Scarlett Sep 21** (she cut "One click, no questions." from the P.S.):
  - Subject: `You're on the list for the next {schoolNickname} {classType} class`
  - Preheader: `We'll email you the moment registration opens`
  - Body:
    ```
    Hi {parentFirstName},

    Thanks for your interest — you're on the list. As soon as registration opens for the next {classType} class at {schoolName}, we'll email you at this address so {studentFirstNameOrYourStudent} can grab a spot before it fills.

    In the meantime, if you'd like a head start, 1-on-1 tutoring is available any time:

    [button:Schedule a free consultation]({inquireLink})

    {contactBlock}

    P.S. Signed up by mistake? [Take me off this list]({interestUnsubscribeLink}).
    ```
  - Generic (no-school) variant: "…the next {classType} class…" without the school. One email per address per school per 30 days (dedupe), never to a suppressed address.
- When a class for that school OPENS, the existing "tell me when a class opens" notification (`notified_at`) must actually exist and send — verify it does; if it doesn't, that is the other half of this item (DRAFT copy, Scarlett reviews).
- **College Prep Compass opt-in** on the interest form AND on the register page's full/closed states (PL-480 ②③④): one UNCHECKED checkbox — "Also send me the College Prep Compass — free test-prep and college-admissions tips by email." Checked → subscribes through PL-477's Compass path (same consent record, same welcome). Never pre-checked; interest-list and Compass stay separately unsubscribable. Depends on PL-477 — ship dark behind it if needed.
- Staff: interest sign-ups visible per school (count on the school card + list), exportable; they are NOT leads unless the person also inquires.

## PL-485 — CTA wording pass (Scarlett, walkthrough Sep 21)

### ✅ SHIPPED (Sep 21) — ONE constant `CONSULT_CTA = 'Schedule a free consultation'` (`ClassStateCard.tsx`)
Sites changed: `ClassStateCard` (every closed / cancelled / no-class card) · `view.tsx` in-progress "Missed this one?" block · `view.tsx` class-page footer line (was "Questions? Talk to us") · `/team` button (was "Work with us — free consultation", now → `/inquire?source=team`). The /classes button is the ONE exception — "Talk to us" → `/inquire?source=classes` (`data-testid="classes-talk-to-us"`), plus its "nothing open" inline link. `PUBLIC_STATE_LANDINGS` key kept for the CTA gate.
"Talk to us — free consultation" → **"Schedule a free consultation"** everywhere it appears on public pages (class closed / in-progress / no-upcoming cards, class page footer CTA, /team) — EXCEPT the /classes page button, which Scarlett set to **"Talk to us"** (PL-479). One shared constant; list the sites changed.

## Addendum after the batch-51 verification (Claude, Sep 21 evening)
- **Numbering:** the batch-51 close proposes a timecard `void` status "as a PL-478 candidate" — PL-478 is taken (public header). It is **PL-486** below.
- **PL-486 — Timecards can be voided.** No void/delete transition exists, so the three Sep 1–15 class-hours cards (Gwen 6h, Kevin 7.25h, Rebecca 6h) + Billy's 6h MIS-placeholder card sit `open` forever and would read as payable hours in any payroll view. Add `void` (admin-only, reason required, excluded from totals/exports/approval queues, never re-created or re-announced by the sweep, visible in history) and void those four with reason "records-only class — paid outside the portal".
- **PL-487 — Scripts tolerate a failed temp-dir cleanup.** `project-sends.mjs`, `import-class-registrations.mjs` (and any sibling using `rmSync` on a `.tmp-*` build dir) crash with EPERM at the END of an otherwise successful run inside Claude's sandboxed shell (it cannot delete files). Wrap the cleanup in try/catch + a one-line warning, and build into a fresh `mkdtemp` dir each run instead of `rmSync`-ing a fixed path at the start (that start-of-run delete is what blocked the importer outright). Exit code must reflect the real work, not the cleanup.
- **For Scarlett (not Code):** review-send the two new drafts `IQ_INQUIRY_ACK` + `PT_PARTNER_ACK`; supply the Compass welcome content; decide required fields per inquiry embed; eight stale MIS session events remain on Billy's Google calendar (hand sweep); the International Classes calendar stays ungated (it is unconfigured — no veto needed until someone sets `intl_classes_calendar_id`).


## ✅ PL-486 — SHIPPED (Sep 21) — migration `20260921000002` APPLIED (`timecards.status` check gains `void`; `void_reason / voided_by / voided_at`)
`void` is admin-only (the route checks the caller's role; the panel shows the control to admins only), a reason is REQUIRED (inline input — no native dialogs), only `open` / `tutor_confirmed` cards qualify; a voided card keeps its history row (struck-through VOID chip on the Timecards list) but its sessions are un-stamped (`sessions` + `tutoring_sessions.timecard_id → null`), it is excluded from the tutor's portal (`tutor-view` filters it), the approval queue (only `tutor_confirmed` rows ever showed) and the QBO push (approved only), and **the sweep never re-creates or re-announces it** (`sweepTimecards` skips a void (tutor, period); `recomputeTimecard` no-ops on void). **The four cards:** `scripts/pl486-void-records-only-timecards.mjs` (dry run lists them; `--apply` voids with reason "records-only class — paid outside the portal") — **run with `--apply` after the deploy that carries the void status is Ready** (noted in the close). Panel E2E: the four open cards show the void control for the admin session.

## ✅ PL-487 — SHIPPED (Sep 21)
57 scripts patched by one pass: every fixed-path `.tmp-build-*` + start-of-run `rmSync` became a fresh `mkdtempSync` dir per run (the start-of-run delete is what blocked the importer outright in the sandbox), and every end-of-run cleanup goes through `safeRm()` (try/catch + a one-line "could not remove temp dir … delete it by hand" warning). Exit codes reflect the real work only. `node --check` on all 57: clean; the gate battery (which runs many of them) passed.
