# Portal fixes — batch 49 (CLOSED for hand-off Sep 20, 2026 — go-live batch, 5 items, PL-456…460)

**Standing rules:** all prior (batch 41/42/44/45/46 lists).

(Opened + closed Sep 20, 2026 during the go-live walkthrough. Next PL: PL-461.)

**Ship ALL FIVE together (Scarlett's call).** The cutover is waiting on PL-457 + PL-458 specifically (purge → she creates the SLS / ASF / Leone XIII classes → silent import), so within the batch do PL-458, PL-457, PL-456 FIRST and say so in the header note the moment they are on prod — but the batch closes as one. PL-456 stops for Scarlett's OK on the match table before writing; PL-458 ships the script only — **Code does NOT run `--apply`; Scarlett + Claude run the dry run together and apply it themselves.**

**Go-live context (Sep 20):** Resend is on Pro (50k/mo), highergroundlearning.com verified. SLS crest re-uploaded; Deep Dive + PSAT evergreen codes set. The four live classes (SLS, Leone XIII, MIS, ASF — AISCT was cancelled) end ~Sep 30–Oct 1 and have had everything through the 2nd diagnostic reminder from MailerLite. **Decision (Scarlett):** MailerLite finishes ALL comms for those four; the portal gets their families/students/enrollments (then scores) as RECORDS ONLY via a silent import (PL-457). Every class that opens registration from here on is born in the portal. Order now: QA purge → silent import of the four → public-page walkthrough → domain/EULA/Intuit → QBO production → Stripe live → link pre-flight → DNS → sqsp repoint → MailerLite closes after its last automation (~Oct 5).

## ⏸ PL-456 — STOPPED FOR SCARLETT'S OK (Sep 20) — match table below; **nothing written yet**
Matched on the row (case/punctuation-insensitive name compare against `instructors.name`); the row's display name is kept as-is, nothing renamed. All 22 rows currently have a blank phone. The PL-226 editor saves `phone` as free text, trimmed (`phone.trim() || null`) — so the write is VERBATIM as on the sheet, trimmed, nothing normalized, no +1 prepended (Kelsey Lee's international number stays `3530858152597` as given — but see her row).

| sheet name | phone (verbatim) | instructors row | row display name | verdict |
|---|---|---|---|---|
| Billy Thomas | (801) 712-2778 | 74bc29e7 | Billy Thomas | write |
| Eric Brown | (215) 704-9783 | 028cbd89 | Eric Brown | write |
| Kelsie Rank | (801) 440-8702 | 0d9235f8 | Kelsie Rank | write |
| Kaile Cota | (231) 944-2203 | 97f662d3 | Kaile Cota | write (spelling matches the row) |
| Gwen De Silva | (660) 238-6363 | 60ca44b9 | Gwen De Silva | write (the row is literally "Gwen De Silva", not Gwendolyn) |
| Rebecca Baumher | 215-892-5455 | 1f50c13f | Rebecca Baumher | write |
| Kevin Marren | (650) 464-0600 | d43e5756 | Kevin Marren | write |
| Linden Hughes | (801) 450-6880 | f8916d8a | Linden Hughes | write |
| Julia Fusia | (209) 605-1726 | 953f835f | Julia Fusia | write (spelling matches) |
| Jason Topa | (740) 398-1374 | 26d362cb | Jason Topa | write (spelling matches) |
| Alexa Jordan | (630)-731-5390 | 4b600769 | Alexa Jordan | write (verbatim, odd hyphen and all) |
| Heather Witzel Lakin | (215) 764-0525 | 1a41a9fd | Heather Witzel Lakin | write |
| Austin Webb | (520) 419-8405 | fb71740c | Austin Webb | write |
| Quinn Murphey | 210-367-6117 | 7b115bab | Quinn Murphey | write (spelling matches) |
| Ashley Khouri | (814) 553-6439 | 15bea3e1 | Ashley Khouri | write |
| Charlotte Thayer | 817-521-5785 | 271021a2 | Charlotte Thayer | write |
| Ava Alexander | (570) 396-2370 | a795ab3d | Ava Alexander | write |
| **Kelsey Lee** | 3530858152597 | **— no row** | — | **NO instructors row — reported, NOT created.** (Is she a current instructor who needs a row via the Tutors panel, or a former one? Scarlett decides.) |
| Katie Horvath | (801) 891-5686 | 53fd2e95 | Katie Horvath | write |
| Andie Arnold | (402) 301-1173 | bfd7dc39 | Andie Arnold | write |
| Alex Cook | — | af107a78 | Alex Cook | leave blank (sheet: "never call") |
| Delaney Hall | — | af0ffe55 | Delaney Hall | leave blank (no number given) |
| (no list entry) | — | ed449916 | Janet Amaya Pisco | stays blank (row without a list entry) |

**Plan on OK:** 19 rows written verbatim, 2 left blank by instruction, 1 row untouched (no entry), 1 name unmatched (Kelsey Lee). **Side notes NOT imported anywhere:** the list as handed to Code carried exactly two annotations — Alex Cook "never call" and Delaney Hall "none given" — no hours/wk, "online only" or "case by case" notes were in it (those live in the July `tutor_notes` staff notes, e.g. Kaile's "Only reach out case by case"). If Scarlett wants "never call" recorded, `tutor_notes` (the staff notes on the instructor row) is the existing place — say the word. **Scarlett: reply "OK" (or amend rows) and the write runs as a one-time script with the exact table above; ship note then lists rows written.**

## PL-456 — Instructor phones (completes the stopped PL-444) (Scarlett, Sep 20)
Scarlett re-shared the staff phone list (below, at her instruction). Enter VERBATIM into `instructors.phone` — no guessing, no reconstruction from area codes.
- **Discipline:** first print a match table (list name → instructors row id + display name + current phone) and STOP for Scarlett's OK before writing. Names come from an informal staff sheet and may not match rows exactly (e.g. "Gwen De Silva" is likely Gwendolyn; check the spellings of Kaile Cota, Julia Fusia, Jason Topa, Quinn Murphey against the table) — match on the row, keep the row's existing name, never rename. No row → report it, don't create one. Row with no list entry → stays blank.
- Store in whatever shape the PL-226 profile editor itself saves (if it normalizes, normalize identically; if free text, store as given). Kelsey Lee's is an unformatted international number — store as given, do NOT prepend +1.
- NO NUMBER, leave blank: Alex Cook (sheet says "never call"), Delaney Hall (listed without a number).
- Do NOT import the sheet's side notes (hours/wk, "online only", "only reach out case by case", "never call") into any field — report them back in the ship note so Scarlett can decide whether they belong anywhere.
- Ship note: final match table, rows written, rows left blank and why.

**The list (verbatim from Scarlett, Sep 20):**

| Name (as on the sheet) | Phone |
|---|---|
| Billy Thomas | (801) 712-2778 |
| Eric Brown | (215) 704-9783 |
| Kelsie Rank | (801) 440-8702 |
| Kaile Cota | (231) 944-2203 |
| Gwen De Silva | (660) 238-6363 |
| Rebecca Baumher | 215-892-5455 |
| Kevin Marren | (650) 464-0600 |
| Linden Hughes | (801) 450-6880 |
| Julia Fusia | (209) 605-1726 |
| Jason Topa | (740) 398-1374 |
| Alexa Jordan | (630)-731-5390 |
| Heather Witzel Lakin | (215) 764-0525 |
| Austin Webb | (520) 419-8405 |
| Quinn Murphey | 210-367-6117 |
| Ashley Khouri | (814) 553-6439 |
| Charlotte Thayer | 817-521-5785 |
| Ava Alexander | (570) 396-2370 |
| Kelsey Lee | 3530858152597 |
| Katie Horvath | (801) 891-5686 |
| Andie Arnold | (402) 301-1173 |
| Alex Cook | — (never call) |
| Delaney Hall | — (none given) |

## ✅ PL-457 — SHIPPED (Sep 20) — `--silent` on the import + ONE gate in sendOnce; migration `20260918000001` (enrollments.comms_muted) APPLIED column-first
**A. `--silent`:** `scripts/import-class-registrations.mjs --silent` claims EVERY `SEQUENCE` step × parent/student per Paid row (not just `dueSteps`) through the existing cancelled-`email_sends` claim (same dedupe keys, same 23505 tolerance) PLUS the four non-sequence family sends the sweeps key per enrollment (`thank_you_s`, `tutoring_upsell`, `class_survey`, `class_survey_reminder`) — 21 claim rows per Paid row — and stamps the enrollment `comms_muted = true`. Summary prints "SILENT: all 7 sequence steps claimed per Paid row (N claim rows written)"; the dry run prints the claim count it WOULD write per row. Without the flag: byte-identical to before (the gate proves it — 3 claims, 14 projected legs, a real send goes out). **B. Audit — every path that can reach a family, student, school contact or instructor keyed on an enrollment or class, and what gates it for a `source='import'` `--silent` enrollment:** the smallest change that holds turned out to be a per-enrollment flag read at THE choke point — `sendOnce` refuses any send whose `enrollmentId` is muted (returns `suppressed`, records a cancelled row "comms muted — …" so the send log shows why, never reaches Resend). Every family-facing send below passes an `enrollmentId` except two loops, which skip muted rows themselves. No class-level mute was needed.

| path | who | fires for a silent import? | gate |
|---|---|---|---|
| lifecycle SEQUENCE (E2/E3/E4/E5/E6/E7/E8, parent + student legs, + the PL-78 instructor FYI copy) | family/student (+ instructor copy) | no | claim rows (7×2) + sendOnce mute; the FYI copy only rides a parent send that happened |
| thank-you E1 (+ student leg), E9 upsell, class survey + reminder | family/student | no | claim rows + sendOnce mute |
| PR1–4 payment ladder, expiry | family | no (imports are Paid; imported Pendings already exempt, PL-363) | existing `source='import'` skip |
| waitlist offers / release / rescue | family | no (imports are never Waitlisted here; if one were: sendOnce mute) | sendOnce mute |
| SU schedule-update (bulk edit + single-session edit, `class-session` route) | family/student | no | loop skip `commsMuted` (these two sends carry no enrollmentId) |
| class cancellation CX_FAMILY / CX_WAITLIST (`cancel-class`) | family/student | no | paid filter excludes muted + sendOnce mute |
| instructor "message the class" (`portal/message-class`) | family/student | no | sendOnce mute (passes enrollmentId) |
| follow-on FO campaign (3 stages) | family/student | no | seeding loop skips `commsMuted` |
| staff-built campaigns (segments) | family/student | not as "students of this class" | `campaigns.ts` drops muted rows from a family's classRows |
| comms projector (Scheduled view / what-will-send) | — | projects nothing for muted rows | `projectBundle` skip |
| PL-219 class report, scores | — | not emails (downloads / portal) | n/a |
| E0 confirmations, LR welcome, staff payment link | family | no (import claims the 3 confirmation keys; never runs checkout) | claims |
| tutoring T-series, agreements, availability, intake | family | not enrollment-keyed — only if the family starts tutoring in the portal (none of these families will) | n/a (by design: records only) |
| login link (family-initiated) | family | yes, if they ask for one | by design — a record can be viewed; nothing is pushed |
| counselor digest CD, FP push, CR classroom requests, CS class-confirmed, collateral follow-up | school contact | no contact rows exist for these classes (Scarlett adds none until they end); CS/CR are staff-triggered | absence of contacts + staff action; NOT enrollment-gated — do not add contacts to these classes before Oct 5 |
| instructor IN_WELCOME / DIGEST / MIN_DECISION, T3 tutor notices | instructor | yes (staff-side, keyed on class/instructor, gated by `instructors.comms_enabled`) | listed, allowed |
| AL_* staff alerts (registration, roster report, min-enrollment, blank details, synap nudge, …) | staff | yes | fine (staff only) |

**C. Gate:** NEW `regress:silent-import` (17 checks): a self-cleaning future QA class + fixture CSV imported with `--silent` → both rows muted, all 21 claim rows present and cancelled with the silent reason, the projector projects ZERO rows, `sendOnce` refuses two differently-keyed probes ('suppressed' + the cancelled record — the flag holds, not just the claim); then the same import WITHOUT the flag → not muted, exactly the 3 confirmation claims, 14 projected legs, and a real probe SENDS (one QA send). The lifecycle sweep itself is the cron route (not callable in isolation without sweeping every prod class); the projector is the state-driven twin of what it would send and sendOnce is the wire every sweep send crosses — both covered. **D. Class rows on prod (read Sep 20, unchanged from the finding):** MIS = `f7446506-aa14-4729-bba5-e563f378f6b2` `mis-sat-prep-fall26`, 8 sessions Sep 8→Oct 1 (its one Paid enrollment is QA and goes with the purge). **No real row exists for the current SLS cohort** (`5fac1a68` `sls-sat-prep-fall26` is the cancelled Aug 5–8 one → in PURGE_CLASS_IDS), **none for the current ASF cohort** (`ef4b8ae6` `asf-sat-prep-spring26` is the April QA cohort, 0 sessions → PURGE), and **Leone XIII has neither a class nor a school row** (schools on prod: SLS, Nido, MIS, ISD, ASF, AISCT). Scarlett creates the school (Leone XIII) then the three classes via the wizard AFTER the purge and BEFORE the import; Code does not seed them. Until the import lands: nobody edits those four classes' schedules in the portal (the SU sends are gated, but the rule stands until the rows exist).

## PL-457 — Silent import mode for classes whose comms stay in MailerLite (Scarlett, Sep 20)
Today `scripts/import-class-registrations.mjs` claims only the ALREADY-DUE sequence steps; future steps fire naturally. For the four live classes that means `review_request` (last+1) and `tutoring_offer` (last+4) would send from BOTH systems. Scarlett wants the portal to send those families NOTHING, ever, for these enrollments.
- **A. `--silent` flag:** claim EVERY `SEQUENCE` step × (p, s) per Paid enrollment — not just `dueSteps` — via the existing cancelled-`email_sends` claim (same dedupe keys, same 23505 tolerance). Summary prints "SILENT: all N sequence steps claimed". Without the flag, behavior is byte-identical to today. Dry run prints the claim count it WOULD write.
- **B. AUDIT first, in the ship note:** enumerate every send path that can reach a parent, student, school contact, or instructor keyed on an enrollment or class (SEQUENCE, PR ladder, waitlist, schedule-change notices, cancellation/CX_FAMILY, score/report emails, PL-219 class report, instructor reminders, anything else). For each: does it fire for a `source='import'` `--silent` enrollment, and what gates it? Staff-only alerts (AL_*) are fine but list them. Any family/school-facing path NOT gated → gate it on the claim or on a per-enrollment marker (smallest change that holds; say which). No class-level mute unless the audit shows per-enrollment claims can't cover it.
- **C. Gate:** a regress check that imports a fixture CSV with `--silent` into a self-cleaning QA class whose last session is in the FUTURE, then asserts zero sendable rows for those enrollments across all 7 steps and that the lifecycle sweep sends nothing for them. The same check covers today's behavior without the flag.
- **D. Class rows:** confirm real class rows exist on prod for all four (the REAL current cohorts — not the QA "ASF April" / "MIS July" cohorts the purge script removes). Report ids/slugs + session dates; if Leone XIII or ASF has no real class row, say so — Scarlett creates it via the wizard, Code doesn't seed it.
- Until the audit is in: nobody edits those four classes' schedules in the portal after import.
- **Finding, Sep 20 (read-only prod inventory):** of the four, only MIS has a real row (`mis-sat-prep-fall26`, Sep 8→Oct 1; its single Paid enrollment is QA). There is NO row for the current SLS cohort (`sls-sat-prep-fall26` is the cancelled Aug 5–8 one), none for the current ASF cohort (`asf-sat-prep-spring26` is the April QA cohort, status still `open`, 0 sessions), and Leone XIII has neither a class nor a SCHOOL row. Scarlett creates those three (school first for Leone XIII) AFTER the purge and before the import, with a past registration deadline and NO real school contacts added until the classes end.

## ✅ PL-458 — SHIPPED (Sep 20) — **script only; `--apply` NOT run (Scarlett + Claude run the dry run together and apply it themselves)**
`scripts/purge-qa-data.mjs` rewritten from the July name-list to the whole-table mode. **A. The bug:** the old `email_sends` wipe passed all 738 ids in ONE `.in('id', …)` (URL too long → `TypeError: fetch failed`); every delete is now CHUNKED (100 ids per request), and every other `.in()` in the script is chunked the same way (the app's own `.in()`s on id lists are all small, bounded lists — audited, none the same shape). **B. Mode:** whole-table wipe of 40 transactional tables in FK-safe order (leaves → roots; the list is in the script with a reason per non-obvious table: legacy `email_log`, the QA FO campaign run, `dashboard_notes` (both QA, both cleared), `class_interest` (one QA sign-up), `student_materials` rows (storage file stays), drift + ledger, tutor pending notices, session notes, record matches, fact-edit trail) + the class-scoped rows (classroom requests, page analytics, class-scoped site blocks, sessions) of the classes in `PURGE_CLASS_IDS`, then those class rows. **KEPT, never touched:** schools, instructors + `tutor_notes` (staff notes on the instructor rows — configuration, not transactions), templates + versions, app_settings, staff prefs/alert subscriptions/saved segments, tutoring_offers/packages, products, subjects, agreement templates, short links / evergreen codes / course_meta / short-link clicks, global site content, logos/crests + storage, gcal + qbo connections, admin/manager profiles, team_access_audit, and every class not listed. **C. Guards:** refuses to run at all (even dry) past `CUTOFF_DATE = 2026-10-15`; refuses `--apply` when any enrollment has `source='import'`, when the Stripe key is live, when `QBO_ENVIRONMENT=production` or the connected QuickBooks realm isn't a sandbox company, and when ANY stored PaymentIntent fails to retrieve as a test-mode object (each id is checked against Stripe with the test key — a live id returns 404 — so a real payment can never be purged). Prints why. **D. Snapshot:** `--apply` first reads every row it will delete into `scripts/.tmp-purge-snapshot-{ts}.json` (gitignored; one array per table + the parent profiles), reads it back to verify, and aborts before any delete if that fails. NEW `--snapshot-only` rehearses exactly that and stops (run today: 1293 rows across 46 tables written, nothing deleted, file removed). **E. Logins:** dry run lists the 3 parent-role profiles (`qa-availability-pl19@example.com`, `bru@ce.thomas`, `billy+qaparent@…`); `--apply` removes their profile + auth user; `eric@highergroundlearning.com` is in `PROTECTED_LOGINS` and shows as "kept (manager)"; the two admin profiles untouched. **F. Post-check** runs after every mode: families/leads/contacts/email_sends/enrollments/students/engagements/invoices counts + the surviving class rows. **G. Class decisions encoded:** `KEEP_CLASS_IDS` = `f7446506` mis-sat-prep-fall26; `PURGE_CLASS_IDS` = the other nine (full UUIDs resolved from prod, each commented with its slug — the Aug-5 cancelled `sls-sat-prep-fall26` is labelled NOT the current SLS cohort). Any class row in neither list prints as **NEW → KEPT** with a hint to add its id (the SLS / ASF / Leone XIII cohorts Scarlett creates after Sep 20 land here). **Dry run today (prod):** 1290 rows + 3 logins would go; guards CLEAR (no imports, Stripe test-mode only — every stored PaymentIntent retrieved as `livemode:false` — QuickBooks sandbox); post-state 15 families / 8 leads / 4 contacts / 738 sends / 19 enrollments … ten class rows surviving until apply. Cutover checklist step 3b rewritten to the new discipline (dry run → review together → amend `PURGE_CLASS_IDS` → `--snapshot-only` → `--apply`).

## PL-458 — Purge script: from a July name-list to "everything transactional goes, configuration stays" (Scarlett, Sep 20)
Scarlett wants ONE purge of everything unwanted. The Sep 20 dry run + a read-only inventory of prod show the list approach is obsolete: **all 15 families on prod are QA** (4 arrived after the PL-48 list: `billy+qa-pl99@…`, `wthomas@wustl.edu` Janie/Janeson Foney Fony, `billy+qa-pl183@…`, `william.thomas@alumni.esade.edu` Kokobunji), all 8 leads are QA (3 have a null email so the email match misses them), all 4 contacts + 4 school_affiliations are QA/Billy aliases, all 5 engagements / 9 invoices / 11 scores / 11 qbo_sync_log rows / 738 email_sends are QA- or staff-addressed. No real family has ever been in this database.
- **A. Bug (would bite on --apply):** the `email_sends` wipe does `.in('id', sendIds)` with every id in ONE request → URL too long → `TypeError: fetch failed` in the dry run. Chunk the ids (or delete by the same OR-filter in pages). Audit every other `.in()` for the same shape.
- **B. Mode:** replace the email list with a whole-table transactional wipe in FK-safe order — families → students → enrollments → add-ons/attendance/agreements/availability/scores; leads; contacts + school_affiliations; tutoring engagements/sessions/invoices/lines/timecards; email_sends + email_events; qbo_sync_log; gcal_sync_log; waitlist + classroom_requests of removed classes — plus the class ids Scarlett lists in `PURGE_CLASS_IDS` (she decides per class; the dry run prints every class row with status, dates, enrollment count so she can). **KEPT, never touched:** schools, instructors, templates + versions, settings, short links / evergreen codes / course_meta, tutoring_packages, site content, logos/crests, storage, admin profiles, and every class not listed.
- **C. Guard so this can never be re-run after launch:** refuse to run (even dry) past a hard-coded cutoff date, AND refuse `--apply` if any enrollment has `source='import'` or any `stripe_payment_intent_id` from a LIVE-mode payment, or if `QBO_ENVIRONMENT`/qbo status is production. Print why.
- **D. Snapshot before delete:** `--apply` first writes every row it is about to delete to a gitignored local JSON file (one array per table) and prints the path; abort the apply if the snapshot write fails.
- **E. Auth users/profiles:** list (dry run) and remove (apply) the QA auth users + `profiles` rows with role `parent` (`qa-availability-pl19@example.com`, `bru@ce.thomas`, `billy+qaparent@…`); **do NOT delete `eric@highergroundlearning.com`** — Eric Brown is HGL's manager + an instructor (Scarlett, Sep 20); his stored role was the column default `parent`; promoted to `manager` on prod Sep 20 (one-row update, done). Admin profiles untouched.
- **F. After apply**, the dry run must print: 0 families, 0 leads, 0 contacts, 0 sends, and the surviving class rows. gcal QA events stay a by-hand sweep of Billy's tutor calendar (unchanged).
- **G. Scarlett's class decisions (Sep 20): keep ONLY `mis-sat-prep-fall26` (`f7446506-aa14-4729-bba5-e563f378f6b2`).** Its 8 sessions were verified against the live sqsp calendar at hgl.co/mis (Tue/Thu Sep 8→Oct 1, 18:30–20:30 — exact match); its single Paid enrollment is QA and goes with the families. `PURGE_CLASS_IDS` = every other class row on prod at apply time: `ef4b8ae6` asf-spring26 · `a996f604` mis-summer26 · `ca303f03` nido-fall26 · `5fac1a68` sls-fall26 (Aug, cancelled) · `aaa5490f` psat-prep-fall26 · `efa3ae76` R/W deep dive · `20f41e6b` aisct-fall26 · `38d79c4f` isd-fall26 · `5f8a66de` math deep dive winter26 — resolve the full UUIDs from the dry run, and list any class created AFTER Sep 20 (Scarlett is adding the current SLS / ASF / Leone XIII cohorts) as KEPT unless she says otherwise. Evergreen codes / course_meta survive the class rows by design.

## ✅ PL-459 — SHIPPED (Sep 20) A–E built and verified at 375px on the Janeson fixture; **F: the family-facing re-send of a revised proposal does NOT exist today — STOPPED on that leg, Scarlett writes that copy** (everything else in F is built)
**Doc-premise check, verified:** the email's claim that "the proposal page and invoice update automatically" was HALF true — the family's proposal page reads live sessions (so a moved session shows), but NOTHING recomputed the invoice's lines/total after a staff reschedule (only the family's own move path called `rebuildProposalInvoice`). Fixed: the session route now rebuilds the draft/proposed invoice the moved session bills (reached through its line row — proposed sessions carry no `invoice_id`). **A. Every session line is a door:** the change-request box lists each session line as a link → `/admin/tutoring?session={id}&reschedule=1&why=change-request:{invoiceId}` → THE reschedule path (the PL-446 dialog: windows line, live slot check, arm-then-confirm override note) opens on that session with a new origin banner **quoting the family's request verbatim**; saving lands back on `?invoice={id}&replied=1` (the invoice row highlighted, a "Session rescheduled — mark the request handled below" notice). No second editor built; forfeit/no-show stay in the same dialog as before. **Session lines carry the start time everywhere** ("Oct 19, 1:00 PM" vs "Oct 19, 3:00 PM" — one `sessionWhen()` composer feeds all three line builders; existing lines gain times on the next rebuild). **B. The box leads with the action:** request text (quoted, bordered) → "Reschedule a session ↓ — tap the one to move:" → the session lines → then, secondary, a reason field + "mark handled". **C. Pre-highlight:** the line whose date the request names (month-name or `10/27` forms; exactly one match) is ringed + tagged "named in the request" — convenience only, nothing auto-edits. **D. Email copy** (the hand-built staff alert in `tutoring-billing.ts` `requestChanges` — not a registry template): "**Open the October invoice** — tap the session to reschedule it. The invoice total and the family's proposal page update when you save; then mark the request handled (the family's confirmation window restarts from your reply). …" — **Scarlett reviews the sentence.** **E. Honest state everywhere, derived (no new stored status — `change_requested_at` on a draft/proposed month IS the state):** invoices panel pill "**Change requested — needs our reply**" in amber, and those rows sort FIRST in their month; family profile billing list says "change requested — needs our reply"; the family portal Billing pill says "Change requested — we'll reply"; Needs Attention gains a row "Change requested — needs our reply" (waits on STAFF, lands on the invoice row). It returns to "Awaiting family confirmation" once staff replies. **F. Replying IS the handling — built:** `mark_change_handled` now requires EITHER a session on the month changed since the request (the reply is the new schedule) OR a one-line reason ("agreed by phone: …"); either way it appends "[ts] HGL reply (who): …" to the request trail, restarts the family's confirmation window **from the reply** (`proposal_sent_at` re-stamped, `nudge_sent_at` cleared — the sweep derives everything from those; note the T1B nudge therefore fires again 2 days after the reply, which is the fresh window working as designed), and writes an activity-feed line ("billy@… replied to the October 2026 tutoring change request — a session was rescheduled (Janie Foney Fony)"). **F — STOPPED:** there is NO family-facing re-send for a revised proposal: T1 dedupes on `t1_proposal:{invoice.id}` (a second send returns duplicate), no resend action exists, and no "here's what changed" template exists (SU's `{changesBlock}` is the class-schedule email, not a proposal). So today the family learns of the granted change from the proposal page (which the T1B nudge re-links 2 days later) — not from an immediate "we moved Oct 27 to 5:00 AM" email. Per the item: not invented. **Proposed PL-461: T1R "Updated proposal" (registry template, Scarlett's copy) sent by mark-handled with a what-changed line + the proposal link; until then the box's "mark handled" says so.** **Family side:** after a request the proposal page now says "We got your request — we'll reply by email with an updated schedule. Nothing to do until then…" and the Confirm / Request-changes buttons are GONE (before: "You can still confirm the current schedule below" with a live Confirm on the schedule they just rejected — verified and replaced). **Verified at 375px (DOM-verified; the hidden pane screenshots blank) on Janeson:** email link → row highlighted + pill "needs our reply" + 7 session links + Oct 27 pre-highlighted, no horizontal overflow → tap Oct 27 → dialog opens in Reschedule with the quoted request → 5:00 AM picked → availability warning armed → "Reschedule anyway (1 warning)" → landed on `?invoice=…&replied=1` → lines rebuilt with times, Oct 27 now "5:00 AM", total $525 (same hours) → family proposal page (token URL) shows the received copy, no Confirm button, "5:00 AM–6:00 AM" → Needs Attention row present → mark handled (no reason; a session changed) → 200, pill back to "Awaiting family confirmation", box gone, dashboard row gone, `proposal_sent_at` re-stamped 18:16, trail carries the HGL reply line, feed line present → phone path: re-opened a QA request via the family API → mark handled without reason → 400 with the plain message → with reason → 200 (`changed: 0`) → again → 400 "No open change request". The Janeson October month now carries this QA trail (it goes with the purge).

## PL-459 — Change-request landing: the invoice row must let staff MAKE the change, not just mark it handled (Scarlett, Sep 20)
Scarlett requested a change as a parent on the October proposal ("Can't do Oct 27 at original time. Needs to be 5:00 am"). The staff email (`tutoring-billing.ts` ~1248, "Tutoring schedule change requested") says "Edit the sessions on the tutoring page (this month's invoice row opens highlighted)" and deep-links `/admin/tutoring?invoice={id}`. The landing (`invoices-panel.tsx`, the yellow "Change requested — auto-confirm is paused" box) offers exactly ONE control: **mark handled**. The session lines below it ("Janeson — AP/IB Calculus $75.00 with Billy Thomas, Oct 27") are plain text. The email promises an action the landing does not offer; staff have to go find the session somewhere else, then come back. Seen at phone width (375px) — the fix must work there.
- **A. Each session line on an invoice row is actionable** while the invoice is unconfirmed/proposed: tap a line → the ONE reschedule path (the PL-446 dialog — windows line, slot check, override note) opened on that session, with `why=change-request:{invoiceId}` carried like the other origins so the dialog's banner QUOTES the family's request text. Cancel/remove a session from the same dialog if that path already exists there; do not build a second editor. Invoice total + proposal page update through the existing recompute (the email already claims they do — verify it).
- **B. The change-request box leads with the action:** the request text, then "Reschedule a session ↓" / session lines highlighted as the things to tap; **mark handled** stays but is secondary, and after a session was changed from this box it offers "mark handled + re-send the proposal to the family" as one step (if a re-send path exists; if not, say so in the ship note — don't invent a template).
- **C. If the request names a date** that matches exactly one session on the invoice (here: Oct 27), pre-highlight that line. Parsing is a convenience only — never auto-edit.
- **D. Email copy matches the landing** once A–C ship: "Open the October invoice — tap the session to reschedule it." (Scarlett reviews the final sentence; registry template if this send is in the registry, else the code string.)
- **Two Oct 19 lines** render identically in the list (two sessions the same day) — add the start time to every session line so they are distinguishable and so "original time" in a request means something on this screen.
- Verify at 375px on the Janeson fixture BEFORE the PL-458 purge removes it (or on a self-cleaning QA engagement after): email link → row highlighted → tap Oct 27 → dialog shows the quoted request → reschedule → invoice + proposal page reflect it → mark handled → auto-confirm clock resumes.
- **E. The status is wrong while a change request is open (Scarlett, Sep 20):** the row's pill reads **AWAITING FAMILY CONFIRMATION** — but the family HAS answered; the ball is in HGL's court. While a change request is open (unhandled), the pill everywhere this invoice/month's state renders (invoices panel, family profile, dashboard / Needs Attention counts, activity feed, any filter or tally keyed on the state) reads **"Change requested — needs our reply"** in the needs-action color, and the row sorts/counts with things waiting on STAFF, not with things waiting on families. Derive it from the open request (no new stored status unless the existing model can't express it — say which in the ship note). It returns to "Awaiting family confirmation" only once staff has replied (F).
- **F. Replying IS the handling — two outcomes, both end with the family hearing back:** ① **Grant it** — reschedule the session(s) via A → the family gets the updated proposal (existing re-send/T1 path) with a line that says what changed; ② **Propose something else** — staff can't do the requested time: pick a different slot via the same dialog and/or write a short note → the family gets the updated proposal carrying the note, and can confirm or request again. "Mark handled" without either (e.g. resolved by phone) stays possible but asks for a one-line reason that lands in the activity feed. In all cases the auto-confirm clock restarts from the reply, never from the original send — the family must get a fresh full window on the schedule they were actually shown. If no family-facing re-send path exists today, STOP and report rather than inventing a template (Scarlett writes that copy).
- **Family side, same honesty:** after submitting a change request, the family's proposal page says their request was received and HGL will reply — not a still-live "Confirm" on the schedule they just said doesn't work (verify what it shows today; report).

## PL-460 — AUDIT: every "go do this" link, for EVERY kind of user, lands where the thing can actually be done (Scarlett, Sep 20)
Scarlett: "this type of thing has been a persistent problem." "Include not just staff things, but also all other users of the portal. We don't want anyone else to run into this problem." Standing rule to adopt from here on (add it to the standing-rules list): **an alert, email, banner, card, or Needs-Attention row that tells ANYONE — staff, parent, student, instructor/tutor, school contact, billing contact, lead — to act must land on a surface where that exact action can be completed in place — one tap to the control, the subject pre-selected. "Mark handled/dismiss" is never the only control unless acknowledging IS the whole job.**
- **Inventory (ship note, table form):** every staff-facing call-to-action — all `AL_*` alerts, the hand-built staff emails in `tutoring-billing.ts` / `qbo-sync.ts` / `intake-emails.ts` / cron routes, Needs Attention rows, dashboard cards, drift/conflict banners, the activity feed, QBO failed-queue rows, leads follow-ups, waitlist/claim notices, missing-class-details warnings, payment-failed / past-due / late-fee flags, availability + assignment-conflict cards. Columns: source · the sentence/label that promises the action · link target · **what control actually exists at the landing** · is the subject pre-selected/highlighted · works at 375px · verdict (OK / DEAD-END / DETOUR = action exists but ≥2 more navigations away / COPY-LIES = text promises something the page can't do).
- **Every audience is first-class in the inventory, same columns, one section each:** PARENT/FAMILY (registration confirmation + payment links, PR1–4 pay-balance, waitlist claim/decline, schedule-change notices, proposal confirm / request-change, autopay consent + failed-payment, invoices + receipts, intake/availability forms, Synap/diagnostic links, review request, tutoring offer, login links, email-preferences/unsubscribe, the billing-contact variants from PL-454) · STUDENT (the `_s` sequence twins, diagnostic links) · INSTRUCTOR/TUTOR (session reminders, timecards, handoff notes, availability, class roster/report links, drift/conflict notices) · SCHOOL CONTACT (digests + frequency links, final-days push, classroom request + nudges, class-confirmed welcome, collateral downloads, class card/report links) · LEAD (inquiry replies, scheduling links). For tokenized links also record: works LOGGED OUT, on a phone, after the token's natural lifetime (expired token → a page that says what to do next, never a bare error or a login wall with no way forward), and lands on the specific item, not a generic portal home.
- A non-staff dead end is worse than a staff one (they can't work around it and they write in) — fix those first.
- **Fix in this batch:** every DEAD-END and COPY-LIES whose fix is wiring an EXISTING control/dialog to the landing or correcting the sentence. Anything needing a new surface → listed with a proposed PL, not built.
- **Gate:** extend `regress:links` (or a sibling) so each CTA in the inventory has an assertion that its target renders the named control (a test id per control) — a new alert without a registered landing control fails the gate.

