# Portal fixes — batch 41 (ACCUMULATING — opened Aug 19, 2026)

**Standing rules:** all prior (plain-English statuses incl. dates + city time labels · no internal shorthand · alerts deep-link record + resolution surface · samples from composers · `git push` · PL-x IDs · check off here when shipped · registry edits = new versions under anchor guards, twins+seeds lockstep · composer-path verification · inline confirms only · NO native dialogs · **test/QA email → billy@ / billy+*@ ONLY** · crashed gate ≠ green gate · imports fire NO emails · real-renderer previews · review ≠ edit, badges re-raise touched-only · one collision validator across shortlink namespaces · UI labels not internal names · FKs between much-embedded tables break PostgREST — validate app-side · [button:] is block-level always · capture conflict specifics at alert time, freebusy keeps no history).

(CLOSED for batch 41 hand-off, Aug 19. Next PL: PL-406.)

**Batch closed Aug 29 — all 11 items + both carried phase-2 items shipped** (per-item ✅ notes below). Gate battery green at close: `tsc` · `next build` · `smoke:public` · `regress:links` · `regress:pronouns` · `regress:client-imports`. Migrations `20260911000001` (PL-402 drift alerted columns), `20260911000002` (PL-384 phase-2 `marketing_url` DROP), `20260911000003` (PL-399 HQ address wording) — **all APPLIED via the Management API**. Real-data fixes along the way: 3 duplicate Google events deleted from Billy's calendar (the 4× screenshot), 1 duplicate live portal session row (Fakey Aug 31, the pre-batch-40 [Mon 16:00 ×2] leftovers) deleted with its event, PL-401 identity markers backfilled onto all 16 future synced events. **Carried phase-2 items:** `marketing_url` dropped (post-drop grep: comment-only references) — `classes.short_link` is NOT drop-ready (collateral still reads it as the pre-fold fallback) and stays; counselor assignment now editable post-creation in the Edit-class-details section.

## PL-395 — Staff attributions render the NAME, not the email (Scarlett, Aug 19)
The roster's "Running below minimum — decided 13 August 2026 by billy@highergroundlearning.com" banner shows a raw email. Render the staff member's name ("by Billy Thomas"), resolved from the staff/instructor record by email; unknown address falls back to the email honestly. SWEEP: every admin surface + email that attributes an action to a staff email (decision banners, deadline-override records, offline-payment "recorded by", staff-signup chips, review footers, cancellation records, contact-edit audit trails — plus any others a grep for rendered `*_by` fields finds) goes through ONE resolver (name ?? email); ship notes list each surface converged. Internal identifiers stay in tooltips/history where already present.

**✅ SHIPPED (Aug 29).** ONE resolver: `app/utils/staff-names.ts` (contacts < instructors < profiles, `name ?? email` — instructors.name, never public_name) behind `/api/admin/staff-names` + a `useStaffName()` hook (module-cached, email is the honest fallback while loading/unknown). Surfaces converged: min-enrollment decision banner ("by Billy Thomas"), converted-by, class-draft/template-version/site-block created/updated/reviewed-by, timecard approved-by, comms cancelled-by, QBO dismissed/connected-by, GCal connected-by, dashboard pinned-note author, engagement-wizard + tutor-portal handoff-from (portal resolves via instructors directly), schedule-draft created-by (the `split('@')[0]` half-fix retired), leads assigned-to (both sites; the email moved into the tooltip), classroom answered-by (counselor, via contacts), plus the two alert-email bodies that interpolated a raw caller email (attach-payment, classroom-request). VERDICTS: staff-enroll/offline-payment tooltips keep the email (internal identifiers stay in tooltips, per this item's own rule); the Team-access audit keeps emails (that screen is ABOUT login addresses).

## PL-396 — Embed preview page is blank on prod: script src points at localhost (Scarlett, Aug 19)
`public/sqsp-embed-test.html` ships `<script src="http://localhost:3000/embed/upcoming-classes.js">` — fine in Code's dev, dead on the deployed site, so Scarlett's preview renders heading + blank (the checklist's real snippet at step 9b is correct). Fix the test page to a RELATIVE `/embed/upcoming-classes.js` (works on the vercel host today and on hgl.co after cutover). Also verify in-browser on the DEPLOYED page: cards render, ?preview=empty state, mobile width — a preview page that was never viewed on prod is exactly the crashed-gate lesson, page edition; ship note states it was checked live. Sweep the repo for other `localhost:3000` references that could leak into any served page/snippet/doc (grep; dev-only files exempt but listed).

**✅ SHIPPED (Aug 29).** Test page src now RELATIVE (`/embed/upcoming-classes.js`) — resolves on the vercel host today and hgl.co after cutover; the checklist's real sqsp snippet (step 9b) stays absolute by design. **Checked live on the deployed page** after push: cards render, `?preview=empty` interest state renders, 375px mobile no-overflow. Repo-wide `localhost:3000` sweep: zero other served-page leaks — runtime hits are `NEXT_PUBLIC_APP_URL` dev fallbacks (emails guarded by emailBaseUrl + sendOnce's non-production-origin refusal); dev-only: 4 regress/smoke scripts; docs: setup/readme/historical write-ups (correct as dev instructions).

## PL-397 — Classes → Calendar: event heights don't reflect duration (Scarlett, Aug 19)
Every event renders ~45 minutes tall regardless of length: Janeson's 1-hour session doesn't fill its hour row, and the 3-hour PSAT class block (5:00–8:00 PM) is the same height as a 1-hour session. Fix the block geometry: height proportional to real duration against the hour grid (1h = one row, 3h = three), top edge at the true start offset, minimum height only as a floor for very short events (with the floor stated in a comment, not silently). Check overlap layout while in there: the Mon 4 PM Fakey block shows squeezed fragments of side-by-side chips ("R  Fa  4  4…") — overlapping events should share the column width legibly, and confirm it's genuine multiple events, not another self-echo (PL-388's lesson — say which in the ship note). Verify in real Chrome across the fixture week: 1h, 30min (if any), 3h class, and an overlap case; mobile width too.

**✅ SHIPPED (Aug 29).** The wrapper's height math was always duration-correct — the CHIP inside never filled it (no h-full), so every event's visible color was content-height (~45 min). Chip wears `h-full`; overlaps get a real greedy lane layout (first free lane at start; a connected cluster splits the column width evenly) replacing the 14px cascade that buried earlier chips; floors stated in comments (0.4h endH clamp + 18px px floor — very short events stay clickable, ≥30 min renders true). Verified in the browser across the fixture week: Janeson 1h fills its row, SAT Deep Dive 2h spans two, the PSAT 3h class block spans three (5–8 PM), the Mon-4PM cluster renders three legible side-by-side chips; mobile 375px scrolls the grid inside its own container. **The overlap was GENUINE multiple records, not a self-echo**: Roman + the Fakey [Mon 16:00 ×2] duplicate pair — the one surviving LIVE duplicate (Aug 31) was deleted with its Google event (`scripts/pl397-cleanup-duplicate-fakey-session.mjs`); past-week duplicates remain as history (completed rows are never deleted).

## PL-398 — Admin surfaces still say "America/Denver" — retire the zone-id displays too (Scarlett, Aug 19)
Classes → Calendar shows "all times in **America/Denver**" in the intro AND "· America/Denver" beside the week label (the earlier scheduling view had one too). The PL-353 rule carved out admin surfaces as allowed to keep IANA ids — Scarlett's verdict: no, staff read city names too. Replace admin-facing zone-id DISPLAYS with the same resolver language ("all times in Salt Lake City time"; per-class contexts use the class's own city via publicTimeCityLabel where a specific class is in view). SWEEP all admin surfaces rendering IANA ids as display text (calendar header ×2, scheduling views, timecard/payroll headers, class cards' "Timezone: Europe/Rome" line — that one is a SETTING readout, decide: keep the id in the picker/setting context where precision matters, but the display label alongside reads the city; note each verdict). Timezone PICKERS keep ids+offsets (they're settings). Update the PL-353 rule text: public AND staff displays = city names; ids live in pickers/settings/debug only.

**✅ SHIPPED (Aug 29).** PL-353's rule amended at the source (dates.ts docstring): public AND staff displays speak city names; IANA ids live in pickers/settings/debug only. New `staffTimeCityLabel` (America/Denver → "Salt Lake City" via HGL_HOME_CITY — the zone city "Denver" is the exact confusion PL-305 fixed for parents; any other zone reads its own city). Converged: calendar intro + week-label chip (×2), tutoring schedule-view (header, session drawer, reschedule + correct-time forms), engagement-wizard recurrence hint, tutors-panel timezone COLUMN (city; id in the tooltip), tutor portal "Times in …", family-hub header, admin class card. Per-class contexts use `publicTimeCityLabel` with the class's full facts (schools.city added to the roster select). VERDICTS noted: wizard "Timezone:" lines are SETTING readouts — id stays with the city label alongside; the HGL fixed line already names Salt Lake City (kept); timezone pickers keep ids+offsets; payroll headers state no zone at all (inverse gap, noted not changed).

## PL-399 — "Open in Google Maps" searches "Room 204" (Scarlett, Aug 19, seen on the ACT sample page)
The Where-we-meet maps link builds its query from the raw location string — a room-only location ("Room 204") produces a useless Maps search. Compose the query from a REAL street address, resolved in order: location already address-shaped (PL-305 heuristic) → use it verbatim · HGL in-person class (room-only or blank location) → the HQ address, one constant: **380 W. Pierpont Avenue, Salt Lake City, UT 84101 USA** (zip CONFIRMED 84101 by Scarlett, Aug 19; one source shared with the email footer's address so they can't diverge) · school class with a school address on record → the school's address (does that field exist? if not, room-only school classes OMIT the maps link rather than search garbage — honest absence) · online → no maps link (should already be the case; verify). The visible location text ("Room 204") stays as-is — this is only the maps query. Sweep every maps-link composer (class pages, emails' location blocks, collateral) through the one resolver. Verify: sample + a real HGL class link opens the actual HQ pin; ISD case per its record; room text unchanged on page.

**✅ SHIPPED (Aug 29).** ONE source `HGL_HQ_ADDRESS` = "380 W. Pierpont Avenue, Salt Lake City, UT 84101 USA" (`app/utils/hgl-address.ts`) shared by: the email footer (code fallback + `app_settings.business_address` updated by migration `20260911000003` so the live footer matches), the maps link, the JSON-LD, the collateral letterhead (**which had been printing zip 84109 — fixed**), and the wizard's at-HGL preset. The maps QUERY resolves: address-shaped location (PL-305 postal shape, exported as `isAddressShaped`) → verbatim; room-only/blank at-HGL → HQ; the visible "Room 204" text unchanged. Sweep found exactly ONE maps composer repo-wide (the /c view — /{code} serves it in place); emails and collateral compose no maps links. School classes: `schools` has NO address column, and the composer already gates `!school` — honest absence confirmed, noted for a future schools.address if wanted. Online: no link (gate verified). JSON-LD Place.address gets the same resolution (school room-only keeps name, omits address).

## PL-400 — Dashboard "starts Invalid Date": PL-383's in-passing fix double-formats (Scarlett, Aug 19)
DIAGNOSED: /api/admin/dashboard already returns `upcoming[].startDate` FORMATTED (plainDate → "October 13, 2026"), but dashboard-panel.tsx now renders `formatDateAdmin(c.startDate)` (PL-383's "in passing: two admin raw-ISO start dates → formatDateAdmin") — which slices the first 10 chars ("October 13") and parses as ISO → Invalid Date on every browser. Classic double-format: the raw-ISO cleanup wrapped a string that was never raw. Fix: ONE side formats — prefer the API sending the raw ISO date and the panel formatting (matches the rest of the codebase's formatter-at-render pattern), or drop the wrapper; either way check the SECOND site that in-passing fix touched for the same regression, and grep the panel/API pair for any other formatted-then-formatted fields. Guard for the future: formatDateAdmin (and siblings) should REFUSE non-ISO input loudly in dev (throw or console.error) instead of rendering "Invalid Date" to staff — silent garbage is how this shipped. Verify on the deployed dashboard (both cards' dates real), mobile + desktop.

**✅ SHIPPED (Aug 29).** Fix took the doc's preferred side: `/api/admin/dashboard` ships the RAW ISO `startDate` and the panel formats at render (`formatDateAdmin` — dashboard now matches the admin-wide date format); the type is annotated raw-ISO. The SECOND PL-383 site (collateral class picker, `admin/page.tsx`) passes the raw `classes.start_date` column — verified correct, untouched; no other formatted-then-formatted field exists in the pair (checked every standalone field; other pre-formatted dates ride inside prose strings). GUARD: `utcAnchor` — the single chokepoint under every calendar-date formatter — now REFUSES non-ISO input loudly (throw in dev, console.error in prod). Verified in the browser: "starts 13 October 2026" / "starts 03 November 2026" on the dashboard card; deployed dashboard re-checked after push.

## PL-401 — Google sync creates DUPLICATE events for one session (Scarlett, Aug 19 — screenshot: four identical "Tuto… 15:0…" blocks)
The tutor's real Google Calendar shows the same session synced 4×. Find every source: the batch-40 duplicated-recurrence bug was deduped at the generator, but (a) the sync itself must be IDEMPOTENT per session (one deterministic portal-event identity per session id — search-before-create or stable event id/extended property; retries/re-syncs adopt, never re-create), and (b) the duplicates already on calendars need a CLEANUP sweep: for each session, keep one event, delete the extras (portal-created events only — the identity marker gates deletion; dry-run count first, logged, reported in ship notes). Check whether duplicate events also re-armed drift/busy machinery (they do: each stray copy is a future false busy-block and false drift source — quantify in the ship note how many were cleaned). Verify against Billy's real calendar: one event per session after sweep, re-running sync creates zero new events.

**✅ SHIPPED (Aug 29).** Root cause CONFIRMED: event identity was pointer-only (`gcal_event_id`), and the worker's pointer write was UNCHECKED — a lost write after a successful Google create left the queue row pending, and every retry minted a twin. Fixes: (1) every synced event carries a `hglSessionId` private extended property (deterministic identity); (2) `createOrAdopt` — search-before-create on the marker; retries/re-syncs ADOPT, never re-create; (3) the pointer write is checked (failure → row fails → retry adopts); (4) coverage tutor-flips release the OLD tutor's event before reassign (the new tutor's patch 404'd and stranded a copy); (5) reschedule rolls back its replacement insert if the original's update fails (two rows can't share one event id). CLEANUP (`scripts/pl401-dedupe-gcal-events.mjs`, dry-run first): **3 duplicates found and deleted** on Billy's real calendar — exactly the screenshot's 4× "Tutoring: Fakey — ASVAB" Sep 7 (1 live + 3 twins); re-run finds 0; one PL-62 QA orphan honestly SKIPPED (no matching session). Markers BACKFILLED onto all 16 future synced events (`scripts/pl401-marker-backfill.mjs`), and the adoption E2E against Billy's real calendar passed: pointer nulled on purpose → re-sync adopted the SAME event id, zero new events. Drift/busy note: busy surfaces were already protected by PL-388's title predicate; drift keys on sessions (one row each), so duplicates never multiplied emails 1:1 — see PL-402 for what actually flooded the inbox.

## PL-402 — Drift alerts: fire ONCE, immediately, grouped — not one email per event per day (Scarlett, Aug 19)
Scarlett received the same "Billy deleted Janeson's session event" email for DAYS. Two fixes: **A. Timeliness:** drift detection currently rides the periodic audit; on detection the alert should go out promptly after the change lands (whatever the mechanism — webhook/watch channel if available, else the existing cadence is the floor but see B for why repeats must stop). **B. One alert per drift, grouped:** an alert email covers ALL drifts detected in that pass as ONE summary (grouped by tutor → student, each line = change + when + link to its banner), and a drift that has ALREADY been alerted NEVER re-alerts — the Needs Attention row + banner are the persistent reminder (PL-393's design); the email is the doorbell, rung once. Record alerted_at per drift row; re-alert only if the drift CHANGES (e.g. a second move of the same event). If PL-401's duplicates were multiplying alerts (one per duplicate copy), say so in the ship note — the dedupe + identity fix should collapse them. Verify E2E: move 3 events in one pass → ONE email listing 3; next audit pass → zero emails; move one AGAIN → one new email.

**✅ SHIPPED (Aug 29; migration 20260911000001 APPLIED).** What actually hit Scarlett's inbox: **19 per-session alert emails Aug 17–26 (Roman 8, Fakey 6, Janeson 5), 13 of them in ONE pass at Aug 18 23:05** — the per-row loop fired one email per drift row when Billy mass-deleted portal events while cleaning his calendar; the (session, cal-time) dedupe key did prevent day-over-day repeats per session, but new deletions each day made it feel continuous. **B (grouped, once):** `sendGroupedDriftAlert` (in gcal-sync, so the harness can prove it) sends ONE email per audit pass covering every not-yet-alerted drift, grouped tutor → student, each line = change + when + banner deep-link (conflict specifics captured at alert time); `calendar_drift.alerted_at/alerted_signature` (preserved across the audit's upserts) make an alerted drift permanently silent until its calendar state CHANGES again; the email says so ("this email won't repeat — the banners are the reminder", PL-393's design); a failed send skips marking so the doorbell retries. E2E (`scripts/pl402-drift-alert-e2e.mjs`, fake Google + captured sends + real table): 3 changes → ONE 3-line email · next pass → zero · one re-move → one new 1-line email · restore clears the table — ALL PASS. **A (timeliness):** the hourly sweep stays the floor (detection also runs on every drift-banner mount); a Google push/watch channel needs a public webhook + renewal machinery — flagged as future work, not built. PL-401's duplicates did NOT multiply emails 1:1 (alerts key on sessions, not events) — the duplicate clutter is what prompted the mass deletion that became the burst; with idempotent sync + grouping, both halves are closed.

## PL-403 — Final-count email arrived after the first session ended (Scarlett, Aug 19)
The "Registration is closed — this is the final count" instructor email for HGL PSAT Prep landed at 11:05 PM on Aug 18 — the class's first session (5:00–8:00 PM that day) was already over; the email's entire purpose is the instructor seeing the final roster BEFORE walking into day one. Diagnose why it fired ~6h after the cutoff (registration closes at first session by default, so the count was final at 5:00 PM — did it ride a daily/backstop cron instead of the hourly sweep? Denver-midnight bucketing?) and fix the trigger to fire promptly at the actual close moment (the hourly cadence is an acceptable floor: close at 5:00 → email by ~6:00 at worst — but late-evening batching is not). **Scarlett's operating model (design to THIS):** the roster is mostly final at the REGISTRATION DEADLINE; one or two stragglers may trickle in until class start. So the instructor briefing sends promptly at the deadline — "here's your roster (N students); registration technically stays open until the first session, so a straggler or two may still join" — and any straggler who joins after triggers ONE short roster-updated note to the instructor ("+1: {student} — now N"; grouped if several land in one pass). No "final count" claim until nothing more can change; the deadline email is the briefing moment, the class start is just the hard stop. Sweep siblings: every time-anchored one-shot email (deadline reminders, day-one location sends, T-minus steps) checked for the same rides-the-wrong-cron lateness — the roster-report clock env lesson applies; list each in ship notes with its actual firing lag. Verify with a fixture class closing mid-day: email lands within the hour, before the first session.

**✅ SHIPPED (Aug 29).** DIAGNOSED: `registration_close_date` is a DATE — close means local MIDNIGHT, so the `today > regClose` flip + the hourly sweep + **no hour gate on the instructor sweep** landed the "final count" at ~00:05 class-local = 11:05 PM Denver, after day one ended. REDESIGNED to Scarlett's model: the **deadline briefing** (`deadline_briefing` variant) fires the morning (localHour ≥ 8) the `effectiveDeadline` arrives — "here's your roster (N students); registration technically stays open until the first session, so a straggler or two may still join — you'll get a short note if anyone does"; each payment landing after the last roster email triggers ONE grouped `roster_addition` note ("+1: {student} — now N"; several joiners in a pass share one email; md5-of-ids key so a failed send retries and a new joiner regroups); the briefing key carries the deadline so extending it re-arms (the min-enrollment pattern); **"final" is claimed never** — nothing can change after close and the +1 notes already told the story; the old `registration_closed` variant is deleted. E2E (`scripts/pl403-briefing-e2e.mjs`, fixture class w/ deadline=today, real sends to billy@ only, fixture cleaned): briefing landed the SAME pass (before the first session) · re-run silent · straggler → one +1 note · re-run silent — ALL PASS. Live classes checked: none mis-fires on deploy (PSAT is past close; the two upcoming are before their deadlines). SIBLING SWEEP w/ actual lags: weekly IN_DIGEST had the same no-hour-gate hole (fired 00:05 Monday — now gated ≥8); the 7 family SEQUENCE steps, payment ladder (hour-since anchors), waitlist release/rollover, surveys, FO sends, counselor pushes all ride the hourly sweep with hour gates — ~1h worst case with pg_cron healthy (daily Vercel backstop = up to ~24h if pg_cron dies, unchanged); E4's held-on-blank-details is deliberately unbounded with its own daily alert; `AL_ROSTER_REPORT`'s Monday gate uses DEFAULT_TIMEZONE (America/Mexico_City — the org default oddity; flagged, not changed).

## PL-404 — Tutor (and family) portal: collapsible sections / left menu (Scarlett, Aug 19)
Tutor view stacks My Classes above My tutoring — a tutor with many classes scrolls forever to reach tutoring. Make the big sections collapsible AND/OR give the portal a left menu (Scarlett suggested left menu; Code picks the pattern that fits the portal's existing chrome, mobile included — a left menu that collapses to top tabs at phone width, or sticky section jump-links; state-driven open/closed per the admin's collapse rules, remembered per user). Same treatment on the FAMILY portal where it helps — but designed to appear ONLY when necessary: a family with one student and one enrollment sees today's simple single-column page, no nav chrome for nothing (compose from what the account actually has — sections gate on content the way blocks gate on class facts). View-as must render exactly what the family sees (pointer-events rule unchanged). Verify: tutor with many classes reaches tutoring in one click; single-enrollment family page unchanged vs today; mobile widths.

**✅ SHIPPED (Aug 29).** Left menu it is (Scarlett's suggestion): `PortalNav` — sticky left menu on desktop collapsing to a horizontal chip row at phone width; pure server-rendered anchor links (zero state → View-as pixel-identical under the PL-325 interceptor; per-user open/closed memory unnecessary since nothing collapses). **Composed from what the account has**: fewer than two destinations renders NOTHING — a single-student single-enrollment family keeps today's simple single-column page (verified: Roman-only family, no nav; two-student family, per-student jumps). Tutor: ONE shared `TeachingView` body (menu + My classes + My tutoring + My students / Timecards / Email preferences) used by BOTH the portal and View-as — a tutor with many classes reaches tutoring in one click (verified: jump lands the section at viewport top past a 6500px class stack). Family: per-student jumps when >1 student + "1-on-1 tutoring" when the family actually has it (probe mirrors TutoringSection's own gate). FIXED IN PASSING: View-as's family preview rendered TutoringSection TWICE (ParentView already includes it) and its tutor preview showed only the tutoring half — both now render exactly what the person sees (verified: #portal-tutoring appears once; tutor preview shows the full menu). Mobile: chip row renders; the only horizontal overflow at 375px is pre-existing data tables (not the nav).

## PL-405 — Cancelled future sessions may RESUME after the period boundary (Scarlett, Aug 19 — Roman SAT w/ Billy) + per-student recent-activity pane
**A. Investigate Roman's case FIRST, in the data:** Scarlett cancelled future sessions for Roman (SAT, Billy) and believes sessions reappeared for later periods. Prime suspect: cancelling SESSIONS doesn't touch the recurrence/engagement, so the next monthly generation (or a regenerate/edit-save) re-projects the same weekly slots fresh — cancellation only reached as far as the sessions that existed at the time. Check Roman's rows (what was cancelled when, what exists now, what created the later ones — source/created_at tell the story) and report what actually happened before fixing. **B. Fix the mechanic:** "cancel future sessions" must mean what it says — either it ENDS/PAUSES the engagement's recurrence from that date (the honest mechanic; the existing end/pause machinery is right there) or, if staff genuinely cancelled a bounded window, the generation must never resurrect slots inside a window staff cancelled (tombstones). The UI should say plainly at cancel time what will happen ("this cancels the N remaining scheduled sessions AND stops the weekly pattern" vs "…sessions through {date}; the pattern resumes after") — inline, chosen explicitly, never guessed. Align PL-387's edit/regenerate with whichever rule lands. **C. Per-student recent-activity pane (admin):** the family/student admin page gains a compact activity feed — schedule edits, cancellations, regenerations, payments, emails sent, drift adoptions, who/when — composed from the existing audit/history data (sendlog, *_by/at columns, engagement events; read-only, deep-links each record). That's how Scarlett would have SEEN what happened to Roman without asking. Verify: reproduce Roman's sequence on a fixture (cancel-future → generation runs → nothing resurrects under the chosen rule); activity pane shows the whole story in order.

**✅ SHIPPED (Aug 29).** **A — what actually happened to Roman (from the rows):** Scarlett's cancel = both engagements (SAT + French, Billy) set `status=ended, end_date=2026-08-18` at 12:27/12:29 PM. Generation behaved (monthly cycle filters status=active — no new projections). But (1) `clearFutureSessions` deliberately preserved reschedule-TARGET make-ups (PL-122), leaving a confirmed Sep 14 SAT session alive inside the ended engagement, and (2) on **Aug 25** the family picked an offered replacement slot in the portal — the offer-accept path never checked engagement status and MINTED a fresh confirmed **Sep 15** session (synced to Google). That's the "resumed after the period boundary." **B — the mechanic, fixed:** ending/pausing now clears reschedule targets too (`includeRescheduleTargets`; a plain regenerate still preserves them — they're agreed commitments); the family reschedule machinery refuses non-active engagements in plain English; the taken-sets in BOTH generators treat `forfeited`/`no_show` as tombstones (admin cancels write those — they were missing, so any regenerate resurrected staff-cancelled slots: the other latent resurrection vector, now closed and aligned with PL-387's regenerate); pause/end confirms state the full consequence inline ("stops the weekly pattern AND removes every remaining scheduled session — rescheduled make-ups included; invoiced ones stay billed") and the completion toast reports the REAL removed count from the API. **C — activity pane:** the family profile hub gains a "Recent activity" sidebar section — schedule creates/ends, cancellations with WHO (PL-395 names; drift-banner resolutions flagged as such), moves incl. self-serve, payments, registrations, availability, every email sent — from the ONE dashboard-activity builder (PL-344) with a `familyId` scope, server-paged, read-only, rows deep-link. Verified on Roman's family: the pane shows the whole story in order (schedules ended Aug 18 → drift-banner resolutions Aug 22 → the Sep 14→15 self-serve move Aug 25). QA quirk noted: the Tomás QA family's parent email is billy@, so staff mail addressed to billy@ shows in THAT family's feed — real families have unique parent addresses.
