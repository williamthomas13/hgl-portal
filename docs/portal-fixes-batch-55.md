# Portal fixes — batch 55 (OPEN — Sep 23, 2026): PL-498 subdomain-canonical public host

**Batch closed Sep 23 — PL-498 shipped.** Gate battery vs the PROD build on :3100: `tsc` · `next build` · `regress:canonical-hosts` **79** (was 49 — rewritten for the redirector model: 9 portal-only paths 308, 12 public paths 301 with path + query, POST 308, root permanent to the main site, the two-hop unknown-path chain, 15 paths never 200 on hgl.co, 9 portal-host pages 200, canonical / og:url / JSON-LD + sameAs on the portal host, zero hgl.co in canonical / OG / sitemap / llms.txt / embed) · `smoke:public` (114) · `regress:cta-landings` (232). No migration, no env var, no DNS touched. The short-host half is proven on the build with `curl -H "Host: hgl.co"` (hgl.co reaches Vercel only at row 8); the portal-host half is re-checked on prod after the deploy. Scarlett + Claude may start the cutover from row 1 once Vercel reports this commit Ready.

**Standing rules:** all prior. Next PL after this file: PL-499. **Ship BEFORE the DNS cutover rows that touch hgl.co** (host table rows 4, 8, 19, 20) — Scarlett + Claude start the cutover from row 1 once this is on prod.

## PL-498 — portal.highergroundlearning.com is canonical for the PUBLIC pages too; hgl.co becomes a pure redirector (Scarlett, Sep 23 — revises the Sep 21 domain-pair decision)

**Why (decision record, Scarlett Sep 23):** hgl.co has no domain history, so making it the canonical home of /classes, /team, /inquire and the school codes would split the brand across two domains for search engines and LLM entity resolution. The long-term plan (scoped for AFTER the Synap replacement) is to move the whole marketing site onto Vercel at the apex; in the meantime every public page should be canonical on a highergroundlearning.com host so no links or search history accrue to hgl.co that would have to be migrated twice. Printed codes keep working because hgl.co/{code} redirects.

**New pair:**
- `portal.highergroundlearning.com` — canonical for EVERYTHING the app serves: signed-in surfaces, emailed links, AND the public pages (/classes, /team, /inquire, /compass, /partner, /{code}, /{code}/register, /c/{slug}, the embeds). Sitemap, `<link rel=canonical>`, OG url, JSON-LD, llms.txt and the homepage-embed "More info" links all point here.
- `hgl.co` / `www.hgl.co` — 301 EVERY path to the same path + query on the portal host (`hgl.co/sls` → `https://portal.highergroundlearning.com/sls`), except the bare root, which keeps its existing 301 to the main site (PL-349, host-conditioned in `next.config.ts`), and the `[...forward]` behaviour for paths the portal doesn't own (unknown paths still 301 to `https://www.highergroundlearning.com/{path}` — the standing "hgl.co/sat → main-site /sat" rule). Nothing is ever *served* on hgl.co.
- `highergroundlearning.com` — Squarespace, untouched; its own /classes and /team pages become Squarespace URL mappings (301) to the portal host and the main-site menu items point there (Scarlett does this in Squarespace at cutover row 20).

### ✅ SHIPPED (Sep 23) — one commit; gate battery vs the prod build in the close line

- **1. `proxy.ts`:** on a `SHORT_PUBLIC_HOSTS` host every path redirects to `https://${canonicalPortalHost()}${path}${search}` — **301** for a public GET/HEAD, **308** for `isPortalOnlyPath()` or any non-GET (a POST is never downgraded); `/` → 301 the main site as the belt to next.config's brace. **Measured:** next.config's host-conditioned `permanent: true` fires FIRST and Next emits a **308** for it (PL-349 shipped that way — the doc's "existing 301" was always a 308 in practice); the proxy's 301 is the fallback if that order ever changes. The gate accepts either — permanent, to the main site — and next.config is untouched. The matcher is now a catch-all (`/((?!_next/static|_next/image|favicon\.ico).*)`) because a per-prefix list can't mean "every path"; on the portal host the early return keeps every non-session path a no-op, so only /admin, /portal, /login pay the auth round-trip. The portal host is never redirected — no loop.
- **2. `publicSiteOrigin()`** returns `emailBaseUrl()`; `PUBLIC_SHORT_ORIGIN` is retired — if set it is ignored and `console.warn`ed once per process (the gate compiles base-url.ts with the var set to `https://hgl.co` and asserts it is ignored). The nine call sites are untouched; the header block and comments describe the new pair. `MAIN_SITE_ORIGIN` constant added (the proxy's root target).
- **3. `[...forward]`:** unchanged. With the proxy redirecting every hgl.co path, it only ever runs on the portal host, where `/{code}` → `wildcardForward()` still 301s unknown paths to `https://highergroundlearning.com/{path}`, never to the portal host. **Honest note:** the doc's gate line reads "`hgl.co/sat` → 301 main-site /sat" as ONE hop; it is TWO permanent hops (hgl.co → portal/sat → main-site/sat), because telling a code from an unknown path needs a DB read the edge proxy doesn't make (and shouldn't — printed codes are the traffic that matters, and they'd redirect either way). The gate asserts the chain: hop 1 is a 301 to the portal host, hop 2 is permanent to the main site, never back to hgl.co.
- **4. Collateral** still prints `hgl.co/{code}` (`collateral.ts` untouched). Click counting: `bumpCodeVisit()` runs in the `/{code}` and `/{code}/register` pages with no host condition, so the visit is recorded when the 301 lands on the portal host as a normal GET; the class-page `arrival:shortlink` analytics fire on `viaCode` (mode = code), also host-independent. Nothing to change — verified by reading, not assumed.
- **5. `docs/dns-cutover-host-settings.md`:** Decision line rewritten to the new pair (+ the why); row 4 RETIRED; row 8's verify column = the redirect chain; row 19 noscript + "More info" = the portal host; row 20 = portal-host buttons + the two Squarespace URL mappings + the menu swap; the "Registration started on hgl.co" paragraph says it no longer applies.
- **6. JSON-LD `Organization`** on /team and the class page: `url` = `publicSiteOrigin()` (the portal host), `sameAs: ["https://www.highergroundlearning.com"]`, `@id` unchanged. `sitemap.ts` / `llms.txt` already ride `publicSiteOrigin()` — now the portal host by construction.
- **Gate (`regress:canonical-hosts`, rewritten):** A. catch-all matcher; every `PORTAL_ONLY_PATH_PREFIXES` entry covered; the proxy's three rules present by text; `PUBLIC_SHORT_ORIGIN` read nowhere but base-url.ts; `publicSiteOrigin() === emailBaseUrl()` with the retired var set. B. with `Host: hgl.co`: nine portal-only paths → 308; twelve public paths (`/sls`, `/sls?via=flyer`, `/classes`, `/team`, `/inquire?source=…`, `/compass`, `/partner`, `/c/anything`, `/sls/register`, `/embed/upcoming-classes.js`, `/sitemap.xml`, `/llms.txt`) → 301 same path + query; POST `/api/inquiry` → 308; `/` → 301 main site; `/sat` → the two-hop chain; fifteen assorted paths never 200. With the portal host: nine public URLs 200; canonical, og:url and JSON-LD on `/sls` and `/team` on the portal host with `sameAs`; canonical/OG on three pages never name hgl.co; the sitemap lists only the portal host; llms.txt and the homepage embed link the portal host and never hgl.co. `smoke:public` + `cta-landings` unchanged.
- **What prod can and can't prove today:** hgl.co isn't attached to Vercel until row 8, so a request with `Host: hgl.co` can't reach the deployment from outside — the short-host half of the gate is proven on the prod BUILD locally (curl with the Host header) and will be re-proven end-to-end at row 8's verify column. The portal-host half (canonical / OG / JSON-LD / sitemap / llms.txt) is checked on prod after the deploy.

**Code changes**
1. `proxy.ts`: on a `SHORT_PUBLIC_HOSTS` host, redirect **every** path (not only `isPortalOnlyPath`) to `https://${canonicalPortalHost()}${path}${search}` — 301 for public GETs (cacheable, passes link equity), 308 for anything else / non-GET so a POST is never downgraded. Keep the "never redirect the portal host" rule so there is no loop. The root `/` on hgl.co is still handled by the `next.config.ts` redirect (fires first) — assert both orders in the gate.
2. `publicSiteOrigin()` in `base-url.ts`: `PUBLIC_SHORT_ORIGIN` is retired (never set it); the function returns `emailBaseUrl()` (= `PRODUCTION_BASE_URL` = the portal host). Keep the function so its nine callers don't change; update its comment and the file header. If the env var IS set, ignore it and log once at boot (so a stale Vercel env can't resurrect hgl.co canonicals).
3. `[...forward]`: unchanged in behaviour but re-read it — with the proxy now redirecting all portal-owned paths away from hgl.co, `[...forward]` only ever sees hgl.co paths the app has no route for. Confirm it still 301s those to the main site and never to the portal host.
4. Collateral (`app/utils/collateral.ts`) keeps printing `hgl.co/{code}` — that is the point of the short host. Short-link click counting: check the click-history counter in `app/api/admin/evergreen` / the `/{code}` route still records a visit when the request arrives via the 301 (it lands on the portal host as a normal GET — if the counter keyed on the hgl.co host, key it on the referrer-less first hit or drop the host condition).
5. `docs/dns-cutover-host-settings.md`: rewrite the **Decision** line to the new pair; row 4 → "RETIRED — never set (PL-498)"; row 6 unchanged; row 8's Verify column → `https://hgl.co/sls` → 301 `https://portal.highergroundlearning.com/sls`, `https://hgl.co/` → 301 main site, `https://hgl.co/sat` → 301 main-site /sat; row 19's noscript link → `https://portal.highergroundlearning.com/classes`; row 20's button links → `https://portal.highergroundlearning.com/{code}/register` and `…/inquire?source=sqsp-<page>` (printed collateral stays `hgl.co/{code}`), plus Squarespace URL mappings `/classes -> https://portal.highergroundlearning.com/classes 301` and `/team -> https://portal.highergroundlearning.com/team 301`; the "Registration started on hgl.co" paragraph → no longer applies (nothing is served there) — say so.
6. `llms.txt`, `sitemap.ts`, JSON-LD `Organization` — `url` = the portal host; add `sameAs: ["https://www.highergroundlearning.com"]` if not already there so the entity links back to the brand domain.

**Gate:** `regress:canonical-hosts` gains: with `Host: hgl.co`, `/sls`, `/classes`, `/team`, `/inquire`, `/c/anything`, `/sls/register`, `/embed/upcoming-classes.js` each → 301 to the portal host, same path + query; `/login` and `/portal/x` → 308; `/` → 301 main site; `/sat` → 301 main-site /sat; no response on hgl.co is ever 200. With the portal host: every public page 200, canonical + OG + JSON-LD url on the portal host, sitemap entries all on the portal host, zero `hgl.co` in any canonical/OG/sitemap/llms.txt output. `smoke:public` + `cta-landings` unchanged.
