# Portal fixes — batch 40 (ACCUMULATING — opened Aug 18, 2026)

**Standing rules:** plain-English statuses incl. dates and time labels (class's own city, never zone city) · no internal shorthand · every alert deep-links its record + resolution surface · samples from composers · `git push` after committing · PL-x IDs in commits · check items off here when shipped · registry template edits ship as new versions via anchor guards, twins+seeds lockstep · verify composed blocks via the composer path · inline confirm banners only · NO native browser dialogs · schedule-change baseline = what the family SAW at THEIR registration (incl. imports) · financial/class facts never hand-typed into marketing copy or structured markup · two-phase column drops incl. post-drop grep · **test/QA email → billy@ / billy+*@ ONLY** · crashed gate ≠ green gate · imports fire NO emails · previews use the REAL renderer · review ≠ edit (reviewed_by; badges re-raise only on touched blocks) · one collision validator across all shortlink namespaces · tell Scarlett the UI's own labels and locations, not internal names.

(Accumulating. Next PL: PL-395.)

## PL-383 — "Cities shown with the times" is set-at-creation-only; make it editable on the class card (Scarlett, Aug 18)
The field lives only in the class wizard (details step, open-enrollment online classes), and the wizard only CREATES classes — the card's inline edits (location, Synap group, slug, deadline, marketing name) never included cities, so an existing class's cities can't be changed at all (the R/W Deep Dive is stuck saying "Rome time"). Fix it with a cleanup, not another loose row (Scarlett's call): collapse the card's editable class-setup fields into ONE expandable **"Edit class details"** section — collapsed by default so the card stays clean (roster/status/actions prominent), expanding to the wizard-style fields: "Cities shown with the times" (same label + hint as the wizard, open-enrollment online classes only; saving updates `display_cities` and every consumer — page + email labels via publicTimeCityLabel — follows immediately), plus the existing inline edits (marketing page URL, location, Synap group, deadline/cutoff, marketing name) moved inside; registration link + Copy stay visible on the card (daily-use, not setup). Same inline-confirm patterns, no behavior changes to the moved fields; "edit slug" keeps its extra warning. Checkbox-toggles (has diagnostics, follow-up, auto-extend) go inside too — they are setup, not status. While in there, sweep the wizard's other fields for the same trap — anything settable at creation with NO post-creation edit surface gets listed in ship notes with a verdict (add inline edit / deliberately immutable + why; slugs stay immutable-by-rule, minted at creation). Verify: edit the R/W Deep Dive live → its /c page and a composer-path email render the new cities; a school class and an in-person HGL class don't show the field.

## PL-384 — ONE link model: every code is evergreen; /register suffix; retire point-in-time codes + Marketing page URL (Scarlett, Aug 18)
Scarlett's simplification, adopted: a school/course has exactly ONE hgl.co code, evergreen — no point-in-time shortcode layer, no manual repointing, no separate marketing URL. **A. Unify:** the existing class shortcodes (isd/mis/nido/sls — printed, so the STRINGS must survive) become evergreen codes on their schools; the class_shortcode namespace folds into evergreen (one table, one validator; migration converts, honest history kept). Resolution: newest open class of the school/course → its /c page; nothing open → the existing interest-capture page. The PL-349 repoint-nudge machinery retires (nothing to repoint). **B. /register suffix:** `/{code}/register` resolves the SAME class and lands on its registration form; nothing open → the same capture page (a register link never strands). Analytics ?via carries through both forms. **C. Pin escape hatch** for the two-open-classes case (fall still registering when spring opens — "newest open" would steal the link mid-campaign): a per-code optional pin to a specific class, plainly badged "pinned to {class}" in the table, with a one-click unpin; pinned + class closes → falls back to auto-resolution (never a dead link). **D. Marketing page URL retires:** every follow-up email "More info" link + register-page reference that consumed it now uses the class's own /c page (composed, not a field); two-phase drop of the column per standing rule, ship notes list every consumer repointed. The class card keeps registration link + Copy (daily use); its Short-links-related clutter drops. **E. Docs/UI:** Classes → Short links explains the model in one sentence ("one link per school or course — always works, even between classes"); cutover checklist references unchanged printed codes. Verify: /isd resolves ISD's open class then (fixture) capture page when closed; /{code}/register both states; pin honored + auto-fallback on close; zero remaining marketing_page_url consumers post-drop grep; printed codes unchanged end-to-end.

**PL-384 amendment (Scarlett, Aug 18):** (1) **Full link-machinery convergence audit** — the link layers were built across many batches (PL-155b domain pair, PL-349 shortcodes + repoint nudges + bare-domain 301s, PL-361 SR resume links, PL-363 import repointing, PL-378 evergreen/legacy/browse, marketing-page-URL consumers, footer portal URL, campaign/analytics ?via, sitemap/JSON-LD canonicals, origin-guard allowlist): inventory EVERY place a link is minted, stored, resolved, redirected, or rendered; converge on the one model; ship notes = the inventory with each site's verdict (converged/retired/left + why). No stray resolver survives the audit. (2) **Family-visible URLs stay short:** codes should SERVE the landing page in place (rewrite, not redirect) so hgl.co/sls stays in the address bar — no /c/{slug} flash; /{code}/register same treatment. **CANONICAL = THE CODE URL (Scarlett's call — nothing is indexed yet, and a stable per-school/course URL accumulating authority beats fresh per-cohort slugs):** sitemap lists /{code} URLs (+ /classes, /team); the code-served view carries the class's JSON-LD; /c/{slug} survives ONLY as the internal unique address a class needs when it isn't the one its code currently shows (sibling sections, pinned overlaps, past classes) — those views are noindex with a canonical pointing at their code's URL (or plain noindex when the class has no code); slugs-never-rewritten rule unchanged. The interest-capture state serves at the same /{code} URL (one URL, honest states). Dropping the /c prefix entirely stays declined — it's internal plumbing families and Google now never see.

**PL-384 note:** /classes browse cards (and any other internal link surface the audit finds — emails, campaign links, admin copy-link buttons) link to the CODE URL when the class's school/course has one, falling back to /c/{slug} only for codeless classes — shared/bookmarked links from anywhere should be the permanent address.

## PL-385 — Auto-updating "Upcoming classes" panel for the sqsp homepage, fed by the portal (Scarlett, Aug 18)
The brand-site homepage's manually-maintained Upcoming classes strip stays (it's browse-appeal — "oh cool, they teach in Chile" — not primary discovery) but should feed from the portal. **A.** A self-contained embed the sqsp page includes via a code block: portal-served, renders the open classes as cards matching the current strip (school logo — HGL logo for no-school classes per PL-375 —, class name, plain-English date range, a one-line composed blurb: campus/city + duration + the exam date it prepares for, ALL from the record, never hand-typed), each "More info" → the class's permanent code URL (PL-384 preference). Auto-reflects open/close; nothing open → the section renders a modest "join the interest list" line pointing at /classes, never an empty hole. Cached + fast (it's on the homepage), no cookies/analytics beacons into the sqsp page (PL-350 rules apply portal-side only), graceful if the script is blocked (link to /classes as noscript/fallback). **B.** Implementation is Code's call (script-injected markup vs iframe w/ auto-height), but the embed snippet must be paste-once: all future changes ship portal-side with no sqsp re-edit. Document the snippet + where it goes in the cutover checklist. **C.** Checklist addition: the post-cutover Squarespace DOWNGRADE target plan must still allow code/embed blocks (Business tier or equivalent) — record the chosen plan; if Scarlett ends up on a plan without embeds, the honest fallback is a plain "See upcoming classes →" button to /classes (note it, don't fake it). Verify: embed rendered inside a real sqsp page (test page is fine) across open/none-open states + mobile width; class opened/closed in the portal reflects without touching sqsp.

## PL-386 — Family/contact records: staff-editable everywhere a fact shows (Scarlett, Aug 18)
Contacts → family pages show contact facts with almost no edit surfaces: the parent card exposes ONLY billing-address edit (email/phone read-only), the student card shows email/phone/learning notes with no edit at all, and the "Who do I contact?" panel shows the parent's phone that the Household card has no way to add/change. Make the family profile's facts staff-editable in place (inline edits per the existing card patterns): parent name/email/phone/contact-preference, student email/phone/pronouns/grade/learning notes, emergency contact, the who-to-contact arrival instruction. **Permissions:** admin/ops staff only — instructors/tutors do NOT get edit surfaces (view stays); families never edit billing through THEIR portal (unchanged); the manager role follows its existing no-dollars lens (billing address stays manager-visible per current rules, contact facts editable). **Email edits are load-bearing:** parent email = portal sign-in + where everything sends — reuse the existing instructors-panel email-edit warning pattern (plain-English inline warning + confirm, recorded who/when); dedupe honestly (editing to an email another family uses → refuse with a link to that family, never silent merge). **Also (small):** the "Who do I contact?" arrival banner says "text the student" — render the student's NAME ("text Janeson"), pronoun-correct via the existing machinery, everywhere that instruction composes. Verify: each field round-trips live; instructor sim sees no edit affordances; email-edit warning + dedupe refusal E2E; banner shows the name.

## PL-387 — Edit an existing student's tutoring schedule: form AND calendar (Scarlett, Aug 18)
Today an existing engagement's schedule can't be edited at all from the admin: Tutoring → students' "manage" offers regenerate/pause/end — and regenerate's note says "use after editing" with no edit surface anywhere; Scheduling's "New Student Schedule" is creation-only and Current Student Schedules doesn't allow dragging. Provide BOTH paths, mirroring creation: **A. Form path:** an "Edit schedule" on the engagement (same fields as New Student Schedule — days/times/duration/tutor — prefilled with the current pattern); saving follows the established change rules: future sessions re-project, past/completed sessions untouched, the PL-314-style baseline + family comms per the existing schedule-change machinery, Google sync updates, invoices/timecards only affected where sessions actually moved (the regenerate note finally has something to point at — wire the flow so edit→regenerate is one guided motion, or fold regenerate into save if that's the honest mechanic; decide + note). **B. Calendar path:** in Current Student Schedules, drag a session to move it / resize duration (single-session move = the existing reschedule mechanics incl. the 24h fee rules where applicable — surface the SAME plain-English consequences inline before confirming, inline confirm not native dialog); offer "just this session" vs "this and future weekly slots" where the drag matches the recurring pattern. Tutor Google-busy conflicts warn like creation does. Instructors/tutors don't get drag-edit (view + their existing self-serve rules unchanged). Verify E2E: form edit re-projects future only; drag single + drag recurring; fee-window drag shows the fee consequence; gcal + timecard + invoice convergence on a moved session; pause/end unaffected.

## PL-388 — Scheduling calendar: sessions render twice (greyed twin underneath) (Scarlett, Aug 18)
Symptom (Scarlett's screenshot, Billy's tutor-week): each portal session block has the same session's text greyed behind it ("1:00–2:00 PM Tutoring: Janeson — AP/IB Calculus" under the solid block). Likely cause TO VERIFY, not assume: the Google-busy shading renders the tutor's gcal events — including the events the PORTAL ITSELF synced for these same sessions — so every session shows as itself + its own Google echo. Fix accordingly: busy shading excludes portal-synced events (they carry identifiable markers/ids per gcal-sync), so gray means "busy with something OTHER than portal tutoring"; if investigation finds a different cause (double-fetch, overlapping layers, duplicate rows from repeated emails — Scarlett's alternate hypothesis), fix the real one and say so in ship notes. Check the all-tutors·day view for the same artifact. Verify in real Chrome (the embedded-preview lesson): one block per session, real external gcal busy still shades, portal echo gone.

## PL-389 — Continue-tutoring "needs scheduling": false conflicts + a dead-end alert (Scarlett, Aug 18)
Two connected defects around the BL continuation flow: **A. Investigate the false conflict FIRST.** Faker confirmed 5 more hours; the alert claims "a conflict or no workable recurring time" — with nothing but test data on the calendar, that's suspicious. Prime suspect (verify, don't assume): the continuation reservation check counts the tutor's Google-busy blocks INCLUDING the portal's own synced sessions (the PL-388 echo, load-bearing this time) — the continuing slots are the SAME recurring times the student already held, so the reservation "conflicts" with the outgoing sessions' own gcal events or their portal rows (also check: does the reservation window overlap the block's final sessions, and does the checker exclude the engagement's OWN sessions?). Fix the real cause; ship notes say which it was. The alert copy should also say WHICH slot conflicted with WHAT ("Tuesdays 1:00–2:00 conflicts with Janeson's session") — "a conflict or no workable recurring time" is undiagnosable. **B. The alert must land on a resolution surface** (standing rule; today "Open the engagement" offers regenerate/pause/end — nothing schedules a continuation): once PL-387's edit-schedule form exists, the engagement page gets a "Schedule the continuation" action pre-filled with the continuation context (hours confirmed, prior recurring pattern, suggested next slots), and THIS alert deep-links straight into it. Completing it clears the Needs Attention row (state-driven). Verify E2E: continuation with the same recurring slot free of REAL external conflicts reserves cleanly (no false conflict); genuinely conflicted continuation → alert names the specific collision → link lands in the pre-filled scheduler → completing clears the row.

## ✅ PL-390 — Staff/admin email footer: wrong sign-in line (Scarlett, Aug 18)
**Shipped.** **ONE staff footer source:** `footerStaff()` in email.ts (same address + reply lines as the family footer) with the staff pointer — "Staff sign-in is always at {portal}/login — use your @highergroundlearning.com Google account." (the real staff surface: /login's Google SSO; URL from emailBaseUrl so the domain cutover updates it in one place, footer-URL-pinned rule intact). **Audience audit, every sender:** ADMIN — sendAdminAlert's code twin + every AL_* registry render (incl. the roster report and sweep-watch) → staff footer; TUTORS (confirmed all on @highergroundlearning.com) — T5 timecard, T6 notes (EOD/weekly/nudge), SUB_* coverage, T3_TUTOR_NOTICE twins + registry → staff footer; INSTRUCTORS — IN_WELCOME/DIGEST/FYI/MIN_DECISION + ADMIN_INSTRUCTOR_NUDGE → staff footer; **COUNSELORS — decided: keep the family-style pointer** (they sign in at /portal with just their school email, exactly as the line says — CS_*/CD_*/CR_* unchanged); FAMILIES — family footer verbatim, untouched (IM_INSTRUCTOR_MESSAGE recipients are families → family footer; the instructor's own FYI copy is IN_FYI → staff). Registry plumbing: `staffAudienceTemplate(key)` (AL_/IN_/SUB_ prefixes + the named tutor keys) drives the footer choice inside renderVersion, so live template renders and code twins can't disagree. **Verified** (`scripts/verify-pl390-footers.mjs`, 10/10): fresh composer renders per audience — T5 + AL_ROSTER_REPORT carry the staff line and NOT the family line, E0 family footer verbatim, CS counselor keeps the family pointer — one send per audience → billy+pl390-{tutor,admin,family}@; grep gate: zero staff-audience senders reference the family footer.

Original: Admin/staff-audience emails (roster report, Needs Attention alerts, timecards, digests) carry the FAMILY footer: "Your Higher Ground portal is always at hgl-portal.vercel.app/portal — sign in with just this email address, no password needed." Staff don't sign in there. Replace on every staff/admin-audience email with the staff line: sign in with your @highergroundlearning.com email (link the staff sign-in URL; wording plain, e.g. "Sign in to the admin at {url} with your @highergroundlearning.com email"). ONE staff footer source (like the family footer's), so the domain cutover updates it in one place (footer-URL-pinned rule applies). Audit every sender for audience: family emails keep the family footer verbatim; tutor-audience emails (timecard) get the staff line IF tutors sign in with org addresses (they do — confirmed all tutors have @highergroundlearning.com); mixed/counselor audiences decided explicitly and noted. Verify: fresh renders of one email per audience → billy@, correct footer each; grep: no staff-audience template/twin renders the family sign-in line.

## ✅ PL-391 — VFAQ email: orphaned bridge line when the gated FAQs drop (Scarlett, Aug 18, seen in a PL-379 test render)
**Shipped.** The bridge is now the composed `{vfaqBridgeLine}` — it renders only when a gated Q&A (diagnostics or strategy) actually follows it, i.e. when the "regular FAQs" section holds ≥2 entries; on the Deep-Dive shape (neither) it composes away and the miss-class FAQ flows without an intro. E3_VFAQ **v5 published** under an exact anchor on the hand-written line; twin (faqEmail) carries the identical condition; seed in lockstep. **All four gating combinations rendered via the composer path** (both / diagnostics-only / strategy-only / neither — 26/26 asserts in `scripts/verify-pl391-392-vfaq.mjs`): bridge present exactly when content follows, twin agrees in every shape, no orphaned intros, no unresolved variables, remaining FAQ flows clean. Test send of the no-diagnostics shape → billy+pl391-vfaq-deepdive@.

Original: On a no-diagnostics/no-strategy class (Deep Dive), the VFAQ email's gated Q&As correctly compose away — but the bridge line "Are you still here? You are? Okay, here are a few regular FAQs, just for you:" still renders, followed by just ONE remaining FAQ, so the joke sets up a list that isn't there. Make the bridge compositional like everything else: render it ONLY when the section it introduces actually has enough content to warrant it (e.g. ≥2 following FAQs AND at least one earlier gated section rendered — the line's humor depends on there having been a wall of FAQs before it); otherwise drop the line and let the remaining FAQ(s) flow without it. While in there, check the email's other connective tissue survives every gating combination (diagnostics-only, strategy-only, neither, both) — no orphaned intros, no "a few" preceding one item, counts/plurals honest in all four shapes. Registry rule (new version, twin+seed lockstep); composer-path renders of all four combinations + test send → billy@ of the no-diagnostics shape.

## ✅ PL-392 — Buttons never render mid-sentence (Scarlett, Aug 18, seen in a PL-379 test render)
**Shipped.** **The named offender:** `{vfaqDiagnosticQa}` recast — "No problem — the test is right here, due Friday, September 4, 2026, the day before your first class." with the button on its OWN line; the faqEmail twin matches (its spam-folder aside stays as a following sentence in the same paragraph). **Sweep results (every template seed + code twin + live registry body + composed variable):** live bodies = ZERO offenders (a python sweep of all 94 active bodies found no non-standalone button line — T8's live v5 already had the clean shape); seeds = ONE offender, T8_WELCOME_HANDOFF's "[button:Add to your calendar] — subscribe once…" trailing clause, seed realigned to the live v5 shape (button alone, explanation as its own following line); code twins = clean (the faqEmail fragments were the only prose case, fixed with PL-391; a fragment-tell scan of every `button()` call site found only false-positive code assignments); composed variables = `{lrDiagnosticSection}`, `{diagnosticPsE4Block}`/`E5` and the recast `{vfaqDiagnosticQa}` all carry standalone `\n\n[button:` lines. **Renderer enforcement (decided YES):** comms-md now treats `[button:]` as ALWAYS block-level — a button embedded in a paragraph is pulled onto its own CTA line in source order and the surrounding fragments render as their own visible paragraphs, so broken copy LOOKS broken in every composer-path render instead of hiding an inline button (asserted with a synthetic mid-sentence button). Verified in the PL-391 harness (26/26, all four gating shapes assert zero mid-sentence buttons in registry AND twin renders) + the same test send → billy@.

Original: The VFAQ diagnostic Q&A composes its [button:Take the diagnostic test] INSIDE the sentence ("…right here: [BUTTON]. It's due Friday…") — the button renders inline with sentence fragments hanging off both sides. Fix that block: recast so the prose reads whole ("No problem — the test is right here, due Friday, September 4, 2026, the day before your first class.") with the button on its OWN line after the sentence. Then SWEEP: grep every template/twin/seed + composed variables for [button:…] markup embedded mid-sentence (punctuation immediately after the button tag is the tell) and give each the same treatment — buttons are always their own block-level line; ship notes list each occurrence fixed or confirmed-already-clean. Consider enforcing at the renderer level: the email markdown renderer could treat [button:] as block-level always (surrounding sentence fragments would then LOOK broken in composer-path renders and get caught) — Code's call, note the decision. Registry rule for touched live templates; composer-path renders + one test send → billy@.

## PL-393 — Calendar-drift banner: the past-session case shows nothing (Scarlett, Aug 18)
The adopt/revert banner EXISTS (tutoring page, drift-banner.tsx) — but Scarlett clicked the alert's link after the moved session's time had passed and saw NO banner and no explanation, leaving the drift unresolvable and the alert a dead end. Diagnose exactly what happens to a drift row once the session start passes (silently filtered? cleared? still pending but unrendered?) and fix so it NEVER vanishes silently: a past-session drift stays visible with honest copy ("Billy moved this session in Google, and its time has now passed — record what actually happened") and past-appropriate resolutions: adopt-as-happened (session at the moved time: timecard/billing per the normal late-reschedule/reserved-time rules) or record it per what actually occurred (no-show/cancelled mechanics) — exact options per the existing session-state machinery, plain-English consequences inline. The email alert should also say the banner is time-sensitive is NOT acceptable as the fix — the fix is the banner surviving; but the alert copy may state plainly where the banner lives ("on Roman's tutoring page" naming the student, not "the tutoring page"). Verify E2E: drift + click before session → adopt + revert both work; drift + session passes unresolved → banner persists with past-tense options, resolving clears alert row; nothing auto-vanishes.

## PL-394 — Schedule-change email: "What changed" comes FIRST (Scarlett, Aug 18)
The tutor schedule-change recap currently leads with the full upcoming-sessions list and buries "What changed:" underneath — but the change IS the point of the email. Reorder: what-changed block first (right after the greeting/heading), then the full upcoming list as the reference below (its "worth a quick glance" line adjusting to the new order). Applies to every variant of the schedule-change recap (tutor AND family editions if the family one shares the shape — audit both; same reorder wherever a what-changed block exists, ship notes list which templates moved). Registry rule (new versions, twins+seeds lockstep); composer-path render + test send → billy@.
