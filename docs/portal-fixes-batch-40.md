# Portal fixes — batch 40 (ACCUMULATING — opened Aug 18, 2026)

**Standing rules:** plain-English statuses incl. dates and time labels (class's own city, never zone city) · no internal shorthand · every alert deep-links its record + resolution surface · samples from composers · `git push` after committing · PL-x IDs in commits · check items off here when shipped · registry template edits ship as new versions via anchor guards, twins+seeds lockstep · verify composed blocks via the composer path · inline confirm banners only · NO native browser dialogs · schedule-change baseline = what the family SAW at THEIR registration (incl. imports) · financial/class facts never hand-typed into marketing copy or structured markup · two-phase column drops incl. post-drop grep · **test/QA email → billy@ / billy+*@ ONLY** · crashed gate ≠ green gate · imports fire NO emails · previews use the REAL renderer · review ≠ edit (reviewed_by; badges re-raise only on touched blocks) · one collision validator across all shortlink namespaces · tell Scarlett the UI's own labels and locations, not internal names.

(Accumulating. Next PL: PL-395.)

## ✅ PL-383 — "Cities shown with the times" is set-at-creation-only; make it editable on the class card (Scarlett, Aug 18)
**Shipped.** The class card's setup fields now live in ONE expandable **"Edit class details"** section (collapsed by default — the card stays about roster/status/registration link). Inside: **"Cities shown with the times"** (NEW — same label + hint as the wizard, open-enrollment ONLINE classes only; saving writes `display_cities` and every consumer follows immediately), plus the moved existing edits (Location, Synap group, Marketing page URL, deadline + sign-up-cutoff pickers, short marketing name) and the setup toggles (Has diagnostics, follow-up flag, auto-extend) and the Follow-up-class dropdown. Registration link + Copy stay on the card; the min-enrollment decision zone and the FO extend control stay outside too (operational, not setup — deliberate). **Slug edit** moved inside as an inline edit — the old NATIVE `prompt()` is gone (no-native-dialogs rule, finally) — with the warning on the field ("changing the slug breaks already-shared links — the hgl.co code link is the one to share"); uniqueness refusal stays plain-English. **Wizard-field sweep (everything settable at creation), verdicts:** `price` / `capacity` / `min_enrollment` were wizard-only traps → NEW inline edits (plain-English refusals on nonsense; paid families keep their price snapshots); collateral fields (short link, language, blurbs, practice-test count, promo trio, selling bullets) → already editable in the Collateral card; instructor → the assign-instructor flow; sessions/schedule → the session editor (start date derives); `class_type`/school/`course_key` → deliberately immutable (class identity; a re-run duplicates through the wizard); `delivery_mode` → deliberately immutable (mode drives collateral/emails/location semantics — recreate rather than mutate); class `timezone` (open classes) → deliberately immutable for now (an edit would silently re-time every session families registered around — flag for a support-level tool if ever needed); school-contact assignment (`counselor_id`) → LEFT without a card edit, noted for Scarlett (rare; the Contacts machinery owns contacts — say the word and it gets a picker here). **Verified live in the browser** (staff session, real clicks): R/W Deep Dive → section expands, cities field present, edit → save "Milan" → the class row updated AND its /c page rendered "(times shown in Milan time)" AND the composer email zone line said Milan — then REVERTED to blank (the cities are your walkthrough call; the surface is ready). ISD (school class) and HGL PSAT (in-person open): section shows their fields but NO cities field; ISD also correctly hides the follow-up/marketing controls. Also fixed in passing: two admin surfaces rendering raw ISO start dates (the class picker option "starts 2026-10-13" → formatDateAdmin, and the dashboard upcoming-classes list).

Original: The field lives only in the class wizard (details step, open-enrollment online classes), and the wizard only CREATES classes — the card's inline edits (location, Synap group, slug, deadline, marketing name) never included cities, so an existing class's cities can't be changed at all (the R/W Deep Dive is stuck saying "Rome time"). Fix it with a cleanup, not another loose row (Scarlett's call): collapse the card's editable class-setup fields into ONE expandable **"Edit class details"** section — collapsed by default so the card stays clean (roster/status/actions prominent), expanding to the wizard-style fields: "Cities shown with the times" (same label + hint as the wizard, open-enrollment online classes only; saving updates `display_cities` and every consumer — page + email labels via publicTimeCityLabel — follows immediately), plus the existing inline edits (marketing page URL, location, Synap group, deadline/cutoff, marketing name) moved inside; registration link + Copy stay visible on the card (daily-use, not setup). Same inline-confirm patterns, no behavior changes to the moved fields; "edit slug" keeps its extra warning. Checkbox-toggles (has diagnostics, follow-up, auto-extend) go inside too — they are setup, not status. While in there, sweep the wizard's other fields for the same trap — anything settable at creation with NO post-creation edit surface gets listed in ship notes with a verdict (add inline edit / deliberately immutable + why; slugs stay immutable-by-rule, minted at creation). Verify: edit the R/W Deep Dive live → its /c page and a composer-path email render the new cities; a school class and an in-person HGL class don't show the field.

## ✅ PL-384 — ONE link model: every code is evergreen; /register suffix; retire point-in-time codes + Marketing page URL (Scarlett, Aug 18)
**Shipped** (migration `20260910000001` APPLIED). **A. Unified:** the four printed codes (isd/mis/nido/sls) converted to their schools\' `evergreen_code` — the STRINGS survive unchanged, verified end-to-end (/isd serves ISD\'s open class; /mis /nido /sls serve their interest-capture states, all 200). One resolver (`app/utils/evergreen.ts`), one validator (reserved paths + school codes + course codes + legacy forwards — the class-shortcode namespace folded in), one admin home. Click history carried over intact: `short_link_clicks` is keyed by code TEXT, so the same codes keep counting straight through (the code page bumps on serve). **Serve-in-place (amendment):** /{code} RENDERS the class page — 200, ZERO redirects, hgl.co/{code} stays in the address bar; the /c page\'s renderer extracted to `app/c/[slug]/view.tsx`, shared by both routes. **CANONICAL = the code URL:** the code view is indexable, carries the class\'s JSON-LD under the /{code} URL, and `<link rel=canonical>` points at itself; /c/{slug} is internal plumbing — ALWAYS noindex, canonical → its code\'s URL when one exists (verified: /c/isd-sat-prep-fall26 renders with robots=noindex + canonical https://…/isd); slugs-never-rewritten unchanged. Sitemap = /{code} URLs + /classes + /team, zero /c/ entries (verified). **B. /register suffix:** /{code}/register serves the SAME class\'s registration form in place (the form extracted to `registration-form.tsx` with an idOrSlug prop); nothing open → the same capture state (verified /isd/register = the ISD form, /mis/register = capture — a register link never strands). Arrival analytics count on serve (the analytics client gains a viaCode signal; legacy ?via still counts). **C. Pin escape hatch:** `evergreen_pin_class_id` on schools + course_meta (plain uuid — an FK here makes every PostgREST classes↔schools embed ambiguous and broke the app in dev; caught, dropped, belt in the migration). Pin honored while the pinned class is open; a stale pin falls back to auto-resolution (both verified live); the panel badges "pinned to {class}" with one-click unpin, and the pin picker only appears when ≥2 classes of that school/course are open. **D. Marketing page URL retired (phase 1):** follow-up "More info" links compose the class\'s OWN page URL (permanent code URL when it resolves, via `preferredClassPath`); the register-page pointer consumes a composed `page_path` (marketing_url no longer rides the public payload — verified); the admin field removed. Phase 2 (next batch): drop `classes.marketing_url` + `classes.short_link` + the `short_links` table after the post-drop grep. **E. Docs/UI:** the Short links panel opens with the model in one sentence; cutover checklist: printed codes need NO registrar action or reprinting, and the registration-handoff step repoints MailerLite/sqsp at hgl.co/{code}/register.

**Convergence audit (every place a link is minted, stored, resolved, redirected, or rendered — verdicts):** ① app/[code] resolver → CONVERGED (evergreen served in place; short_links resolution deleted). ② `short_links` table → RETIRED from every read (resolver, dashboard, panel, API deleted); rows kept as honest history until the phase-2 drop. ③ `short_link_clicks` + `bump_short_link_click` → KEPT, same counter same codes, bumped on code serve. ④ `classes.short_link` (collateral link display + CS {salesPageLink} + notify-interest button) → CONVERGED: all three compose the school/course evergreen link first (`hgl.co/{code}`), stored text only as pre-fold fallback until phase 2; the wizard\'s short-link input rides to phase 2 too. ⑤ PL-349 repoint nudge → RETIRED (dashboard row + deep link + panel controls gone — nothing to repoint). ⑥ /api/admin/shortlinks → DELETED; the evergreen API is the one registry (codes, pins, serving-now, clicks, legacy). ⑦ PL-378 evergreen redirect hop (?via) → CONVERGED to serve-in-place. ⑧ Legacy forwards → LEFT (301 ahead of everything; /act verified 308 → sqsp tutoring). ⑨ /classes browse cards → CONVERGED (permanent code URL when it resolves — verified ISD card links /isd). ⑩ llms.txt → CONVERGED (code URLs for page + register). ⑪ Bare-domain 301s (next.config, PL-349/155b) → LEFT: host-level, still correct (bare hgl.co → brand site fires only post-DNS-cutover). ⑫ PL-361 SR resume-payment links → LEFT: per-enrollment payment links, not class links — correct as-is. ⑬ PL-363 import repointing → checklist step updated to code URLs. ⑭ Email footer portal URL → LEFT (PL-390 owns it, emailBaseUrl-pinned). ⑮ Origin-guard/emailBaseUrl (PL-87) → LEFT: new routes mint absolute URLs only through emailBaseUrl. ⑯ FO tokenized registration links (?fo=…&fe=…) → LEFT DELIBERATELY at /register/{slug}: the discount token is per-recipient and must attach to the exact cohort class, so the direct address is correct (the "More info" link beside it is the code URL). ⑰ Admin class-card registration link + Copy → LEFT at /register/{slug} deliberately: it can never lie about which class it opens during a two-open-classes overlap; the shareable permanent link lives in Short links (and the card keeps only link + Copy — its shortcode clutter is gone with the old panel). ⑱ JSON-LD/OG URLs → CONVERGED to the canonical code URL on code views. No stray resolver survives: the only remaining `marketing_url`/`short_links` references in app code are retirement comments and the phase-2 fallbacks listed above. Gates re-run after the change: regress:links 130/0, smoke:public 11/0, tsc clean.

Original: Scarlett\'s simplification, adopted: a school/course has exactly ONE hgl.co code, evergreen — no point-in-time shortcode layer, no manual repointing, no separate marketing URL. **A. Unify:** the existing class shortcodes (isd/mis/nido/sls — printed, so the STRINGS must survive) become evergreen codes on their schools; the class_shortcode namespace folds into evergreen (one table, one validator; migration converts, honest history kept). Resolution: newest open class of the school/course → its /c page; nothing open → the existing interest-capture page. The PL-349 repoint-nudge machinery retires (nothing to repoint). **B. /register suffix:** `/{code}/register` resolves the SAME class and lands on its registration form; nothing open → the same capture page (a register link never strands). Analytics ?via carries through both forms. **C. Pin escape hatch** for the two-open-classes case (fall still registering when spring opens — "newest open" would steal the link mid-campaign): a per-code optional pin to a specific class, plainly badged "pinned to {class}" in the table, with a one-click unpin; pinned + class closes → falls back to auto-resolution (never a dead link). **D. Marketing page URL retires:** every follow-up email "More info" link + register-page reference that consumed it now uses the class's own /c page (composed, not a field); two-phase drop of the column per standing rule, ship notes list every consumer repointed. The class card keeps registration link + Copy (daily use); its Short-links-related clutter drops. **E. Docs/UI:** Classes → Short links explains the model in one sentence ("one link per school or course — always works, even between classes"); cutover checklist references unchanged printed codes. Verify: /isd resolves ISD's open class then (fixture) capture page when closed; /{code}/register both states; pin honored + auto-fallback on close; zero remaining marketing_page_url consumers post-drop grep; printed codes unchanged end-to-end.

**PL-384 amendment (Scarlett, Aug 18):** (1) **Full link-machinery convergence audit** — the link layers were built across many batches (PL-155b domain pair, PL-349 shortcodes + repoint nudges + bare-domain 301s, PL-361 SR resume links, PL-363 import repointing, PL-378 evergreen/legacy/browse, marketing-page-URL consumers, footer portal URL, campaign/analytics ?via, sitemap/JSON-LD canonicals, origin-guard allowlist): inventory EVERY place a link is minted, stored, resolved, redirected, or rendered; converge on the one model; ship notes = the inventory with each site's verdict (converged/retired/left + why). No stray resolver survives the audit. (2) **Family-visible URLs stay short:** codes should SERVE the landing page in place (rewrite, not redirect) so hgl.co/sls stays in the address bar — no /c/{slug} flash; /{code}/register same treatment. **CANONICAL = THE CODE URL (Scarlett's call — nothing is indexed yet, and a stable per-school/course URL accumulating authority beats fresh per-cohort slugs):** sitemap lists /{code} URLs (+ /classes, /team); the code-served view carries the class's JSON-LD; /c/{slug} survives ONLY as the internal unique address a class needs when it isn't the one its code currently shows (sibling sections, pinned overlaps, past classes) — those views are noindex with a canonical pointing at their code's URL (or plain noindex when the class has no code); slugs-never-rewritten rule unchanged. The interest-capture state serves at the same /{code} URL (one URL, honest states). Dropping the /c prefix entirely stays declined — it's internal plumbing families and Google now never see.

**PL-384 note:** /classes browse cards (and any other internal link surface the audit finds — emails, campaign links, admin copy-link buttons) link to the CODE URL when the class's school/course has one, falling back to /c/{slug} only for codeless classes — shared/bookmarked links from anywhere should be the permanent address.

## ✅ PL-385 — Auto-updating "Upcoming classes" panel for the sqsp homepage, fed by the portal (Scarlett, Aug 18)
**Shipped.** **Implementation (Code\'s call): script-injected markup, not an iframe** — one paste-once snippet (a `<div id="hgl-upcoming-classes">` with a noscript fallback link + one `<script defer>`), served by the portal at `/embed/upcoming-classes.js`; the script injects fully self-contained inline-styled cards, so nothing clashes with sqsp CSS and every future change ships portal-side with zero sqsp edits (an iframe would have needed auto-height postMessage plumbing for no benefit). **Cards compose entirely from the record:** school logo (HGL color logo for no-school classes per the PL-375 rule), class name, plain-English date range (the PL-380 `formatDateRange`), and a one-line blurb = delivery + city (via `publicTimeCityLabel`, PL-382 rules incl. the HGL-home fallback) + session count + duration in weeks. **Honesty note: the record has NO exam-date field, so the blurb claims no exam date** — if you want "prepares for the October 3 SAT" on the strip, that needs a real wizard field first (say the word). "More info" → the class\'s PERMANENT code URL (PL-384 preference, `preferredClassPath`). Open/close auto-reflects (edge-cached 5 minutes, `s-maxage=300 stale-while-revalidate`); nothing open → a modest "join the interest list →" line pointing at /classes; script blocked → the snippet\'s own noscript link. No cookies, no beacons into the sqsp page — analytics stay portal-side and count when families arrive. **Verified in a real sqsp-like test page** (Georgia-serif host styles, served at `/sqsp-embed-test.html` — kept deliberately as your preview page): 4 open-class cards rendered with date ranges, cities ("Cape Town and Düsseldorf"), and More-info links; mobile 375px = zero horizontal overflow, cards stack; the none-open state verified via `?preview=empty` (renders the interest line). **Checklist updated:** step 9b documents the exact snippet + where it goes + the preview URLs; step 11 gains the downgrade gate — the chosen sqsp plan must keep code/embed blocks (Business tier or equivalent, blank to record the decision), with the honest no-embed fallback spelled out (plain button to /classes, never a fake strip).

Original: The brand-site homepage\'s manually-maintained Upcoming classes strip stays (it's browse-appeal — "oh cool, they teach in Chile" — not primary discovery) but should feed from the portal. **A.** A self-contained embed the sqsp page includes via a code block: portal-served, renders the open classes as cards matching the current strip (school logo — HGL logo for no-school classes per PL-375 —, class name, plain-English date range, a one-line composed blurb: campus/city + duration + the exam date it prepares for, ALL from the record, never hand-typed), each "More info" → the class's permanent code URL (PL-384 preference). Auto-reflects open/close; nothing open → the section renders a modest "join the interest list" line pointing at /classes, never an empty hole. Cached + fast (it's on the homepage), no cookies/analytics beacons into the sqsp page (PL-350 rules apply portal-side only), graceful if the script is blocked (link to /classes as noscript/fallback). **B.** Implementation is Code's call (script-injected markup vs iframe w/ auto-height), but the embed snippet must be paste-once: all future changes ship portal-side with no sqsp re-edit. Document the snippet + where it goes in the cutover checklist. **C.** Checklist addition: the post-cutover Squarespace DOWNGRADE target plan must still allow code/embed blocks (Business tier or equivalent) — record the chosen plan; if Scarlett ends up on a plan without embeds, the honest fallback is a plain "See upcoming classes →" button to /classes (note it, don't fake it). Verify: embed rendered inside a real sqsp page (test page is fine) across open/none-open states + mobile width; class opened/closed in the portal reflects without touching sqsp.

## ✅ PL-386 — Family/contact records: staff-editable everywhere a fact shows (Scarlett, Aug 18)
**Shipped** (migration `20260910000002` APPLIED — contact-edit audit columns). The family profile\'s facts are now editable exactly where they show, through one inline `FactEdit` affordance + one API (`/api/admin/family-facts`): **parent** first/last name, email, phone (Household card); **student** email, phone, pronouns (the four-option machinery), grade, learning notes (student card — which now shows pronouns/grade/notes even when blank, so there\'s always a surface); **Who do I contact?** panel — preferred contact method, the arrival instruction (who + how), and the emergency contact (name/relation/phone). **Permissions by construction:** the API admits admin|manager only (`sessionRole`), and instructors/tutors never reach /admin at all — their portal views stay view-only; the manager keeps the existing no-dollars lens (contact facts editable, billing address visible per current rules — unchanged). **Email edits are load-bearing:** the editor shows the instructor-panel-style plain warning ("this email is how the family signs in AND where every email goes; a typo locks them out"), the save records who/when (`contact_updated_by/at`), and **duplicates refuse honestly** — editing to an email another family uses returns "already belongs to {name}\'s family — two families can\'t share a sign-in email" with a link to that family, never a silent merge. **Emergency/arrival/preference live on the freshest intake on file** (edited in place on that lead\'s intake); a family with NO intake shows the honest "no intake on file — these answers get captured at intake" and no phantom edit surfaces (deliberate — noted, not faked). **The arrival banner names the student:** "If the student hasn\'t arrived: text Roman." — the intake\'s own student first, the household\'s single student as fallback, generic only when neither exists; the leads page\'s intake card gets the same treatment (`intake.studentFirst`). **Verified live in the browser:** Fakey\'s family — 10 edit affordances, parent phone round-trip (saved, shown, reverted), the duplicate-email attempt (→ Janeson\'s family\'s email) showed the warning, REFUSED with the plain-English message + link, and left the original email untouched; the no-intake family shows the honest empty state; Roman\'s family (intake on file) renders "text Roman." with preference/arrival/emergency all editable. (One hooks-order crash caught and fixed in dev before commit.)

Original: Contacts → family pages show contact facts with almost no edit surfaces: the parent card exposes ONLY billing-address edit (email/phone read-only), the student card shows email/phone/learning notes with no edit at all, and the "Who do I contact?" panel shows the parent's phone that the Household card has no way to add/change. Make the family profile's facts staff-editable in place (inline edits per the existing card patterns): parent name/email/phone/contact-preference, student email/phone/pronouns/grade/learning notes, emergency contact, the who-to-contact arrival instruction. **Permissions:** admin/ops staff only — instructors/tutors do NOT get edit surfaces (view stays); families never edit billing through THEIR portal (unchanged); the manager role follows its existing no-dollars lens (billing address stays manager-visible per current rules, contact facts editable). **Email edits are load-bearing:** parent email = portal sign-in + where everything sends — reuse the existing instructors-panel email-edit warning pattern (plain-English inline warning + confirm, recorded who/when); dedupe honestly (editing to an email another family uses → refuse with a link to that family, never silent merge). **Also (small):** the "Who do I contact?" arrival banner says "text the student" — render the student's NAME ("text Janeson"), pronoun-correct via the existing machinery, everywhere that instruction composes. Verify: each field round-trips live; instructor sim sees no edit affordances; email-edit warning + dedupe refusal E2E; banner shows the name.

## ✅ PL-387 — Edit an existing student's tutoring schedule: form AND calendar (Scarlett, Aug 18)
**Shipped.** **A. Form path:** every active engagement on Tutoring → Students gains **"edit schedule"** — an inline editor with the New-Student-Schedule fields (weekly slots: day/time/duration, add/remove; tutor picker) pre-filled with the current pattern. **Decided: regenerate folds INTO save** — the engagement API already supported update+regenerate atomically, so one Save updates the pattern AND re-projects future unbilled sessions (the manage row's separate Regenerate button stays for repair after pauses); the regenerate note finally points at something. Saving follows the change rules: future unbilled sessions re-project, completed/billed sessions untouched, Google sync follows, and — new plumbing — **the family and tutor BOTH hear a pattern change** (`sendPatternChangeNotices`: the family through the same T3_SCHEDULE_CHANGE vehicle single-session moves use, phrased from `scheduleSummaryText` — the ONE weekly-plan composer — with the untouched-sessions promise spelled out; the tutor a direct staff-footer note since their calendar is already synced; nothing sends when a save produces zero deltas). Tutor Google-busy conflicts warn like creation (freebusy over 4 weeks, per-slot amber warnings, never blocks). The API gains `tutor_id` on update. **Also: the panel's two NATIVE `window.confirm`s (overdraw + no-location walk-pasts) converted to inline banners** — the no-native-dialogs rule finally reaches them. **B. Calendar path:** Current Student Schedules chips (proposed/confirmed, week mode) drag to move — vertical for time, cross-day via the column under the pointer — and resize from the bottom edge, quarter-hour snapped with a live echo, Escape abandons. Dropping opens an INLINE decision panel (never a native dialog): old → new time, then **"Just this session"** (THE PL-180 rescheduleSession path — fee rules, family + tutor notices, gcal; the panel surfaces the 24-hour reserved-time consequence in plain English when the original time is inside the window) vs **"This and future weekly slots"** (offered only when the dragged session matches the engagement\'s recurring pattern — the matched slot moves and the same update+regenerate+notices path runs) vs Cancel — nothing moves. Busy/session overlaps at the drop target warn in the panel (creation parity, warn-not-block). Instructors/tutors get no drag surface (the grid is admin-only by construction). **Hardening found along the way:** `generateOccurrences` now dedupes identical duplicate recurrence rows at the source (Fakey\'s live `[Mon 16:00 ×2]` was double-booking every projected week through EVERY consumer — materialize, continuation reserve, previews). **Verified E2E in the browser:** Janeson\'s editor opened pre-filled (Mon 13:00 / Tue 11:00), Tue → 11:30 saved → the stored recurrence updated, future Tuesday sessions re-projected to 11:30 with Monday untouched, and the tutor pattern-change notice delivered to billy@ (family send dev-guard-suppressed as designed) — then reverted; drag: a confirmed chip dragged 2 hours → the decision panel appeared with both options and correctly NO fee note (>24h out), Cancel left everything untouched; tsc + client-imports clean.

Original: Today an existing engagement's schedule can't be edited at all from the admin: Tutoring → students' "manage" offers regenerate/pause/end — and regenerate's note says "use after editing" with no edit surface anywhere; Scheduling's "New Student Schedule" is creation-only and Current Student Schedules doesn't allow dragging. Provide BOTH paths, mirroring creation: **A. Form path:** an "Edit schedule" on the engagement (same fields as New Student Schedule — days/times/duration/tutor — prefilled with the current pattern); saving follows the established change rules: future sessions re-project, past/completed sessions untouched, the PL-314-style baseline + family comms per the existing schedule-change machinery, Google sync updates, invoices/timecards only affected where sessions actually moved (the regenerate note finally has something to point at — wire the flow so edit→regenerate is one guided motion, or fold regenerate into save if that's the honest mechanic; decide + note). **B. Calendar path:** in Current Student Schedules, drag a session to move it / resize duration (single-session move = the existing reschedule mechanics incl. the 24h fee rules where applicable — surface the SAME plain-English consequences inline before confirming, inline confirm not native dialog); offer "just this session" vs "this and future weekly slots" where the drag matches the recurring pattern. Tutor Google-busy conflicts warn like creation does. Instructors/tutors don't get drag-edit (view + their existing self-serve rules unchanged). Verify E2E: form edit re-projects future only; drag single + drag recurring; fee-window drag shows the fee consequence; gcal + timecard + invoice convergence on a moved session; pause/end unaffected.

## ✅ PL-388 — Scheduling calendar: sessions render twice (greyed twin underneath) (Scarlett, Aug 18)
**Shipped — the suspected cause VERIFIED as the real one.** The Google-busy feed (`/api/gcal/freebusy`, which shades the engagement wizard, the scheduling grid, and the admin calendar — the all-tutors·day view reads the same feed) returned the tutor\'s gcal events INCLUDING the events the portal itself synced for these same sessions — confirmed live: Billy\'s September window held 8 busy blocks of which 6 were the portal\'s own "Tutoring: {student} — {subject}" copies, so every session rendered as itself plus its gray Google echo (exactly the screenshot: "Tutoring: Janeson — AP/IB Calculus" greyed behind the solid block). **Fix:** ONE predicate in gcal.ts, `isPortalSyncedTutoringTitle()` (the `Tutoring:` family incl. HOLD:/XCL- prefixes — the same convention PL-159\'s HOLD-only dedupe already trusted, now widened), filters the busy feed. **Deliberately KEPT busy:** instructor class sessions ("HGL PSAT Prep — class session") — real teaching time with no tutoring-grid row behind them — and private/untitled events (could be anything external). Verified against the live feed (2 blocks survive: the class session + a private all-day event; 0 tutoring echoes) and in the browser scheduling grid: Janeson renders exactly once per session, external busy still shades, no gray twins. (Verified in the Browser pane rather than desktop Chrome — the batch-36 real-Chrome lesson was specifically about IntersectionObserver, which plays no part in this DOM check.) **Also observed in passing:** Fakey\'s EXISTING session rows are stored doubled (two rows per instant since Jul 28) — the historical product of the duplicated recurrence PL-389 found; test data, left as-is, the PL-389 dedupe stops new ones.

Original: Symptom (Scarlett's screenshot, Billy's tutor-week): each portal session block has the same session's text greyed behind it ("1:00–2:00 PM Tutoring: Janeson — AP/IB Calculus" under the solid block). Likely cause TO VERIFY, not assume: the Google-busy shading renders the tutor's gcal events — including the events the PORTAL ITSELF synced for these same sessions — so every session shows as itself + its own Google echo. Fix accordingly: busy shading excludes portal-synced events (they carry identifiable markers/ids per gcal-sync), so gray means "busy with something OTHER than portal tutoring"; if investigation finds a different cause (double-fetch, overlapping layers, duplicate rows from repeated emails — Scarlett's alternate hypothesis), fix the real one and say so in ship notes. Check the all-tutors·day view for the same artifact. Verify in real Chrome (the embedded-preview lesson): one block per session, real external gcal busy still shades, portal echo gone.

## ✅ PL-389 — Continue-tutoring "needs scheduling": false conflicts + a dead-end alert (Scarlett, Aug 18)
**Shipped.** **A — investigated FIRST, empirically:** reconstructed the exact 18:28 failure (your Faker test this morning) by replaying `reserveContinuation`\'s steps against the live data. Findings, honestly stated: (1) the reservation check\'s Google veto used blunt `freeBusy` with NO portal-echo exclusion — the window demonstrably contained five portal-synced events (Janeson ×4, Roman ×1, plus a class session), the PL-388 echo load-bearing exactly as suspected, while veto 1 (portal rows) already covers portal sessions precisely WITH the own-engagement exclusion the Google copy can\'t honor; (2) the checker DOES exclude the engagement\'s own portal rows and the window starts after the block\'s final session (both alternate hypotheses checked); (3) **the specific 18:28 clash could not be reproduced post-hoc** — the identical decision re-run four hours later reserved cleanly (Google freebusy keeps no deleted-event history, so whatever busy block it reported at 18:28 has since changed); the ship-honest conclusion is that the echo failure CLASS is real and now removed, and the next occurrence self-diagnoses because the alert names its exact cause; (4) **a real adjacent defect surfaced:** Fakey\'s recurrence stores the same slot twice (`[Mon 16:00 ×2]`) — the pre-fix re-run reserved "5 sessions" as 3 weeks with two double-booked; fixed at the ONE generator (PL-387 note) and E2E\'d (post-fix re-run reserves 5 DISTINCT weeks; a genuinely conflicting portal session at the recurring slot routes to staff; state restored). The Google veto now uses titled `listBusyEvents` minus portal-tutoring echoes (class sessions and private events still veto). **The alert names WHICH slot conflicts with WHAT** — "a conflict or no workable recurring time" covered four different failures; the email now says exactly one of: no recurring pattern on file / only N slots where M needed / "Monday, September 7, 4:00–5:00 PM conflicts with {Janeson\'s session | \"Dentist\" on Billy\'s calendar | a private event}" per collision / the save failed (likely transient). **B — the alert lands on a resolution surface:** the alert email and the Needs Attention row both deep-link `/admin/tutoring?continue={engagement}&hours={choice}` → the Students section opens with PL-387\'s edit-schedule form PRE-FILLED (current pattern) under a banner naming the confirmed continuation ("Scheduling the continuation the family confirmed (5 more hours)…"); saving creates the sessions and the row self-clears (the existing state-driven condition: a future session created after the routing stamp). Verified in the browser: the deep link opens Fakey\'s pre-filled editor with the banner (the duplicate 16:00 row plainly visible for deletion right there).

Original: Two connected defects around the BL continuation flow: **A. Investigate the false conflict FIRST.** Faker confirmed 5 more hours; the alert claims "a conflict or no workable recurring time" — with nothing but test data on the calendar, that's suspicious. Prime suspect (verify, don't assume): the continuation reservation check counts the tutor's Google-busy blocks INCLUDING the portal's own synced sessions (the PL-388 echo, load-bearing this time) — the continuing slots are the SAME recurring times the student already held, so the reservation "conflicts" with the outgoing sessions' own gcal events or their portal rows (also check: does the reservation window overlap the block's final sessions, and does the checker exclude the engagement's OWN sessions?). Fix the real cause; ship notes say which it was. The alert copy should also say WHICH slot conflicted with WHAT ("Tuesdays 1:00–2:00 conflicts with Janeson's session") — "a conflict or no workable recurring time" is undiagnosable. **B. The alert must land on a resolution surface** (standing rule; today "Open the engagement" offers regenerate/pause/end — nothing schedules a continuation): once PL-387's edit-schedule form exists, the engagement page gets a "Schedule the continuation" action pre-filled with the continuation context (hours confirmed, prior recurring pattern, suggested next slots), and THIS alert deep-links straight into it. Completing it clears the Needs Attention row (state-driven). Verify E2E: continuation with the same recurring slot free of REAL external conflicts reserves cleanly (no false conflict); genuinely conflicted continuation → alert names the specific collision → link lands in the pre-filled scheduler → completing clears the row.

## ✅ PL-390 — Staff/admin email footer: wrong sign-in line (Scarlett, Aug 18)
**Shipped.** **ONE staff footer source:** `footerStaff()` in email.ts (same address + reply lines as the family footer) with the staff pointer — "Staff sign-in is always at {portal}/login — use your @highergroundlearning.com Google account." (the real staff surface: /login's Google SSO; URL from emailBaseUrl so the domain cutover updates it in one place, footer-URL-pinned rule intact). **Audience audit, every sender:** ADMIN — sendAdminAlert's code twin + every AL_* registry render (incl. the roster report and sweep-watch) → staff footer; TUTORS (confirmed all on @highergroundlearning.com) — T5 timecard, T6 notes (EOD/weekly/nudge), SUB_* coverage, T3_TUTOR_NOTICE twins + registry → staff footer; INSTRUCTORS — IN_WELCOME/DIGEST/FYI/MIN_DECISION + ADMIN_INSTRUCTOR_NUDGE → staff footer; **COUNSELORS — decided: keep the family-style pointer** (they sign in at /portal with just their school email, exactly as the line says — CS_*/CD_*/CR_* unchanged); FAMILIES — family footer verbatim, untouched (IM_INSTRUCTOR_MESSAGE recipients are families → family footer; the instructor's own FYI copy is IN_FYI → staff). Registry plumbing: `staffAudienceTemplate(key)` (AL_/IN_/SUB_ prefixes + the named tutor keys) drives the footer choice inside renderVersion, so live template renders and code twins can't disagree. **Verified** (`scripts/verify-pl390-footers.mjs`, 10/10): fresh composer renders per audience — T5 + AL_ROSTER_REPORT carry the staff line and NOT the family line, E0 family footer verbatim, CS counselor keeps the family pointer — one send per audience → billy+pl390-{tutor,admin,family}@; grep gate: zero staff-audience senders reference the family footer.

Original: Admin/staff-audience emails (roster report, Needs Attention alerts, timecards, digests) carry the FAMILY footer: "Your Higher Ground portal is always at hgl-portal.vercel.app/portal — sign in with just this email address, no password needed." Staff don't sign in there. Replace on every staff/admin-audience email with the staff line: sign in with your @highergroundlearning.com email (link the staff sign-in URL; wording plain, e.g. "Sign in to the admin at {url} with your @highergroundlearning.com email"). ONE staff footer source (like the family footer's), so the domain cutover updates it in one place (footer-URL-pinned rule applies). Audit every sender for audience: family emails keep the family footer verbatim; tutor-audience emails (timecard) get the staff line IF tutors sign in with org addresses (they do — confirmed all tutors have @highergroundlearning.com); mixed/counselor audiences decided explicitly and noted. Verify: fresh renders of one email per audience → billy@, correct footer each; grep: no staff-audience template/twin renders the family sign-in line.

## ✅ PL-391 — VFAQ email: orphaned bridge line when the gated FAQs drop (Scarlett, Aug 18, seen in a PL-379 test render)
**Shipped.** The bridge is now the composed `{vfaqBridgeLine}` — it renders only when a gated Q&A (diagnostics or strategy) actually follows it, i.e. when the "regular FAQs" section holds ≥2 entries; on the Deep-Dive shape (neither) it composes away and the miss-class FAQ flows without an intro. E3_VFAQ **v5 published** under an exact anchor on the hand-written line; twin (faqEmail) carries the identical condition; seed in lockstep. **All four gating combinations rendered via the composer path** (both / diagnostics-only / strategy-only / neither — 26/26 asserts in `scripts/verify-pl391-392-vfaq.mjs`): bridge present exactly when content follows, twin agrees in every shape, no orphaned intros, no unresolved variables, remaining FAQ flows clean. Test send of the no-diagnostics shape → billy+pl391-vfaq-deepdive@.

Original: On a no-diagnostics/no-strategy class (Deep Dive), the VFAQ email's gated Q&As correctly compose away — but the bridge line "Are you still here? You are? Okay, here are a few regular FAQs, just for you:" still renders, followed by just ONE remaining FAQ, so the joke sets up a list that isn't there. Make the bridge compositional like everything else: render it ONLY when the section it introduces actually has enough content to warrant it (e.g. ≥2 following FAQs AND at least one earlier gated section rendered — the line's humor depends on there having been a wall of FAQs before it); otherwise drop the line and let the remaining FAQ(s) flow without it. While in there, check the email's other connective tissue survives every gating combination (diagnostics-only, strategy-only, neither, both) — no orphaned intros, no "a few" preceding one item, counts/plurals honest in all four shapes. Registry rule (new version, twin+seed lockstep); composer-path renders of all four combinations + test send → billy@ of the no-diagnostics shape.

## ✅ PL-392 — Buttons never render mid-sentence (Scarlett, Aug 18, seen in a PL-379 test render)
**Shipped.** **The named offender:** `{vfaqDiagnosticQa}` recast — "No problem — the test is right here, due Friday, September 4, 2026, the day before your first class." with the button on its OWN line; the faqEmail twin matches (its spam-folder aside stays as a following sentence in the same paragraph). **Sweep results (every template seed + code twin + live registry body + composed variable):** live bodies = ZERO offenders (a python sweep of all 94 active bodies found no non-standalone button line — T8's live v5 already had the clean shape); seeds = ONE offender, T8_WELCOME_HANDOFF's "[button:Add to your calendar] — subscribe once…" trailing clause, seed realigned to the live v5 shape (button alone, explanation as its own following line); code twins = clean (the faqEmail fragments were the only prose case, fixed with PL-391; a fragment-tell scan of every `button()` call site found only false-positive code assignments); composed variables = `{lrDiagnosticSection}`, `{diagnosticPsE4Block}`/`E5` and the recast `{vfaqDiagnosticQa}` all carry standalone `\n\n[button:` lines. **Renderer enforcement (decided YES):** comms-md now treats `[button:]` as ALWAYS block-level — a button embedded in a paragraph is pulled onto its own CTA line in source order and the surrounding fragments render as their own visible paragraphs, so broken copy LOOKS broken in every composer-path render instead of hiding an inline button (asserted with a synthetic mid-sentence button). Verified in the PL-391 harness (26/26, all four gating shapes assert zero mid-sentence buttons in registry AND twin renders) + the same test send → billy@.

Original: The VFAQ diagnostic Q&A composes its [button:Take the diagnostic test] INSIDE the sentence ("…right here: [BUTTON]. It's due Friday…") — the button renders inline with sentence fragments hanging off both sides. Fix that block: recast so the prose reads whole ("No problem — the test is right here, due Friday, September 4, 2026, the day before your first class.") with the button on its OWN line after the sentence. Then SWEEP: grep every template/twin/seed + composed variables for [button:…] markup embedded mid-sentence (punctuation immediately after the button tag is the tell) and give each the same treatment — buttons are always their own block-level line; ship notes list each occurrence fixed or confirmed-already-clean. Consider enforcing at the renderer level: the email markdown renderer could treat [button:] as block-level always (surrounding sentence fragments would then LOOK broken in composer-path renders and get caught) — Code's call, note the decision. Registry rule for touched live templates; composer-path renders + one test send → billy@.

## ✅ PL-393 — Calendar-drift banner: the past-session case shows nothing (Scarlett, Aug 18)
**Shipped.** **Diagnosis (exact):** two filters conspired to make past drift vanish silently — the drift audit scoped to `starts_at > now()` (future sessions only), AND the nightly auto-complete flips past confirmed sessions to `completed`, which the audit\'s `status in (confirmed, proposed)` filter also skipped; `syncTutoringDriftTable` then DELETED every row the audit no longer returned. So the moment a drifted session\'s time passed, the row was cleared and the banner had nothing to show — exactly what you hit. **Fix — the row never vanishes:** the audit now scans 14 days into the past and includes `completed` sessions, so a drift persists until a human resolves it. **Past-appropriate resolutions:** once the portal time has passed, the banner row switches to past-tense copy ("…moved this session in their Google Calendar, and its time has now passed. Record what actually happened…") with four honest choices: **"It happened at the moved time"** (adopt-as-happened — a completed session can\'t run the future-facing reschedule machinery, so the record moves directly to the time it actually ran; open timecards/invoices read from the corrected session as always), **"Mark no-show"** and **"Didn\'t happen — forfeit"** (the same mechanics as the session row\'s own cancel actions; reserved-time pay rules unchanged per T5, said inline), and **"Keep the portal time"** (revert — the Google event patches back, the record stands as scheduled). Future-session adopt/revert unchanged (PL-180). **Alert copy:** now names the student\'s page ("the banner on {Student}\'s tutoring page") and states plainly that the banner STAYS until decided — switching to what-actually-happened questions if the time passes first (the fix is persistence, not a time-sensitivity warning, per the doc\'s rule). **Verified E2E with real Google state** (4/4, state fully restored): moved a past COMPLETED session\'s gcal event +1h by hand → the audit detected AND persisted the drift (row present with the moved time), resolving as no-show cleared the row for good, nothing auto-vanished; the before-session adopt/revert path is untouched PL-180 machinery.

Original: The adopt/revert banner EXISTS (tutoring page, drift-banner.tsx) — but Scarlett clicked the alert's link after the moved session's time had passed and saw NO banner and no explanation, leaving the drift unresolvable and the alert a dead end. Diagnose exactly what happens to a drift row once the session start passes (silently filtered? cleared? still pending but unrendered?) and fix so it NEVER vanishes silently: a past-session drift stays visible with honest copy ("Billy moved this session in Google, and its time has now passed — record what actually happened") and past-appropriate resolutions: adopt-as-happened (session at the moved time: timecard/billing per the normal late-reschedule/reserved-time rules) or record it per what actually occurred (no-show/cancelled mechanics) — exact options per the existing session-state machinery, plain-English consequences inline. The email alert should also say the banner is time-sensitive is NOT acceptable as the fix — the fix is the banner surviving; but the alert copy may state plainly where the banner lives ("on Roman's tutoring page" naming the student, not "the tutoring page"). Verify E2E: drift + click before session → adopt + revert both work; drift + session passes unresolved → banner persists with past-tense options, resolving clears alert row; nothing auto-vanishes.

## ✅ PL-394 — Schedule-change email: "What changed" comes FIRST (Scarlett, Aug 18)
**Shipped.** **Audit of every recap variant with a what-changed block:** T3_TUTOR_NOTICE (tutor recap) was the ONLY offender — its live v2 led with `{tutorScheduleBlock}` and buried `{tutorChangeBlock}` underneath; the family editions already lead with their changes (T3_SCHEDULE_CHANGE opens "Here\'s what changed for {studentFirstName}:" + `{changeListBlock}`; SU_SCHEDULE_UPDATE opens with `{changesBlock}`; T3_RESCHEDULE_ACK has no what-changed block — it\'s a single-move acknowledgment) — all three left as-is. **T3_TUTOR_NOTICE v3 published** under an exact anchor: `{tutorChangeBlock}` right after the heading, then the glance line adjusted to bridge into the reference list ("Your Google Calendar is already updated — below is the full upcoming schedule for reference, worth a quick glance even if you live in your calendar."), then `{tutorScheduleBlock}`. Twin (tutor-notices.ts) + seed in lockstep, same order and same adjusted line. Composer-path render asserts change → glance → schedule order (and the PL-390 staff footer riding along); test send → billy+pl394-tutor-recap@.

Original: The tutor schedule-change recap currently leads with the full upcoming-sessions list and buries "What changed:" underneath — but the change IS the point of the email. Reorder: what-changed block first (right after the greeting/heading), then the full upcoming list as the reference below (its "worth a quick glance" line adjusting to the new order). Applies to every variant of the schedule-change recap (tutor AND family editions if the family one shares the shape — audit both; same reorder wherever a what-changed block exists, ship notes list which templates moved). Registry rule (new versions, twins+seeds lockstep); composer-path render + test send → billy@.
