# Portal fixes — batch 51 (CLOSED for hand-off Sep 21, 2026 — DNS-cutover batch, 8 items, PL-470…477)

**Standing rules:** all prior, incl. the PL-460 CTA rule.

(Opened + closed Sep 21, 2026. Next PL: PL-478.)

**ORDER: PL-471 FIRST (real instructor emails went out today — stop the class of bug), then PL-470 + PL-472 (the DNS flip waits on them), then PL-474, PL-476, PL-473, PL-477, PL-475 (held until Scarlett confirms Vercel Pro).** Note each group in the header as it reaches prod.

**Go-live state (Sep 21):** purge applied; SLS / MIS / Leone XIII / ASF rosters silently imported (50 muted enrollments, 61 families, 0 sends); real instructors assigned Sep 21 (Gwen / Rebecca / Kevin / Kevin) with zero sends and zero calendar writes at assignment time; Leone + ASF evergreen codes set. DNS flip is the next public step.

## PL-470 — A class that is RUNNING with registration closed gets a useful page, not a dead-end card (found in the DNS pre-flight, Sep 21)

### ✅ SHIPPED (Sep 21) — with PL-472; E2E-verified on the dev server at 375px with a self-cleaning QA school + class (registration closed 3 days ago, one session behind, three ahead), then on the PROD build in the gate battery

- **The state.** `view.tsx` derives `inProgress = closed && !cancelled && today ≤ last session` (class-local). In that window `/{code}` and `/c/{slug}` render the class page, not the card: hero + bullets + blurb as usual; the price / Register button / "closes in N days" line replaced by a calm banner *"This class is under way — registration closed {date}."* + *"Next session: Wed, Sep 23, 6:30–8:30 PM Milan time · QA470 campus · Room 12"* (`data-testid="in-progress-banner"` / `next-session-line`; venue · room per PL-468, or "Live online"); the FULL session calendar with past sessions greyed (`data-session-state="past"`, 50% opacity) and the next one highlighted (blue rule, "Next" in place of the number — `data-session-state="next"`); the existing "Add the schedule to your calendar →" link (Google / Apple / .ics on `/classes/{id}/calendar`); a quiet *"Already enrolled? Sign in for your class link and diagnostic tests →"* (`/login`, `data-testid="enrolled-signin"`). NO meeting link and NO Synap link on the public page. Hidden in this state: the full-class banner, FAQ section, closing "Ready to get started?" CTA, the fine print (minimum-enrollment line), every `data-track="register"` control. The consultation CTA moves to the bottom as the secondary "Missed this one?" block (`data-testid="in-progress-closing"`) with the interest-list capture beside it. The hero subtitle now says "started {date}" and — a PL-468 gap caught here — names **venue · room** ("In person · QA470 campus · Room 12"), not the bare room.
- **After the last session:** the evergreen resolver (`evergreen.ts` `resolveClass` + the pin) skips classes whose last session has passed (`classEnded()`), so `/{code}` falls through to the PL-472 interest-list state — "open" is a registration status, not a lifecycle one. The internal `/c/{slug}` address of an ended class still renders the closed card (now with the capture + contextual consult door). Verified: moving the fixture's sessions into the past flipped `/qa470` to *"No upcoming class at QA470 right now, but we'll likely have one set up soon."* on the next request. Side effect stated: `preferredClassPath()` returns `/c/{slug}` for an ended class (its code no longer serves it) — correct for the /classes "Recent classes" links PL-476 builds.
- **`/{code}/register` while closed** keeps its closed notice + interest capture and now links *"See the class page — schedule, venue and calendar →"* (`data-testid="back-to-class-page"`, from class-info's `page_path` = `/{code}`). Verified at 375px.
- **Records-only classes** behave identically — the page is public information, not a send (SLS / Leone / MIS / ASF all render in-progress pages right now on the code URLs).
- **375px:** no horizontal overflow (measured 0 px); the banner wraps cleanly. **JSON-LD:** `offers.availability` = `SoldOut` while closed (validThrough = the close date), `CourseInstance.eventStatus` = `EventScheduled`. Metadata stays **noindex** for the closed/in-progress window (PL-359's rule — a temporary state, canonical unchanged); stated, not changed.
- **Scarlett's addition — the card always offers an action.** `ClassStateCard` gained `consultHref` (context-carrying: `/inquire?source=class-page:{code|slug}&interest={classType}&school={nickname}` via `consultHrefFor()`) and `interest` (the inline `InterestCapture` → `class_interest` for that school, `data-testid="state-interest-capture"`). Every closed / in-progress / cancelled render of a SCHOOL class carries both; open-enrollment (no-school) classes keep the consult door only (the capture keys on a school — honest limit, said here). `/inquire` reads `?source|src`, `?interest`, `?school` and pre-fills the school + "What would you like help with?" (PL-473 stores the tags). E2E: submitted the capture on the in-progress page → `class_interest` row `billy+pl470@… · QA Student · public_form · SAT Prep · QA470` (cleaned).
- **CTA registry:** `PUBLIC_STATE_LANDINGS` in `cta-landings.ts` (sign-in · consult · calendar · interest API · closed-register back-link) — `regress:cta-landings` checks file + control + params (215 → 228). **Gate:** `smoke:public` builds its own in-progress fixture (QA school `qasmoke` + class): 11 → 29 checks — both addresses render the banner not the card, next highlighted, past greyed, no register/price/FAQ, sign-in + calendar + consult + capture present, JSON-LD SoldOut/EventScheduled, `/{code}/register` still serves the route, 375px overflow 0, the back-link on the closed notice.
Pre-flight of every hgl.co path on the portal host: `/mis`, `/sls`, `/asf`, `/leone` each render only a card — "Registration for this class has closed … If you missed it, talk to us" + two buttons. Today those same URLs (hgl.co → Squarespace) show the full class page: the session calendar, times, online/in-person, what's included, Download Calendar. The people who open `hgl.co/sls` during a running class are overwhelmingly ENROLLED families and students (printed flyer, the school's newsletter, a MailerLite email) looking for "when is the next session?" — the closed card treats them as someone who missed out. After the DNS flip that is a visible downgrade for four live classes.
- **From registration-close until the last session ends**, `/{code}` (and `/c/{slug}`) renders the class page in an "in progress" state: the hero + facts as usual, a calm banner "This class is under way — registration closed {date}", the FULL session calendar with past sessions greyed and the next one highlighted ("Next session: Wed Sep 23, 18:30–20:30 Milan time"), venue/room per PL-468 or "Live online", Add-to-calendar / calendar download, and for enrolled families a quiet "Already enrolled? Sign in for your class link and diagnostic tests →" (/login). NO meeting link and NO Synap link on the public page. The "missed it?" consultation CTA moves to the bottom, secondary.
- **After the last session:** today's honest no-active-class / interest-list state for that school (unchanged).
- `/{code}/register` while closed keeps its closed message but links back to the in-progress page.
- Records-only classes (all enrollments comms-muted) behave identically — the page is public information, not a send.
- Works at 375px; selling-only sections (promo, "only N seats", register buttons, FAQ about signing up) are hidden in this state; JSON-LD `eventStatus`/offers availability reflect closed registration.
- CTA registry: register the new state's links. Gate: extend `smoke:public` with one in-progress class.
- **Scarlett, Sep 21 — the closed card must always offer an action:** keep "Talk to us — free consultation" (→ /inquire, school + class pre-filled as context) on every closed / in-progress / no-class state, and add the interest-list capture inline (email + optional student name → `class_interest` for that school) so a visitor who missed this class can ask to hear about the next one without leaving the page.

## PL-471 — Records-only / finished classes: the REST of the class-keyed machinery goes quiet too (found Sep 21, real emails went out)

### ✅ SHIPPED (Sep 21) — commit below; pushed; on prod the moment Vercel reports Ready (noted in the header)

**The class of bug:** three audits each followed ONE key — PL-457 followed enrollments (sendOnce's mute gate), PL-463 followed the instructor object (`loadClassInstructor`), and the timecard sweep follows neither: it selects `sessions` joined to `classes.instructor_id` and builds a card for every instructor with taught sessions in the closed period. Assigning real instructors to four records-only classes therefore created three cards and sent three T5s at the 16:06 UTC sweep; the class-details HOLD alert (class-keyed too: "families are waiting") fired for Leone + MIS the same minute. **Fix: ONE class-level predicate, applied to every class-keyed decision, plus the pre-flight that answers "what will send?" before a sweep.**

**A. `app/utils/class-quiet.ts` — the shared predicate.** `classQuietReason(cls)` returns the same four reasons PL-463's rule did (cancelled · ended · records-only = every paid enrollment comms-muted · running with no portal roster), derived from state every time; `instructorQuietReason()` now delegates to it. A SECOND rule, `classPayableThroughPortal()`, is the timecard rule — because "ended" ≠ "unpayable": a live portal-run class whose last session was yesterday still pays its instructor for the closing period; cancelled and records-only / no-roster classes never do (those instructors are paid outside the portal). `quietInputFromRow()` + `QUIET_SELECT` adapt raw `classes` selects so the raw-row sweeps read the same rule. Applied:
- **Timecards (`timecards.ts`):** `payableClassSessions` and the sweep's tutor selection both subtract `classIdsNotPayableThroughPortal()` → no card, no stamp, no T5 for a records-only class. The tutor selection is now ONE exported function (`tutorsWithPayableActivity(period)`) the sweep AND the projector call.
- **Cron route:** the class-keyed staff/school sweeps — missing-details warning (`AL_MISSING_DETAILS`), minimum-enrollment brief + follow-up (`AL_MIN_ENROLLMENT`), instructor nudge (`ADMIN_INSTRUCTOR_NUDGE`), final-days push / class-full note (`FP_*`), classroom request + nudges (`CR_*`) — run only when `classQuietReason(bundle)` is null (counter `classes_quiet` in the sweep response). The class-details **HOLD alert** now counts only UNMUTED paid rows as "families waiting" — a records-only class never raises it; a mixed class still does. Follow-on: a quiet feeder runs no FO campaign (per-row mutes already covered families; the cohort-level extend nudge to staff is now covered too).
- **Counselor digests:** `digestClasses()` excludes quiet classes (moved to `utils/counselor-recipients.ts` with `loadCounselorsBySchool` / `contactsForClass` so the projector shares them).
- **Roster report (Monday `AL_ROSTER_REPORT`):** records-only classes are still LISTED (they are real history) but flagged *"(records only — imported roster; no portal emails, timecards or counselor loop)"* and never in the "in-person classes under minimum" list.
- **Collateral + Synap nudges:** each sweep is now `candidates()` (the selection, exported) + the send loop; candidates skip quiet classes.
- **Dashboard Needs Attention:** the five class-keyed rows (needs an instructor · collateral · Synap · minimum decision · details missing) come from `portalRunClasses` only. Kept on purpose: *"Class assignment conflicts with tutoring"* (a real instructor double-booked against real tutoring is a scheduling fact regardless of who emails the class) and *"Timecard awaiting approval"* (only `tutor_confirmed` cards).
- **International Classes calendar sync — NOT gated, stated:** today it is UNCONFIGURED (`intl_classes_calendar_id` unset → the sync is a no-op for all four classes; the pre-flight says so every run). When Scarlett does enable it, records-only classes WILL appear on it — deliberately: it is the ops team's own calendar of what is running (cancelled classes turn red rather than vanish, past classes stay as history), never a send to anyone. **Flagged for veto** if she'd rather records-only classes stay off it.
- Also from the stop-gap notes: the class wizard's new-school step now asks for the **city** (`data-testid="new-school-city"`; the Schools-panel form already did) — without it the public label fell back to the TIMEZONE's city (Leone XIII read "Rome"). The remaining fallback is handled in PL-476's /classes rewrite (never a zone city presented as the school's city).

**B. AUDIT — every sweep/route that selects from `classes`/`sessions` and can email anyone or write a calendar, keyed on CLASS, cron route read top to bottom (state: records-only class with a real instructor assigned):**

| # | Sweep / route (order in the cron) | Fires for a records-only class with an instructor? | What gates it |
|---|---|---|---|
| 1 | cancelled / 30-day-dead guards → `cancelScheduledForClass` | no send (audit-cancels rows) | class status / last session |
| 2 | Feature-A projector `projectScheduledSends` | writes email_sends rows only; projects nothing for muted rows | PL-457 `commsMuted` per row |
| 3 | `sweepPaymentReminders` (PR1–4, expiry) | no | enrollment-keyed; imported Pendings exempt (PL-363); sendOnce mute gate |
| 4 | `sweepInstructorComms` (IN_WELCOME / IN_DIGEST / briefing / +1) | **no** | `loadClassInstructor` → `classQuietReason` (PL-463, now the class rule) |
| 5 | `syncInstructorClassCalendar` | **no write of any kind** | `classQuietReason` (PL-463) |
| 6 | `sweepCompletion` (Paid → Completed) + `sweepWaitlistRelease` | status flip YES (kept — the roster must read Completed after the last session); release send only to Waitlisted rows (none) | enrollment-keyed; sendOnce |
| 7 | `sweepThankYou` / `sweepUpsell` | no | pre-claimed by `--silent`; sendOnce mute gate |
| 8 | `sweepFollowOnForBundle` (FO pair + staff extend nudge) | **no (NEW)** | `classQuietReason` at the top; per-row mutes |
| 9 | `sweepSequence` #2–#8 + **class-details HOLD alert** | per-row sends no (mute); **HOLD alert no (NEW)** | `paid` counts only unmuted rows |
| 10 | `sweepScheduleUpdates` (SU + instructor FYI) | no | per-row sendOnce mute; FYI via `loadClassInstructor` |
| 11 | `sweepWaitlist` (offers, rollover alert) | no (no Waitlisted rows) | enrollment-keyed |
| 12 | `sweepAdminCheckpoints` (AL_MISSING_DETAILS, AL_MIN_ENROLLMENT ×2) | **no (NEW)** — this is the second Sep-21 send class | `classQuietReason` in the main loop |
| 13 | `sweepInstructorNudges` (ADMIN_INSTRUCTOR_NUDGE) | **no (NEW)** | same |
| 14 | `sweepDeadlinePush` (FP_DEADLINE_PUSH / FP_ALT_CLASS_FULL) | **no (NEW)** (also past reg-close) | same |
| 15 | `sweepClassroomRequests` (CR_* + `classroom_requests` insert) | **no (NEW)** (also past first session) | same |
| 16 | `sweepClassSurveys` (SV) | no | pre-claimed by `--silent`; sendOnce |
| 17 | `sweepCounselorDigests` (CD) | **no (NEW)** (also past reg-close) | `digestClasses` excludes quiet |
| 18 | `sweepAdminRosterReport` (AL_ROSTER_REPORT, Mondays) | lists it **flagged "records only"** (NEW); never in under-minimum | — (informational to staff only) |
| 19 | QBO queue / health / unsynced payments | n/a (not class-keyed) | — |
| 20 | gcal queue / watch channels / tutoring drift | n/a (tutoring sessions) | — |
| 21 | `syncInternationalCalendar` + drift audit | **would create span + session events when ENABLED — today unconfigured → no-op** | `intl_classes_sync_enabled`; deliberately NOT quiet-gated (see A) |
| 22 | `autoCompleteSessions` | n/a (tutoring) | — |
| 23 | **`sweepTimecards` (card + T5_TIMECARD_READY)** | **no (NEW)** — this was the Sep-21 bug | `classPayableThroughPortal` inside `tutorsWithPayableActivity` + `payableClassSessions` |
| 24 | block confirmations / billing cycle / proposals / collections / add-on nudges / schedule approval / agreements / tutor notices / conversion follow-ups / session-note reminders | n/a (tutoring or enrollment-keyed with mutes) | — |
| 25 | `sweepCollateralNudges` / `sweepSynapNudges` (creator doorbells) | **no (NEW)** | `candidates()` skip quiet |
| 26 | campaigns resume/dispatch | no | `campaigns.ts` drops `comms_muted` rows (PL-457) |
| 27 | `sweepLeadOutcomes` | no send | — |
| D | Dashboard Needs Attention (instructor / collateral / Synap / minimum / details) | **no rows (NEW)**; assignment-conflict + timecard-approval rows kept | `portalRunClasses` |
| E1 | `assign-instructor` route | no send; triggers only the intl calendar sync (row 21) | — |
| E2 | `class-min-enrollment` (IN_MIN_DECISION) | no | `instructorQuietReason` (PL-463) |
| E3 | `class-session` (admin schedule edits → SU + FYI) | no | per-row mute + `loadClassInstructor` |
| E4 | `instructor-comms` pref toggle (welcome backfill + calendar) | no | `loadClassInstructor` + sync gate |
| E5 | `cancel-class` (CX + staff alert) | staff-initiated only; muted rows skipped | — |
| E6 | `class-confirmed` (counselor welcome / CS-F) | staff button only | — |

**C. Clean-up — STOPPED, as instructed.** The `timecards` table allows exactly `open → tutor_confirmed → approved → exported` (+ `reopen`); the only staff actions are approve / mark_exported / reopen / push_qbo. **There is no void or delete transition**, so the three Sep 1–15 class-hours cards (Gwen 6 h · Kevin 7.25 h · Rebecca 6 h) and Billy's MIS-placeholder card (6 h) stay on the Timecards list untouched. What IS true now: (1) the next sweep cannot re-create or re-announce them — the tutor selection excludes the four classes, and every T5 key is already spoken for (delivered rows for `2026-09-01`, Claude's cancelled pre-claims for `2026-09-16` and `2026-10-01` stay in place); (2) if anyone touches a card (approve / reopen), `recomputeTimecard` now un-stamps the records-only sessions and retotals it to **0 h** — which is not a void either, just an honest number. **Proposal (Scarlett decides; PL-478 candidate):** a `void` status (migration extends the check; `voided_by`/`voided_at`/`reason`), a staff "Void" action beside Reopen, voided cards hidden from the tutor portal and the approval row, and the sweep refusing to recreate a voided (tutor, period) — one deliberate delete-shaped transition instead of a DB edit. Until then the four cards read as open and should be left alone. **Also noted, not acted on:** MIS still carries 8 class-session events on Billy's Google calendar from his placeholder-instructor days (the PL-463 sync returns before the reassignment delete for a quiet class, by design) — Billy removes them by hand or says the word.

**D. "What will send?" is now answerable BEFORE the sweep.** `app/utils/send-projection.ts` projects every audience over a window from the SAME rules the sweeps use — instructor (welcome / Monday digest / deadline briefing / FYI copies), school contacts (digest / final-days push / classroom request + nudges), staff alerts (HOLD, missing details, minimum brief, instructor nudge, collateral + Synap doorbells), timecards (card + T5 per closing period, via `tutorsWithPayableActivity`), calendar writes (instructor session events; International Classes when enabled) and the family sequence (the Feature-A projector) — naming each dedupe key and skipping keys already spoken for (sent / cancelled pre-claim; `held` rows are shown as held). Three doors: **`node scripts/project-sends.mjs --hours 24 [--class <id>] [--json]`** (the standing pre-flight — checklist step 3d added), **`GET /api/admin/comms/projection?hours=`**, and the **"Every audience — what the next N hours of sweeps would send" panel** at the top of Contacts → Communications → Upcoming (`data-testid="send-projection"`). It states its boundary every run: event-driven sends (payments, waitlist offers, cancellation blasts, milestone pings, FO extend nudges, drift alerts, queue retries) are not sweep-timed and are listed as not projected. **Run against prod right now: 0 projected actions for the next 24 h; SLS / Leone / MIS / ASF each named "records-only class (every paid enrollment is a comms-muted import)".**

**Gate — `regress:silent-import` 39 → 50 checks (ALL PASS):** the class rule agrees with the instructor rule on the fixture · records-only NOT payable · ended-but-portal-run IS payable · cancelled never · the fixture is in the not-payable set · the fixture instructor is absent from `tutorsWithPayableActivity` for the last closed period · **the REAL `sweepTimecards()` on the muted fixture (sessions moved into the closed period, instructor assigned) creates 0 cards, sends 0 T5, stamps 0 sessions** · collateral + Synap selections skip it and the real nudge sweeps ring nothing · the 30-day projection lists ZERO sends/writes and names it quiet · **control:** the same fixture made LIVE (unmuted roster, future sessions, blank location) projects the HOLD alert, the instructor welcome + digest + FYI, 3 calendar events, 8 family rows and a timecard + T5 at the period close. `tsc` clean; touched files lint clean (pre-existing wizard warnings untouched).

**Files:** `app/utils/class-quiet.ts` (NEW) · `app/utils/counselor-recipients.ts` (NEW) · `app/utils/send-projection.ts` (NEW) · `scripts/project-sends.mjs` (NEW) · `app/api/admin/comms/projection/route.ts` (NEW) · `app/admin/communications/projection-panel.tsx` (NEW) + `page.tsx` · `app/utils/timecards.ts` · `app/utils/instructor-comms.ts` · `app/utils/follow-on.ts` · `app/utils/roster-report.ts` · `app/utils/collateral-nudge.ts` · `app/utils/synap-nudge.ts` · `app/api/cron/reminders/route.ts` · `app/api/admin/dashboard/route.ts` · `app/admin/class-wizard.tsx` · `docs/cutover-decommission-checklist.md` (step 3d) · `scripts/regress-silent-import.mjs`.
PL-463 gated `instructor-comms.ts`. Two hours after Scarlett approved assigning the real instructors (Gwen De Silva → SLS, Rebecca Baumher → MIS, Kevin Marren → Leone XIII + ASF), the 16:06 UTC sweep sent:
- **`T5_TIMECARD_READY` to gwen@, kevin@ and rebecca@** ("Your timecard for September 1 – 15, 2026 is ready to confirm") — `sweepTimecards()` builds a card for every instructor on a non-cancelled class with sessions in the closed period (`timecards.ts` PL-103 branch); it never asks whether the class is records-only. These instructors are paid for these classes OUTSIDE the portal. Three `open` timecards now exist (6h / 7.25h / 6h) + Billy's from the MIS placeholder days.
- **`AL_CLASS_DETAILS_HOLD` ×2 to staff** ("families are waiting on it") for Leone XIII + MIS because location was blank — false: every family there is comms-muted and the step is claimed; nothing was ever going to send.
Stop-gap already applied by Claude: `t5_timecard:{tutor}:2026-09-16` and `…:2026-10-01` pre-claimed (cancelled rows) for the three tutors; MIS location set to Rebecca's Meet link, Leone to "Live online"; Leone school city set to Milan (the /classes "Recent classes" line said Rome — derived from the timezone because `schools.city` was null; **the add-school form should ask for the city**, and the public label should never fall back to the tz city name without saying so).
- **A. One shared predicate** — lift `instructorQuietReason` into a class-level `classQuietReason(bundle)` (cancelled · ended · records-only · running-with-no-portal-enrollments) and apply it to EVERY class-keyed sweep that is not already gated by a muted enrollment: timecards class-hours branch (no card, no T5 — class hours of a records-only class are not payable through the portal), class-details HOLD + missing-details warnings, min-enrollment decision/deadline alerts, classroom-request ask + nudges, counselor digest/final-days lines for that class, AL_SYNAP_NUDGE, roster-report rows (list them, but flagged "records only"), Needs-Attention rows, the International Classes calendar sync (say what it does today for these four).
- **B. AUDIT TABLE in the ship note, same shape as PL-457's but keyed on CLASS not enrollment:** every sweep/route that selects from `classes` or `sessions` and can email anyone or write a calendar — does it fire for a records-only class with an instructor assigned? what gates it? PL-457's audit missed timecards because it followed enrollments; PL-463's missed them because it followed `instructor-comms.ts`. Follow the cron route top to bottom this time.
- **C. Clean up:** void/delete the three Sep 1–15 class-hours timecards for Gwen / Kevin / Rebecca (and Billy's MIS-placeholder one) through whatever the honest existing transition is; make sure a voided card is not re-created or re-announced by the next sweep (the pre-claims stay until then).
- **D. "What will send" must be answerable BEFORE the sweep** (Scarlett: "couldn't you check to see if they're queued?"): the portal has no queue — sends are computed at sweep time — and the comms projector only projects FAMILY sends. Extend the projector (admin Scheduled view + a `scripts/project-sends.mjs --hours 24` dry run) to every audience: instructor, school contact, staff alerts, timecards. That script becomes a standing pre-flight before any data change during cutover.
- Gate: `regress:silent-import` gains: records-only class WITH an instructor → zero timecard rows, zero T5, zero HOLD alerts, zero calendar writes across a simulated sweep.

## PL-472 — No-upcoming-class copy (Scarlett, Sep 21)

### ✅ SHIPPED (Sep 21) — with PL-470

The copy lives in code, not a seeded block (`app/[code]/page.tsx` + `app/[code]/register/page.tsx`, the `EvergreenCapture` card) — so no seed AND live block to update; said here. Heading (school variant, verbatim): **"No upcoming class at {school} right now, but we'll likely have one set up soon."** · sub: **"Add your email to the interest list to be contacted when registration opens for the next course."** — the email field sits directly under it (the card already had it inline: email + optional student name → `class_interest`, source `evergreen-link`). Generic (course-code) variant: "No upcoming {label} class right now, but we'll likely have one set up soon." + the same sub. Verified on the dev server: `/nido` (a school with no open class) renders both sentences; the PL-470 ended fixture flipped to the same state.
"No upcoming class at {school} right now" → **"No upcoming class at {school} right now, but we'll likely have one set up soon. Add your email to the interest list to be contacted when registration opens for the next course."** — with the interest-list email field directly under it (not a link away). Same sentence on the generic (no-school) variant without the school name. If this copy lives in a seeded content block, update the seed AND the live block.

## PL-473 — Embeddable inquiry form for the Squarespace pages + source tagging (Scarlett, Sep 21)
Read-only audit of highergroundlearning.com (Sep 21, 118 sitemap URLs): the site's lead capture is ONE Squarespace form block (First/Last name, Email, Phone w/ country, Message → "Send") repeated on **/contact, /get-started, /about, /academic-support, /sat, /act, /ap-ib (×2), /university-applications, /gre-gmat**, plus distinct forms on **/examzen** (school-partnership lead) and **/college-prep-compass** (lead magnet), a footer newsletter block on EVERY page, and a WhatsApp link (wa.me/18015240817) site-wide. Header CTAs everywhere: "Free Consultation → /get-started", "Contact → /contact". The portal has `/inquire` (full page) and an upcoming-classes embed, but no embeddable form.
- **`/embed/inquire.js`** — same pattern as `upcoming-classes.js`: one CODE-block snippet renders the portal inquiry form inline in the host page's width, posts to the portal, shows the thank-you in place, `<noscript>` fallback link to `/inquire`. No iframe scrollbars; inherits nothing from Squarespace CSS that breaks it; works at 375px.
- **Source tagging:** the snippet takes `data-source` (default = the host page path) and optional `data-interest` ("SAT", "ACT", "AP/IB", "University applications", "GRE/GMAT", "Academic support", "School partnership") → stored on the lead and shown on the Prospective Students card + filterable, and pre-selects "What would you like help with?". `/inquire?source=…&interest=…` does the same for plain links.
- **Parity check vs the Squarespace form:** country/dial-code on phone (international families), required-field set (Scarlett decides: today sqsp requires phone + message; the portal requires only name + email), spam protection (honeypot + rate limit; say what exists), the staff new-inquiry alert, and the auto-reply to the family (does one exist? show it).
- School-partnership (/examzen) leads are a different lead type — propose how they should land (a lead with `kind='school'` vs the family pipeline); don't silently mix them.
- **Scarlett, Sep 21 (later): /examzen and /college-prep-compass are IN scope — see PL-477.** The footer newsletter block on every page IS the College Prep Compass signup.
- Deliver the exact paste-ready snippets per page in the ship note.

## PL-474 — Two hosts, one session: canonical-host rules for the domain pair (Scarlett's decision Sep 21: `portal.highergroundlearning.com` + `hgl.co`)
Decision: signed-in surfaces and every emailed link live on **portal.highergroundlearning.com**; **hgl.co** stays the short public front (`/{code}`, `/{code}/register`, `/classes`, `/team`, `/inquire`, wildcard → main site). Verify, and build what's missing:
- Requests for signed-in or auth routes on hgl.co (`/login`, `/portal`, `/admin/*`, `/auth/*`, tokenized family links, `/class-report/*`) 308 to the same path on the portal host, so nobody ends up with a session cookie on the wrong host. Public routes on the portal host keep working (no redirect loops); canonical/OG/sitemap URLs for public pages use hgl.co, as PL-384 intended — confirm.
- Registration started on hgl.co → Stripe → success/cancel URLs → which host? State it; make it deliberate.
- Staff footer "Staff sign-in is always at hgl-portal.vercel.app/login" and every other literal of the old host in templates/collateral/code → derived from the base URL (list the sites found).
- `ADDITIONAL_PRODUCTION_HOSTS` must include both `hgl-portal.vercel.app` and `hgl.co` on cutover day — confirm the origin guard treats hgl.co links in template bodies as production.
- Origin checks / CSRF / Supabase redirect allow-list / QBO redirect URI / Resend + Stripe webhooks: list each host-sensitive setting and its required value for the pair, as a table Scarlett + Claude can tick through (it replaces section 2–4 of `claude/hgl-dns-cutover-runbook.md`).

## PL-475 — Vercel Pro: the hourly sweep moves home (Scarlett is upgrading)
On her word that the project is on Pro: `vercel.json` cron → `0 * * * *`, delete `.github/workflows/hourly-sweep.yml`, note the `CRON_SECRET` GitHub secret to remove, confirm exactly one sweep per hour afterwards (no double-run window), per the PL-155b runbook steps 3–4 in `base-url.ts`. Do NOT ship this until Scarlett confirms the upgrade happened — on Hobby the hourly schedule is rejected at deploy.

## PL-476 — Past classes backfill: /classes "Recent classes" and the school code pages reflect HGL's real history (Scarlett, Sep 21)
"Add the most recent international classes that we're showing on the Squarespace site to the portal — each with the closed card." The Squarespace store lists 20 school classes; the portal knows 4 (the running ones) + 4 schools without a class. To add (school · code · mode · when it ran, from the sqsp product pages; **Claude supplies the exact first/last session dates + times per class as JSON after reading each calendar image — do not guess them**): International School of Milan `ism` online Jun 2026 · International School of Düsseldorf `isd` online Feb–Mar 2026 · United Lisbon International School `ulis` online Feb–Mar 2026 · American Overseas School of Rome `aosr` online Feb–Mar 2026 · Aquinas American School `aas` online Feb 2026 · American School of Madrid `asm` in person Feb 2026 · American International School of Jeddah `aisj` in person Jan 2026 · American International School of Vienna `aisv` online Nov–Dec 2025 · International School of Panama `isp` in person Aug 2025 · Shanghai American School (Puxi) `sas` in person May 2025 · Casablanca American School `cas` online Apr 2025 · American School of Brasilia `eab` online Apr 2025 · Stockholm International School `sis` online Mar 2025. (Nido / CAC / AISCT fall-26 cohorts were CANCELLED — they are not "recent classes"; their schools already exist. CAC needs its code `cac`.)
- **`scripts/backfill-past-classes.mjs --json <file> [--dry-run]`** — creates missing schools (nickname, full name, city, timezone, evergreen code; NO contact) and one ENDED class each THROUGH THE SAME creation path the wizard uses (slug, course key, snapshots), sessions from the JSON, no instructor, registration dates in the past, `$749` unless the JSON says otherwise. Idempotent. Runs only after PL-471 is on prod; its dry run ends with the PL-471 D projector proving ZERO sends / calendar writes for the new rows. Scarlett + Claude run it, not Code.
- **/classes:** "Recent classes" = ended classes that actually ran (not cancelled), newest first, school name + city + month/year ("Milan · June 2026"), each linking to its school's code page; cap the list sensibly (say 12) with "and N more schools". Records-only and backfilled classes count. Never list a cancelled cohort.
- **Code pages** for these schools render the PL-472 no-upcoming-class state (copy + inline interest list). A code that collides with a live main-site page (`/ism`, `/isd`, … exist on Squarespace today) keeps the evergreen route's existing collision warning — after the flip the portal page wins; say so in the ship note so Scarlett can retire those sqsp pages deliberately.
- Instructors/enrollments are NOT backfilled (no rosters, no pay, no emails) — this is public history only.

## PL-477 — The other two Squarespace forms: school partnerships (/examzen) and College Prep Compass (footer newsletter + /college-prep-compass) (Scarlett, Sep 21)
- **School-partnership inquiry** (today: the /examzen "Get started with Higher Ground" form): an embeddable + full-page form (`/embed/partner.js`, `/partner`) for SCHOOLS — contact name, role, school, country/city, email, phone/WhatsApp, which tests (SAT/ACT/other), rough cohort size, on-campus vs online vs school-sponsored, timing, message. Lands as its own lead KIND (`school`) in Prospective Students with its own filter/pill and its own staff alert — never mixed into the family pipeline; converting one creates the school + a contact (the existing add-school path, PL-467) rather than a family. Auto-reply to the school contact: DRAFT for Scarlett's copy review. Read the live /examzen form first and match its fields; list any you dropped or added.
- **College Prep Compass** (the footer block on EVERY Squarespace page and the /college-prep-compass page are the same MailerLite signup — a newsletter/lead magnet with an existing subscriber list): (1) `/embed/compass.js` + `/compass` signup (email, first name, parent/student, optional grad year) writing to the portal's marketing audience with explicit consent text + double-opt-in if the campaigns roadmap specifies it — check `claude/hgl-campaigns-roadmap.md` / the PL-201 campaign machinery and reuse its subscriber model rather than inventing one; (2) the welcome/delivery email for the Compass itself (what do subscribers receive today? — STOP and ask Scarlett for the current MailerLite automation content; do not write it); (3) **`scripts/import-mailerlite-subscribers.mjs --csv … [--dry-run]`** — imports the Compass list as marketing subscribers with source + original opt-in date, de-duped against families/leads by email (link, don't duplicate), and runs AFTER `import-mailerlite-suppressions.mjs` so no unsubscribed address is ever re-added; imported subscribers receive NOTHING until Scarlett sends a campaign. Scarlett + Claude run it.
- Both embeds follow PL-473's pattern (noscript fallback, source tagging, 375px, spam protection). Paste-ready snippets per page in the ship note.
- Out of scope here: moving the /1on1 tutoring-hours store and the merch store (separate scoping — Claude is writing the brief; merch = the existing Printful plan).

