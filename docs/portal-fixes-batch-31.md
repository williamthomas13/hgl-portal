# Portal fixes — batch 31 (opened Aug 8, 2026 — PL-299 GO leads; Scarlett's pipeline review)

**Standing rules:** plain-English statuses · no internal shorthand · every alert deep-links its record · samples from composers · `git push` after committing · PL-x IDs in commits · check items off here when shipped · registry template edits ship as new versions via anchor guards · verify composed blocks via the composer path · inline confirm banners only · NO native browser dialogs (prompt/confirm/alert) — the pipeline "Close — not now" prompt() found below is the live offender.

## PL-299 — GO (plan approved Aug 8; the plan in docs/portal-fixes-batch-30.md is authoritative) ✅ SHIPPED Aug 8
Build the hours-block exhaustion confirm flow exactly as planned: size-scaled thresholds (15h→3h left / 10h→2h / 5h→1h), parent email via registry, no-token confirmation (signed-in portal control or reply) + admin mirror action, scheduling/generation holds past the block until confirmation, PL-84/86 conversion on confirm, acknowledge surviving only as the legacy surface for already-overdrawn engagements.

**Ship note (Aug 8):** `tutoring_engagements.block_confirmation` state machine (migration 20260904000001 APPLIED): null = pre-flow (PL-197 behavior exactly — Roman's 24/15 carry + ack surface untouched; the sweep also explicitly skips already-overdrawn engagements so legacy carries never freeze behind a hold) → asked → confirmed/declined. Core in `app/utils/block-confirm.ts`: exact-size thresholds (other sizes 20% rounded up), `sweepBlockConfirmations()` on the hourly reminders cron (dedupe `block_confirm_ask:{id}`, flips to asked only on sent/duplicate), `recordBlockDecision()` refuses pre-ask. BL_BLOCK_CONFIRM seeded as DRAFT v1 (code twin sends until Scarlett ramps it) — plain numbers, no token, portal button + reply-works line. Holds: generation writes no overflow/carry lines while asked/declined (twin gates in tutoring-billing.ts), engagement create/update-regenerate hard-409s past the block (the walk-past `confirm_overdraw` flag can NOT bypass the family's answer). Confirm surfaces: portal amber banner w/ inline-armed Continue/Stop buttons (`/api/portal/block-confirm`, parent-email-matched) + admin mirror buttons on the engagement row (`/api/admin/tutoring/block-decision`). Confirmed = PL-197 Case-A billing IS the standard monthly path (no funding flip needed). Dashboard rows: asked → awaiting-confirmation; declined+overflow → urgent. Verified: regress:package-overdraw extended to 17 checks, ALL GREEN (thresholds, hold states, sweep ask at threshold w/ plain-numbers email, asks-once dedupe, asked→overflow never bills, confirm→the held overflow bills at the engagement rate, pre-ask decision refused plainly); regress:monthly-generation + regress:mid-month-start green (null keeps legacy byte-for-byte); regress:links green on the BL draft.

## PL-300 — Pipeline: contact email (and every intake field) must be addable/editable on the pipeline card (Scarlett, Aug 8, w/ screenshots) ✅ SHIPPED Aug 8
The card says "Add a contact email to send the intake form" and "next: get a contact email," and "Create family + student" refuses with "They need a contact email first." — but there is NO visible way to enter an email on the card. "Just a name (or an email) is enough to start" is therefore not honest: phone-call leads start without an email and then dead-end. Fix: every field from "Add a prospective student" (parent/contact name, phone, email, student name, source, school, grade, interest, subjects/test, notes) is editable inline on the pipeline card. The intake-form send and Create family + student then unblock naturally once an email lands.

**Ship note:** the expanded card now opens with the same field grid as "Add a prospective student" (contact name/phone/email, student name, source, school, grade, interest, subjects/test — notes was already there), all saved by the card's Save button through the existing update path (LEAD_FIELDS already accepted them server-side; this was UI-only). The email-less hint now says "Add a contact email above (then Save) to send the intake form" — once the email saves, the intake send and Create family + student unblock on their own. Verified: field save lands via API, close-without-reason still refused server-side.

## PL-301 — Pipeline "Close — not now…" is a native prompt() with free-text reasons (Scarlett, Aug 8, w/ screenshot) ✅ SHIPPED Aug 8
Two problems: (a) the link reads like "skip adding the email for now" but actually closes the lead — rename/redesign for clarity (e.g. "Close lead…"); (b) it opens a NATIVE window.prompt asking to type one of "price · timing · went elsewhere · no response · other" — free text guarantees dirty data ("choose someone else" ≠ "went elsewhere", "too expensive" ≠ "price") AND native dialogs freeze the automation bridge (this is one of the two known remaining prompt() sites). Replace with an inline dropdown of the five reasons (+ optional note on "other"), stored as a clean enum so closes are sortable/reportable.

**Ship note:** promptCloseReason() (two window.prompt + two alert) is GONE. Both doors — the renamed **"Close lead…"** link and picking "Closed — not now" in the status dropdown — open one inline amber panel: dropdown of the five enum reasons + note field (required for "other", optional otherwise), explicit "Close the lead" / "never mind". Stored values stay the clean lost_reason_kind enum (server validation unchanged). Same rule swept the rest of the file AND the templates page: the add-form's exact-duplicate window.confirm is now an armed two-click Add ("Add anyway — different person", disarmed by further typing), the row-level intake-send confirms use ConfirmAction with inline errors (no more alert()), and the templates page's live-toggle/revert/discard-unsaved/dirty-test-send confirms route through one inline armed-confirm banner. Zero native dialogs remain on either page.

## PL-302 — Consultation scheduling: 5-minute increments, 30-minute default (Scarlett, Aug 8) ✅ SHIPPED Aug 8
The date-time picker increments minutes by 1 — use 5-minute steps. Consultations default to 30 minutes.

**Ship note (premise correction included):** the picker already had 5-minute steps (step={300} since the PL-233 work) — the real gap was duration: the Google event was hardcoded to 60 minutes. New Length select (15/20/30/45/60/90, defaults 30) rides schedule_consult as duration_minutes; the server validates 5–240 in 5-minute steps (plain-English refusal) and sizes the calendar event from it. Verified: 7-minute request refused, default-30 schedule synced to GCal.

## PL-303 — Phone consult recorded while an in-person consult is scheduled: offer the cancel (Scarlett, Aug 8) ✅ SHIPPED Aug 8
Scenario she hit: consult scheduled with Billy → the family talks it out on the phone with Kelsie instead → "Record phone consult" saved. The scheduled consult remains on Billy's calendar silently. After recording a phone consult for a lead with a scheduled-and-not-yet-happened consult, surface the scheduled one with an explicit choice: keep it (maybe they still come in) or cancel it — so anything remaining on a calendar is there on purpose.

**Ship note:** record_phone_consult now reports back when a scheduled-and-not-yet-happened consult was superseded; the card surfaces an inline keep-or-cancel: **Keep** (the meeting stays on the owner's calendar on purpose — the "currently…" line then says "an earlier scheduled meeting was kept on the calendar") or **Cancel** via NEW cancel_scheduled_consult action, which pulls the Google event off the owner's calendar (best-effort, already-gone counts as removed, failure says "delete it from the calendar by hand") and clears the reference. Verified E2E against real GCal: scheduled consult → phone consult recorded → supersededConsult surfaced → cancel → gcal 'removed'.

## PL-304 — Offers: admin-only, deletable when unused, drop the internal note (Scarlett, Aug 8, w/ screenshot) ✅ SHIPPED Aug 8
(a) The Offers panel is restricted to the Admin role (billy) — managers/others don't see it. (b) Offers get a **delete** button, allowed only when no prospective students (or converted records) carry the offer — Scarlett created a test offer ("1 free hour of test prep", 110 hrs — value typo'd, another argument for delete) and cannot remove it; inactive-but-undeletable is not enough. (c) Remove the "none active at launch" copy from the panel subtitle — internal launch note, meaningless to other users.


**Ship note:** (a) the Offers panel renders only for the Admin role — the page asks NEW `GET /api/admin/leads` who's calling, and create/update/delete_offer all re-enforce admin server-side (managers get "Offers are managed by the admin."). (b) NEW delete_offer action + Delete button (inline armed confirm): allowed only when zero leads rows — converted and closed included — carry the offer; otherwise refused with the count and "detach it from them (or leave it inactive) instead". The stuck "1 free hour of test prep" (110 hrs) offer carries 0 attachments, so it's now deletable from the panel whenever Scarlett wants it gone. (c) subtitle is now "Promotions that can be attached to a prospective student"; the empty-state launch-note copy trimmed too. Verified: role GET returns admin, attached-delete refused with count, detached-delete succeeds.

## PL-305 — Timezone label should speak the class's city, not the IANA zone's city (Scarlett, Aug 8, w/ screenshot) ✅ SHIPPED Aug 8
The register page's "(times shown in Denver time)" should read "(times shown in Salt Lake City time)" for the HGL PSAT Prep class — the IANA zone is America/Denver but the class is in Salt Lake City, and not every parent knows they're the same zone. Derive the friendly label from the class's own location/city where one exists (in-person classes have an address; the HGL location is Salt Lake City), falling back to the zone city for online classes or where no city is known. Apply everywhere the "(times shown in …)" phrasing renders (register page, class-info, calendar page, emails via the composed variables) — one source for the label, per the timezone-precedence machinery.

**Ship note:** ONE source — `friendlyZoneCity(timezone, location)` in dates.ts: when the class's location string ends like a postal address ("380 W. Pierpont Ave, Salt Lake City, UT[ 84101]"), the city segment becomes the label; room strings, buildings, and meeting links can never masquerade as a city, so online/school-room classes fall back to the IANA zone city as before. Applied at every "(times shown in …)" render — SessionCalendar (register page, parent portal, admin, wizard preview) computes it from its own defaultLocation, /classes/[id]/calendar likewise (that page ALSO gained the missing classes.timezone-over-school precedence) — and in zoned deadline copy: zonedDeadline() takes an optional location, wired through PR payment-reminder expiry, W2 waitlist-offer deadline, and the {expiryDate} composed variable. Bonus fix found while wiring: the parent portal's class card never selected any timezone (the zone line silently never rendered there) — now selects class+school zones with class precedence. Verified: 11 compile-and-call helper checks green; register + calendar pages render "(times shown in Salt Lake City time)" for the HGL PSAT class in the browser; regress:links green.

## PL-306 — Course calendar page needs a "Continue registration" path back (Scarlett, Aug 8, w/ screenshot) ✅ SHIPPED Aug 8
Mid-registration, clicking "Add to your calendar / subscribe →" lands on /classes/<id>/calendar with no way back but the browser button. Add a **Continue registration** button on the course-calendar page that returns to the register flow where the parent left off (carry the return context — e.g. only show it when arrived from registration, or always show it linking to the class's register page, whichever is cleaner — parents must never lose their place or their filled-in state).


**Ship note:** both halves — (1) the register page's "Add to your calendar / subscribe →" now opens the calendar page in a NEW tab (target=_blank on SessionCalendar's link), so the register tab and everything typed into it stay exactly where the parent left them; (2) the calendar page itself gained a **"Continue registration →"** button (rendered while the class is open) linking /register/{slug|id}, covering parents who arrive from an email button instead. Verified in the browser: link carries target=_blank rel=noreferrer, button renders on the HGL PSAT calendar page.

## PL-307 — Domestic (at-HGL) add-on tutoring pricing tier (Scarlett, Aug 8, w/ screenshot) ✅ SHIPPED Aug 8
The register flow's "Add 1-on-1 tutoring?" hour-block dropdown currently always shows the international discount tiers (5 @ $120 / 10 @ $105 / 15 @ $95). For classes held AT Higher Ground Learning (in-person-at-HGL flavor), show a domestic tier instead: **5 hours @ $100/hr · 10 @ $90/hr · 15 @ $80/hr** — implemented the same way as the international tiers: derived from the base hourly rate (per-tier discount off base), so if base prices rise the discounted prices rise automatically. Selection keyed off the class's location flavor (at-HGL vs school/online), one source with whatever the intl mechanism uses; receipts/QBO see the true composed price via the existing snapshot machinery.


**Ship note (premise correction included):** the international tiers are NOT derived from a base rate — they're hand-entered rows in tutoring_packages (5/$120/$600 · 10/$105/$1050 · 15/$95/$1425, regular_hourly_rate 130). "Same way as the intl mechanism" therefore means: NEW `tier` column ('international' default | 'domestic', migration 20260904000002 APPLIED) + three seeded domestic pre-class rows — 5 @ $100/$500 · 10 @ $90/$900 · 15 @ $80/$1200, named "N-Hour Package (at HGL)". A price change stays a one-row edit, same as intl. **regular_hourly_rate on the domestic rows is set to $110 — inferred, no figure was given; it only affects the strike-through "regularly $X/hour" copy, never the charge — Scarlett should confirm or correct it (one UPDATE).** Selection: `classTutoringTier()` in tutoring-tier.ts (at-HGL = school_id IS NULL AND delivery_mode='in_person') — class-info filters the dropdown by it AND checkout re-validates the chosen package's tier against the class (plain refusal on mismatch, so a stale/hand-crafted package id can't buy the wrong price sheet). Receipts/QBO see the composed price through the existing PL-142 snapshots; the new package names surface as new QBO item keys via the normal mapping to-do. Post-class hourly rows stay international-tagged and shared (no domestic post-class figures were given — flagged for Scarlett if wanted). Verified: HGL PSAT class-info serves the three at-HGL tiers, Cape Town class still serves intl tiers, no school_id/delivery_mode leak in the public payload.

## PL-308 — Payment-success page: Scarlett's copy, student name composed (Scarlett, Aug 8, exact strings) ✅ SHIPPED Aug 8
Title: **"Registration and Payment Successful!"** Body:
> Your registration is officially confirmed. We have sent a confirmation receipt to your email, and {studentFirstName} has been added to the class roster.
>
> Class details will arrive in the days before the first session. We're looking forward to having {studentFirstName} in class!
Compose the real student first name. Checkout is one-for-everyone: with multiple students, compose naturally ("Bunji and Maya have been added to the class roster" / "…having Bunji and Maya in class") — no dangling singular. Keep the "Back to Higher Ground Learning" button.

**Ship note:** the page is now a server component reading ?session_id → enrollments by stripe_session_id (stamped by checkout BEFORE the redirect, so no webhook race — names are ready the instant the parent lands). Scarlett's exact strings verbatim; names compose naturally with verb agreement ("Reggie has been added" / "Bunji and Bunjibrother have been added … looking forward to having Bunji and Bunjibrother in class!"; three-plus students get the serial-comma form). No-match falls back: ?already_paid=1 gets "You're all set!" (already-paid revisits), anything else (add-on hours purchases land here too, their session id lives on enrollment_addons) gets generic "Payment Successful!" — never a wrong roster claim. The "Back to Higher Ground Learning" button stays. Verified in the browser: sibling checkout renders both names, single renders singular, both fallbacks render.
