# Portal fixes — batch 35 (ACCUMULATING — opened Aug 13, 2026)

**Standing rules:** plain-English statuses · no internal shorthand · every alert deep-links its record + click-through to a resolution surface · samples from composers · `git push` after committing · PL-x IDs in commits · check items off here when shipped · registry template edits ship as new versions via anchor guards, twins+seeds lockstep · verify composed blocks via the composer path · inline confirm banners only · NO native browser dialogs · schedule-change baseline = what the family SAW at registration · financial facts never render into marketing copy · two-phase column drops incl. post-drop grep · **TEST-SEND RULE (Scarlett, Aug 8): while we're pre-launch, every test/QA email goes to billy@ (or billy+*@) ONLY — never to info@, kelsie@, or any other real @highergroundlearning.com inbox.** That includes alert-subscription fan-outs (PL-309).

## ✅ PL-339 — Calendar blocks show start–end ranges (Scarlett, Aug 13)
Blocks on the scheduling calendar render only the start time ("4:00 PM Roman / French"). Show the full range: "4:00–5:30 PM Roman / French" (en dash; drop the leading meridiem when both ends share it — "4:00–5:30 PM", "11:00 AM–12:30 PM"). Applies to session blocks, Google busy blocks where a range is known, AND the PL-337 proposed blocks ("12:00–1:30 PM proposed"). Blocks too short to fit two lines keep the range and truncate the label, not the other way around — the time is the point of a calendar.
**Scope widened (Scarlett, Aug 13): end times EVERYWHERE, not just calendar blocks.** Anywhere a time renders start-only or start+duration, show the range: the proposal panel's slot rows ("Wed 8:00–9:00 AM" instead of "8:00 AM · 60 min"), the wizard's suggested-time chips and slot rows, roster/profile session lists, and email-land where a session time is quoted (composer-path check). Duration can stay as a secondary note where useful; the range is primary.

**Shipped:** THREE leaf helpers in dates.ts (`timeRangeLabel` = the shared-meridiem elision, `formatTimeRange` for instants+zone — a null end degrades to start-only, never lies — and `hhmmRange` for wall-clock start+duration), unit-proven 8/8 incl. AM/PM boundary and midnight-cross. **Grid:** session blocks read "4:00–5:30 PM Roman" with the time nowrap FIRST so a short block truncates the NAME (the tooltip carries the range too); Google busy blocks show the range when the event sits fully inside the day (all-day/clamped multi-day spans stay label-only — their geometry says it, and a fake midnight range would lie); PL-337 proposed blocks read "12:00–1:30 PM proposed"; the proposal panel rows read "4:00–5:30 PM · 90 min" (duration secondary). **Wizard:** suggested-time chips ("Tue 4:00–5:30 PM + Thu …"), every slot row shows "= 4:00–5:30 PM" beside its pickers, outside-availability warnings and the conflict list both range (the conflicting occurrence AND the busy block). **Lists:** engagements panel recurrence ("Tue 4:00–5:30 PM", was raw "Tue 16:00 (90m)") + "next:" (query gained ends_at), drafts-card summaries, family-profile "Next:" (end derived from duration), portal engagement cards ("Tuesdays 4:00–5:30 PM" + Next), the parent reschedule OFFER buttons, and the admin activity feed's moved/asked lines. **Email-land (composer path):** T3_RESCHEDULE_ACK's `{sessionWhenPhrase}` is now a range (var description + sample updated — value-shape change, no template version needed), T3 change lines (moved/no-show/forfeit ×2 sides), all six coverage sends (`fmtWhen` gained the end), block-continuation reserved times, T6 daily + weekly note-nudge lists, and the T3-T tutor notice — `TutorChange`/`CollapsedDelta` gained OPTIONAL end instants (writers thread them; rows stored before the fields render start-only rather than wrongly). Deliberately left: drift-alert lines (they compare start instants — a range would muddy the delta), month-mode calendar chips (too compact), and already-ranged surfaces (T1/intake schedule blocks, schedule-approval summary, portal upcoming list). Gates: links 129, alert-pins 18 (coverage pins are COMPUTED from the composer, so they moved with it), coverage-note 18 — all green.

## ✅ PL-340 — Drag-to-propose: show the drag while it happens (Scarlett, Aug 13)
Dragging on the grid gives no feedback until mouse-up — the proposed block only appears on release. Render the rubber-band live: on mousedown+move, draw the forming block under the cursor (same dashed proposed styling, time range updating as it stretches — "8:00–9:15 AM" per PL-339), snapping to the 15-min grid as it goes. Escape or dragging back to zero height cancels. Same live feedback for move and resize of existing proposed blocks.

**Shipped:** ROOT CAUSE was one guard — the live echo existed since PL-337 but `proposalBlocksForDay` returned `[]` while the proposal list was still empty, so the FIRST drag (the one that matters) showed nothing until mouse-up. The rubber band now renders from the first pointermove: dashed proposed styling, 15-min snapped, range label live per PL-339, and it inherits the in-place conflict flagging (a band over busy shading goes red as you drag). Move and resize previews were already live and stay so. Escape cancels any in-flight drag (forming band vanishes; a move/resize snaps back to the stored time); a sub-15-minute release still creates nothing. Verified live in the browser: mid-drag read "9:00–10:15 AM proposed" on a first drag, Escape wiped it, no block created.

## ✅ PL-341 — "Use this schedule" ADDS slots to the wizard, never replaces (Scarlett, Aug 13)
Real students mostly come 2+×/week, and the natural flow is: propose Monday → Use this schedule → back to the calendar for Wednesday → Use this schedule again. Today the second use REPLACES the wizard's weekly slots. Instead: MERGE into 4 · Weekly schedule — append the new slot rows, keep existing ones, dedupe exact duplicates (same day+time+length), bump Sessions-per-week to match the row count, and re-run the wizard's own validation. If the proposal's tutor differs from the wizard's already-chosen tutor, that's a real conflict — inline banner asking which tutor wins (never silently swap; picker-parity vetoes re-run either way). The proposal panel keeps supporting multi-day proposals directly (drag two days before Use), so both paths land in the same merged state.

**Shipped:** the wizard's proposal adoption forks on content: an EMPTY wizard gets the plain prefill exactly as before; a wizard with anything on it (slots, student, subject, tutor, notes) MERGES — new slot rows append, existing rows stay, exact duplicates (same day+time+length) dedupe, Sessions-per-week bumps to the merged row count, and a plain-English note says what happened ("Added 1 weekly time from the calendar proposal — existing rows kept." / "Those weekly times are already on the schedule — nothing duplicated."). Saved-draft RESUME still replaces (resuming means restoring), and merging a proposal onto a resumed draft keeps updating that draft. Different tutors = an inline amber banner at step 3 ("drawn against X's calendar, but Y is picked — which tutor should this schedule use?" with Use-X / Keep-Y buttons); never a silent swap, and either answer re-runs the conflict checks because the busy fetch + warnings already follow the tutor select. Validation (slot clashes, availability warnings, PL-168 tally) recomputes from the merged rows on its own. Verified live: Mon drag → Use → Wed drag → Use = two rows + Sessions-per-week 2; identical Wed → Use = dedupe note, still two rows. The tutor-conflict banner wasn't exercisable live (one active tutor pre-launch) — logic + tsc reviewed.

## ✅ PL-342 — Instructors table: the Actions column must be visibly there (Scarlett, Aug 13, screenshot)
On Contacts → Instructors, the rightmost column(s) fall outside the visible card — the EMAIL PREFS summary wraps to five skinny lines ("notes on · digests off · FYI off") and whatever sits right of it (Actions) is pushed out of view with no cue it exists. Fix the layout so every column including Actions is plainly visible at normal widths: compact the prefs summary to one line (short labels or icon-style chips w/ tooltip; the full three-control detail lives in the profile editor anyway per PL-327), tighten Name/Phone/Meeting-link widths ("—"/"none" columns shouldn't be that wide), and if horizontal squeeze is still needed prefer dropping the standalone width hogs into the row (e.g. meeting link under the email) over hiding a column. Same 1280px probe as the PL-242 overflow audit; check the collapsed-section audit didn't leave other tables with the same off-card tail.

**Shipped:** Contacts → Instructors is five columns that fit: the default meeting link now rides UNDER the email (truncated, full link in the tooltip — the widest column gone), Phone/Name are `whitespace-nowrap`, and the prefs summary is ONE line of compact chips ("notes on · digests off · FYI off" as pills, off-state grayed; the long mandatory-emails explanation lives in the tooltip; full controls stay in the profile editor per PL-327). Actions was already nowrap — it's simply on-card now. Sweep: every `min-w-full` table missing a scroll container got one — the QBO sync log (6 cols), the 1-on-1 Tutors table (7 cols incl. Matching notes), and the Timecards table; Campaigns' recipient preview box went `overflow-y` → `overflow-auto`. (`w-full` tables — leads, agreements — squeeze instead of overflowing, left alone.) 1280px probe in the browser pass below.

## ✅ PL-343 — Who-could-teach view: calendar controls sit next to the calendar (Scarlett, Aug 13, screenshot)
On the read-only Calendar page in who's-free-to-teach mode, the controls card (Week/Month, ‹ today ›, everyone / all-schools / all-statuses filters) renders ABOVE the tall "Who could teach {class}?" suggestions panel — so paging the calendar means scrolling past 20 instructor rows every time. When the suggestions panel is present, move the controls card to sit directly BELOW it / immediately above the grid (the suggestions panel keeps its own minimize link and the jump-to-class-dates link at top). Normal calendar mode (no suggestions panel) keeps the current order. While in there: the "Week of 2026-11-09" heading is a raw ISO date — plain-English it ("Week of November 9, 2026"), same family as PL-330/335E.

**Shipped:** the controls card is ONE JSX const with two slots — `?suggest={classId}` mode renders it directly below the suggestions panel (and below the minimized stub), immediately above the grid; normal mode keeps the exact current order. Suggestions panel keeps its minimize link and jump-to-class-dates. Heading is now `Week of {formatDateOnly(...)}` → "Week of November 9, 2026" (month mode was already plain-English).

## ✅ PL-344 — Recent Activity: bounded window + "Show earlier" (Scarlett, Aug 13)
Today the feed is a flat most-recent-40 with no time bound (April rows still showing). Change to: show the most recent 20 items OR this calendar month's items, whichever is SMALLER — except when that yields a near-empty panel (< 5 items this month), extend into the previous month so the feed never looks dead on a quiet month. "Show earlier activity" at the bottom pages further back (server-paged, keeps the PL-134 same-day grouping and type filters working across pages; filters apply to the whole history, not just the loaded page). Nothing is deleted — the feed stays derived and read-only; this is a display window, not retention.

**Shipped:** the whole feed now comes from ONE builder, `utils/dashboard-activity.ts` `loadActivity({before, limit, type})` — the route's seven inline activity sources (registrations, payments, availability, timecards, new + converted leads, PL-191 schedule events) moved there verbatim, each paging on its own clock column with a shared `before` cursor (engagement rows carry two clocks, so the cursor re-applies per produced row), fetch limit+1 for an honest `hasMore`. Page one applies the window in the dashboard route: Denver-month match, min(20, this month), and a `< 5`-row month extends into the previous month so a quiet month never reads dead; the old flat most-recent-40 (April rows included) is gone. New `GET /api/admin/dashboard/activity?before&type&limit` (staff-gated) serves history; a `type` param makes a filtered page a page OF that type (each source maps to exactly one chip type, so non-matching sources are skipped server-side — filters walk the whole history). Client: "Show earlier activity" appends into a separate `earlier` list (the 60s refresh only replaces page one), combined feed dedupes by id, PL-134 grouping + chips run over the combined list. The sweep-recovery System line stays route-side (it's a live note, ≤ 7 days old by construction). Display window only — zero deletions, zero schema changes.

## PL-345 — Dashboard Reports snapshot card (Scarlett, Aug 13, screenshot)
The term report is hiding behind a bare text link under the tiles. Replace it with a proper "This term at a glance" snapshot CARD in the tile row: a few headline numbers plus a prominent "See enrollment & revenue reports →" button to the full report page.
**Snapshot contents (role-aware — reuse the PL-204 machinery, don't recompute):** enrollments this term (paid count, + this month's delta), active classes (live/scheduled count), tutoring hours delivered this month, and — ADMIN ONLY — revenue collected this term (classes + packages + paid tutoring invoices, same paid columns the term report reads) and projected next generation (PL-333 preview total). Managers get the enrollment-side numbers only — the manager payload never carries dollar fields (PL-204 stripRevenue rule, enforced server-side, not hidden in UI).
Every number deep-links to its surface (report page anchored to the right section; hours → timecards; projection → billing preview). Plain-English labels, no raw enums/ISO. Keep the card informational — actions stay on their own surfaces.

## PL-346 — Report page: "Revenue by service" breakdown w/ chart (Scarlett, Aug 13, TutorBird screenshot)
Add a Revenue-by-service section to the Revenue & enrollment page: table (service · percent · dollars · total row) + a proportional chart beside it, like the old TutorBird report. Slices = our real service lines: class enrollments (group classes), 1-on-1 monthly invoices, hours packages — with a secondary by-subject/class-type cut if cheap (the same filters up top apply: school / months / class type). Same PL-204 rules as everything on this page: computed from the SAME paid columns QBO sync reads (no new revenue math), ADMIN-ONLY — the whole section is stripped for managers server-side, date range from the existing month filters with the range stated plainly in the header. Chart: accessible palette + labels readable without color; percentages sum sanely with rounding; every slice/table row deep-links to its detail (classes table / invoices / packages). Read the dataviz guidance before building the chart.
