# Portal fixes — batch 56 (OPEN — Sep 24, 2026): cutover corrections + Scarlett's class-history / findability batch

**Batch closed Sep 24 — 6 of 6 shipped (PL-499 · 500 · 501 · 502 · 503 · 504).** Standing pre-flight before the gates that write QA rows: `NODE_USE_ENV_PROXY=1 node scripts/project-sends.mjs --hours 48` → **0 projected actions**. Gate battery vs the PROD build on :3100: `tsc` · `next build` · `smoke:public` (114 — the /team circle assertion swapped in) · `regress:cta-landings` (232) · `regress:canonical-hosts` (**79**, unchanged) · **`regress:class-groups` (20, NEW)** · **`regress:school-classes` (11, NEW)** · **`regress:import-past-scores` (20, NEW)**. **No migration** (student_scores needed neither a source value nor an index; see the PL-504 departure for why the enrollment carries `source='import'`). No DNS / Vercel domain / env var touched; the tree's one uncommitted line went out under PL-499. QA fixtures self-cleaned (0 leftovers checked). Three lint errors on `app/admin/page.tsx` (setState-in-effect) pre-date this batch — the committed file carries six. **Decisions / departures recorded in the notes:** PL-500 circle measured on the main site's image WRAPPER (border-radius 50%); PL-503 cancellation note is COMPOSED (no stored free text); PL-504 enrollment source = `import` + `sheet_import:<by>` provenance (CHECK constraint), sheet attendance = a count on the enrollment note (no per-session records to build); the browser pane could not render /admin on the local prod build ("This page couldn't load", no server error) — the admin walk was verified in headless Chrome with a staff session instead.

**Standing rules:** all prior. Next PL after this file: PL-506. **Do NOT touch DNS, Vercel domains or env vars** — the cutover is mid-flight (rows 1 · 7 · 2 · 3 · 9 · 10 · 12 · 13 · 14 · 15 · 16 DONE on prod, hgl.co row 8 still pending; `claude/hgl-cutover-progress-2026-09-23.md` in the project has the state). Working tree carries ONE uncommitted line from the cutover — commit it under PL-499. Cutover verifies still owed by Scarlett (not Code): row 12 Connect QuickBooks round-trip, row 14 one test registration.

## PL-499 — cutover doc corrections + the pg_cron BASE commit (docs only + 1 line)

### ✅ SHIPPED (Sep 24, e6a3c73) — the pg_cron BASE line committed (NOT re-run); host table: rows 13/14 corrected (edited in place — one secret per handler), rows 2/3 typed Config (NEXT_PUBLIC_APP_URL re-added Production + Preview), row 7 = the issued CNAME target `2a6a09ba301356ea.vercel-dns-017.com`, row 15 "at the next sweep (20:01 UTC)", rows 1 · 2 · 3 · 7 · 9 · 10 · 12 · 13 · 14 · 15 · 16 ticked DONE Sep 23 (rows 12/14 verifies noted as Scarlett's).

1. Commit `scripts/setup-pl273-pg-cron.mjs` with `BASE = 'https://portal.highergroundlearning.com'` (already changed in the tree and RUN against prod Sep 23 20:05 UTC — both jobs active; do not re-run).
2. `docs/dns-cutover-host-settings.md`: **row 13 + 14** — "keep the old endpoint" is wrong: the Resend and Stripe handlers each verify ONE secret (`RESEND_WEBHOOK_SECRET`, `STRIPE_WEBHOOK_SECRET`), so a second endpoint (own secret) fails signature checks. Both were **edited in place** (URL only; secret + events unchanged). Say so; the old-host emails don't care which endpoint receives events. **Row 2/3**: Vercel now refuses `NEXT_PUBLIC_*` vars typed Secret — record that `PRODUCTION_BASE_URL` and `NEXT_PUBLIC_APP_URL` are **Config** type (NEXT_PUBLIC_APP_URL was deleted + re-added as Config, Production + Preview). **Row 7**: actual CNAME target Vercel issued = `2a6a09ba301356ea.vercel-dns-017.com` (new-style; legacy `cname.vercel-dns.com` still valid). **Row 15**: healed at the first sweep after row 2 (20:01 UTC), not "within the hour" — say "at the next sweep".
3. Tick rows 1 · 2 · 3 · 7 · 9 · 10 · 12 · 13 · 14 · 15 · 16 DONE (Sep 23) in the table's When column.

## PL-500 — /team instructor portraits: circular

### ✅ SHIPPED (Sep 24) — `rounded-full` on the same 307px `aspect-square object-cover` box; the initials placeholder is round too. Reference measured by DOM on https://www.highergroundlearning.com/team: each portrait's image wrapper carries `border-radius: 50%` (the images themselves lazy-load at 0×0 in a headless read, so the circle is the wrapper's — the same rule we apply). The class page's instructor block was already `w-24 h-24 rounded-full object-cover` — same classes, nothing to change there. Smoke: the /team assertion now reads the computed radius (≥ half the box; 9999px on a 307px tile) and object-fit.

**Amendment (Scarlett, Sep 24 — "space them out like highergroundlearning.com/team"):** measured on the main site at 1280 (headless DOM, images scrolled into layout): a **1096px-wide row of three 244px circles with 182px between them** (first circle at x=92), the name **~26px under the circle at 20px serif regular**, role and bio at **15px** body. Ours were 307px circles 32px apart. Now: `max-w-[1136px]` (1096 + the two 20px gutters), `lg:gap-x-[182px]`, each circle fills its 244px column; name `mt-6` at 20px (Source Serif 4 at 600 — the face loads 600/700 only; 400 would render faux), role/bio 15px; `gap-y-24` between rows; tablet/phone keep proportionate air. Measured on our prod build at 1280: **244px circles at x=92, gaps 182 · 182, name 24px under** — the main site's numbers. The bio stays behind the tap-to-expand (PL-358's decision — the main site shows every bio in full, which is a one-line change if Scarlett wants the row rhythm to match too). Smoke gains the spacing assertion (three 244±6px circles, 182±6px apart) → **115**.

Batch 54 (PL-493) made the /team portraits 307px squares to match the main site's measurements; Scarlett wants them **circular, matching the main site's Team page** (https://www.highergroundlearning.com/team — the reference; measure the circle there via DOM, not remembered values): `rounded-full` on the same 307px box, object-cover crop centred on the face, and the placeholder-initials tile also round. If the portal already renders instructor photos circular elsewhere (tutor portal / admin), reuse that class rather than a new one. Same on the class page's instructor block if it shows a portrait. Gate: `smoke:public` /team assertion updated.

## PL-501 — admin Classes list: findable at 13+ past classes

### ✅ SHIPPED (Sep 24) — grouped · searchable · sortable; the rules are a pure module the gate runs

- `app/utils/class-groups.ts` (pure): `classGroup()` — cancelled (status, whatever the dates) · ended (last session / start date before today) · open (status open, close date today or later) · in-progress (first session on or before today) · upcoming (the rest — first session ahead with registration closed early, or a non-open status); `matchesSearch()` token-AND over nickname · school name · code · slug · class type · short name · course key · instructor FIRST name · term (`classTerm()` = the slug's trailing spring/summer/fall/winter token) · "online"; `sortClasses()` newest / oldest / A–Z by school; `groupClasses()` composes the five groups with counts.
- The page: the per-class tabs + the single "Past & cancelled" bucket are gone. Five headers with counts ("Ended (13)"; "2 of 13" while searching), the three live groups open, Ended/Cancelled collapsed — remembered per admin in `localStorage` `hgl-admin-class-groups` (try/catch'd); a search auto-expands the groups with hits; a sort select per group (shown when open with > 1 row); a deep link (`?class=`) expands whichever group holds the class and lands on `#class-card-{id}`. Each card keeps every action and gains a header line with the 72px `SchoolTile` (logo / accent monogram; the HGL mark for open classes) + composed label + dates + instructor. `schools` now selects `evergreen_code, logo_url, accent_color` for this.
- Gate `regress:class-groups` (compile-and-call, 20 synthetic classes, 4 per state): order, counts, a cancelled class with past dates never under Ended, live/cold flags, search on every field incl. token-AND and cross-group term hits, the three sorts, per-group sort isolation — 20 checks.

Today the admin Classes panel is one flat list; with the PL-476 backfill it holds 4 live + 13 past/cancelled + the new ones and it's hard to find a class. Build:
1. **Grouped headers, collapsed by default for the cold groups:** Open for registration · In progress · Upcoming (no registration yet) · Ended · Cancelled. Counts in the header ("Ended (13)"). Live groups expanded, Ended/Cancelled collapsed, state remembered per admin in localStorage.
2. **Search box** above the groups (matches school nickname, school name, code, slug, class type, instructor first name, term like "fall26"); filters across all groups and auto-expands the groups with hits.
3. **Sort control** per group: start date newest-first (default), oldest-first, A–Z by school. 
4. Each row keeps its existing actions; add the school logo tile (the 72px one from PL-490) so scanning is visual.
Gate: a regress that seeds 20 classes across the five states and asserts grouping, counts, search across every matched field, and that a cancelled class never appears under Ended.

## PL-502 — the "long" class URLs: label them honestly (no routing change)

### ✅ SHIPPED (Sep 24) — labels only, routing untouched

- Class detail (the expandable setup section): "Registration URL slug" → **"Permanent link (slug)"**, the help line beneath: *"Every class keeps this link. Families and print use the school code (hgl.co/{code}), which always points at the school's current class."* (the edit warning kept).
- The card's link block: when the school/course code serves THIS class (the shortlinks panel's own `/api/admin/evergreen` serving fact, PL-436/496) → **School code** first with a green **"now showing at hgl.co/{code}"** pill, then **Permanent link** (`{origin}/c/{slug}`, full URL shown, its own Copy). When the code has moved on → the permanent link only, with *"hgl.co/{code} now shows {nickname} {serving label} — this class is reachable only here"*. Slug generation unchanged. The wizard's "Printed hgl.co link" help is about the printed link and stays.

**Why Scarlett sees `/c/sis-sat-prep-spring25` and "Registration URL slug: sls-sat-prep-fall26" instead of `/sis`:** the **code** (`/sis`) is the school's evergreen address — it always serves whichever class at that school is current (open / in progress / most recently ended, per the PL-384 serving rules), so a *past* class can't be reached through it once a newer class exists. The **slug** (`/c/{slug}`) is the one permanent address every class keeps, which is why old classes only show that one. It doesn't "forward to /sis" — the code page and the slug page are the same renderer; for the *current* class they happen to show the same thing. So the routing is right; the admin labelling isn't:
1. In the class editor / wizard / class detail: rename "Registration URL slug" → **"Permanent link"** with the full URL shown (`https://portal.highergroundlearning.com/c/{slug}`) and one line of help: "Every class keeps this link. Families and print use the school code (`hgl.co/{code}`), which always points at the school's current class."
2. Show the **code URL first** wherever the class is currently the one the code serves (with a "now showing at hgl.co/{code}" pill — the Short links panel already computes this via the public serving function, PL-497 — reuse it), and the permanent link second. For classes the code no longer serves, show only the permanent link and say why ("hgl.co/sis now shows SIS SAT Prep Fall 26").
3. Slug generation stays as is (school-type-term).

## PL-503 — Contacts → Schools: class history per school

### ✅ SHIPPED (Sep 24) — `SchoolClasses` on every school card (Contacts → Schools) + `/api/admin/school-classes`

- Rows: every class with that `school_id`, newest first — term · type · dates · instructor · paid/enrolled · a group chip (the PL-501 rule). Expand → roster (student, parent email, payment, comms state), scores per student (diagnostic / final / Δ / attendance %) with the class average, average improvement and n, an attendance line (average % and how many tracked), and the two links (class detail `/admin?class=`, class report `/class-report/{id}`). **Reuses `loadClassReport()`** — the PL-219 loader — for scores + attendance; only the roster's parent email / comms columns are a new read.
- A cancelled class shows a **cancellation note instead of scores** (`report: null`, no report link). Departure recorded: the cancel route stores NO free-text note, so the note is composed from what it did record per enrollment — *"Cancelled with N on the roster — n refunded, …"* (or *"Cancelled before anyone registered."*).
- Gate `regress:school-classes` (prod build, staff session): one QA school × three classes (ended with 2 scored + attended students, in progress, cancelled with a refunded roster) — 11 checks incl. 403 signed-out, order, row facts, the expansion payload (class average 1100 → 1200, +100, attendance 75% over 2), links, the cancellation note, the empty in-progress case. Self-cleaning.

On each school's page (admin Contacts → Schools → school), add a **Classes** section: every class ever run at that school, newest first, one row each (term · type · dates · instructor · enrolled/paid count · status). Each row expands to: roster (name, parent email, paid/refunded, comms state), scores (diagnostic 1 / 2 / final per student from `student_scores`, with the class average and average improvement), attendance summary when session attendance exists (PL-103 timecards / roster attendance — whatever the class report already reads), and a link to the PL-219 class report + the class detail. Reuse the class-report's data loaders rather than new queries. This is the "one place to look up what happened at school X" view for calls with school contacts. Gate: regress with one school × three classes asserting rows, expansion payload and that a cancelled class shows its cancellation note instead of scores.

## PL-504 — historical roster + score import from the Google Sheets, and an all-time results view

### ✅ SHIPPED (Sep 24) — `scripts/import-past-scores.mjs` + `/admin/results` (+ `/api/admin/results`, `app/utils/results-aggregate.ts`); **no migration**

- **Importer:** CSV (columns in the script header — Claude cleans each Sheet export into it, the `.tmp-import/` pattern) → dry-run projector by default (`--json` for the gate), `--apply` only after Scarlett reads it. Class by slug, or by school code when the school has exactly one class (else refused "ambiguous — use the slug"); unknown class → REFUSED, listed. Student: student email → parent email + name → NEW family + student through the one path (`upsertFamilyAndStudent`, compile-and-call). Enrollment: `Completed` (a finished class), `comms_muted`, `enrolled_at` = D1 date, note "Imported from the historical Google Sheet (PL-504). Attended N sessions (sheet count)." Scores: Diagnostic 1 / Diagnostic 2 / Final with `section_scores` {Reading & Writing, Math}, total (composed from the sections when blank), `taken_at`, `source='sheet_import'`, `recorded_by`. Idempotent (existing enrollment kept; existing student+class+label skipped; in-file duplicates planned once). **Departure:** `enrollments.source` is CHECK-constrained to `staff | import` (PL-361), so the enrollment carries `source='import'` (the PL-363 historical-import value — same meaning) with the sheet provenance in `source_recorded_by='sheet_import:<by>'` + the note; `student_scores.source='sheet_import'` as specified (free text — no migration needed, and the class_id index already exists). Silent by construction: the script never composes an email, and a class whose paid rows are all comms-muted imports reads records-only to every sweep (PL-471).
- **Results:** `aggregateResults()` (pure, shared by the API, the CSV and the gate) pairs each student+class's earliest test with its latest different test (the class report's rule) → all-time / per school / per term / per class buckets: scored, with-final, improved + %, avg diagnostic, avg final, avg Δ total and per section. `/admin/results` (Classes sidebar → Results): the **marketing block** — average improvement, % improved, students served — each with its n and date range; a bar per class (chronological) and a line across terms (inline SVG, no dependency); the three tables; **Export CSV** (`?csv=1`). Cancelled classes are excluded from the aggregate.
- Gate `regress:import-past-scores` (prod build): the 6-row fixture (2 existing — one by student email, one by parent email + name — 2 new, 1 unknown class refused, 1 duplicate row) → dry run writes nothing; apply creates 2 families / 2 students / 4 enrollments / 9 scores with the right provenance; a second apply is idempotent (0 / 0 / 0 / 0, 12 skipped); the Results API shows the class at n=4, +87.5, 100% improved with per-section Δ, in the school + term buckets and the marketing block; CSV export; 403 signed-out; the page renders — 20 checks. Self-cleaning.

**What exists:** `student_scores` (PL-37; manual entry, `source` = manual | synap_csv | synap_api) and the per-class PL-219 report. **What does NOT exist:** any all-time aggregate — there is no "internal score data and graphs" beyond per-class reports, and the 13 backfilled past classes carry NO rosters or scores (PL-476 backfilled dates, logos, calendar images only). So marketing stats need the import first.
1. `scripts/import-past-scores.mjs` — CSV in (one row per student: class code/slug, student name, parent email, student email, diagnostic 1 R&W/Math/total + date, diagnostic 2 …, final …, attendance count if present) → dry run projector listing: matched existing student (by student email, then parent email + name), new family/student to create (silent, `comms_muted`, tagged `source=sheet_import`), `student_scores` rows to insert with `source='sheet_import'`. `--apply` only after Scarlett reads the projection. Refuse rows whose class isn't found. Scarlett exports each Google Sheet to CSV; Claude cleans them into the column layout (same pattern as `scripts/.tmp-import/`).
2. Admin **Results** page (`/admin/results`): all-time and per-school / per-class / per-term averages — diagnostic → final improvement (R&W, Math, total), % of students who improved, n, and the same as a simple chart (bar per class, line for the all-time trend). One "marketing block" at the top: the three numbers Billy would put on the website (average improvement, % improved, students served), each with its n and date range so they're defensible. Export CSV.
Gate: importer regress on a 6-row fixture (2 existing students, 2 new, 1 unknown class refused, 1 duplicate score idempotent); results page regress on the same seed.

## Not for Code — pilot plan for 1-on-1 tutoring with Kelsie (Claude + Scarlett)
Nothing in the tutoring build is blocking a pilot — the whole journey (intake → availability → wizard → agreement → autopay → sessions → notes → monthly T1/T2) was QA-walked in July and the timecard rule is fixed (PL-471). What gates a **real-money** pilot is the locked launch order: **QBO production** (cutover step 6a — Intuit prod keys exist; row 12 now carries the final host) → **Stripe live** (6b — live webhook endpoint + live keys; the row-14 test endpoint stays for test mode) → then families. The **QBO customer import** is `scripts/import-qbo-families.mjs` (PL-34): it pulls Customers from whichever company the portal is connected to, creates one family per customer email that has no family row, dry-run by default — so it runs right after QBO production connects, and Kelsie adds students per family afterwards. Suggested pilot: one tutor (Billy or Kevin — real `offer_windows` set), 2–3 families who know HGL, on live Stripe with real small invoices, one monthly cycle (generation run → T1 confirm → T2 → autopay) before opening it up. Alternatively a no-money pilot can start any time on the current sandbox/test keys with a QA family — but that repeats July's QA walk rather than proving the money path, which is the part not yet exercised with real people.
