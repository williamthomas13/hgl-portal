# Portal fixes — batch 45 (ACCUMULATING — opened Aug 31, 2026)

**Standing rules:** all prior (see batch 41/42 headers).

(CLOSED for batch 45 hand-off, Aug 31. Next PL: PL-439.)

**Batch closed Aug 31 — the TASK + all 8 items (PL-431–438) shipped**, per-item ✅ notes below. Gate battery green at close: `tsc` · `next build` · `smoke:public` · `regress:links` · `regress:pronouns` · `regress:client-imports` · `regress:alert-pins` · `regress:alert-categories` · `regress-cancel-class` (11) · `regress-resume-addon` (13) · the extended `pl402-drift-alert-e2e` (12) — smoke + the two E2E gates run against the PROD build on :3100, pass COUNTS confirmed (crashed gate ≠ green gate). **Migrations `20260914000001` (PL-437 drift alert ledger) · `…02` (PL-435 default_location_source) — both APPLIED via the Management API; nothing pending for Scarlett.** Doc-premise corrections flagged inline: the stale PL-431 migrate-to-Stripe amendment paragraph is SUPERSEDED by the REVISED item (not implemented); PL-437's identity-churn suspicion DISPROVED in the data (the real defect was mortal alert memory + two environments racing one calendar). Real outcomes left standing deliberately: MIS SAT Prep now carries Billy's default meeting link with 'instructor_default' provenance (the PL-435 fix applied to the live case), and its 4 real assignment conflicts sit in Needs Attention for Scarlett to resolve through the new PL-434 surfaces. Drafts awaiting Scarlett's ramp (review sends in her inbox): `AL_AVAILABILITY_UPDATED` v1 · `AL_COLLATERAL_NUDGE` v1.

## ✅ TASK — draft-review test-sends (Scarlett's ramp) — SENT Aug 31 → billy@
Both rendered from the ACTUAL DRAFT VERSION bodies (`renderVersion` — the live gate would have swapped in the code twins) with contexts/extras mirrored LINE-FOR-LINE from the production composers (never editor samples): **`[DRAFT REVIEW: AL_AVAILABILITY_UPDATED]`** — real composed diff via the `availability-diff.ts` leaf on the Janeson fixture (Monday unchanged · Tuesday ADDED · Sunday REMOVED), real parent/student names, the real `?availability=` review link ("Janie updated Janeson Foney Fony's availability"); **`[DRAFT REVIEW: AL_COLLATERAL_NUDGE]`** — the skipped-collateral ISD fixture with the real `/admin?collateral=` deep link. Both `sent`. Flip live in the editor when approved — the sweeps are already wired and send twin-identical copy meanwhile.

## ✅ PL-431 — SHIPPED (Aug 31) — per the REVISED item: the server-side validation is UNTOUCHED; UX only. **DOC-PREMISE NOTE: the stale "PL-431 amendment — found reality" paragraph below (migrate to Stripe promotion codes) is SUPERSEDED by this REVISED item and was NOT implemented** — the portal-side scoping (class + cohort + class-component-only) is exactly what a Stripe migration would weaken.
**A. De-cluttered AND relocated (the allowed option):** the field moved to the add-on/confirm page — the page whose action prices the class component and the one place a refusal can be fixed inline — as a plain "Discount code" field (label suffix, DEEPDIVE50 placeholder, and the we'll-check-later explainer all gone; page 1 carries no discount clutter at all). Validation still runs server-side at checkout, BEFORE session creation, byte-for-byte the same seam. **B. The dead end is dead:** refusals return `discountError: true` and render INLINE at the field in red, plainly worded per the verdict — "That code doesn't match — check the spelling in your email." / "That code isn't for this class — check the class name in the email it came from…" (NEW wording for a typed code on a class with no live offer — the old generic was a dead end) / "This discount ended {date}…" (existing, now continuable) — each with **"remove the code and continue at full price"** (sets skip-discount; checkout proceeds clean). The full-stop "Checkout error:" state is gone, and the page-2 message block now colors errors red (it rendered refusals as calm blue). A token-path refusal gets the same continue-at-full-price affordance. **C. Stripe's own promo box is OFF on all three family session-creation sites** (checkout, resume-payment, waitlist-claim — it rendered but accepted nothing; one discount entry, ours). PL-280's segment chip keeps its source (`promo_code_used` stamps only on our validated path — verified). **Also fixed: the guaranteed-dead-end field** — `promoAvailable` now mirrors redeemability (open-enrollment target + complete promo); a school class's promo fields feed flyer copy, not this seam, and used to render a field whose every submission refused. **D.** Token auto-apply untouched (the field stays hidden for auto-applied visitors; the skip flag never resurrects it). **E2E (10/10 + the expired verdict en passant):** valid code → Stripe session line items show the class line at $299 (=$349−$50) NAMED with the code, the tutoring add-on line untouched, `allow_promotion_codes` false on the live session; wrong code → inline spelling verdict; code on a different class → the different-class reason; `promo_code_used` stamped. UI verified at phone height: no page-1 field, plain page-2 field, inline red refusal + working clear-and-continue, no full-stop state. Fixtures restored.

## PL-431 (original) — Discount code UX: keep the settled server-side validation, fix the dead end (Scarlett, Aug 31 — REVISED after checking the settled design)
**Settled design stays (PL-279/280, confirmed correct):** codes validate SERVER-SIDE against the specific class + the family's own cohort, apply to the CLASS component only (never tutoring hours or physical add-ons), and refuse rather than silently charge full price. This is exactly the scoping Scarlett requires (an MIS early-reg code can never work on ASF or 1-on-1) — do NOT migrate codes to Stripe promotion codes; that would weaken the scoping. Fixes are UX only:
**A. De-clutter the field:** drop "(optional — from our email)", the DEEPDIVE50 placeholder, and the we'll-check-later explainer — a plain "Discount code" field (still on the page where the class component is priced, since that's what it discounts; if moving it to the final confirm page reads better, fine — but it must validate BEFORE session creation as today).
**B. Kill the dead end:** an invalid/inapplicable code NEVER strands the family — inline error at the field, plainly worded per the verdict ("this code is for a different class" / "this code has expired" / "code not recognized"), with the flow fully continuable: fix the code or clear it and proceed at full price. The "Checkout error: This class has no discount offer right now" full-stop state is removed.
**C. Stripe's own "Add promotion code" box is DISABLED on our sessions** (allow_promotion_codes off / omitted) — it currently renders but accepts nothing (portal codes aren't Stripe codes, per the PL-280 note), which misleads. One discount entry, ours, correctly scoped.
**D.** Follow-on cohort discounts that arrive by token keep auto-applying exactly as today. Verify E2E: valid code applies to the class line only (tutoring/add-on totals untouched); each invalid verdict shows its inline reason and the family can proceed; MIS code refused on an ASF fixture class with the different-class reason; Stripe page shows no promo box.

## ✅ PL-432 — SHIPPED (Aug 31)
The flow has a real "← Back to your details" on the add-on page. Mechanics: the page gate became explicit view state (page 2 used to render purely on `pendingCheckout`, making Back structurally impossible); page-1 inputs are uncontrolled, so submit snapshots every answer and Back remounts the form with the snapshot as defaultValues — **everything returns verbatim** (parent block, per-student fields, pronouns, notes; verified live incl. a notes round trip). Forward re-arrives with page-2 state intact (picks/quantities only reset on a fresh journey), and re-submitting after a Back is SAFE — `/api/register` reuses live enrollments (verified: exactly ONE enrollment after the full Back/forward trip). **Browser back/forward behave identically** (history.pushState on entering the add-on page + a popstate handler — verified both directions), and composes with PL-427: both transitions land at the top, heading focused, reduced-motion aware (verified at phone height). **State choice, noted per the item:** in-memory ONLY — the snapshot holds names and emails (PII), so neither localStorage nor sessionStorage; honest limit: a mid-flow REFRESH starts the journey over on a blank page 1 (the Pending enrollments it already created are reused on the next submit, so no double-booking) — stated plainly rather than half-solved with stored PII.

## PL-432 (original) — Registration flow: real Back buttons, state preserved (Scarlett, Aug 31)
Recovering from the discount dead-end took the BROWSER back button — which wiped page 1's form fields for re-entry. Give the flow its own Back: each page past the first gets a Back control (top or beside the primary action) returning to the previous page with EVERYTHING intact (fields, picks, notebook quantities, hours selection); forward again re-arrives with the later page's state intact too. State held client-side for the flow's life (in-memory/session — not localStorage with PII, note the choice); browser back/forward should behave identically where feasible (history state), and a mid-flow refresh should at minimum not lose page 1 on a normal journey — state the achieved behavior honestly in ship notes. Composes with PL-427's scroll-to-top (Back also lands at top). Verify: fill page 1 → page 2 → Back → everything intact → forward → page-2 picks intact; browser-back parity; phone.

**PL-431 amendment — found reality (verified in code, Aug 31):** today's codes are NOT Stripe promotion codes — /api/checkout/route.ts validates portal-side (PL-279) and creates the session at the pre-reduced price; the PL-280 comment says outright "Stripe-page promotion codes are invisible to us" (why the portal field exists, and why Stripe's Add-promotion-code box currently accepts nothing). So this is a MIGRATION: (1) create real Stripe coupons + promotion codes for every code the portal machinery can issue, minted/expired/extended in lockstep (the follow-up auto-extend sweep extends the Stripe code too); (2) enable allow_promotion_codes on the session and have the WEBHOOK read session.total_details/discounts to record amount_paid/class_price_paid/QBO true; (3) the PL-280 "used a promo code" segment chip re-points at the webhook-recorded Stripe discount (its portal-side source dies with the field); (4) deterministic follow-up cohort discounts still prefer server-side AUTO-APPLY (as a Stripe discount on the session, so the paper trail is uniform). Sandbox E2E covers each path's recorded money.

## ✅ PL-433 — SHIPPED (Aug 31)
Instructor-fit converged on the PL-388 pattern: titled `listBusyEvents` with the portal's own echoes skipped — `isPortalSyncedTutoringTitle` PLUS a new `isPortalSyncedClassTitle` sibling ("… — class session", the PL-79 convention), because the portal-side check already counts BOTH kinds precisely (class-session echoes double-counted identically). Genuine external busy survives and is NAMED when the calendar shares the title ("busy per Google Calendar: \"Dentist\" at Sep 7, 1:15 PM"), generic for private/untitled blocks (the block-confirm veto's own copy shape). On a titles-read failure the ranking stays honestly portal-only (`googleChecked=false` — the existing flag) rather than falling back to unfilterable raw freebusy and resurrecting the double-count. **Verified live on the REAL MIS fixture (Billy already assigned — Scarlett's own case):** five portal conflicts each listed ONCE with "(portal)", zero echo duplicates, and a planted external "Dentist" event named once (fixture removed after). **FINAL freeBusy sweep — every caller + verdict:** `instructor-fit` → CONVERGED (this item); `reschedule-offers` → CONVERGED TOO (same bug class, unreported: portal sessions vetoed offer slots twice, and a forfeited session's XCL- event shaded a genuinely free slot; the original-being-moved's own old slot now vetoes via its portal row explicitly — the intent its comment hung on the echo); `gcal/connect` DWD probe → LEGITIMATE (result discarded); `/api/gcal/freebusy` degraded fallback → LEGITIMATE (visual shading only, after a titles failure). **The helper cannot be retired** (two real callers) — the honest close-out is the verdict comment now sitting ON the helper: adding a third caller needs a written reason. This class of bug is closed.

## PL-433 (original) — "Who's free to teach it?" double-counts every conflict (Scarlett, Aug 31) — diagnosed: the one busy surface PL-388 missed
The instructor-fit checker (/api/admin/instructor-fit) still uses raw title-less `freeBusy`, so it cannot filter the portal's own synced events — each portal tutoring session appears TWICE ("tutoring — Janeson … (portal)" + "busy per Google Calendar at …", Scarlett's 8-conflicts screenshot = 4 real). Converge on the PL-388 pattern: titled `listBusyEvents` minus `isPortalSyncedTutoringTitle` echoes (portal sessions are already counted precisely by the portal-side check; genuine external Google busy stays). Conflict lines also improve while in there: name what the Google busy IS when the title is available ("busy per Google Calendar: 'Dentist' at Sep 8, 11:00 AM" — tutors' private titles: show only if the calendar shares them with our connection; otherwise keep the generic line), and the count reflects deduped reality ("conflicts with 4 sessions"). FINAL sweep: grep for any remaining raw `freeBusy(` caller — this was supposed to be converged in batch 41; list every caller + verdict so this class of bug is closed for good (retire the raw helper if nothing legitimate remains). Verify: the MIS assignment fixture shows 4 conflicts once each; an external busy event still appears; assignment confirm proceeds.

## ✅ PL-434 (+ amendment) — SHIPPED (Aug 31)
ONE conflict source (`instructor-conflicts.ts`: the per-class list + a batched counts variant sharing the same interval math and hold predicate) feeds every surface, so the count can never disagree. **A.** `assign-instructor` now returns the assignment's conflicts (computed AFTER the write — the override stays deliberate) and BOTH callers render the shared `AssignmentConflicts` prompt: the calendar suggester's success banner and the roster path's confirmation ("Billy Thomas is assigned." + the list; a conflict-free assignment says "No conflicts with their tutoring schedule." and renders no list). Each line: full date + range + subject + **the amendment's two doors side by side** — the student's NAME opens the family record (contact facts, "prefers a text", recent activity — the call-first context) and "reschedule →" opens the PL-387 session machinery pre-loaded. Nothing auto-moves; the footer says class sessions win by default assumption only. **B.** State-driven **Needs Attention** row ("Billy's MIS SAT Prep assignment conflicts with 5 tutoring sessions — each needs a reschedule or a deliberate keep."), recomputed from reality on every dashboard load via ONE batched tutoring query (no stored flag — resolving via ANY path clears it), deep-linked to a NEW resolution card at `/admin/tutoring?assignment={classId}` that re-fetches the same computation and shows the honest all-clear at zero. **C.** Counts are the PL-433 deduped portal truth by construction (the util never touches Google). No family emails fire from assignment — comms ride the normal reschedule machinery, unchanged. **E2E on the real MIS case:** assign → response carried 5 deduped conflicts with family ids; NA row present with the right copy; the resolution card listed all 5 with both links working; removing one conflicting class session decremented the row to 4 live (recompute proven); the remaining **4 are Scarlett's real conflicts, left standing for her to resolve — exactly what this feature is for**; the conflict-free QA assignment produced neither prompt nor row.

## PL-434 (original) — Assigning over conflicts: prompt to resolve + Needs Attention until resolved (Scarlett, Aug 31)
Assigning an instructor despite conflicts works (deliberate — the override is right), but the conflicts then just… exist. After a with-conflicts assignment: **A.** the confirmation surface immediately offers the next step — "Billy is assigned. 4 tutoring sessions conflict with class sessions — reschedule them:" with per-conflict links into the PL-387 edit machinery (each tutoring session's reschedule pre-loaded; class sessions win by default assumption but nothing auto-moves — staff decides per session). **B.** a state-driven **Needs Attention** row ("Billy's class assignment conflicts with 4 tutoring sessions", deep-linked to the same resolution list) that self-clears as conflicts are resolved (session moved, cancelled, or the class session changed) and disappears entirely at zero — recomputed from reality, not a stored flag, so resolving via ANY path clears it (standing state-driven rule). **C.** conflicts counted per the PL-433 dedupe (4, not 8), and only REAL overlaps qualify (portal sessions + genuine external Google busy; echoes excluded). No family emails fire from the assignment itself — comms happen when a session is actually rescheduled through the normal machinery. Verify: assign-with-conflicts fixture → prompt lists each once w/ working links; NA row present; resolving one decrements; resolving all clears; a conflict-free assignment produces neither.

**PL-434 amendment (Scarlett, Aug 31):** each conflict line in the prompt AND the Needs Attention resolution list also links the STUDENT'S record (the family page — names-are-doors), since resolving often means a phone call or email first: the family page carries the contact facts, preferences ("prefers a text"), and the Recent-activity context needed before touching the schedule. Both links side by side per conflict: "reschedule →" and the student's name as the door to their record.

## ✅ PL-435 — SHIPPED (Aug 31)
**Provenance decision (the ship-note choice the code map demanded): column-based** — new `classes.default_location_source` ('instructor_default' | NULL; migration `20260914000002` — APPLIED via the Management API). NULL means explicit-or-legacy and is SACRED (we cannot distinguish a hand-typed legacy link from an old auto-fill, so sacred-by-default is the safe backfill — none mutated). Writers all stamp honestly: the wizard's creation auto-fill stamps 'instructor_default'; the assignment route applies-and-stamps; a hand edit on the class card clears provenance; a counselor's classroom-request answer clears provenance. **The assignment rule:** an ONLINE class with no link (or a default-provenance one) gains the new instructor's default on assignment; swapping instructors replaces a default-provenance link with the NEW instructor's default, or CLEARS it when they have none (an empty honest location beats a link to the wrong person's Zoom room); an explicit link is never touched. The response names an auto-apply ("Meeting link set to their default (…)") so nobody wonders where it came from. The tutor card's "(your default link)" badge is now provenance-aware (it survives the link being written to the class record — the old NULL-inference badge would have silently died for exactly the classes this fixes). **Verified end-to-end, including ON THE REAL BUG:** MIS SAT Prep (online, Billy assigned post-creation, location NULL — Scarlett's literal case) → re-assign applied Billy's Zoom link with 'instructor_default' provenance, **left in place as the production fix**; QA-class swap matrix: Billy→Charlotte replaced the link with hers; →Kelsie (no default) cleared it; hand-typed link + re-assign Billy → untouched (sacred). Downstream: sessions/ICS/E5 compose from the record as always; assignment precedes family comms here so nothing sends from the fill itself (baseline rule — a later change would ride the normal schedule-change machinery). **In-person sweep verdict: nothing pretends** — `instructors.default_location` was dropped in PL-175 (one default = the meeting link), and every fallback is already gated `delivery_mode === 'online'`.

## PL-435 (original) — Online class: instructor's default meeting link doesn't apply on post-creation assignment (Scarlett, Aug 31)
The wizard promises an online class's location fills from the instructor's default meeting link — true when the instructor is picked AT creation, but assigning afterwards (the roster dropdown / who's-free-to-teach flow, exactly how Scarlett just assigned Billy to MIS) leaves the location empty. Fix at the assignment path(s): when an ONLINE class with NO meeting link gains an instructor who has a default link, apply it (same rule as the wizard; recorded as the default-link provenance the tutor-class card already shows — "(your default link)"). Never overwrite an explicitly-set link; changing instructors later re-applies only if the current link is the OLD instructor's default (provenance-aware — an admin-typed link is sacred). Downstream: sessions/calendar/ICS pick it up; IF families were already told a different link/location, the normal schedule-change machinery decides what sends (baseline rule — likely nothing here since assignment usually precedes comms, but state it); the E5/location emails compose from the record as always. Sweep: the same gap for IN-PERSON assignment? (no default to apply — confirm nothing pretends otherwise). Verify: create online class w/o instructor → assign Billy via who's-free → location = his Zoom link w/ default badge; explicit link untouched by re-assignment; instructor swap updates a default-provenance link only.

## ✅ PL-436 — SHIPPED (Aug 31)
The card now leads with **"Class page:"** and renames the form URL to **"Direct registration link:"** (own Copy each — copy state is keyed per button so two Copies never flash together). The class-page line resolves through the ONE source (`/api/admin/evergreen`, the shortlinks panel's own facts — school code first, course code for school-less classes) with **resolution honesty**: this-class-is-served → "hgl.co/{code}" (grey-prefix style matching the shortlinks panel, clickable — the href is the working same-origin `/{code}` pre-DNS, the display shows the hgl.co form; Copy takes the working absolute URL) + the quiet read-only pointer "set in Classes → Short links"; served-elsewhere → the /c link labeled plainly with "hgl.co/{code} currently points at {other class}" — and the serving label now carries the START DATE ("SAT Prep (starts Sep 8)") so two same-type cohorts read apart (the evergreen route's label widened); codeless → the /c link cleanly. Verified live, all three variants: MIS (code URL + pointer), ISD under a pinned-away fixture (the honest points-elsewhere line, pin restored after), and the codeless Deep Dive (/c/{slug}).

## PL-436 (original) — Class card links: "Direct registration link" + the class page's code URL beside it (Scarlett, Aug 31)
The card shows only "Registration link:" (the /register form URL). Rename to **"Direct registration link"** and show the **class page** above/beside it: the permanent code URL when the class's school/course has an evergreen code ("Class page: hgl.co/mis" — clickable, opens the served-in-place page, + its own Copy), read-only with a quiet pointer to where codes live ("set in Classes → Short links") — NOT editable here, per Scarlett. Resolution honesty: show the code URL only when this class IS what the code currently resolves to (pinned elsewhere / not-newest → show the internal /c link labeled plainly instead, e.g. "class page (direct): …/c/{slug} — hgl.co/mis currently points at {other class}"); codeless class → the /c link. Pre-DNS the display uses the vercel host but shows the hgl.co form it will become (match how the shortlinks panel already renders codes — consistent). The PL-384 rule (shared links prefer code URLs) already governs emails/browse; this is the admin card catching up so the creator remembers the distinction. Verify: MIS card shows both links + Copy each; a pinned-away fixture shows the honest variant; codeless class degrades cleanly.

## ✅ PL-437 — SHIPPED (Aug 31) — investigated in the data FIRST; **identity churn DISPROVED**; the real defect was mortal alert memory
**The story (evidence snapshotted to scripts/.tmp-pl437-evidence/ before touching anything):** the two alerts were **21:30:01 and 21:31:51 UTC Aug 31** (2:30/2:31 PM Pacific), and they were **true at their instants** — they described the batch-44 PL-420 E2E's own transient deletions of Janeson's Sep 1/Sep 7 events on Billy's real calendar (deleted ~21:24–21:28, revived by PATCH at 21:31:30/21:32:27 per the sync log), observed by PROD's push audit racing the local dev session on the ONE shared calendar + DB. "Events still visible" ✓ — everything was restored within minutes; "without dates" = the PL-438 copy bug; the earlier 18:30:07 deletion alert was a separate, genuine drift (resolved by the 19:47 revert in the log — the system working as designed). **Identity churn: disproved.** Every event on the calendar is the ORIGINAL Aug-18 mint — one per session, `hglSessionId` markers intact, times correct, pointers consistent (full dump in the evidence dir); the "recreations" were PATCHes that revived the same event ids (Google un-cancels on patch), so adopt-before-create held on every path and no write path creates instead of adopting. **The real defect (the grouping violation):** PL-402's `alerted_signature` lived ON the calendar_drift row — and every resolution path (adopt, revert, the audit's own delete-then-upsert refresh) deletes that row, so pass #2's re-detection had NO memory of pass #1's ring and alerted again. **Fixes:** (1) the once-only memory is now DURABLE — new `calendar_drift_alert_ledger` (migration `20260914000001` — APPLIED via the Management API): a (session, signature) pair that rang within 24h stays silent across any row churn; a genuinely new drift shape — or the same shape a day later — still rings. (2) **Deletion is double-checked before it's declared**: when `events.list` omits an event, the audit now GETs it directly (helper extended with start/end) — only a true 404/`cancelled` counts as deleted; a live GET result feeds the normal moved/XCL checks with ITS times, so eventual-consistency list lag can never ring a false deletion again. **E2E:** the PL-402 harness extended with both regression scenarios and ALL PASS — 3 changes → ONE grouped email; row-churn re-detection rings NOTHING (the exact Aug 31 case); list-omits-a-live-event reads as its true state, zero alerts; a re-move still rings once; restore clears. Live re-check: calendar carries exactly one event per session. ENVIRONMENT NOTE (unavoidable pre-launch, now harmless): dev E2Es on the real calendar ring prod's push doorbell — the ledger + GET-check make the system robust to exactly this class of transient.

## PL-437 (original) — Drift alerts firing falsely/late AGAIN: suspected event-identity churn; grouping violated (Scarlett, Aug 31 — investigate FIRST, in the data)
Evidence: two deletion alerts a MINUTE apart (2:30 + 2:31 — the PL-402 one-grouped-email-per-pass rule violated), for events that are STILL VISIBLE on the Google calendar, reported without dates, well after any actual change. Prime suspect: identity churn — something re-synced Janeson's sessions and created NEW events (the calendar screenshot shows blocks at shifted times), so the audit's stored pointer/hglSessionId lookup missed and read the old ids as DELETED while twins exist. Investigate in the data before fixing: the two drift rows (what signature, which pass, why two passes a minute apart — push debounce failed? two calendars? poll+push racing?), the events on Billy's real calendar (ids, hglSessionId properties, times — are they the PL-401 backfilled originals or fresh mints? what created them, when?), and whether a reschedule/edit path re-syncs by CREATE instead of PATCH (identity rule: adopt-before-create must hold on EVERY write path, including edits/moves — grep all event-writing call sites). Fix the real cause; ship notes tell the story. Also verify the PL-402 grouping under push: two doorbells a minute apart must coalesce or at least the second pass must respect alerted_signature (it appears not to have). E2E: reproduce the churn path found → zero false deletion alerts, one grouped email max, calendar carries exactly one event per session afterward.

## ✅ PL-438 — SHIPPED (Aug 31)
**A.** Alert lines and the banner both speak full dates + ranges now — "(Tuesday, September 8, 11:00 AM–12:00 PM)" (verified live in REAL Chrome; the alert side proven at compose level: "Monday, September 7, 1:00–2:00 PM"). **B.** The alert's link is now `…/admin/tutoring?drift={sessionId}` ("open the decision banner") — it lands ON the drift banner with the row scrolled into view + a 4s highlight ring (real-Chrome verified: row in view on arrival); when the drift no longer exists by click time, the anchor says so plainly: "The calendar change that alert pointed at has since been resolved or withdrawn — nothing here needs a decision. (A fresh scan just confirmed it; anything still pending is listed below.)" (real-Chrome verified after reverting the fixture drift). NOTE: no resolution timestamp exists to quote (resolutions delete the row and nothing logs the instant), so the note is dateless-but-honest — flagging rather than inventing a date. In passing: the banner's "1 session need a decision" grammar fixed ("needs").

## PL-438 (original) — Drift alert copy + click-through: full dates, and land ON the drift (Scarlett, Aug 31)
**A.** Alert lines say "Monday 1:00 PM" — reads as TODAY; render the session's full date per the standing rule ("Monday, September 7, 1:00–2:00 PM"). Same in the banner text. **B.** "open Janeson's banner" landed on the generic student card — no banner, no indication what was deleted or needs attention. The link must land on the drift banner itself (scroll/anchor + highlight); if the drift row no longer exists by click time (resolved, superseded, or the PL-437 false-positive cleaned up), the page says so plainly at the anchor ("this calendar change was resolved/withdrawn on {date}") instead of silently showing nothing — an alert's link never dead-ends into ambiguity (standing resolution-surface rule). Verify: fresh drift → link lands highlighted; resolved drift → honest note; dates everywhere.
