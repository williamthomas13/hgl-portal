# Portal fixes — batch 47 (ACCUMULATING — opened Sep 1, 2026)

**Standing rules:** all prior (batch 41/42/44/45/46 lists).

(CLOSED for batch 47 hand-off, Sep 1. Next PL: PL-453.)

## ✅ PL-443 — SHIPPED (Sep 1) — every reverted spot listed
Rule refined and applied: **zone labels attach to TIMES (and datetime deadlines), never to bare date ranges** — nothing to convert in "August 16 – 31, 2026". Full sweep of PL-409's commit (5 files) — the three date-only spots that gained the label, all reverted: **①** `timecards.ts` `periodLabel` — ONE const drove all four email renders (subject "Your timecard for … is ready to confirm", the `<h2>` heading, the preheader, and the `{payPeriodRange}` variable the live T5 template interpolates) — label stripped at the source; the T5 seeds/registry samples never carried a literal label, so no template version needed; payroll boundary math still anchors America/Denver. **②** tutor portal timecard header (`timecard-panel.tsx`) — the "Salt Lake City time" note beside the date range removed. **③** admin payroll per-period header (`timecards-panel.tsx`) — the label removed, the "payday the 20th/5th" clause KEPT (PL-409 had prepended onto a pre-existing payday note — partial revert, not a deletion). **Kept, correctly (not date-ranges):** the Schools card's timezone readout ("· Düsseldorf time" — a settings readout PL-409 fixed in passing) and the activity-feed due-date change (never had a zone label). `tutor-hours-report.ts`/`report-period.ts` confirmed label-free. Every time-of-day render keeps its label (~30 `staffTimeCityLabel` call sites checked — none came from PL-409, all attach to actual times).

## PL-443 (original) — Drop the timezone label from date-only period ranges (Scarlett, Sep 1)
PL-409 overcorrected: the timecard subject/heading now says "August 16 – 31, 2026 (Salt Lake City time)" — a pure DATE range needs no zone label (nothing to convert). Rule refinement: zone labels attach to TIMES (and datetime deadlines), never to bare date ranges. Fix the timecard subject/heading/period lines + sweep the PL-409 additions for other date-only spots that gained the label; time-of-day renders keep their labels. Ship notes list each reverted spot.

## ✅ PL-444 — STOPPED, per the item's own instruction: **NO preserved source carries the phone numbers — Scarlett, please re-share the list**
Searched every candidate: **① `docs/TUTOR_ROSTER_SEED.md`** (the July port of "the HGL employee spreadsheet — screenshots from Scarlett") is the closest miss and the proof the numbers existed: its timezone column preserves AREA CODES as tz hints ("~America/New_York (215)", "(650)", "(231 MI)", "(209)", "(740 OH)") — the full numbers were visibly in the source screenshots but only the area codes were transcribed; the doc has no phone column. **②** `docs/pl358-flagged-team-entries.json` + the PL-358 team port: names/credentials/photos/bios only. **③** A phone-shaped-number grep across all of docs/, scripts/, and every migration: every hit is the HGL office line (+1 801 524-0817/0827), QA fixtures (555 numbers), or one tutor's Zoom meeting id. **④** The roster seed migration (`20260721000001`) seeded no phones; `instructors.phone` (PL-226) exists and is empty on all 22 rows. Nothing was scraped, guessed, or reconstructed from area codes (a 215 area code is not a phone number). **Nothing to build:** the Tutors panel already renders the Phone column and the profile editor already edits `instructors.phone` (PL-226) — the moment Scarlett re-shares the list, the numbers can be entered verbatim there (or handed to Code to enter).

## PL-444 (original) — Instructor phone numbers: populate from Scarlett's shared contact info (Scarlett, Sep 1)
The Tutors panel's Phone column is all "—" but Scarlett previously shared staff contact info that included phone numbers. Locate the preserved source (the batch-35/PL-358 team-entries JSON, intake docs, or wherever that contact record was captured) and populate `instructors.phone` from it — VERBATIM from the record, no invented/guessed numbers; entries without a phone in the source stay honestly blank. If NO preserved source carries the phones, STOP and say so in the ship note (Scarlett will re-share the list — do not scrape or guess). Phones render where the panel already shows them; editable per the existing profile editor.

## ✅ PL-445 — SHIPPED (Sep 1) — RAMPED LIVE, activation only, 11/11
`AL_SYNAP_NUDGE` v1 flipped LIVE with zero copy edits: the active version IS the reviewed one (asserted against the `[DRAFT REVIEW: AL_SYNAP_NUDGE]` send's `body_snapshot_id` — the batch-46 proof pattern), bytes byte-identical to the seed, live gate now serves the REGISTRY body (`renderRegistered` versionId = the active version; code twin stays the fallback), live render **byte-for-byte identical on subject + html** to the `renderVersion` review path on the same MIS fixture (body mirrored line-for-line from `sweepSynapNudges`, subject matches the review send's own `subject_rendered`). Spot send `[RAMPED LIVE: AL_SYNAP_NUDGE v1]` → billy@ `sent`. **Drafts-awaiting-ramp returns to EMPTY.**

## PL-445 (original) — Ramp AL_SYNAP_NUDGE (Scarlett approved, Sep 1)
Activate v1 LIVE byte-for-byte vs the review render (body_snapshot_id proof per the batch-46 pattern), live gate serves the registry body, spot send → billy@. Drafts-awaiting-ramp returns to EMPTY.

## PL-446 — Reschedule dialog: show WHY, show the student's availability, and check the new slot (Scarlett, Sep 1)
The reschedule dialog reached from the conflict-resolution card is context-blind: Scarlett moved Janeson's session an hour earlier — STILL conflicting with the MIS class — with no idea whether it fits Janeson's shared availability. Three additions, all composed from data the system already has: **A. The reason, up top:** when the dialog is opened from a conflict (the ?assignment= card, a drift banner, the availability review surface), it says what it's resolving ("Conflicts with MIS SAT Prep — Tuesdays 11:00 AM–1:00 PM through Oct 1") so the target is visible while picking. **B. The student's stated availability, visible:** render Janeson's shared windows (the intake/PL-424 grid) beside/below the slot picker ("Janeson is usually free: Mon 4–6 PM · Wed 4–6 PM · Fri 4–6 PM"); no windows on record → say so ("no availability shared"). **C. Check the chosen slot BEFORE committing:** validate the new slot against (1) the original conflict source + the tutor's real busy (the PL-433 deduped machinery) and (2) the student's windows — a slot that still conflicts or falls outside the windows gets a plain inline warning with the specifics ("still overlaps MIS SAT Prep" / "outside Janeson's shared availability — Tuesdays aren't in their windows") requiring an explicit confirm to proceed anyway (warn-not-block: staff may know better, e.g. phone-arranged; the confirm is recorded in the note trail). Applies to the SAME dialog wherever it opens (calendar drag included — PL-387's decision panel gains the same availability line + slot check). Scarlett's actual mis-move is the test case: reproduce, see both warnings, fix to a clean slot, confirm the conflict count decrements. Verify E2E per the above + no-windows and no-conflict-origin renders.

## ✅ PL-447 — SHIPPED (Sep 1)
The Course section now derives from **EVERY course key that has (or has ever had) a no-school class** — the schools-list pattern: the API merges `course_meta` rows with a distinct-key scan of no-school classes (any status, so the cancelled R/W Deep Dive keeps its row exactly like a school with no open class does), resolving each display name per the existing chain (course_meta → newest class's type → prettified key). A `course_meta` row is NOT required to appear; the code-save upsert mints it on first save (and the pin action became an upsert too — a pin on a not-yet-minted row works), so **zero Code round-trips**: current and future courses show an empty code box the moment their first class exists. Existing rows untouched. **Verified live:** all four courses render — HGL ACT Prep (`/actprep`, unchanged) + **SAT Deep Dive: Mastering Advanced Math Concepts + SAT Deep Dive: Mastering the Reading and Writing Sections + PSAT Prep**, each with the empty box **Scarlett can now fill**; a QA code saved onto the virtual PSAT row minted its record and immediately computed serving ("PSAT Prep (starts Aug 18)"), the ONE collision validator refused the same code on a second course with the plain reason, and the QA code was cleared after. A brand-new no-school class's course appears on its own by construction (the section is derived from `classes`).

## PL-447 (original) — Course links list every no-school course automatically (Scarlett, Sep 1)
The Short links Course section renders only courses with a course_meta row — just "HGL ACT Prep" (the one PL-376 minted). The Deep Dives and PSAT Prep have classes and course keys but no row, so Scarlett cannot set their evergreen codes at all. Fix like the schools list: derive the section from EVERY course key that has (or has had) a no-school class — auto-mint the course_meta row on first render or on code-save (display name per the existing resolution: course_meta → newest class's name → prettified key), so current AND future courses appear with an empty code box, zero Code round-trips. Existing rows untouched. Verify: both Deep Dives + PSAT appear immediately; a brand-new no-school class's course shows up on its own; codes save with the collision validator as always.

## ✅ PL-448 — SHIPPED (Sep 1) — registrar parity, forever; the inventory task is RETIRED
**Resolution order, documented in the code and here: reserved portal routes (Next's own router precedence) → evergreen codes (served in place) → legacy overrides (redirect to their stored destination) → the WILDCARD permanent redirect to the same path on highergroundlearning.com.** One shared `wildcardForward()` in evergreen.ts feeds three surfaces: the `/{code}` page's unknown/malformed branches, `/{code}/register`'s (an unknown code's register subpath is just another unknown path — forwards path-preserved), and a NEW root `[...forward]` catch-all so **multi-segment** unknown paths forward too (the registrar's wildcard never cared about depth; route precedence keeps it the last resort). Paths forward VERBATIM — case preserved, segments re-encoded. The honest "no link we know" card is retired for unknown paths (the main site's 404 owns that job, as today); **known codes with nothing open keep the interest-capture state** — a printed code still never strands. **Consequences delivered:** the cutover-checklist inventory step is RETIRED in the doc with the why (there is no per-path registrar inventory to migrate — the registrar's real setup IS one wildcard, so nothing can "die" post-cutover); **the `/act` row was retired from `legacy_redirects`** (the path-preserving wildcard reproduces it byte-for-byte — verified: `/act` redirects to highergroundlearning.com/act with NO table row); the Legacy section's explainer, the retire-confirm copy, and the Course section's "/act is taken" line all reworded to the overrides-only reality (a row belongs there only when a path must go somewhere DIFFERENT than its same-named main-site page; retiring hands the path back to the wildcard). **Collision validator gains the warn tier:** claiming a code takes precedence over the wildcard, so a successful code save now PROBES `highergroundlearning.com/{code}` — a live main-site page there (= plausibly a shared link) returns a plain-English heads-up rendered amber in the panel, never a block (verified live: saving 'act' as a course code warned exactly as designed; probe is best-effort with a 4s timeout — no warning beats a wrong one). **Verified:** `/act` → main-site /act with no row · `/some-random-path` and `/nested/deep/path` → same paths on the main site · `/UPPER-Case-Path` preserved verbatim · `/mis` serves in place (200) · `/mis/register` serves the form · `/aisct` (known code, nothing open) keeps the capture state · the bare-domain 301s in next.config untouched. Note on the status code: `permanentRedirect` emits **308** (permanent, method-preserving) — the same permanence class the portal has always used here (batch-40's E2E recorded "/act 308").

## PL-448 (original) — hgl.co unknown paths: replicate the registrar's WILDCARD forward (Scarlett, Sep 1 — supersedes the per-path inventory)
New fact: today's registrar setup is ONE wildcard — hgl.co/{anything} forwards to highergroundlearning.com/{anything}; hgl.co is shared mainly as the bare short address. So: post-cutover, any hgl.co path that is NOT a known code (evergreen/school/course, /classes, /register subpaths, reserved portal paths) 301s to highergroundlearning.com/{same path} — replicating today's behavior exactly, forever. Consequences: the per-path legacy-forwards inventory is UNNECESSARY (Scarlett's task retired — remove the cutover-checklist inventory step, note why); the /act row becomes redundant (path-preserving fallback covers it — keep or retire, note); the legacy table's remaining purpose = overrides where a code must go somewhere OTHER than its same-named path (reword the section's explainer accordingly, incl. that claiming a path as an evergreen code takes precedence over the wildcard — the collision validator should WARN when a new code shadows a plausibly-shared legacy path, plain-English). The honest "not a link we know" page retires for hgl.co paths (the main site's own 404 takes over that job, as today). Resolution order documented: reserved → codes → legacy overrides → wildcard 301. Verify: /act redirects w/o a table row; /some-random-path lands on highergroundlearning.com/some-random-path; codes still serve in place; bare domain 301 unchanged.

## ✅ PL-449 — SHIPPED (Sep 1) + VERIFIED ON PROD — one root for investigation (2), a second real defect behind (1)
**THE ROOT (investigation 2, diagnosed from the actual failure mode on prod FIRST):** `sharp`'s module-scope import was failing on the Vercel runtime, which killed the whole module graph of every importing route BEFORE any handler ran — bisected live: all four MIS downloads, both previews, the send-panel GET, the SLS upload route, and the site-content image route returned Next's raw `__next_error__` 500 page **on ANY request, including 404-shaped ones** (a bogus artifact name, a GET on a POST-only route), while non-sharp routes were healthy and the same requests worked locally. Not the recent-batch suspects: PL-399 was letterhead text, PL-408's fonts never touched collateral (Poppins is self-hosted in public/collateral), PL-384's short_link read was benign — and deps hadn't changed since July. Cause class: sharp loads its platform binaries (`@img/sharp-linux-x64` + libvips) via computed require paths the file tracer can't follow, so the lambdas lacked them. **Fixes, both belts:** (a) sharp now loads **lazily and guarded** (`logo-process.ts`) — a native-load failure degrades exactly one FEATURE with a logged reason (upload refuses plainly with 503 + nothing stored; collateral passes the stored logo through) and can never again kill a route module; (b) explicit `outputFileTracingIncludes` for sharp + the linux `@img` binaries on all four sharp/chromium routes — **including `/api/admin/class-confirmed`, which was never in the tracing map at all** (its lambda lacked `public/collateral` AND the Chromium binary — the send-with-attachments path was latently broken on prod since PL-214). **Verified on prod post-deploy via a temporary staff-gated in-function diag (removed after the read):** `sharp: ok` on linux-x64 with `sharp-linux-x64`/`sharp-libvips-linux-x64` present, chromium bin + collateral assets present; **all four MIS artifacts render 200 on prod**, the send-panel GET is 200, and both sharp routes answer 405-not-error-page to method probes. **A (the SLS logo — a SECOND, independent defect):** decode-probed EVERY stored image (6 school logos, all 46 school-assets/class-pages objects incl. block/team/hero variants): exactly ONE corrupt asset — SLS's Aug-6 logo, which serves 200 `image/png` but whose every high byte is the UTF-8 replacement sequence (`EF BF BD` — the binary went through a text decode somewhere upstream; ASCII bytes survive, the rest are destroyed — **unrecoverable**, and undetectable by status/content-type probing alone). The `schools.logo_url` pointer is CLEARED (the corrupt object left in the bucket as evidence); **Scarlett re-uploads the real crest via the fixed route** — which now also **refuses non-image bytes by magic-byte sniff** (this corruption class can never land again) and refuses plainly when the image library is down instead of storing unvalidated bytes. **Soft-fail (the amendment's principle):** a missing/dead/corrupt logo now renders the school's NAME in the flyer's logo slot (verified: the logo-less SLS flyer renders cleanly with "St. Louis School" in the top-right blob) — one element degrades, the document never dies; amber notes on the collateral card and the send panel say so plainly; `schoolLogoDataUrl` logs every failure branch and no longer poisons the warm-lambda cache on transient failures. **B:** upload E2E green — a real PNG uploads (200) and the stored object decodes with alpha (flood-fill applied); the UTF-8-mangled twin of the same PNG is refused 422 with the plain reason. **C:** `fetchErrorLine` (`fetch-error.ts`) is the ONE plain-error composer — a short `text/plain` or `{error}` JSON body (our routes' own crafted refusals) shows as the reason, HTML NEVER renders (unit-proven on the HTML-500/plain-422/JSON-503 shapes); the sweep found exactly two raw `res.text()`-into-UI sites (counselors-panel + school-branding-panel logo uploads) plus the wizard's status-less line — all three converged. Preview thumbnails now FETCH the artifact and show the route's plain reason on failure (the old bare `<img>` swallowed every 400/500 into an alt-text box — exactly the MIS symptom). **Send-panel contract (amendment 2, confirmed as the design):** the composed email preview opens EXPANDED + the exact attachment filenames are listed BEFORE any send; the Send button is disabled until the preview has rendered; failures name what broke ("couldn't build the preview — the class record is missing …") instead of "Failed to load.".

## PL-449 (original) — SLS logo: broken render, 500 on re-upload, and raw-HTML error vomit (Scarlett, Sep 1)
Three layered defects from one attempt: **A.** SLS's stored logo doesn't render (alt "SLS logo" shows) — find why (dead/stale URL? old bucket path? a past upload that recorded a row but never landed bytes?) and fix the record; sweep all schools/instructors/blocks for other broken stored image URLs (probe each, report). This also blocked the accent-picker flow that reads the logo. **B.** The re-upload itself 500'd server-side — diagnose the upload route's crash (the error page is a Next 500; find the actual exception in logs/repro) and fix; the upload should succeed or refuse with a plain reason (file type/size limits stated). **C.** The client rendered the 500's ENTIRE raw HTML into the panel. Error handling rule: a failed request renders a one-line plain-English message ("Couldn't upload the logo — try again; if it keeps failing, tell Code" + the honest status), NEVER response-body text verbatim. Fix this handler and SWEEP every fetch-error render in the admin for the same text()-into-UI pattern (grep for error rendering of response bodies); ship notes list each converged. Verify: SLS logo uploads + renders + accent picker works; a forced 500 shows the one-liner; probe sweep results reported.

**PL-449 amendment (Scarlett, Sep 1 — CORRECTED: the collateral failures below are on the MIS class; SLS is only part A's logo/upload bug):** the MIS class's collateral PREVIEWS are broken the same way (flyer.jpg/letter.jpg render as alt-text boxes) — likely the same root: the flyer/letter generator composes the school LOGO, so a dead logo asset may be crashing the whole render (which would also break the Flyer/Letter download buttons — test them). Diagnose whether A's broken asset is the cause or the preview route fails independently; either way the generator should fail SOFT on a missing/dead logo (render the collateral with the text fallback + a plain admin note "school logo missing — upload it in Branding") rather than dying, per the fail-closed-but-alive principle. Verify previews + all four download buttons on SLS after the logo fix AND on a deliberately logo-less fixture school.

**PL-449 amendment 2 (Scarlett, Sep 1):** the counselor "everything's ready" send panel on MIS also dies — "Failed to load." where the pre-send preview should be. Likely the same generator chain (the welcome attaches the flyer+letter, so a crashing collateral render kills the panel's preview fetch) — diagnose whether it's that or an independent break, fix, and confirm what Scarlett expects IS the design: the panel shows the composed email preview (rendered via the composer path) + the attachments it will carry BEFORE any send, with the send gated behind seeing it. "Failed to load." is also too bare for the plain-error rule — say WHAT failed ("couldn't build the preview — the flyer render failed: school logo missing"). CONFIRMED (Scarlett): all four download buttons (Flyer/Letter × PDF/JPG) fail on MIS too — the whole collateral generator is down, not just previews, — all on MIS, whose logo RENDERS fine on its class page. So the dead-SLS-logo theory does NOT explain the collateral failures: the generator is broken for a class with a healthy logo, pointing at a shared asset (the hero/brush/fonts in public/collateral?), a runtime/dependency break in the PDF/JPG renderer (sharp/chromium on Vercel?), or a regression from a recent batch (PL-399's letterhead address change touched collateral-templates; PL-408's fonts; PL-384's short_link reads). Diagnose from the actual server error FIRST, then fix; the whole chain (downloads, previews, send panel) shares the root. This is LAUNCH-PATH — collateral is how classes get announced to schools. Two distinct investigations, possibly one root: (1) SLS logo asset + upload 500 + raw-HTML error (part A–C); (2) MIS collateral generator down across downloads/previews/send-panel. Verify on MIS end-to-end (all four buttons download real files, panel preview + send work) AND SLS after its logo fix; logo-less fixture school shows the soft-fallback flyer.

## ✅ PL-450 — SHIPPED (Sep 1) — **migration `20260916000001` (drop classes.short_link) APPLIED post-deploy**
Collateral now composes the printed "more info & registration" destination through **`preferredClassPath` — the ONE preference logic everywhere else uses** — which also closes the asymmetry the old compose had: the flyer prints `hgl.co/{code}` only when the school/course code **currently RESOLVES to this class** (pin honored), never a code that would land a family on a different class; no code serving it → the honest full registration URL prints. Verified by compile-and-call: MIS composes `hgl.co/mis` straight from the schools table; the SLS fixture (code serves nothing) composes the honest register URL + the missing-link gate reason. Renaming a code in Short links changes the very next render structurally (the value is composed at render time from the schools/course_meta row — no stored copy exists to go stale). **The editable field is GONE** (panel + wizard step 4): the panel shows the READ-ONLY composed value in the PL-436 grey-prefix style with the quiet "set in Classes → Short links" pointer; codeless shows the amber nudge ("No short code is serving this class — the flyer prints the full registration URL. Add the school's code in Classes → Short links…"); the wizard's slot became the same pointer note. Send-gate wording updated to reality: "a printable hgl.co link (set the school or course code in Classes → Short links)". **PL-384 phase 2 finished — every remaining reader converged in this batch** (the full inventory): collateral model (compose), collateral-nudge sweep + dashboard NA row + class-confirmed attachment default + the class-picker suffix (all four dropped the legacy `!short_link` completion proxy — the stamp alone is the condition now, per PL-429's real signal), notify-interest (code URL else register URL — the stored-text middle branch gone), notifyInterestMsg (reads the resolved evergreen facts), counselor-class-card ×2 (evergreen-only), counselor-view + class-roster selects, wizard type/state/write/duplicate-prefill, CollateralFields type + form + save. **Migration order honored (two-phase):** code deployed first, then the drop applied — and the migration first CLEARS the skip stamp on classes whose short_link marked them legacy-complete (the live case: MIS SAT Prep carried both the stamp and 'hgl.co/mis' — under the stamp-only condition it would have started nagging about collateral it demonstrably has), so today's behavior reproduces exactly. **Post-drop grep clean:** the only `short_link` matches left are the unrelated `short_links`/`short_link_clicks` routing tables and the RPC name. Known edge, noted: the counselor portal's materials gate uses evergreen-code presence without the serves-this-class check (client-side twin) — in the rare two-open-classes state its buttons would 400 against the server's stricter gate (fail-closed, never wrong output).

## PL-450 (original) — Collateral's editable short-link field retires; collateral composes the evergreen code (Scarlett, Sep 1)
The Branding & collateral panel still has an editable "hgl.co short link" field per class — the last consumer of legacy `classes.short_link` (the not-drop-ready column PL-384's ship note flagged). Codes now live in ONE place (Classes → Short links): collateral composes the printed "more info & registration" destination from the class's resolved code URL (`preferredClassPath` — same preference logic as everywhere: code URL when the school/course has one, /c fallback), shown READ-ONLY on the panel with the quiet "set in Classes → Short links" pointer (PL-436's pattern). The editable field goes; the flyer/letter/one-pager/CS-welcome renderers read the composed value. Codeless class → the honest fallback URL, with a nudge line ("no short code set for this school — add one in Classes → Short links for a printable link"). THEN finish PL-384's phase 2: with the last reader converged, `classes.short_link` takes the two-phase drop (post-drop grep per standing rule) — unless something else still reads it (report). The "Needs the short link and deadline set" send-gate wording updates to match reality (the code comes from the school now). Verify: MIS flyer prints hgl.co/mis composed from the schools table; renaming the code in Short links changes the next flyer render with no collateral edit; codeless fixture shows the nudge + honest URL; post-drop grep clean.

## ✅ PL-451 — SHIPPED (Sep 1) — **before/after screenshots for Scarlett's veto: `docs/pl451-hero-scrim/` (before/after × desktop/375px)**
Scrim eased exactly one step: `bg-hgl-slate/80` → `bg-hgl-slate/75`, on BOTH surfaces that wear the brand-mural hero (the class page and /classes — one design). **Contrast measured, not eyeballed:** the hero's text column was sampled pixel-by-pixel (its lightest regions are genuinely white — max 255, 99th percentile 224); at /75 the extrabold headline holds ≥3.6:1 against the absolute brightest pixel (large-text AA is 3:1 — passes everywhere) and body bullets hold ≥4.0:1 at the 99th percentile / ≥4.25:1 at the 95th (the handful of sub-4.5 white specks sit behind the headline area, not the bullets; at the old /80 the same worst case was already 4.0 — this was never a strict-AA surface over pure white). Desktop + 375px captured; per-class UPLOADED hero photos verified out of scope — **doc-premise note: they never sit under the scrim** (PL-351 renders them FRAMED inside the hero below the text; the scrim only ever covers the brand shot), so no text-over-photo case exists. One Tailwind class per surface — trivially revertible if Scarlett vetoes.

## PL-451 (original) — Class-page hero: lighten the scrim (Scarlett, Sep 1)
The default hero (the brand mural shot) reads too dark under the PL-374 scrim — brighten it slightly: ease the overlay opacity/darkness a step, keeping text contrast accessible (white headline + bullets must stay comfortably readable — check WCAG-ish contrast over the image's lightest regions, desktop + 375px, and per-class UPLOADED hero photos too since they share the scrim). Small visual change, Code's eye for the exact value — show a before/after screenshot in the ship note for Scarlett's veto.

## ✅ PL-452 — SHIPPED (Sep 1) — chip-as-doorway only, per the revised item
**The deliberate-removal decision is RECORDED** (a load-bearing comment now sits at the chip's render site: the roster card is Kelsie's quick-roster view — do NOT restore collateral downloads or send machinery to it; collateral creation lives with class creation under Branding & collateral; downloads live ONLY there). **A. The chip:** a small amber "**not yet announced →**" pill in the card's right-hand stack, state-driven off the SEND LOG (a new staff-gated `/api/admin/class-announcements` returns the class ids with a real `CS_CLASS_CONFIRMED` send — both welcome variants count, the collateral-only follow-up deliberately doesn't; no stored flag, recomputed with every rosters refresh, so a send from ANY path clears it). Renders only on live school-class cards (school + not cancelled + run not over); announced and no-school classes render nothing at all; until the log has loaded, no chips render (never a guessed chip). No nudge email — the chip informs. **B. The doorway:** clicking deep-links `?announce={id}` → lands on Branding & collateral with the class pre-selected AND the send panel already open at the composed preview (the PL-449 panel: preview expanded + attachment filenames listed + send gated behind the preview); the panel's open-button and the doorway share ONE loader. A successful send refreshes the rosters so the chip clears immediately. **Verified live:** exactly one chip on the whole roster — MIS (the only school class with no CS send on record; ISD's Jul-28 welcome keeps it chipless, the no-school classes and past ASF show nothing); chip → `?announce=` → panel open, MIS preview visible, attachments listed; a synthetic `CS_CLASS_CONFIRMED` send row for MIS cleared the chip on the next fetch (row removed after — **MIS stays honestly un-announced for Scarlett to announce for real through the now-working panel**).

## PL-452 (original) — "Announce to the school" front and center on the class card (Scarlett, Sep 1)
Announcing a new class to its school contact is buried: the counselor welcome lives only under Classes → Branding & collateral (the roster card's Collateral section was removed DELIBERATELY — Scarlett's design: the roster card is Kelsie's quick-roster view, uncluttered by tools she won't use; collateral creation belongs with class creation. Honor that — do NOT restore collateral downloads to the card; record the decision). Fix, minimally: **A.** a state-driven **"not yet announced"** chip on live school-class cards whose CS welcome hasn't been sent (recomputed from the send log, clears on send — the not-forgettable pattern; no nudge email). **B.** The chip itself is the doorway: clicking it deep-links to the send panel in Branding & collateral with the class pre-selected (?announce={id}, panel open at the preview) — one small chip, zero card clutter, and an announced class's card shows nothing at all. No separate button, no inline machinery on the card. Verify: MIS card shows the chip → click → lands on the pre-selected preview → send → chip gone; announced + no-school classes render nothing.
