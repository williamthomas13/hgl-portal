# Portal fixes — batch 32 (ACCUMULATING — opened Aug 8, 2026)

**Standing rules:** plain-English statuses · no internal shorthand · every alert deep-links its record · samples from composers · `git push` after committing · PL-x IDs in commits · check items off here when shipped · registry template edits ship as new versions via anchor guards · verify composed blocks via the composer path · inline confirm banners only · NO native browser dialogs anywhere.

## PL-309 — Staff notification subscriptions for [HGL Admin] alerts (Scarlett, Aug 8; design approved)
Today every [HGL Admin] alert goes to the single `ADMIN_EMAIL` env address (Billy) — the manager (Kelsie) receives nothing and cannot be added without redirecting the lot. Build:
1. **A Settings panel** listing staff (admin + manager roles) × per-category toggles for the alert families — categories along the lines of: new registrations · payments & QuickBooks failures · minimum-enrollment checkpoints · coverage/substitute requests · timecards · reschedule requests · pipeline/lead alerts · agreements · waitlist — Code to derive the exact category list from the existing AL taxonomy (every AL template maps to exactly one category; no orphans).
2. **Defaults:** Billy = everything ON (today's behavior preserved, `ADMIN_EMAIL` kept as fallback so alerts never go nowhere). Kelsie = the tutoring-side subset ON: reschedule requests, timecards, coverage/substitute requests (+ tutoring-scheduling-flavored alerts in that family), PLUS the PL-313 close-match prompts (Scarlett: Kelsie needs to see those too).
3. **Permissions:** a manager can edit her OWN subscriptions but only within categories an admin has granted her (toggle granted ones on/off; cannot self-grant new categories). Admin (Billy) can grant/revoke categories for anyone.
4. Sends fan out to all subscribed recipients per category (dedupe per address); the alert sender stays one code path — recipients resolve from the settings, not per-callsite.


## PL-310 — Merge "Has diagnostics" + "Has Synap" into ONE toggle (Scarlett, Aug 8, w/ screenshot — reverses her earlier two-switch ask)
The roster card's two class switches should be a single toggle. Scarlett's rationale: if a class has diagnostics, they'll run through Synap or similar, so a separate Synap switch models a distinction that doesn't exist. One toggle (e.g. **"Has diagnostics"** — Code to propose the label), off = the email sequence drops diagnostic/Synap content for this class, exactly as the hint says today. Everything keyed off either switch (the PL-274 amendment-B composed variables, E-series conditionals, sweep skips, wizard step) now keys off the one. Migration: collapse the two columns to one, existing classes take the OR of their current values (both current test classes should end up unchanged in behavior); wizard + roster UI show the single switch.


## PL-311 — FO-only roster controls + an explicit "follow-on class" checkbox at creation (Scarlett, Aug 8, w/ screenshot)
The FO controls ("Auto-extend follow-on discounts" switch, "Marketing page URL" edit — and any other FO-only fields like the short marketing name) currently render on EVERY class card; they should show only on follow-on classes. Add an explicit **"This is a follow-on class"** checkbox to the class-creation wizard (and editable on the roster card), which is what gates those controls. Scarlett's example: SAT Math Deep Dive is "SAT Prep part 2," marketed to students who took SIS SAT Prep — Deep Dive is the follow-on; SIS SAT Prep is a feeder, not a follow-on. Note for Code: FO-ness is currently implicit (a class is a follow-on because feeders point at it); make the flag explicit, keep it consistent with the pointer machinery (e.g. warn if feeders point at a class not flagged, or flag auto-sets when first linked — recommend which), and hide the controls elsewhere. Split the two directions cleanly (Scarlett, Aug 8, confirming on the real Deep Dive card): a class flagged AS a follow-up (Deep Dive) shows the FO marketing controls (auto-extend, marketing URL, short name) but NOT the "Follow-up class:" feeder-side dropdown — a follow-up won't itself have a follow-up (if we ever chain one, the flag can simply be set on both and the dropdown returns); a feeder (SIS SAT Prep) shows the "Follow-up class:" dropdown but not the FO marketing controls.


## PL-312 — Rename "Follow-on" to "Follow-up" everywhere user-facing (Scarlett, Aug 8)
All user-facing occurrences: template display names (FO-1P "Follow-up announcement (parent)" etc. — display names only, keys stay FO_*), roster-card labels ("Follow-up discount window", "Auto-extend follow-up discounts", the PL-311 checkbox "This is a follow-up class"), the templates-page category header ("Follow-up marketing"), dashboard/admin copy, composed variable DESCRIPTIONS in the palette, and docs-facing hints. Internal identifiers (fo_* columns, FO_* template keys, function names) stay — no migration churn for a label. Verify none of Scarlett's approved email BODIES contain "follow-on" (they shouldn't — her copy says the class names); if any do, flag rather than silently edit her words.


## PL-313 — Close-match detection on incoming records: prompt to link, never auto-merge (Scarlett, Aug 8, w/ screenshot)
Real case from test data: pipeline lead "Bunji" (parent "BunjiPapa Kokobunji") + a later class registration "Bunji Kokobunji" (parent "BunjiPapa Kokobunji") = two unlinked records, and nobody learns the prospective student actually signed up for PSAT Prep. When the portal receives new person info (class registration, intake form, prospective-student add), check for close matches against existing families/students AND open pipeline leads — name similarity (first/last, contained names, "Bunji" ⊂ "Bunji Kokobunji"), matching parent/contact names, shared email or phone. On a plausible match, raise an admin to-do/prompt: "This looks like the same person — link them?" showing both records side by side with **link/merge** (pipeline lead marked converted-won and connected to the enrollment; family records merged where appropriate) and **not the same** (dismiss, remembered so it doesn't re-ask). NEVER auto-merge — admin confirms every link. To-do deep-links per the PL-298 rule. The match to-do/alert is visible to the MANAGER role too (Kelsie), and its alert category is in her PL-309 default subset.


## PL-314 — Schedule changes must notify from the moment a family registers, not from class-details (Scarlett, Aug 8 — supersedes the E4-only gate for schedule updates)
Scarlett's principle: a family registers SEEING the session calendar (register page shows it; they paid for what they saw). So the baseline for "what this family knows" starts at REGISTRATION, not at the class-details email. Real case: Bunji registered with 3 PSAT sessions on screen; session 4 was added; no email went out (current rule only notifies families with a sent E4). Fix: snapshot the session schedule per enrollment at registration time; any session add/edit/remove after that sends the schedule-update pair to every registered family whose last-communicated schedule differs (last-communicated = registration snapshot until E4 exists, then the E4/SU snapshot as today). Both paths (the event-driven per-session edit AND the sweep diff) use the same per-family baseline. Dedupe as today; plain-English change list ("A fourth session was added: Tuesday, September 8, 5:00–8:00 PM").

## PL-315 — Registration-confirmation emails include the full session schedule (Scarlett, Aug 8)
Add the composed session-schedule block (the same one class-details renders: dates, times, location, timezone line) to the #0-P/#0-S registration-confirmation pair, plus the course-calendar subscribe link — more places to see the calendar is better. New anchor-guarded versions, twins+seeds lockstep, composer-verified; degrade cleanly for classes whose schedule is not yet final if that state exists.


## PL-316 — Wizard Sessions step: Edit beside Remove (Scarlett, Aug 8, w/ screenshot)
The "Add a new class" wizard's Sessions list only offers Remove per row; add an **Edit** action (date/start/end — same calendar/time pickers as PL-289) so fixing one session doesn't mean remove + re-add.

## PL-317 — Online no-school class: timezone must be selectable in the Details step (Scarlett, Aug 8, w/ screenshot — likely PL-274 regression/gap)
With the online (not connected to a school) flavor selected, the Sessions step says "All times in America/Denver (the class timezone from the School step)" — but there IS no School step for this flavor, and Scarlett found no way to choose the timezone. PL-274 specced an explicit timezone picker (the PL-233 picker) for online classes; either it regressed or it is not reachable on this path. Fix: the timezone picker lives in step 2 (Details) for no-school flavors (online = must pick explicitly; in-person-at-HGL = fixed Denver as designed), and the Sessions-step hint names the real source ("the class timezone from the Details step"). Verify by creating an online class in a non-Denver timezone end-to-end (register page, calendar, emails all speak it).


## PL-318 — Instructors panel: actions column spills outside the card (Scarlett, Aug 8, w/ screenshot)
On Settings' Instructors table, the per-row actions ("edit profile" / "Make inactive") render past the card's right edge — outside the container border. Fix the layout (actions column inside the card; wrap or widen per the PL-275 overflow patterns) and give the actions column a proper header/width so it reads as part of the table at common widths.


## PL-319 — Recent parent activity: include pending reschedule REQUESTS with an action link (Scarlett, Aug 8, w/ screenshot)
The Tutoring → Recent parent activity feed shows only self-service completions ("family moved X → Y"). It must also show requests families made from the portal that need staff action — e.g. "Roman's family ASKED to move the Aug 8 session — 'Sunday at midnight please.'" — each with its status (pending / handled) and, while pending, an action link straight to the PL-297 approve-or-propose surface. Handled requests show what happened ("→ approved, moved to …" / "→ alternative proposed"). Same plain-English dating as the rest of the feed.


## PL-320 — Tutors table: subjects as grouped category summary, not a comma wall (Scarlett, Aug 8, w/ screenshot)
Billy's row lists ~35 comma-separated subjects and the row is a full screen tall. Replace with a per-tutor **category summary line** — "Math (9) · Science (8) · English (6) · History (5) · Test prep (7) · Languages (2)" — expandable (click/hover) to a chip grid grouped under those category headings. Categories derive from the existing subject taxonomy (Code: if subjects aren't categorized yet, add the mapping once, one source, and reuse it anywhere subjects render). The "any subject" filter keeps working and, when active, auto-expands/highlights the matching chip. Rows stay compact at every subject count; edit-profile subject picking can reuse the same grouping.


## PL-321 — Settings: editable price list as the single pricing source (Scarlett, Aug 8)
A Settings panel showing the price list — seeded from the pricing already in play: base 1-on-1 hourly rates, the add-on hour-block tiers (international: 5 @ $120 / 10 @ $105 / 15 @ $95; domestic at-HGL per PL-307: 5 @ $100 / 10 @ $90 / 15 @ $80 — stored the derived way, i.e. base rate + per-tier discount so tier prices track base changes), and whatever other standing prices the portal references. Editable (admin-only), and every FUTURE price reference resolves from it — e.g. the register flow's "Add 1-on-1 tutoring?" dropdown composes from this list at render time. Existing snapshots/receipts stay frozen as paid (price changes are forward-only, never retroactive). Code: inventory every hard-coded price in the codebase, move them here, and report the list so nothing silently stays hard-coded.


## PL-322 — Domestic pricing completion (Scarlett, Aug 8 — answers PL-307's two open questions)
1. **Domestic regular hourly rate CONFIRMED: $110** (Code's inference was right — keep regular_hourly_rate 110 on the domestic rows).
2. **Domestic post-class rates**, inferred from the intl pattern and confirmed by Scarlett: intl post-class = regular − $5 (1–9h: $125) and regular − $15 (10+: $115) off the $130 regular; the same offsets on the $110 domestic regular give **post-class 1–9h @ $105/hr · 10+ @ $95/hr**. Seed the two domestic post_class rows, tier-selected by the same at-HGL rule as PL-307 (and post-class tier selection follows the STUDENT/engagement's domestic-vs-intl provenance where no class is in play — recommend the rule, e.g. keyed off the feeder class that granted the post-class offer). Fold all of it into the PL-321 price list as the single source.

---

# Ship notes (Aug 8, 2026 — all thirteen items)

## PL-309 ✅ SHIPPED
`staff_alert_subscriptions` (migration 20260905000005 APPLIED): one row per staff email × category, `granted` (admin-controlled) + `enabled` (self-controlled within granted). NEW `app/utils/alert-categories.ts` = the ONE taxonomy: 12 categories; every AL_*/ADMIN_* registry template maps to exactly one (NEW gate `regress:alert-categories` proves no orphans — it also caught that AL_REFUND_REQUEST was sent but never registered; now mapped) and every code-only alert maps by dedupe-key prefix; anything unknown lands in `system`, never nowhere. `sendAdminAlert` is the one sender path: recipients resolve from enabled subscribers of the alert's category (60s cache), the callsite's legacy address stays as the zero-subscribers fallback, and `direct: true` (lead-assignment) bypasses subscriptions for personal alerts. Defaults: both admin identities seeded ALL ON (today's behavior byte-preserved); no manager profile exists yet — when one is created the API self-seeds Kelsie's subset (reschedule requests · timecards · coverage · close-match) and she appears on the panel automatically. Settings → Notifications panel: admin edits everyone (grant/revoke + toggle), manager edits own enabled within granted (server-enforced 403s). Verified live: GET matrix (2 staff × 12 categories), toggle round-trip, gate green.

## PL-310 ✅ SHIPPED
ONE switch. Migration 20260905000001 APPLIED (OR-fold — verified: every class keeps its effective behavior; the PSAT class's diagnostics-on/synap-off collapses to diagnostics-on, and step #2 ran for it before and still does). All reads rekeyed to `has_diagnostics`: stepDisabledForClass (#2 + #6 both key off it), ClassBundle/EmailContext/sample contexts, wizard (one checkbox, merged hint), roster (one checkbox). The physical `has_synap` column dropped via 20260905000002, **APPLIED after the batch-32 deploy went live** (verified: the new /api/admin/pricing route answered on prod first, and class-info stayed 200 after the drop) — nothing pending.

## PL-311 ✅ SHIPPED
`classes.is_follow_on` (migration 20260905000003 APPLIED; backfill flagged Deep Dive from the existing pointer). Wizard: "This is a follow-up class" checkbox (open-enrollment flavors only). Roster split per Scarlett: a flagged follow-up shows the marketing controls (auto-extend · marketing URL · short name) and NOT the feeder dropdown ("a follow-up has no follow-up" — chain by flagging both and the dropdown returns); a feeder shows the dropdown and no marketing controls. Reconcile decision (recommended + built): **auto-set on first link** — linking a feeder flags the target automatically; unchecking later keeps working but the card shows a plain ⚠ when feeders still point at an unflagged class.

## PL-312 ✅ SHIPPED
54 user-facing occurrences renamed follow-on → follow-up across roster labels/tooltips/hints, the six FO display names (code seeds AND the live DB registry rows), the templates-page group header ("Follow-up marketing"), variable palette descriptions, view-as heading, route error strings, wizard closure copy, and the min-enrollment brief prose. Internal identifiers (fo_* columns, FO_* keys, follow-on-extend route path, file names) untouched — no migration churn for a label. **Scarlett's copy check: no live template body or subject contains "follow-on" — nothing to flag.**

## PL-313 ✅ SHIPPED
`record_matches` (migration 20260905000006 APPLIED): one row per (lead × student) pair, pending → linked / not_same; the unique pair IS the memory — a rejected pair can never re-ask (proven: re-insert blocked, rescan finds 0). Matching (`app/utils/close-match.ts`): name containment ("Bunji" ⊂ "Bunji Kokobunji") + 2-edit typo tolerance + shared email; high-precision rule = same email alone, OR student-name AND parent-name both alike (one lone likeness against the whole table would prompt constantly — the PL-194 typing radar covers those). Hooks: registration (behind the response), intake completion, prospective-student add. Surfaces: dashboard "Possible duplicate person" to-do (staff-gated — manager sees it; category `close_match` is in her default subset) deep-linking `/admin/leads?lead=…&match=…`; the lead card renders the side-by-side panel (pipeline lead vs registered record + why it matched) with **Link them** (lead → Started, connected to the family/student — nothing else merges) and **Not the same — don't ask again**. NEW AL_CLOSE_MATCH draft template (twin sends meanwhile). NEVER auto-merges. Verified E2E: QA lead → prompt with both name reasons → link converts the lead (status + both ids) → second lead → not_same → remembered.

## PL-314 ✅ SHIPPED
`enrollments.schedule_snapshot` (migration 20260905000004 APPLIED): stamped at registration (and waitlist-join) with exactly what the register page showed — full session list + first session + location, `origin: 'registration'`. Baseline rule everywhere: registration snapshot until an E4 exists, then the E4 snapshot as today (existing enrollments keep NULL → E4-gated exactly as before; we can't know what they saw). BOTH paths share it: the sweep now also diffs registration-baseline enrollments AND diffs the full session list (E4 snapshots now carry `sessions` too — older ones without it keep the legacy 3-scalar diff), so ANY add/edit/remove notifies — not just first-session moves; the event-driven per-session edit route counts registration-snapshot holders as informed. Plain-English sentences from ONE shared differ (`sessionListChanges`): "A fourth session was added: Tuesday, September 8, 2026 · 5:00 PM–8:00 PM." — Bunji's exact case unit-proven, plus removal/time-change/split-day pairing. Registration-baseline families never get "Instructor: now X" noise (they were never told one). Baselines refresh after send on both paths (transition-hash + seq dedupe preserved). Bonus fix: the SU code twin no longer double-prefixes "now" on pre-phrased change lines. Verified E2E: registered on a QA class (snapshot stamped verbatim) → marked paid → edited a session → SU pair composed and attempted for parent AND student with NO E4 in existence (the PL-87 dev-origin guard refused actual delivery, as designed on a dev machine) → snapshot refreshed so the sweep won't re-announce.

## PL-315 ✅ SHIPPED
NEW `{sessionScheduleBlock}` composed variable (dates · times · location · the PL-305 zone line · course-calendar subscribe link), resolving from the enrollment context so every send path conditions identically; EMPTY (no dangling heading) while a class has no sessions. Published via anchor-guarded script: **E0-P v8 + E0-S v4 LIVE**; seeds + code twins in lockstep — the twin renders the same markdown through the same comms-md pipeline, proven byte-identical to the DB fragment. **Prerequisite bug found & fixed (live since batch 28): solo markdown-flavored block variables inserted RAW — live E3/E4/E5 sends to diagnostics-on classes carried literal `**bold**` and a dead unrendered `[button:Access Diagnostic Tests]`; the six live FO templates' More-info link likewise.** comms-md now renders md-flavored blocks through the pipeline (new `md` flag on 9 block variables); all 10 affected live templates re-verified clean. All portal data is test data — no real family received the artifacts.

## PL-316 ✅ SHIPPED
Wizard Sessions rows: Edit beside Remove — inline editor with the same date input + DateHint + TimeSelect pair + location field as the add form; Save replaces in place.

## PL-317 ✅ SHIPPED (confirmed regression-gap)
Confirmed: `openTimezone` state existed but NO picker ever rendered — online no-school classes silently stayed America/Denver. The PL-233 `TimezoneSelect` now renders in step 2 (Details) for the online flavor; at-HGL shows the fixed-Denver note; the Sessions hint now says "(the class timezone from the Details step)". Verified end-to-end with a QA Europe/Madrid online class: register page renders "(times shown in Madrid time)", the schedule snapshot and SU machinery carry it, and emails' zone lines compose from the same PL-305 source.

## PL-318 ✅ SHIPPED
Instructors table: `overflow-x-auto` scroll container (the missing PL-275 wrapper), a real "Actions" column header, and the actions cell keeps its buttons on one line inside the card.

## PL-319 ✅ SHIPPED
Recent parent activity = one merged feed: self-service moves (as before) AND requests — "family ASKED to move the … session — '…'" with a "waiting on you" chip + "approve or propose →" linking the PL-297 surface (`?session=…&reschedule=1`) while pending (same pending rule as the dashboard: future confirmed session with a request stamp); handled requests show the outcome ("→ moved to …", forfeited/no-show, or went-ahead). Panel subtitle updated.

## PL-320 ✅ SHIPPED
NEW `app/utils/subject-groups.ts` — ONE mapping (Math · Science · English · History & social studies · Languages · Test prep · Other; DB test_prep category always wins; unknown names land in Other, never crash). Tutors table: compact per-tutor summary line ("Math (9) · Science (8) · … · with prep (3)") expanding to a grouped chip grid; an active "any subject" filter auto-expands matching rows and highlights the chip (amber for with-prep); the filter dropdown itself groups via optgroup. Edit-profile picker reuses the same grouping with headings.

## PL-321 ✅ SHIPPED
Prices stored the DERIVED way: `tutoring_packages.discount_per_hour` (NEW) + base `regular_hourly_rate`; a DB trigger recomputes hourly_rate + package_price (migration 20260905000007 APPLIED — backfill reproduced every current price exactly: intl 130−10/25/35 → 120/105/95·600/1050/1425, domestic 110−10/20/30 → 100/90/80·500/900/1200, post-class 130−5/15 → 125/115). Settings → Price list panel (admin-only, self-gating): tier base rates (edit one number, every tier price moves — verified live 110→111 cascaded to 101/91/81 & 505/910/1215, restored), per-row discounts, base 1-on-1 category rates (subjects table; mixed-rate detection), and the fees. **Hard-coded price inventory:** (1) $40/hr late-reschedule fee — computational constant now reads `app_settings.late_reschedule_fee_per_hour` (default 40); (2) 10% late-payment fee — now reads `app_settings.late_fee_percent` at apply time; (3) LEFT hard-coded and reported: the "$40/hour" phrasing inside Scarlett-approved email/template copy (12 sites incl. one seeded template body — her words, changing them is her call on the templates page; the panel says so), sample/preview dollar figures in composer samples (not real prices), and per-class prices/promo amounts (deliberately per-class on rosters, noted on the panel). Register dropdown, checkout validation, and engagement-rate prefills already resolve from the DB at render time, so every future reference tracks the list; paid snapshots stay frozen.

## PL-322 — Domestic pricing completion (Scarlett, Aug 10: regular $110 CONFIRMED) ✅ SHIPPED Aug 10
Seed the two domestic post-class rows (1–9h @ $105 · 10+ @ $95 — the intl −$5/−$15 offsets on the $110 regular), tier-selected by the student's at-HGL provenance where no class is in play (recommend the rule), folded into the PL-321 price list as derived rows.

**Ship note:** the $110 domestic regular is no longer flagged as inferred — confirmed. Migration 20260906000001 APPLIED: "Post-Class Hourly (1–9 hours, at HGL)" and "(10+ hours, at HGL)" seeded the DERIVED way (base 110, discounts 5/15 — the PL-321 trigger computed $105/$95 exactly). Tier now rides `loadTutoringPackages(tier?)` and every consumer picks its sheet from the class in play: the E8 post-class offer + E9 upsell composers (`enrollmentTutoringTier(ctx)` — the same rule on the ctx shape), the sweep's per-bundle package passes, the portal tutoring-addon card (pre AND post pitches), the add-hours page, and the addons checkout now REFUSES a package from the wrong sheet (same fail-closed rule as the register checkout). **Recommended + implemented provenance rule for no-class-in-play flows:** `studentTutoringTier(studentId)` — the student's MOST RECENT paid group-class enrollment's flavor decides (at-HGL → domestic); no class history → international (the pre-existing sheet; nobody gets an unearned domestic discount, and staff can always override the engagement rate). Nothing consumes it yet — every live pricing surface has a class in play — it's the standing rule for future pure-1-on-1 flows. Price list panel: post-class hourly renders per tier with the same editable base + derived discounts as the block tiers. Verified: trigger derived 105/95 byte-exact; Bunji (at-HGL PSAT) → domestic, a school-class student → international, no-history → international; tier-filtered loads return exactly each sheet (pre 100/90/80 · post 105/95 vs post 125/115); tsc + build + links/client-imports/package-overdraw gates green.
