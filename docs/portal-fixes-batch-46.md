# Portal fixes — batch 46 (ACCUMULATING — opened Aug 31, 2026)

**Standing rules:** all prior (batch 41/42/44 lists).

(CLOSED for batch 46 hand-off, Sep 1. Next PL: PL-443.)

## ✅ PL-439 — SHIPPED (Sep 1) + RAMPED LIVE
**Recipient logic:** `classes.created_by` added (**migration `20260915000001` APPLIED** via the Management API; staff email, lowercase, same shape as `class_drafts.created_by`). The wizard stamps it at completion — a resumed draft's own stamp carries through (`resumeDraft.createdBy` flows draft → wizard → class row, per the item; a straight-through run stamps the signed-in staff member; an unresolvable identity stamps NULL rather than blocking the create). **Backfill ran honest and empty:** completed drafts are DELETED at create (PL-370), zero draft rows survive, and no other audit trail ever recorded class creators — so **0 classes backfilled; all 10 pre-existing classes carry NULL and fall back to the admin default** (ASF SAT Prep · MIS SAT Prep Jul (cancelled) · Nido (cancelled) · SLS (cancelled) · PSAT Prep · Online R/W Deep Dive (cancelled) · MIS SAT Prep Sep · AISCT (cancelled) · ISD SAT Prep · Online Advanced Math Deep Dive). The sweep resolves the creator through the new shared `creator-recipient.ts` (also PL-442's reminder): creator counts only while their email still resolves to an ACTIVE staff profile (admin/manager); known+active → **direct send to the creator, no subscription fan-out**; unknown or departed → the standing default (class_ops subscribers w/ the legacy ADMIN_EMAIL fallback) — never silently nobody. The dashboard Needs Attention row is untouched (all admins still see it — the email targets, the row informs). **E2E 12/12:** QA class created_by billy@ → sweep rang exactly once, recipient = the creator alone, `AL_COLLATERAL_NUDGE` template key stamped, subject names the class, re-run deduped; unit checks: departed email → null, parent-role profile (eric@) → null (role matters, not just a profiles row), NULL → null, case/whitespace-insensitive. **Ramp (batch-43 rules) 10/10:** active version IS the reviewed one (v1 `cc11fd36…` — the `[DRAFT REVIEW]` send's `body_snapshot_id`), bytes untouched since seeding, flipped LIVE, `renderRegistered` now serves the registry version **byte-for-byte** vs the `renderVersion` review path on the same ISD fixture, spot send `[RAMPED LIVE: AL_COLLATERAL_NUDGE v1]` → billy@ `sent`.

## PL-439 (original) — AL_COLLATERAL_NUDGE goes to the class's CREATOR (Scarlett, Aug 31 — copy approved as-is)
Scarlett approved the nudge copy; the recipient should be the person who created the class, not the general admin audience — they skipped the collateral, they get the reminder. Send to the creator's staff email: record `classes.created_by` at wizard completion if it doesn't already exist (drafts already stamp who; carry it through create; backfill existing classes where the draft/audit trail knows, else NULL). Fallbacks honest: creator unknown (imports, legacy, backfill-blind) or no longer active staff → the current admin default recipient (never silently nobody); note in ship notes which classes fell back. The dashboard Needs Attention row stays visible to all admins regardless (the email targets, the row informs). Ramp: the copy is approved — activate AL_COLLATERAL_NUDGE v1 as-is once the recipient logic lands (same byte-for-byte rule as the batch-43 ramp).

## ✅ PL-440 — SHIPPED (Sep 1) — RAMPED LIVE, activation only
`AL_AVAILABILITY_UPDATED` v1 flipped LIVE with **zero copy edits — 10/10 checks:** the active version IS the reviewed one (v1 `d2dd5297…` — the batch-45 `[DRAFT REVIEW]` send's `body_snapshot_id`), bytes byte-identical to the seed, live gate now serves the REGISTRY body (`renderRegistered` versionId = the active version, the code twin untouched as fallback), and the live render is **byte-for-byte identical on subject + html** to the `renderVersion` review path on the same Janeson fixture (real composed diff via the `availability-diff.ts` leaf — Monday unchanged · Tuesday ADDED · Sunday REMOVED — real names, real `?availability=` link; subject = "Janie updated Janeson Foney Fony's availability", exactly the reviewed subject). One spot send `[RAMPED LIVE: AL_AVAILABILITY_UPDATED v1]` → billy@ `sent`. **The drafts-awaiting-ramp list from the batch-45 close is now EMPTY** (PL-442 seeds one new draft — see its note).

## PL-440 (original) — Ramp AL_AVAILABILITY_UPDATED (Scarlett approved the review render, Aug 31)
Activate AL_AVAILABILITY_UPDATED v1 LIVE exactly as reviewed — byte-for-byte vs the review render, zero copy edits, live gate serves the registry body, one spot send → billy@ (batch-43 ramp rules).

## ✅ PL-441 — SHIPPED (Sep 1)
**ONE instructor-clock composer:** `instructorWhenPhrase` in `dates.ts` beside `familyWhenPhrase` (PL-419's family pattern, instructor edition — DST-correct via `zonedToUtc`). Instructor's profile zone leads, labeled **"your time"**; class-local rides secondary with the class's public city (`contextTimeCityLabel`) where the zones differ; the secondary carries its own date only when the calendar dates diverge across zones; same zone — or no profile zone — renders **once**, still city-labeled (never bare numbers, never an assumed Denver); a timeless session stays a plain date. **SWEEP — every instructor/tutor-audience surface, verdict each:** ① `IN_WELCOME` `{classScheduleBlock}` (`scheduleListHtml`) — **CONVERTED, the reported bug**: was a bare class-local 24h slice ("18:30–20:30", no zone anywhere); the instructor's `timezone` was already loaded at the callsite and never read. Template is LIVE and untouched — the fix rides the composed variable (the PL-419 lever), zero version changes; the pinned editor sample is now COMPUTED through the composer (PL-137 anti-drift; Stockholm class ÷ SLC instructor so the preview shows the real both-clocks shape). ② `T3_TUTOR_PATTERN` (pattern-change tutor note — the closest thing to a "T_SCHEDULE_SET instructor edition", which as named does not exist) — **CONVERTED**: it reused the FAMILY-clock `scheduleSummaryText`, so a Rome family's plan reached an SLC tutor as "Wednesdays from 12:00–1:30 AM"; the tutor copy now renders on the tutor's own wall clock, "(your time)" labeled. ③ `IN_DIGEST` (weekly digest, min-met, class-full, the PL-403 deadline briefing, roster additions) + `IN_MIN_DECISION` — **NO session times rendered** (calendar dates only via `formatDateFull`) — left as dates by design. ④ `IN_FYI` — embeds the FAMILY's email verbatim ("copy of what your families received") — converting would misquote what families were told; left BY DESIGN. ⑤ Roster report — **admin-addressed** (sendAdminAlert), not an instructor surface. ⑥ Tutoring-side sends (T3_TUTOR_NOTICE, coverage trio, T6 notes, T5 timecard-Denver-anchor) — already recipient-zone (the pattern this item extends); untouched. **Tutor PORTAL verified unchanged** (tutoring panes render on `instructors.timezone` as before; class-session `SessionCalendar` keeps its labeled class-city clock — no portal files touched). **Composer-path verify 18/18, incl. THE REAL MIS CASE:** real bundle + real instructor row → "Tuesday, September 8, 2026 — **10:30 AM–12:30 PM your time · 6:30–8:30 PM in Munich**" per session (doc's illustrative "11:30 AM–1:30 PM" was an hour off — CEST↔MDT is 8h); missing-tz fixture → labeled Munich only; same-zone → renders once (no "6:30 PM your time · 6:30 PM in Salt Lake City"); Sydney date-divergence → secondary carries its own date. **Honesty note:** `instructors.timezone` is `NOT NULL DEFAULT 'America/Denver'` (phase-7a), so live rows always carry a zone and some seeded rows were flagged "timezone ~guessed" at import — the composer honors the profile value the tutors panel shows (fix a wrong zone there and every email follows); the no-tz fallback is real at the composer level and guards any path that loses the profile.

## PL-441 (original) — Instructor-audience class emails: times in the INSTRUCTOR's timezone (Scarlett, Aug 31)
The you're-teaching assignment email lists MIS session times as bare "18:30–20:30" — class-local (Munich), unlabeled, confusing for an SLC instructor. Instructor-audience class emails render times in the INSTRUCTOR's own timezone (from the instructor/tutor profile — the timezone the tutors panel already carries; PL-419's family pattern, instructor edition), labeled, with the class-local time as the secondary where they differ ("11:30 AM–1:30 PM your time · 18:30–20:30 in Munich") — an online class taught across zones needs both, the instructor's first. Profile has no timezone → fall back to labeled class-city time (never bare numbers; never assume Denver). SWEEP every instructor/tutor-audience email carrying class session times (assignment details, final-count/deadline briefing, roster report class rows if instructor-addressed, weekly digest, T_SCHEDULE_SET instructor edition if any) — converge on one instructor-clock composer; ship notes list each surface. The tutor PORTAL already shows their own times (verify unchanged). Registry rule for touched live templates. Verify composer-path: SLC instructor + Munich class = both clocks; instructor-tz-missing fixture = labeled Munich only; same-zone renders once (no silly "6:30 PM your time · 6:30 PM in Salt Lake City").

## PL-442 — Wizard guards: cutoff can't precede the deadline; Synap group can't be forgotten (Scarlett, Sep 1)
**A. Date-order validation:** the wizard accepted a sign-up cutoff (Aug 14) EARLIER than the registration deadline (Sep 8) — nonsensical (decisions would run on a deadline after sign-ups already closed). Validate: cutoff, when set, must be ≥ the registration deadline (and sanity vs first session per the existing blank=first-session rule); refuse with plain English at the field ("the sign-up cutoff can't be before the registration deadline — families could still be deciding") via the wizard's existing 'Next needs:' machinery. Same guard on the Edit-class-details inline edits (PL-383) — both surfaces, one validation source. Backstop: sweep existing classes for the inverted pair (report, don't mutate). **B. Synap group required-with-escape, tied to the diagnostics fact:** the field is skippable silently today. When **Has diagnostics is ON**, the class needs a Synap group for every diagnostic email link — require the URL OR an explicit checkbox "no Synap group yet — I'll add it before family emails send" (deliberate skip, recorded who/when, and it feeds a state-driven reminder exactly like the collateral pattern [PL-429]: dashboard row + one urgency-keyed email to the class creator when the first synap-consuming email approaches with the field still blank; self-clears on fill). When Has diagnostics is OFF, the field is irrelevant — hide it or label it inert ("no diagnostics — not used"), don't require. E0/#2 etc. already guard on hasDiagnostics; this closes the diagnostics-on-but-no-group hole where emails would send with a missing/broken access link — AUDIT what those templates currently render when the group is blank (fail-closed check, PL-429 style; fix any that render a dead button). Verify: wizard refuses the inverted dates w/ the plain reason; diagnostics-on + blank + unchecked refuses Next; checkbox path records + reminds + self-clears; diagnostics-off hides; blank-group render audit verdicts in ship notes.
