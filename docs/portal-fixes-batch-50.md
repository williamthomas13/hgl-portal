# Portal fixes — batch 50 (CLOSED for hand-off Sep 21, 2026 — 9 items, PL-461…469)

**Standing rules:** all prior, plus the PL-460 rule (every call-to-action lands where the action can be done).

(Opened + closed Sep 21, 2026. Next PL: PL-470.)

**Batch closed Sep 22 — 9 of 9 shipped.** Gate battery vs the PROD build on :3100, pass COUNTS confirmed: `tsc` · `next build` · `smoke:public` (11) · `regress:links` (98 — the real-context section SKIPS honestly: no enrolled class exists post-purge; 134 once the import lands) · `regress:pronouns` (61) · `regress:client-imports` (0 violations) · `regress:alert-pins` (23) · `regress:alert-categories` (25 keys) · `regress:billing-recipient` (31) · `regress:cta-landings` (215) · `regress:silent-import` (39) · `regress-cancel-class` (11) · `regress-resume-addon` (13 — harness FIXED this batch: it grabbed the newest open class on prod, which post-purge is a real record-only class with a closed deadline; it now creates its own self-cleaning class) · `pl402-drift-alert-e2e`: **precondition unmet post-purge** ("need 3 future synced sessions, found 0" — every tutoring session was QA and is gone; the gate returns once real tutoring is scheduled). Crashed gate ≠ green gate — both skips are named, not hidden. Migrations `20260919000001`…`04` APPLIED column-first; nothing pending. **Awaiting Scarlett:** PL-464 B "parent wins" veto · PL-461 review send → ramp · PL-469 limit values · PL-463 D instructor assignments (+ Kevin-only on ASF, Eric noted) · the dry runs / imports · Leone XIII's placeholder contact. **Gate note:** `regress:links` real-context section SKIPS while no enrolled class exists post-purge (98 instead of 134 incl. T1R); it returns to full once the import lands.

**THE CUTOVER ITEMS ARE ON PROD (Sep 22): PL-463 · PL-464 · PL-465 · PL-467** — see the ✅ notes; migration `20260919000001` APPLIED column-first. Scarlett can now (1) assign the real instructors to the four classes (PL-463 D — expected: no sends, no calendar events), (2) run the dry runs with the cleaned sheets (outcomes / add-on / no-parent-email rows all handled — Code has not run any import against prod), (3) end Leone XIII's placeholder contact. **PL-464 B "parent wins" is a proposal awaiting her veto.** PL-469, PL-468, PL-466, PL-461, PL-462 follow in this batch.

**ORDER: the cutover waits on PL-463, PL-464, PL-465 — ship those three (and the small PL-467) FIRST and say so in the header note the moment they are on prod.** Then PL-466, PL-461, PL-462. Code does NOT run any import against prod — Scarlett + Claude run the dry runs and the imports themselves.

**Go-live state (Sep 21):** QA purge APPLIED on prod by Scarlett + Claude (1,297 rows + 3 QA logins; snapshot kept locally; post-purge dry run = 0 families / leads / contacts / sends, only `mis-sat-prep-fall26` survives). Eric + Kelsie are managers. Intuit app has BOTH development and production keys issued (Scarlett, Sep 21). Next: create Leone XIII school + current SLS / ASF / Leone XIII classes → silent import of the four rosters → public-page walkthrough → domain → QBO production → Stripe live → DNS.

## ✅ PL-461 — SHIPPED (Sep 22) — T1R seeded as a DRAFT v1 (Scarlett's copy verbatim) for the review send → ramp; migration `20260919000004` APPLIED (`tutoring_invoices.change_replied_at`)
**The idiom matched:** the school announcement panel (PL-449/452 `school-comms.tsx`) — preview EXPANDED (subject + recipients + the rendered body in a frame) before a single **Send**, with **Back**. **Three ways to close, side by side on the change-request box (375px: they wrap):** ① **Send updated proposal** (primary) → opens the preview of exactly what the family gets (To/cc per the PL-454 routing: `T1R_UPDATED_PROPOSAL` is `billing: 'copies-parent'` like T1 — billing contact to, parent cc), Send → T1R goes out, the request closes, the family's window restarts from now; ② **Mark handled — no email** → the reason line is required unless a session already changed; sends NOTHING; the success message says the two next steps plainly ("confirm for family" under details if they agreed on the phone, or leave it and they confirm on their own, fresh window from now); ③ **Reschedule a session** (PL-459) makes the change and does NOT close. The shared text box is the staff reply (goes into the email verbatim as `{staffNoteBlock}`) or, closing without an email, the reason. **Never auto-sends** — the email is a click, never a side effect. **ONE close transition** `closeChangeRequest()` for both: appends "[ts] HGL reply (who): … — updated proposal emailed | — no email" to the trail, stamps `change_replied_at`, clears `change_requested_at`, re-stamps `proposal_sent_at` (window restarts) and the nudge clock, writes the activity-feed line. **Registry:** `T1R_UPDATED_PROPOSAL` (sequence T1r, parent, info@, transactional) with the approved subject / preheader / body byte-for-byte; new variables `{requestQuote}` (the family's latest request, escaped), `{staffNoteBlock}` (omitted entirely when empty — no empty paragraph), `{changeSummaryBlock}` (one line per session moved / added / removed since the family last saw it — the later of the last reply and the proposal send — in the family's timezone: "Tuesday, Oct 27 — moved from 11:00 AM to 5:00 AM"; nothing changed → "No changes to the schedule — see our note above."); sample data + variable docs added; **not in this email:** `{autopayBlock}`, `{packageNote}`, T1's month-end policy sentence. Code twin `t1rUpdatedProposalEmail()` sends identical copy until the draft is flipped live (the PL-445 ramp technique; `regress:links` renders the draft clean with the samples). **Family proposal page:** after either close, Confirm is live again and a small "Updated {date} in reply to your request — confirm below, or request another change." line shows; a new request re-opens the loop (pill back to "needs our reply"). **Staff email (PL-459 D)** unchanged — "then mark the request handled" is now true in both senses. **CTA registry:** the row's landing control is now `send-updated-proposal` (plus `mark-handled-no-email`, `send-updated-proposal-confirm`, `t1r-preview` test ids); T1R's two links are the already-registered `confirmOneTapLink` / `confirmLink`. Gates: `regress:cta-landings` (215), `regress:billing-recipient` (31 — T1R in the verdict table), `regress:links` (T1R draft renders clean). **For Scarlett:** review-send T1R from the templates page, then flip it live (ramp) — until then the twin sends the same words.

## PL-461 — T1R "Updated proposal": the family hears back after a change request — and staff can close one WITHOUT an email (Scarlett, Sep 20–21)
PL-459 F stopped honestly: no family-facing re-send path exists. Build it, with the copy below (APPROVED by Scarlett Sep 21), and make the no-email close a first-class choice.
- **Three ways to close an open change request, side by side on the invoice row's change-request box (375px too):**
  1. **Send updated proposal** (primary once a session was changed or a note is typed) → sends T1R to the family (billing-contact routing per PL-454: same verdict as T1 — billing contact to, parent cc), marks the request handled, restarts the family's auto-confirm window from now.
  2. **Mark handled — no email** → for when staff settled it by phone/WhatsApp ("5:00 am" is unusual enough that Kelsie would rather call). Asks for a one-line reason (activity feed), sends NOTHING, marks handled. Then the row offers the two existing next steps plainly: **confirm for family** (they agreed on the phone — the existing control) or leave it awaiting the family's own confirm (auto-confirm window restarts from now, on the schedule as it now stands). Never auto-sends T1R later because a session changed — the email is a choice, not a side effect.
  3. **Reschedule a session** (PL-459 A) stays the way to make the change itself; it does not close the request.
  Same pattern as other staff-decides-whether-to-email surfaces in the portal (find the closest existing one and match its wording/controls rather than inventing a new idiom; name it in the ship note).
- **Preview before send:** the T1R button opens the rendered email (what changed + note) with Send / Back — staff see exactly what the family will get.
- **Registry template `T1R_UPDATED_PROPOSAL`** — `billing: 'copies-parent'`, audience parent, from info@, transactional, sequence `T1r`:
  - **Subject:** `Updated: {studentNames}'s tutoring schedule for {tutoringMonthLabel}`
  - **Preheader:** `We've replied to your change request — please take a look and confirm`
  - **Body:**
    ```
    ## {studentNames}'s updated {tutoringMonthLabel} schedule

    Thanks for letting us know what wasn't working. You asked:

    > {requestQuote}

    {staffNoteBlock}

    **What changed**

    {changeSummaryBlock}

    Here's the full {tutoringMonthLabel} schedule as it stands now:

    {scheduleBlock}

    {monthTotalLine}

    [button:Confirm schedule]({confirmOneTapLink})

    [Still not right? Request another change →]({confirmLink})

    If we don't hear from you within {autoconfirmDays} days, this updated schedule confirms automatically, exactly as shown.

    {contactBlock}
    ```
  - `{changeSummaryBlock}`: one line per session moved / added / removed since the proposal the family last saw, in the family's timezone — "Tuesday, Oct 27 — moved from 4:00 PM to 5:00 AM". Nothing changed (note-only reply) → "No changes to the schedule — see our note above."
  - `{staffNoteBlock}`: the typed reply, verbatim, as its own paragraph; omitted entirely when empty (no empty heading). `{requestQuote}`: the family's request text, escaped.
  - Deliberately NOT in this email: `{autopayBlock}`, `{packageNote}`, and T1's month-end policy sentence (they saw all three in T1; this email is about the one thing they asked for).
  - Sample data for the comms preview + the variable docs, like every registry template. Ships as a DRAFT for the usual review send → ramp.
- **Family proposal page:** after T1R (or a no-email close), the page shows the current schedule with Confirm live again and a small "Updated {date} in reply to your request" line; a second request re-opens the loop (pill back to "Change requested — needs our reply").
- **Staff email (PL-459 D) stays as approved;** its "then mark the request handled" clause becomes true in both senses once this ships.
- CTA registry: register the three controls + T1R's two links (PL-460 gate).

## ✅ PL-462 — SHIPPED (Sep 22)
Every instructor-facing link that names a class (IN_WELCOME / DIGEST / MIN_DECISION's "Open your class page") now carries `&class={id}`; the instructor view gives each class card `id="class-{id}"` and a small client `FocusParam` scrolls to it and rings it (the portal twin of the admin deep-link focus). Registered in the CTA gate (`instructorViewLink` → `/portal?view=instructor&class={id}`, marker `FocusParam`).

## PL-462 — Instructor view: `?class=` focus (proposed by Code in PL-460)
As proposed in the PL-460 ship note — instructor-facing links that name a class land on that class's card, scrolled + highlighted, not the top of the instructor view. Register in the CTA gate.

## ✅ PL-463 — SHIPPED (Sep 22) — ONE derived rule, `instructorQuietReason()`, in front of every instructor send and every calendar write
**Root confirmed:** `instructors.comms_enabled` was dropped in PL-327; the live gate is `pref_class_digests !== 'off'` (default on), and once-per-class sends (IN_WELCOME, the deadline briefing, milestones) carry only a dedupe key — no date guard — which is exactly why the purge's empty send log re-welcomed MIS mid-run. **A + B, one rule (no new column):** `instructorQuietReason(bundle)` returns a reason for: a cancelled class · a class whose LAST session has passed · a **records-only class** (≥1 paid enrollment and every one is `comms_muted`) · a class **already running with no portal enrollments** (first session passed, zero paid — the MIS-between-purge-and-import case, so assigning Rebecca before her roster lands is safe too). `loadClassInstructor()` returns null when quiet — that funnels IN_WELCOME, both digests, the roster "+1" ping, milestones and the FYI copies; `sendMinEnrollmentDecisionNote` (its own select) got the same guard; `syncInstructorClassCalendar` returns before ANY write — create, patch, and the reassignment DELETE alike (a running class the portal doesn't drive keeps whatever its instructor already has, e.g. Billy's existing MIS events). **Which already had a guard:** none of the five had a date guard; the weekly digest stops at the first session and the roster ping at the last session by their own conditions, the welcome/briefing/milestones/FYI/min-decision had only dedupe keys. **C:** the stale header comment in `instructor-comms.ts` rewritten; the PL-457 audit row in the batch-49 doc corrected. **Gate:** `regress:silent-import` now assigns Billy to the muted fixture class, asserts the quiet reason, runs the real `sweepInstructorComms` (0 welcomed / 0 digested, no `in_welcome:` row) and the real `syncInstructorClassCalendar` (no session gains an event id), plus the ended / running-with-nobody / live cases on synthetic bundles. **Honest note:** the FIRST run of that extended gate, before the fixture was fully muted, DID create six class-session events on Billy's Google calendar (two runs × three fixture sessions, titled "HGL SAT Prep — class session", Oct 4/11/18); a new read-only `listGcalEvents()` helper found exactly those six and they were deleted; the gate's cleanup now removes any event the sync writes. **D (Scarlett):** assign SLS → Gwen De Silva, MIS → Rebecca Baumher, Leone XIII → Kevin Marren, ASF → Kevin Marren (+ Eric Brown — a class holds ONE instructor (`classes.instructor_id`), so Kevin; Eric can be noted on the class), then check the send log for `in_welcome:` / `in_digest` rows and one instructor's calendar — expected: nothing.

## PL-463 — Instructor comms + calendar must stay quiet for records-only and finished classes (found Sep 21 while preparing the silent import)
PL-457's audit row says instructor IN_WELCOME / DIGEST / MIN_DECISION are "gated by `instructors.comms_enabled`" — that column was DROPPED in PL-327 (`20260906000005`); the live gate in `instructor-comms.ts` is `pref_class_digests !== 'off'`, default ON, and the header comment there still describes the old switch. Consequence: assigning a real instructor (Gwen, Kevin, Rebecca…) to the SLS / Leone XIII / ASF / MIS record-only classes would send them an IN_WELCOME mid-class, roster-change pings as the import lands (the `today >= deadline && today <= lastSession` branch), and push class-session events to their Google calendars. **Workaround in force:** the new record-only classes are created with NO instructor (MIS keeps Billy) until this ships; then Scarlett sets the real ones — SLS: Gwen De Silva · MIS: Rebecca Baumher · Leone XIII: Kevin Marren · ASF: Kevin Marren + Eric Brown (her tracker's "Classes & Dates" tab; if a class can hold only one instructor, Kevin, and say so).
- **Evidence (Sep 21, 09:05 UTC):** minutes after the purge cleared the send log (and with it every dedupe key), the hourly sweep re-sent `IN_WELCOME` for `mis-sat-prep-fall26` — a class two weeks into its run — plus a `T5_TIMECARD_READY` for Sept 1. Both went to Billy only because MIS carries Billy as instructor; with Rebecca assigned it would have gone to her. So: once-per-class instructor sends have no date guard, only a dedupe key.
- **Created Sep 21 via the wizard (Claude, at Scarlett's request):** `sls-sat-prep-fall26` (8 sessions Mon/Wed Sep 7–30, 18:30–20:30 Milan, $749, deadline + cutoff Sep 4, NO instructor) and `asf-sat-prep-fall26` (in person, 10 sessions 14:45–16:00 Mexico City incl. the optional Sep 25 Skill Mastery Lab, $749, cap 18, deadline + cutoff Sep 8, NO instructor). `leone-sat-prep-fall26` created the same day (school "Leone — Istituto Leone XIII", Europe/Rome, with `billy@` as a PLACEHOLDER first contact at Scarlett's instruction — the form would not save without one, PL-467; 8 sessions Tue/Thu Sep 8–Oct 1, 18:30–20:30, $749, deadline + cutoff Sep 4, NO instructor). Per-class Synap groups set on all four; MIS price corrected 750 → 749; ASF location = "ASF campus — Room US 109".
- **A.** No instructor send and no gcal class-event write for a class whose last session is in the past (verify each of IN_WELCOME, digest, roster ping, min-decision, FYI; say which already had the guard).
- **B.** No instructor roster/welcome sends caused by `source='import'` `comms_muted` enrollments: a class whose Paid enrollments are ALL muted is a records-only class → instructor sends + gcal writes skip it (derive; no new column unless needed — say which).
- **C.** Correct the PL-457 audit row and the stale header comment in `instructor-comms.ts`.
- **D.** After it ships, Scarlett swaps the real instructors onto the four classes; verify nothing sends and no calendar events appear (send log + one instructor calendar).
- Extend `regress:silent-import` with the instructor assertions.

## ✅ PL-464 — SHIPPED (Sep 22) — **B's rule "the parent version wins" is a PROPOSAL — Scarlett's veto**
**A.** Import: a row with no parent email and a student email imports with `families.parent_email` = the student's address (ONE column, one identity path; the verdict table flags "STUDENT address"). Neither address → refused with a clear line. Staff "add a registration" (`staff-enroll`) had REQUIRED a parent email → now applies the same fallback. The marker is DERIVED (no column): a family whose parent address equals one of its students' addresses shows **"no parent email — using the student's address"** on the family profile header (amber pill, hover text) and on the roster row ("student's address" tag + hover). **B. One person, one email — at THE choke point:** `sendOnce` now checks the STUDENT leg of any enrollment-keyed fan-out (role from the dedupe key / `recipientRole`): if it resolves to the family's parent address, the parent version goes and the student leg is recorded as **cancelled — "same address as the parent leg — one copy sent (the parent version)"**, so the log stays honest and the dedupe key is claimed. Covers the `_p/_s` sequence twins, confirmations, SU schedule updates (those four sends now name their enrollment — they didn't), cancellation, message-the-class, FO stages. Campaign pairs mode: the recipient builder drops a student row whose address equals the family's parent row (never two campaign copies to one inbox). **Proposed rule for Scarlett's veto: PARENT wins** (it carries money/logistics the student copy omits); the alternative — student wins for student-voiced templates like E2_DIAG_STUDENT/SV — is one line to flip. **C.** Billing contact: `familyRecipients()` already treated billing==parent as unset and dedupes the cc list against parent + billing; students are never billing recipients. **D.** A real parent email later: staff edit it through the existing family-facts path; the marker derives away by itself; login links resolve by `families.parent_email`, so logins follow the new address (verified in code — `deriveRoles(email)`). **Gates:** `regress:billing-recipient` (30: `sameAddress` cases + C), `regress:silent-import` (the no-parent-email row imports keyed on the student address; a student-role probe to that address returns `suppressed` with the same-address cancelled row).

## PL-464 — Families without a parent email: key on the student's address, and never send the same person two copies (Scarlett, Sep 21)
"For the missing parent emails, we don't have them either — sub in the student email. Sometimes this happens. We may need logic that sorts by student email if parent email doesn't exist and/or only sends one email (for the times we email both parent and student) if parent and student email are identical." Two real cases in the rosters about to be imported (one ASF, one Leone XIII).
- **A. Import:** a row with a blank parent email and a student email imports with `families.parent_email` = the student's address (the family key stays ONE column — no second identity path), and the family is visibly marked "no parent email on file — using the student's address" wherever staff read the family (profile header + roster hover is enough). A row with NEITHER address is refused with a clear line, as today. Same rule for the staff "add a family/registration" paths if they require a parent email today — report what they do.
- **B. One person, one email:** wherever a send fans out to a parent leg and a student leg (`_p` / `_s` sequence twins, confirmations, SU schedule updates, cancellation, message-the-class, FO campaign, anything else the PL-457 audit listed as family/student), if the two legs resolve to the SAME address (case-insensitive, trimmed) send ONE. **Proposed rule — Scarlett's veto:** the PARENT version goes (it carries the money/logistics the student version omits); the student leg is recorded in the send log as cancelled "same address as the parent leg" so the log stays honest and the dedupe key is claimed. Do it at the ONE choke point if the fan-out has one; otherwise a shared helper both loops call — not per-call-site expressions (the PL-454 lesson).
- **C.** Also collapse when a billing contact (PL-454) equals the parent or student address — never two copies to one inbox.
- **D. When a real parent email turns up later:** staff edit the family's parent email through the existing family-facts path; the marker clears itself; logins follow the new address. Verify; no new UI unless that path can't do it.
- Gate: extend `regress:billing-recipient` (or a sibling) — same-address family gets exactly one send per fan-out across every key in the registry.

## ✅ PL-465 — SHIPPED (Sep 22) — migration `20260919000001` APPLIED (add-on `source='import'` + package-less import add-ons); **the staff-notes surface exists: the family's activity trail on the profile**
**A. `outcome` column** (aliases accepted: refund/refunded, tutoring/1on1/moved_to_tutoring, defer/deferred, cancelled/canceled/class_cancelled; default `enrolled`, or `class_cancelled` under `--records-only`): `enrolled` = today's path (+ `--silent`); `refunded` = enrollment inserted as Paid then set **Refunded through the same transition the roster's "mark Refunded" uses** (Paid/Completed → Refunded, guarded), always comms-muted, all send keys claimed, the sheet's note on `enrollments.notes`, never enqueued to QBO; `moved_to_tutoring` / `deferred` / `class_cancelled` = **NO enrollment** — family + student through THE upsert, school attached by nickname, and a dated note on the record. **The surface:** the family profile's **Recent activity** panel renders `family_fact_edits` (the PL-422C trail) scoped to the family — the note is written there as actor `staff:import`, e.g. "[2026-09-22 import] registered, then moved to 1-on-1 tutoring instead of the class — SLS SAT Prep (paid $749), registered 2026-07-02. Sheet note: "…"", verbatim sheet text included. It shows on the profile under Recent activity and in the dashboard feed; no column nobody renders. **B. `--records-only`** (no `--class`; `--school <nickname>` or a per-row `school` column): family + student + note only; `enrolled`/`refunded` rows refused there; an unknown nickname is REFUSED, never created (Scarlett adds "CAC — Cairo American College" first). **C. `addonHours` + `addonAmount`:** an add-on cannot be recorded "as paid" without what was paid — `enrollment_addons.price_paid` is NOT NULL and there is no honest default — so the sheet needs the amount column mapped; with it, the row gets `hours`, `price_paid`, `source='import'`, `package_id` NULL (the migration extends the package-or-conversion check the way cancellation conversions already use), no Stripe ids, `purchased_at` = the registration date, **not enqueued to QuickBooks**, no email. Hours drawn down through the normal tutoring flow (the engagement wizard's add-on picker; `packageHoursUsedBefore` keys on the add-on id, package or not — the same shape cancellation conversions have run on since PL-84). Without an amount: the enrollment imports and the add-on line prints REFUSED with the reason. **D. `accommodations`** → `students.special_needs` (the field the intake writes, "Learning notes" on the profile) AND `enrollments.accommodations` when an enrollment exists (the roster/emails read that one); `graduatingYear` / `studentEmail` unchanged. **E. Dry run** prints `Row | who | outcome | verdict | detail` with what would be created / matched / skipped and why, then `Totals: enrolled=… refunded=… moved_to_tutoring=… · refused=… · already-on-record=…`; idempotent for every outcome (student+class for enrollments — Refunded now counts as existing; family+note text for notes). **F.** Matching is by parent email (+ student name) — the verdict says "matches family X + existing student Y"; a records-only row for someone already imported adds a second dated note on the same record. **Gate:** `regress:silent-import` imports one row per outcome + an add-on row + a no-parent-email row (dry run + real), asserts every effect, re-runs for idempotency, then `--records-only` with an unknown school (refused) and a known one (same family/student, second note, still no enrollment). **NOT run against prod** — Scarlett + Claude do the dry runs and imports.

## PL-465 — Import everyone the old system knows about — including the people who DIDN'T end up in a seat (Scarlett, Sep 21)
"Should these students be included for accuracy, so that when we switch over they're not lost? We at least have a profile for them with notes of what happened. Same for students in the cancelled classes." The tracker holds four kinds of people beyond seated students: refunded (Leone XIII ×1, Nido ×1), moved to 1-on-1 before/instead of class (MIS ×1, and everyone in the cancelled Nido / Cairo / AISCT cohorts), and deferred to a future course (ASF ×1). Plus one seated SLS student with 5 paid extra 1-on-1 hours.
- **A. Per-row `outcome` column (mapping key `outcome`; default `enrolled`):**
  - `enrolled` → today's behavior (+ `--silent`).
  - `refunded` → enrollment created then set `Refunded` through the SAME path the admin "mark Refunded" uses (so counts/waitlist/suppression behave identically), silent, with the sheet's note (e.g. "Refund 7/17"). Never enqueues QBO.
  - `moved_to_tutoring` · `deferred` · `class_cancelled` → **NO enrollment** (rosters, paid counts, reports and the instructor's class stay truthful — these people never sat in the class): family + student are created/matched through THE one upsert path, school attached by nickname, and a dated staff note is written on the record: what they registered for, that they paid (amount if mapped), what happened, and the sheet's own note text verbatim. Use the existing staff-notes surface on the family/student profile; if there isn't one that shows on the family profile, STOP and say so (don't bury it in a column nobody renders).
- **B. `--records-only` (no `--class`):** for the cancelled cohorts whose class rows no longer exist (Nido Aug, Cairo Aug/Sept, AISCT Oct) — every row is family + student + note, nothing else. Unknown school nickname → reported, not created (Scarlett adds "CAC — Cairo American College" herself).
- **C. `addonHours` column ("Extra 1on1 Hours Paid"):** creates the enrollment's tutoring add-on in the state a PAID add-on has — hours on the books, schedulable and decrementable through the normal tutoring flow — with no Stripe ids, `source='import'`, NOT enqueued to QBO (the old system booked it), no emails. Scarlett wants this one student (5 hours) to be the first real test of running add-on hours through the portal. If an add-on cannot exist without something the import can't supply (a `tutoring_packages` ref, a price snapshot…), report exactly what and propose the smallest honest default — do not fake a payment.
- **D. `accommodations` column** → the student's accommodations field the intake already captures (NOT free-text notes), verbatim. `graduatingYear`, `studentEmail` as today.
- **E. Dry run prints a per-row verdict table** (row → outcome → what would be created / matched / skipped and why) and totals per outcome. Idempotent on re-run for every outcome (student+class for enrollments; student+note-hash for notes).
- **F.** A student who appears in two sheets (e.g. a sibling, or moved between cohorts) matches the same family/student — report matches in the verdict table.
- Extend `regress:silent-import` with one row of each outcome + an add-on row.

## ✅ PL-466 — SHIPPED (Sep 22) — sites checked and fixed
**Fixed:** ① anonymized class report chart labels used the FIRST TOKEN of the composed name (`s.name.split(' ')[0]` → "Stefano") — now the whole first-name field (`firstName`); ② the admin students list sorted by FIRST name — now by the whole last-name field then first ("Marchini Cigognini" under M); ③ same-person prompts (`close-match.ts`) normalize accents + case (NFD, diacritics stripped: "Uzunoglu" ≈ "Uzunoğlu"); ④ the importer's `parentName`/`studentName` "split on the last space" is REFUSED with a clear error — map explicit first/last columns (Claude's cleaned sheets have them); ⑤ registration form: "Include both surnames if you use two." under both Last Name fields. **Checked, already right:** greetings — `{studentFirstName}` / `{parentFirstName}` are the whole first-name fields end to end (lifecycle context ← `students.first_name`; no splitting); search — family/student lookups use `ilike '%x%'` on `first_name` and `last_name` separately, so either surname finds the student; roster/report/PDF columns wrap (no `truncate` on any student-name cell; the CSS `truncate`s found are on locations, links, subjects, tutor names in the week grid, and email addresses); QBO customer: `GivenName`/`FamilyName` are the whole fields (100-char cap), DisplayName carries the email; collateral (flyer/letter) renders no student names; sorts elsewhere compare whole `last_name` fields (counselor card, instructor roster) or composed full names. **Known limitation, unchanged:** instructors have ONE `name` field, and "first name" for a tutor is derived as the first token in ~10 places (handoff notes, "with Billy", the T1 block heading, coverage copy, samples) — an instructor with a two-word first name would read as its first word; no current instructor has one. Fixing it means an `instructors.first_name` column (PL-470 candidate).

## PL-466 — Compound names are first-class (Scarlett, Sep 21)
"A lot of our students have two last names and the portal should be able to handle this." The fields already accept spaces; make sure nothing downstream mangles them. Audit + fix: every place that derives something from a name — greetings ({studentFirstName} must be the WHOLE first-name field: "Stefano Carlo", never the first token), initials/avatars, sort order (by the whole last-name field: "Marchini Cigognini" sorts under M), roster/report/PDF columns (no truncation that drops the second surname; wrap instead), search (either surname finds the student), dedupe/record-match prompts (accent- and case-insensitive; "Uzunoglu" ≈ "Uzunoğlu"), the anonymized class report, collateral, QBO customer display names, and the single-name-column splitters in the importers (`parentName`/`studentName` "split on the last space" is WRONG for these families — when only a full name is mapped, import refuses to guess and asks for explicit first/last columns; Claude is preparing the cleaned files with explicit columns). Registration form: helper text under Last name — "Include both surnames if you use two." Report the list of sites checked.

## ✅ PL-467 — SHIPPED (Sep 22)
Wizard "➕ Add a new school…": the first contact is OPTIONAL (all-or-none — typing any contact field requires all three; leaving them blank saves the school alone, button reads "Save school", the message says nobody gets digests/room requests until a contact is added; the class wizard's existing warnings about who gets room requests stay). Schools panel (Contacts → Schools) gets its own **"+ Add a school"** button → inline form (full name, nickname, timezone, optional city) → the card appears in the existing "No active contact" state. Leone XIII's placeholder `billy@` contact can now be ended on its card (Scarlett) without leaving the school contact-less in a broken way.

## PL-467 — A school can be added without a contact (found Sep 21)
The wizard's "➕ Add a new school…" REQUIRES a first contact ("room requests, digests, and the final-days push all need someone to email") — but after the purge all six existing schools legitimately have zero contacts, the Schools panel already renders "No active contact — the school gets no digests and nobody can open its portal", and Scarlett specifically does NOT want Leone XIII's counselor in the portal until that class ends. Make the first contact optional in the add-school form (same honest "no active contact" state; the class wizard's existing warnings about who gets room requests stay). Also give the Schools panel its own "Add a school" button — today the only door is inside the class wizard. Until this ships Claude/Scarlett add Leone XIII with a placeholder or wait — Scarlett's call.

## ✅ PL-468 — SHIPPED (Sep 22) — migration `20260919000003` APPLIED (`classes.venue` + the ASF data fix); **CR timing DOES change — see below**
**Model:** `classes.venue` (new, optional) = the in-person venue known at sign-up; null composes to "On campus at {school name}" at render. `default_location` keeps meaning the ROOM (or the meeting link online) — blank until the school confirms it; the counselor's answer still writes it; the wizard/roster labels now say **Venue** and **Room** (online: Meeting link). **ONE helper** `app/utils/class-place.ts`: `classPlaceLine()` → room known "ASF campus · Room US 109"; not yet "ASF campus — classroom to be confirmed"; online → the link; plus `classVenue()` / `classRoomPending()`. Wired into: the public class page (hero facts, the session list fallback, the letter blurb's `{address}`; the schema.org place stays keyed on the real room/address), the register page's calendar, the family portal card ("Where:" line + the next-class card), the class calendar page, the E-series emails (`classLocationTailText/Html` + `{classLocationLine}` take the venue → "at ASF campus · Room US 109" / "at ASF campus — classroom to be confirmed"), and the admin roster card (Venue editable for in-person; Room's empty state says "to be confirmed — the school is asked once the class meets minimum"). Per-session location overrides untouched. The class-details (#4) hold-and-alert still keys on the ROOM being blank. Online classes unchanged (venue hidden; the wizard shows it only for in-person school classes). **Data fix applied on prod:** `asf-sat-prep-fall26` "ASF campus — Room US 109" → venue "ASF campus", room "Room US 109". **TIMING — existing behavior changes:** the classroom request used to go out at T-14 regardless of the roster; it now goes out **the moment the class meets minimum** (paid ≥ min, or the "run anyway" decision) — earlier than T-14 for a fast class, later for a slow one, and **never for a class that doesn't reach minimum** (before, every in-person class without a room was asked at T-14). The 11/8-day nudges for an unanswered request and the T-6 #4 backstop are unchanged; a room typed directly still cancels a pending request; a set venue never suppresses the ask. Wizard placeholder copy says so. CTA landings unchanged. Gate: `regress:links` renders every E-series template clean through the venue-aware composer.

## PL-468 — In-person classes: "on campus" is known at sign-up, the ROOM comes later — model both (Scarlett, Sep 21)
"It's normal for us to say the class is on ASF campus so parents know it's not online when signing up. After the class meets minimum registration we get the specific classroom confirmed from the counselor — here it's US 109. Can the portal handle that distinction?" Today it can't: there is ONE field, `classes.default_location`. Typing "ASF campus" into it SUPPRESSES the classroom request (the wizard says "Blank = counselor gets asked 14 days out"), and the counselor's answer OVERWRITES it (`classroom-request` route → `default_location`), so you get either the campus or the room, never "campus now, room later".
- **Venue (known at creation) vs room (confirmed later):** add a class-level venue line for in-person classes — default composed from the school ("On campus at {school name}"), editable — shown on the public class page, register page, flyer/letter, and E-series emails from day one. `default_location` keeps its meaning as the ROOM: blank until confirmed, asked of the school contact by the existing CR flow, never suppressed by the venue being set.
- **Rendering rule, one helper:** room known → "{venue} · {room}" (e.g. "ASF campus · Room US 109"); room not yet known → "{venue} — classroom to be confirmed" (families) and the existing hold/alert logic for the class-details email (#4) still keys on the ROOM being blank. Per-session location overrides keep working.
- **Timing:** Scarlett's real trigger is "once the class meets minimum registration", not only "14 days out" — ask at whichever comes first (min reached, or T-14 if min was already reached earlier), never before min is reached (no point asking a counselor for a room for a class that may not run). Say in the ship note if this changes any existing CR timing.
- Online classes unchanged (venue hidden; location = meeting link). Migration additive. Data fix in the same batch: `asf-sat-prep-fall26` currently carries "ASF campus — Room US 109" in the one field → venue "ASF campus", room "Room US 109".
- CTA registry: the counselor's room form + the staff "Counselor set … location" alert keep their landings.

## ✅ PL-469 — SHIPPED (Sep 22) — migration `20260919000002` APPLIED (`app_settings.updated_by`); **transactional sends are never blocked by this number — confirmed: `sendOnce` reads no cap at all**
**ONE reader** `app/utils/send-limits.ts` `loadSendLimits()` → `{ dailyBrake, monthlyQuota }` from `app_settings` keys `resend_daily_cap` / `resend_monthly_quota` (blank/0 = none) — the three `?? 100` fallbacks (system-health, campaign-send, the campaigns route) are gone. **Settings (Tutoring point of contact page) gains a "Sending limits" card** (admin): "Campaign brake (sends per day)" + "Plan quota (sends per month)" with the plain-language help (Pro has no daily limit; the daily number is a safety brake for campaigns only — they pause at it and resume next day; invoices/schedules always send; the monthly quota is the real limit). Saves record `updated_by` + `updated_at` on the rows — **no settings audit table existed; this is the first setting that records who changed it** (say the word to give the contact/identity settings the same). **Health card** now reads "312 today · campaign brake 1,500" (or "no campaign brake set") and "4,102 this month of 50,000 on the plan" (or "plan quota not set"); it turns red ONLY on real Resend rejections: `sendOnce` stores the actual Resend error on a failed row's payload and the card counts today's failed rows whose error mentions quota → "Resend rejected N sends today for quota — check the plan". Amber = the brake is reached ("campaigns paused until tomorrow; invoices and schedules still send"). **Campaigns:** no brake set → `quotaRemaining()` is unbounded (never pauses on arithmetic); the campaigns page says "Sent today: N — no campaign brake set…" or "…of the 1,500 campaign brake"; pause copy reads "paused at the campaign brake". **Current prod state: both settings UNSET** → no brake, no quota line; Scarlett picks the values in the UI (50000 monthly; a brake only if she wants campaigns to pace themselves).

## PL-469 — The daily email cap is a setting staff can see and change; the health card stops saying 100 (Scarlett, Sep 21)
HGL is on Resend Pro (50k/month, NO daily limit — confirmed by Scarlett). The portal still assumes the free tier: `app_settings.resend_daily_cap` has no row, so `system-health.ts`, `campaign-send.ts` and the campaigns route all fall back to `?? 100` — the health card reads "2 / 100", would warn "approaching the daily cap" at a busy registration day, and campaigns would PAUSE at 80 sends (cap − 20 transactional reserve).
- Make it a real setting under Settings (number + plain-language help: "Your Resend plan's daily limit. Pro has none — this is a safety brake for campaigns, not a Resend limit"), audit-logged like other settings; ONE reader helper instead of three `?? 100` fallbacks.
- Health card: shows sends today against the brake only as a brake ("312 today · campaign brake 1,500"), never "sends are failing" unless Resend actually returned quota errors (that signal should come from real send failures, not arithmetic). Add the month-to-date count vs the plan's monthly quota (also a setting) — that IS the real Pro limit.
- Confirm in the ship note that transactional sends are never blocked by this number (only campaigns pause).
- Until it ships, Claude sets the row by hand to the value Scarlett picks.

