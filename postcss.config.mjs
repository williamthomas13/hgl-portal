// Tailwind v3 PostCSS config. Mirrors postcss.config.js.
// Next.js prefers the .mjs file when both exist, so this is the canonical one.
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}

export default config
