// PL-493 (Scarlett, Sep 22): the PUBLIC contact address — the footer, the
// /inquire "rather just talk to a person?" line, the /partner no-JS note.
// NOT `contact_email` (app_settings): that is the TUTORING contact (Kelsie),
// right for the T-series family emails and their contact block, and it stays
// untouched. Nothing public ever prints a person's inbox.
export const PUBLIC_CONTACT_EMAIL = 'info@highergroundlearning.com'
